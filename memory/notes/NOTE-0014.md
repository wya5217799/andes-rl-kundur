---
id: NOTE-0014
source: handoff
source_path: memory/handoffs/2026-05-17_R58_to_R66_hyper_sweep_handoff.md
date: 2026-05-17
related_rounds: [R58, R60, R61, R62, R63, R64, R65, R66]
topics: [training-infra, hyper-sweep]
extracted_claims: []
status: ingested
---

## Summary
A 13-hour session (R58–R65 committed + R66 in-flight) systematically swept hyper-parameters, finding 4 major sub-optimal defaults vs paper Table I: lr=3e-4→3e-3 (10×), MAX_GRAD_NORM=1.0→0.5, batch_size=256→512, N_SUBSTEPS=5→3. These axes are independent and stack: combined combo (CLM-0086..0092) gives 3-seed TD3 paper-metric SOTA of -0.124 total_cum_rf (+37.5pp robust improvement vs paper DDIC). Q-0007 (best_eval checkpoint selection) empirically validated as +14-20% lift and saved a collapsed s51 SAC seed. Critical discovery: LSTM is hyper-asymmetric from TD3/SAC — LSTM needs lr=1e-4 (not 3e-3), gc env var doesn't reach LSTM, bs flag ignored (lstm_batch_size hardcoded 32).

## Key facts (claim candidates)
- 4 paper Table I defaults sub-optimal in our env: lr (10×), MAX_GRAD_NORM (2×), batch_size (2×), N_SUBSTEPS (5→3 is free 2× faster + +3.6% metric); mechanism: paper tuned for 20× larger action space (CLM-0092, CLM-0086..0088)
- Q-0007 (best_eval checkpoint selection vs best.pt): +14-20% lift empirically validated across SAC and TD3; saved R65 s51 SAC collapsed seed (best.pt=-0.965, best_eval.pt=-0.255)
- LSTM hyper-asymmetry: LSTM needs lr=1e-4 (train.py:305 clamp is correct), gc env var doesn't reach TD3LSTMAgent (architectural), bs flag ignored (hardcoded lstm_batch_size=32) — CLM-0100/0103
- R66 code drift discovery: R57-α s51 reproduces at 0.4259 today vs historical 0.526 = -19% drift; cause suspected to be R61 Q-0007 additions silently affecting LSTM + R63 env var imports causing RNG state shift (CLM-0104)
- R59 PI briefing infrastructure (render.py, validate.py, ADR-0003, _TEMPLATE_VERDICT.md) remained uncommitted throughout this session but was load-bearing for render/validate operations

## Open threads
- R66 Q-0010 fix: eval probe moved after env.close() + RNG state save/restore in train.py; verify training `r66_w2_lstm_q7_fixed_s51` in progress at handoff time
- Code drift bisect: find which commit (R58–R65) caused LSTM -19% regression from R57-α 0.526 to 0.4259
- R59 PI briefing infra commit: land the uncommitted infra that was load-bearing throughout session
- Paper draft: 4 tables ready (TD3 SOTA, SAC SOTA, lr U curve, hyper ablation matrix)
