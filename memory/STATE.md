# Project State — auto-rendered 2026-05-17 22:20

> Do not edit this file. Regenerate via `python memory/tools/render.py`.

## 给 PI 的简报（最新一轮 — R67）

**这周干了啥**：R66 收尾后用户问"参数到没到顶"，我答 "TD3(一种稳健的连续动作 RL 算法)/SAC(一种带熵奖励的 RL 算法) 基本到顶，
但 gamma/tau/buffer 没扫"。用户：'继续挤'。R67 共 6 wave × 3 并行 = 18 个
ANDES(项目用的电力系统仿真器) 训练 (~75 min wall)，扫了 7 个剩余 hyper 轴 + 1 个 LSTM(能记前几步状态的网络) 架构 hypothesis.

**结果（一句话）**：**tau=0.001 是新 SOTA(当前最好水平)** — 3-seed robust +4% improvement
vs R64 baseline (R64 -0.124 → R67 -0.119)，vs paper DDIC 46.5% 从 +37.5pp
推到 ~+39pp。**U 曲线对称确认** (tau=0.0005 worse than tau=0.001 worse than tau=0.005)。
**其它 6 轴全确认 paper 默认或 R63-R64 选择最优**：gamma (paper 0.99 U 谷底)、
hidden_size (R64 选 64 最优，**Q-0012 关闭**)、batch_size (R63 选 512 最优)、
buffer_size (paper 10k 过剩但无害)、explore_noise (paper 0.1 U 谷底)、
warmup_steps (paper 1000，2× warmup -73% 灾难)。**LSTM + Q-0007 paper-metric
完全不收敛** -145%（Q-0007 救命器跟 LSTM 架构 + paper reward shape 互斥）。

**意外**：(1) tau=0.001 是 paper Table I 默认 0.005 的 5× 慢更新，paper 文里写
"reasonable default"，结果实际是 5× 慢更新更优 — paper 选了一个非最优默认；
(2) buffer_size 在我们短训 regime (75 ep × 50 step = 3750 trans) 下，5k 和
10k 完全等价 (buffer 永远不满) — 意味着 paper 写 10000 是 over-provision
(对 2000-ep paper baseline 可能必要); (3) warmup_steps 2000 (paper 1000 的 2×)
**降 73%** — 那是 disaster-level，说明 ANDES + RL 对 warmup 敏感度极高。

**我默认下一步做**：**R67 收摊**。所有合理 hyper 轴扫完了，只剩 tau=0.001
新 SOTA + 6 U-bottom 确认 + 1 LSTM-paper-metric 负面发现。可以**开始写
paper** 了 (4 表已齐，加 R67 tau 轴扫的小表)。下一会话:
1. R67 commit (claims + verdict + 8 个新 result 目录)
2. 可选: R67 SOTA 用 paper-strict 20-scen 全 eval 跑一下，跟 paper Table II 对齐
3. 开始写 paper (Methods + Results section)

**你想插一脚就说**：(1) 是否再扫**额外组合** (tau=0.001 + gamma=0.99 cross-axis
验证？现已 implicitly 包含)；(2) 是否再 sweep SAC 看 SAC paper-faithful 也吃
tau=0.001 加成 (期望吃，预计 R65 SAC -0.194 → ~-0.186)；(3) 是否同意收摊 +
commit R67 + 下次开始写 paper。沉默 = R67 commit + 写 paper。

## Headline Numbers

- CLM-0005 [V] R21 V4 h50_s49 6-axis = 0.444 (post r30/N1c ranker fix; 4.04× no_ctrl 0.104) (R30)
- CLM-0006 [V] HAWE w8515 (85% R21 + 15% ws8) post-fix 6-axis = 0.415 (R30)
- CLM-0007 [V] HAWE w9802 post-fix 6-axis = 0.439 = 99.3% of R21 (fresh-seed reproducible) (R30)

## In-Flight

- R45 — `memory/rounds/R45/plan.md`

## Open Questions

- Q-0004 [opened R46] AndesBaseEnv absorb-into-V4 (complete AD-01) — verify 1e-9 bit-identical from WSL before landing
- Q-0005 [opened R56] Why does TD3+LSTM seed 50 collapse while seeds 49/51 converge?
- Q-0008 [opened R58] Verify paper-metric ranking persists at 500-ep paper convergence horizon

## Recently Closed (last 3)

- Q-0012 closed-negative @ R67, by CLM-0106 — hidden_size U-curve flattens under lr=3e-3 — is h=96 (or h=32) marginally better than h=64 at 3-seed?
- Q-0010 closed-positive @ R66, by CLM-0102 — Why does LSTM + Q-0007 in-training eval probe collapse training? (3× degradation vs R57-α same config no probe)
- Q-0013 closed-negative @ R66, by CLM-0103 — Per-axis ablation of R64 combo for LSTM — which axis kills LSTM transfer?

## Latest Round

R67 — **tau=0.001** is the **new R67 SOTA** for paper-metric mode TD3:

see `memory/rounds/R67/verdict.md`

## Leaderboard (top-10 by metric)

- CLM-0088 [R63] batch_size_best = 512.0000
- CLM-0086 [R63] n_substeps_best = 3.0000
- CLM-0073 [R58] 6_axis = 0.6069
- CLM-0076 [R60] ls1_ls2_improvement_rate = 0.5550
- CLM-0066 [R57] 6_axis = 0.5010
- CLM-0087 [R63] max_grad_norm_best = 0.5000
- CLM-0005 [R30] 6_axis = 0.4440
- CLM-0075 [R60] 6_axis = 0.4400
- CLM-0007 [R30] 6_axis = 0.4390
- CLM-0103 [R66] r57_alpha_today_reproduction = 0.4259

## Stats

108 claims (70 finding / 24 decision / 14 correction) · 56 rounds · 13 questions (3 open / 10 closed)

## 历史简报

- R66: (1) **Q-0010 fix 成功** — eval probe 移到 env.close()
- R65: **SAC 大成** — paper-faithful (radsec) 3-seed mean
- R64: **lr=3e-3 (10× paper 默认 3e-4) 是头号赢家** —
- R63: 三个轴各自给单独提升 (nsub=3 +3.6% / gc=0.5
- R62: **TD3 + Q-0007 拿下新 paper-metric SOTA**——s50 best_eval
