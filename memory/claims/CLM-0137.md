---
id: CLM-0137
type: decision
trust: S
status: current
statement: |
  R78 eval pipeline 整理 + final_eval TDD harden: (1) 单一 dual-eval helper
  `evaluation/summary.py` (`score_trace_files`, `format_headline`) 是 paper §IV-C
  cum_rf + 11-axis geo 的 SSOT; (2) `evaluation/score_seed.py` 从
  `scripts/score_run.py` 提升到 library, `score_run.py` 仅作 CLI 包装 + 多 seed
  聚合 (`aggregate_scores`); (3) `eval_ddic.py` / `eval_ensemble.py` /
  `eval_all_seeds.py` 三个 entry 全部默认写 `<label>_summary.json`
  (paper-metric + 6-axis), 加 `--no-score` 逃生口; (4) `train.py` 加
  `--final-eval / --no-final-eval` BooleanOptionalAction 默认 on, 训练结束自动跑
  LS1+LS2 dual-eval, 写 `<save_dir>/final_eval_summary.json`, 失败不杀 ckpt
  (dump 到 `final_eval_error.txt`); (5) ckpt suffix 优先级 `best_eval` > `best` >
  `final` (R61 Q-0007 路径优先); (6) 5 个 R-driver `_r{58,69,70,75}_*.py` + 1 个
  `_r58_eval_all.sh` 归档到 `scripts/_archive/round_scripts/` (与 R01-R36 惯例对齐);
  (7) 删 `eval_ensemble.py` 的 `_ensemble_action_fn` back-compat alias (R77 verdict
  误述已删, 实际未删), `tests/test_eval_ensemble_recurrent.py` 改 import
  `build_ensemble_action_fn` from library, 删 `sys.path.insert(scripts/)` hack;
  (8) **TDD harden**: `pick_final_eval_suffix` + `run_final_eval` 从 `train.py`
  私有提到 `evaluation/final_eval.py`, `run_final_eval` 接 `score_seed_fn` 参数
  做 DI (default 绑实际 score_seed, 测试注入 stub), 8 个 RED→GREEN test pin 4 路
  suffix 优先级 + happy/fail/skip 三路; **failure swallow 是 load-bearing 安全契约**
  (训练已成功 ckpt 已落盘, eval 异常 MUST NOT 杀 process); `train.py` 留薄
  `_emit_final_eval` shim 只管 human-readable print.
  Verification: 221/221 pytest pass (R77 206 baseline + 7 new score_trace_files
  + 8 new final_eval); 6-axis 数值守恒 (LS1=0.114101 / LS2=0.077035 bit-identical
  pre-refactor regression).
round: R78
provenance:
  - src/andes_rl_kundur/evaluation/summary.py
  - src/andes_rl_kundur/evaluation/score_seed.py
  - src/andes_rl_kundur/evaluation/final_eval.py
  - scripts/score_run.py
  - scripts/eval_ddic.py
  - scripts/eval_ensemble.py
  - scripts/eval_all_seeds.py
  - scripts/train.py
  - tests/test_score_trace_files.py
  - tests/test_final_eval.py
  - tests/test_eval_ensemble_recurrent.py
  - memory/rounds/R78/verdict.md
tags: [eval-pipeline, dual-eval, train-auto-eval, paper-metric, 6-axis, refactor, archive, tdd, dependency-injection]
---
