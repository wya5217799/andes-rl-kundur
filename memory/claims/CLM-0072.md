---
id: CLM-0072
type: decision
trust: S
status: current
statement: |
  R58 decision: **scope-split production recommendation** between 6-axis
  benchmark and paper Sec.IV-C metric, because CLM-0071 demonstrates
  the R56/R57 algorithm ranking does not preserve across benchmarks.

  **Mode 6-axis** (V4 paper-faithful + project ranker, R56/R57 framing):
  - **No change**: CLM-0067 remains valid. Production candidate is
    R57-α s51 ckpt (`results/td3_lstm_h64_warmup5_s51/`) at 6-axis =
    0.543 single-seed SOTA, or R57-β top2 HAWE-LSTM at 6-axis = 0.501
    ensemble SOTA.

  **Mode paper-metric** (paper Sec.IV-C cum_rf, R58 framing):
  - **New production candidate**: V4 TD3-MLP s50 ckpt
    (`results/td3_norm_h64_s50/`) at total_cum_rf = **-0.196** (best
    single seed across all 36 ckpts evaluated). 3-seed mean -0.267,
    2× better than paper-strict TD3 variants.
  - Training cmd: `--algo td3 --normalize-actions --episodes 75
    --seed 50 --hidden-size 64`
  - Wall: ~10 min on CPU.

  **Mode paper-faithful** (paper Eq.11/15-18 strict + rad/s, R58 audit
  A3 finding):
  - **New production candidate**: SAC under `paper_strict_pure_radsec`
    config, s50 ckpt (`results/r58_paper_strict_pure_radsec_sac_s50/`)
    at total_cum_rf = **-0.397** (best single seed under paper-faithful
    units). 3-seed mean -0.518.
  - Training cmd: `--algo sac --normalize-actions --episodes 75
    --seed 50 --hidden-size 64 --reward-config paper_strict_pure_radsec`
  - Wall: ~10 min on CPU.
  - **Caveat**: still 75-ep; paper uses 500-ep. Sanity run pending
    (Q-0008).

  **Supersession scope**:
  - This decision **does NOT supersede CLM-0067** as a whole.
    CLM-0067 remains current for its declared scope (6-axis Mode A/B).
  - This decision **adds** two new scopes (paper-metric, paper-faithful)
    that CLM-0067 did not address.
  - The 3-claim picture: CLM-0064 (R51 SAC, retired by CLM-0067) →
    CLM-0067 (R57 LSTM, 6-axis-current) → CLM-0072 (R58, multi-mode).

  **Why this matters**: prior to R58, the project assumed "best on
  6-axis = best on paper" implicitly. CLM-0071 demonstrates this is
  false. Future paper-comparison work must explicitly pick a scope
  (6-axis project benchmark vs paper Sec.IV-C) before claiming SOTA.

  **Open gates** (do NOT promote without resolving):
  - Q-0007 (best-by-eval-score) still applies to TD3+LSTM s50 (R57
    collapse mechanism persists under paper metric: s50 -0.689 vs s51
    -0.284).
  - Q-0008 (500-ep paper convergence across all 12 cells) is the
    cheapest path to verify whether 75-ep ranking persists.
  - Q-0009 (disturbance-distribution magnitude vs paper) gates any
    direct paper-table comparison.

round: R58
provenance:
  - memory/claims/CLM-0071.md (eval matrix evidence)
  - memory/claims/CLM-0070.md (strict-radsec training evidence)
  - memory/claims/CLM-0067.md (prior decision, scope retained)
  - memory/rounds/R58/verdict.md
  - results/td3_norm_h64_s50/agent_*_best.pt (paper-metric production)
  - results/r58_paper_strict_pure_radsec_sac_s50/agent_*_best.pt (paper-faithful production)
tags: [decision, production-scope-split, td3-mlp-paper-metric, sac-paper-faithful, multi-mode-prod]
---
