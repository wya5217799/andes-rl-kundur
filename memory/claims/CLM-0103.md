---
id: CLM-0103
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R66 — Q-0013 closes negative. Per-axis ablation of R64 hyper combo on
  LSTM s51 shows **2/3 axes architecturally cannot reach LSTM**.

  ### Single-axis result (s51, R57-α default + ONE change)

  | LSTM config | s51 6-axis | actual delta vs R57-α default | reach LSTM? |
  |---|---|---|---|
  | R57-α today reproduction | 0.4259 | (baseline) | — |
  | + `N_SUBSTEPS=3` (env var) | 0.437 | +3 % | ✓ via base_env.py:97 class attr |
  | + `MAX_GRAD_NORM=0.5` (env var) | 0.4259 | 0 % (no change!) | ✗ env var NOT read by LSTM agent |
  | + `--batch-size 512` (CLI flag) | 0.4259 | 0 % (no change!) | ✗ LSTM uses hardcoded `lstm_batch_size=32` |

  ### Architectural unreachability

  **MAX_GRAD_NORM env var path** (R63 W2):
  - `sac_base.py:56` reads `MAX_GRAD_NORM` env var → SAC + TD3 (via `_SACBase`)
  - `td3_lstm.py:98` ctor param `max_grad_norm: float = 10.0`, no env var
  - LSTM grad clip permanently at default 10.0 regardless of env var

  **batch_size flag path**:
  - `train.py:301` hardcodes `lstm_batch_size = 32` regardless of `args.batch_size`
  - LSTM trains with batch=32 sequences regardless of `--batch-size`

  ### Implication

  R65 W2 "LSTM + R64 combo" result (-24 %) is NOT a clean ablation of new hyper
  on LSTM. It's:
  - `nsub=3` change (+3 %)
  - `gc=0.5` UNCHANGED (env var doesn't reach LSTM)
  - `bs=512` UNCHANGED (flag ignored by LSTM)

  → R65 W2 -24 % degradation is **NOT primarily from new hyper combo** — it's
  from **code drift** (CLM-0104).

  ### To actually test LSTM hyper sweep would require

  1. Refactor: extend env var pattern to LSTM (add `MAX_GRAD_NORM` read in
     td3_lstm.py:__init__, add `LSTM_BATCH_SIZE` env var)
  2. Then re-run per-axis ablation
  3. Cost: ~30 min code + ~45 min sweep

  Deferred — diminishing returns on 6-axis path. R57-α historical ckpt
  (0.526) remains SOTA reference. Future LSTM optimization may pursue
  paper-metric path via Q-0007 (now fixed in CLM-0102), not 6-axis.

  ### Q-0013 closed-negative

  Per-axis ablation produced uninformative result due to architectural
  unreachability. No path forward without code refactor.

round: R66
provenance:
  - results/r66_w1_lstm_nsub3only_s51/agent_*_best.pt
  - results/r66_w1_lstm_gc05only_s51/agent_*_best.pt
  - results/r66_w1_lstm_bs512only_s51/agent_*_best.pt
  - src/andes_rl_kundur/agents/sac_base.py (env var path for SAC/TD3)
  - src/andes_rl_kundur/agents/td3_lstm.py (line 98: standalone, no env var)
  - scripts/train.py (line 301: lstm_batch_size hardcoded)
  - memory/rounds/R66/verdict.md
tags: [finding, q-0013-closure, architectural-unreachability, lstm-hyper-decoupled, no-axis-helps]
metric:
  name: r57_alpha_today_reproduction
  value: 0.4259
---
