---
id: CLM-0136
type: decision
trust: S
status: current
statement: |
  R77 — R76 review follow-up. 2 个 TDD cycle (有行为变化) + 7 个 lint 直接改.
  零 6-axis 数值变化, 206/206 pytest pass.

  ### TDD-1: H1 `is_recurrent` 显式契约

  - `BaseAgent` Protocol (`src/andes_rl_kundur/agents/base_agent.py`) 加 `is_recurrent: bool` 声明 + docstring 说明 train loop 读它
  - `_SACBase` (`src/andes_rl_kundur/agents/sac_base.py`) 加 `is_recurrent: bool = False` class attr (SACAgent / SACAgentCTDE / TD3Agent 自动继承)
  - `td3_lstm.py` 已有 `is_recurrent: bool = True`
  - `train.py:540` 删 `getattr(ag, "is_recurrent", False)` fallback → 直接 `ag.is_recurrent`
  - 3 new tests (test_sac_shared_base.py): `_SACBase` / `SACAgent` / `SACAgentCTDE` / `TD3Agent` 各 `is_recurrent is False`; `TD3LSTMAgent.is_recurrent is True`

  ### TDD-2: H6 `TraceScore.summary` bar width 恒等

  - `paper_grade_axes.py:175` 改用 `round(score * W); dots = W - hashes` 替代 legacy `int(score*W) + int((1-score)*W)`
  - Legacy formula 在两 `int()` 都向下截断时给 W-1 (e.g. score=0.333 → 6+13=19; score=0.875 → 17+2=19)
  - 12 new parametrize tests (test_paper_grade_axes_summary.py): score ∈ {0, 0.1, 0.25, 0.333, 0.5, 0.6, 0.7, 0.875, 0.999, 1.0} + 端点 + half
  - 纯 print 格式, 不动 6-axis 数值; 25/25 ranker test 仍通过

  ### Lint / style 直接改 (7)

  | # | 文件 | 改动 |
  |---|------|------|
  | F1 | scripts/eval_ensemble.py | 删 unused `ensemble_action` import + back-compat alias |
  | F2 | scripts/train.py:554 | `_save_checkpoint(coordinator: CTDECoordinator \| None)` |
  | F3 | paper_grade_axes.py:75-76 | `os` / `sys` import 移回 `__main__` block |
  | H2 | train.py:674 | `[l for l in ...]` → `[loss for loss in ...]` (E741) |
  | H3 | paper_grade_axes.py:393 | `Optional[float]` → `float \| None` (UP045) |
  | H4 | train.py:267, 353, 514 | 3 处 quoted `"CTDECoordinator \| None"` 删引号 (UP037) |
  | H5 | eval_all_seeds.py:135 | 删空 f 前缀 (F541) |

  ### 不动

  - `paper_strict_eval.py:159` quoted `"V4Config | None"` — V4Config 未 import (避循环), 引号必需
  - `_NO_CTRL_PREFIXES` tuple 配合 `startswith(tuple)` API 正确, 不改 frozenset

  ### Verification

  - baseline (post-R76): 191 tests
  - 终态: **206 tests pass** (191 + 3 is_recurrent + 12 summary), ~110s
  - paper_grade_axes 25/25 ranker test 仍通过 → 6-axis 数值守恒
  - v4_env_regression 2/2 (1e-9 tolerance) 仍通过 → V4 env 不变
round: R77
provenance:
  - src/andes_rl_kundur/agents/base_agent.py
  - src/andes_rl_kundur/agents/sac_base.py
  - src/andes_rl_kundur/evaluation/paper_grade_axes.py
  - scripts/train.py
  - scripts/eval_ensemble.py
  - scripts/eval_all_seeds.py
  - tests/test_sac_shared_base.py
  - tests/test_paper_grade_axes_summary.py
  - memory/rounds/R77/verdict.md
tags: [decision, tdd, review-followup, lint, is-recurrent, bar-width]
---
