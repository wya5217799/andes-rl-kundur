---
id: CLM-0174
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  **R95-W1 (temporal ramp-up quantification, zero ANDES, builds on
  CLM-0170)**. R92 / CLM-0170 established that R72_w4 SOTA reaches
  ±1 bang-bang saturation by ~step 15 and stays there for the
  remaining 70% of the episode. R95 mines the same cached
  `r84_d2b_q_landscape_trajectory/per_step.json` for the
  **time-resolved magnitude curve** that CLM-0170 only described
  qualitatively ("smoothly slides toward the boundary"):

  Median actor ||a||_2 across 4 agents × 2 scenarios (max ||a|| = √2 ≈ 1.414):

  | step | ||a||_med | as % of max | frac_||a||<0.3 |
  |---|---|---|---|
  | 0  | **0.149** | 10.5% | 100% |
  | 1  | 0.283 | 20.0% | 50% |
  | 2  | 0.442 | 31.3% | 0% |
  | 5  | 0.809 | 57.2% | 0% |
  | 10 | 1.282 | 90.7% | 0% |
  | 15+ | ~1.38 | 97.6% | 0% |
  | 49 | 1.385 | 98.0% | 0% |

  At step 0 the LSTM-actor outputs are essentially zero (10% of max).
  The ramp-up is **monotonic and rapid** (reaches 90% of saturation
  by step 10) but the first 5 steps — the regime that dominates the
  6-axis paper-grade metric via max_df / dD_smooth / settling_time —
  are spent at < 60% of asymptotic action magnitude. The Pearson
  correlation between ||a|| and advantage Q(s, a_sota) − E[Q(s, rand)]
  is **+0.932** overall and **+0.942** in transient (step 0-2),
  consistent with "the critic also wants the actor to reach saturation
  faster, not just reach saturation eventually".

  Action-space allocation in steady-state (step ≥ 10, 320 samples):
  action[1] (ΔD damping) median = **+0.985**, std 0.014 — pinned at
  upper boundary (CLM-0170 confirms across saturation %). action[0]
  (ΔM inertia) median = +0.027, std 0.962 — bimodal at ±1 across
  agents (Kundur 2-area fingerprint, CLM-0170 axis B).

  **Mechanism refinement (refines CLM-0170 / supersedes nothing)**:
  the R72_w4 plateau composition is now (a) **action-space ceiling**
  (CLM-0170 PRIORITY 1, R94 testing) plus (b) **actor LSTM warm-up
  lag** (this claim) — the policy needs ~10 steps of LSTM hidden-state
  accumulation before output magnitude saturates, but the metric is
  dominated by the first 5 steps. Even after widening bounds, if the
  actor still takes 10 LSTM steps to ramp up, the transient response
  will not improve. R94 widen-bound predicts ceiling raised but
  transient axis (max_df) unchanged.

  Caveat: this is the same single-rollout trajectory CLM-0170 used (4
  agents × 2 scen × 50 steps). N=1 trajectory; needs replication across
  more seeds / disturbance variants. CLM-0170 already noted this caveat
  for the saturation finding; same caveat applies to the temporal
  curve.
round: R95
provenance:
  - results/r84_d2b_q_landscape_trajectory/per_step.json
  - results/r84_d2b_q_landscape_trajectory/per_step_phase_breakdown.json
  - memory/claims/CLM-0161.md
  - memory/claims/CLM-0170.md
  - memory/rounds/R95/verdict.md
tags: [plateau, mechanism, actor-rampup, lstm-warmup, temporal-resolution, complement-to-r92, paper-narrative]
metric:
  name: r95_actor_step0_action_norm_pct_of_max
  value: 0.105
---
