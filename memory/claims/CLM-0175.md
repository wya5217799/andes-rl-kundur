---
id: CLM-0175
type: finding
trust: T
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  **R95-W1 theoretical prediction for R94 widen-bound experiment
  (theoretical = T-trust, falsifiable by R94 result)**. Given CLM-0170
  (bang-bang saturation by step 15 + 256-action discrete ceiling) and
  CLM-0174 (LSTM warm-up takes 10 steps; corr(||a||, advantage)=+0.932),
  R94's widen-action-bound training will produce one of two outcomes:

  **Outcome A (widen-bound succeeds at ceiling)**: 11-axis geo crosses
  0.40 (above SOTA 0.391). Mechanism = action-space ceiling was binding
  in steady-state; the policy can now apply ±2 ΔD damping in step 15+
  for stronger settling. But max_df axis (dominated by step 0-5) shows
  ≤ 5% improvement because the actor still ramps up via LSTM warm-up
  even with wider bounds — the new "ceiling" is hit faster but the
  ramp curve shape doesn't change. **Steady-state axes improve,
  transient axes don't.**

  **Outcome B (widen-bound caps near 0.391)**: 11-axis geo stays
  within ±2.5% of baseline. Mechanism = the actor's LSTM warm-up
  lag (CLM-0174) is the binding constraint, not action-bound width.
  Widening provides extra room the actor can't exploit fast enough.
  In this case R96+ priority pivots from "action-space" to
  "actor architecture warm-h_0" candidates: e.g. residual feedforward
  path in actor (h-free direct obs→action shortcut), or learned
  h_0 = MLP(obs_0) initialiser.

  **Why this prediction is useful**: R94 is currently running on R83-
  free WSL slot. When R94 verdict lands, the per-axis breakdown
  (max_df vs settling vs dD_smooth) will distinguish A from B. If A:
  widen-bound is sufficient, R96+ optional. If B: actor-architecture
  fix is mandatory for further gains.

  This claim is `trust: T` (theoretical) because it predicts an
  experimental result not yet observed. Falsifiable: A is "predicted
  outcome (consistent)" iff Δgeo > 0 AND Δmax_df_axis < 0.05; B is
  "predicted outcome" iff Δgeo within ±2.5%. Anything else (e.g.
  Δgeo > 0 AND Δmax_df_axis > 0.10) would partially falsify CLM-0174's
  ramp-up-lag mechanism.
round: R95
provenance:
  - memory/claims/CLM-0170.md
  - memory/claims/CLM-0174.md
  - memory/claims/CLM-0162.md
  - memory/rounds/R95/verdict.md
  - memory/rounds/R94/plan.md
tags: [theoretical, prediction, falsifiable, r94-followup, actor-warmup-vs-action-bound]
---
