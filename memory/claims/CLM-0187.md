---
id: CLM-0187
type: finding
trust: V
status: current
statement: |
  R94-W1 测试 R92/CLM-0170 "bang-bang saturation = bound-limited ceiling"
  hypothesis: R72_w4 baseline hyper + DM_MAX/DD_MAX 600→1500 (2.5×) widen,
  s54 × 75 ep × td3_lstm. 结果 **geo = 0.148** (LS1=0.107, LS2=0.205),
  Δ = -0.243 vs baseline 0.391 (-62% 大幅退化). 但 training reward 反而
  改善 (best -2 @ ep 66, final -3 vs baseline final -39), 经典
  reward-vs-eval objective-mismatch: agent 在 widened bound 下学到 |action|
  大的策略 reward 加分, 但 paper_grade_axes 6-axis 评估的 max_df / settling /
  ΔH range 在 widened bound 下被过激 action 惩罚.

  ## R92 hypothesis 部分证伪 + R93 mechanism 再确认
  R92 提议 widen-bound = PRIORITY 1, 因为 R72_w4 SOTA 是 bang-bang 256-action
  controller pinning ±1 boundary 76% of episode. 假设 "bound = ceiling".
  R94-W1 拒否该假设: widen bound 2.5× 让 training reward 改善但 eval geo
  -62%, 说明 saturation 的 root cause 不是 external bound limit.

  R93-W0b CLM-0181 给出真正 mechanism: **LSTM-cell hidden-state self-drift**
  (||h|| 0.25 → 5.0 in 50 steps even on constant-zero obs, pre-tanh logit
  |z| → 2.6, tanh saturation 38-82% steps). bang-bang 是 LSTM cell internal
  driven, **跟 external action bound 无关**. widen bound 只是把 saturation
  推到更大幅 action, 物理 effect 更激.

  ## R94 implication
  - R92 "widen bound is the fix" 路径 close (R94 negative finding)
  - R100/R101 td3_lstm_hreg path (hidden-state norm regularization) 才是
    attack root cause 的 correct fix
  - paper writeup 可加 ablation: "widen-bound × 2.5 yields -62% eval drop,
    confirming the bang-bang attractor is LSTM-driven (R93), not bound-
    limited (refutes R92 PRIORITY 1 hypothesis); hreg regularization is
    the correct attack on the underlying h-drift root"
round: R94
provenance:
  - memory/rounds/R94/plan.md
  - memory/rounds/R94/verdict.md
  - results/r94_w1_widen_x2p5_s54/final_eval_summary.json
  - results/r94_w1_widen_x2p5_s54/training_log.json
  - results/r94_w1_stdout.log
  - results/r72_w4_lstm_tau001_warmup5_s54 (baseline reference)
  - memory/claims/CLM-0170.md (R92 bang-bang finding)
  - memory/claims/CLM-0181.md (R93-W0b LSTM h-drift mechanism)
tags: [finding, R94, widen-bound, negative-finding, R92-hypothesis-refuted, R93-mechanism-confirmed, plateau, action-saturation]
metric:
  name: r94_w1_widen_x2p5_geo_minus_baseline
  value: -0.243
  unit: dimensionless
  scenario: R72_w4 hyper + DM/DD_MAX=1500 (2.5×), s54 × 75 ep, deterministic eval LS1+LS2
  source: results/r94_w1_widen_x2p5_s54/final_eval_summary.json
---
