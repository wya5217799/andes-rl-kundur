---
id: CLM-0060
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R52-α: Time-in-observation probe (`INCLUDE_TIME_OBS=1`, appends
  normalized episode progress `step_count / STEPS_PER_EPISODE` to
  each agent's obs, OBS_DIM 7 → 8) **does NOT unlock temporal
  variation**. 3-seed sweep at TD3 norm 75ep h=64 (seeds 49/50/51):
  - mean 6-axis = **0.270** (range [0.196, 0.314]) — **−19 %**
    below R48-β baseline (0.334)
  - per-agent dM_span %: 9.6 mean (vs R48-β 9-21 %) — unchanged
  - per-agent dD_span %: 5.0 mean (vs R48-β 11-13 %) — slightly lower
  - dH_util axis: 0.037 (vs baseline 0.040) — essentially unchanged
  - dD_util axis: 0.036 (vs baseline 0.100) — slightly lower
  - max_df axis: 0.77 (vs baseline 0.97) — some frequency degradation
  - settling axis: 0.64 (vs baseline ~0.7) — slightly worse;
    s49/s50 hit 0.86/0.94 settling but s51 collapsed to 0.11
  Mechanism: Adding `t/T_ep` to obs gives the policy explicit phase
  info, but does not FORCE the policy to use that info. The reward
  landscape still favours static setpoints (any time-varying action
  pays frequency-control cost while gaining no additional reward).
  TD3's deterministic policy continues to converge to near-static
  setpoint despite having trajectory-phase access. The 19 % drop is
  attributed to the extra obs dim adding optimization noise without
  signal.
  Together with CLM-0057 (R03 obs aug -21 %), CLM-0058
  (anti-smoothness reward -67 %), and CLM-0059 (SAC h=64 -68 %),
  this completes the FOURTH consecutive negative finding for the
  temporal-flatness bottleneck. **Confirms structural ceiling at
  0.334 / 0.365 (single-seed) / 0.351 (ensemble) for the current
  TD3 + V4 + decentralized + h=64 + normalized paradigm.**
  Remaining levers (none cheap or guaranteed):
  - windowed-horizon smoothness reward (~30 min impl, hijack-resistant)
  - LSTM recurrent actor (~1 day, structural temporal variation)
  - curriculum disturbance magnitude (~2-3 hr impl, harder env forces wider actions)
  - shared-actor parameter sharing (~10 min via --warmstart-shared)
round: R52
provenance:
  - results/research_loop/r52_alpha_timeobs.json
  - results/td3_norm_h64_timeobs_s{49,50,51}/agent_*_best.pt
  - logs/r52_timeobs_s{49,50,51}.log
  - results/research_loop/eval_v4_baseline/td3_norm_h64_timeobs_s{49,50,51}_load_step_{1,2}.json
  - src/andes_rl_kundur/env/andes/base_env.py (INCLUDE_TIME_OBS env var, obs slot append)
  - src/andes_rl_kundur/env/andes/andes_vsg_env_v4.py (V4Config.include_time_obs late-enable)
  - src/andes_rl_kundur/env/andes/v4_config.py (include_time_obs field + post-init mutex check)
  - scripts/train.py (obs_dim_with_optional_action supports both probes)
  - memory/rounds/R52/verdict.md
tags: [finding, time-in-obs, obs-augmentation, td3, h64, structural-bottleneck-quad-confirmed, negative-result]
metric:
  name: 6_axis
  value: 0.270
---
