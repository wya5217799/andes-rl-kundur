---
id: CLM-0059
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R51-α: SAC + normalized + h=64 (the proposed cheap fix to the
  CLM-0048 capacity confound hypothesis) does NOT improve.
  3-seed sweep at seeds 49/50/51:
  - mean 6-axis = **0.1070** (range [0.094, 0.118])
  - dH_utilization: 0.002 (vs R48-β TD3 h=64 baseline 0.040)
  - dD_utilization: 0.002 (vs baseline 0.100)
  - per-agent dM_span over 6s: **0.7 %** of paper (vs 9-21 %)
  - per-agent dD_span over 6s: **0.3 %** of paper (vs 11-13 %)
  - max_df axis: 0.50 (vs 0.97 for TD3 h=64 — frequency control
    materially degraded)
  - settling axis: 0.00 (never settles)
  vs CLM-0048's SAC h=128 mean (0.117) → SAC h=64 is FRACTIONALLY
  WORSE than SAC h=128. The "capacity confound" hypothesis in
  R51 plan is refuted. SAC's structural disadvantage on V4 is
  independent of network capacity.
  Deeper finding (the key insight): SAC's entropy term provides
  **exploration variation** during training, not **policy-internal
  temporal variation** at eval. At eval the deterministic mode
  outputs the policy mean, which converges to the same static
  setpoint as TD3 — per-agent dM_span 0.7 % matches the R50
  anti-smoothness failure mode (0.5 %) and approaches no-control
  zero-action behaviour.
  Together with CLM-0057 (R03 obs probe) and CLM-0058 (anti-
  smoothness reward), this closes the THIRD cheap candidate for
  fixing the temporal-flatness bottleneck. Structural finding:
  **any deterministic-mode policy on V4 + decentralized obs +
  paper-faithful reward converges to static setpoint regardless
  of training algorithm (TD3 / SAC), regardless of obs
  augmentation (omega_dot already present, action_history added
  in R49), regardless of reward shaping (anti-smoothness in R50).
  Temporal action variation requires structural change**:
  recurrent state (LSTM actor) or windowed/episode-level reward
  that bypasses the per-step exploration-noise hijack channel.
round: R51
provenance:
  - results/research_loop/r51_alpha_sac_h64.json
  - results/sac_norm_h64_s{49,50,51}/agent_*_best.pt
  - logs/r51_sac_h64_s{49,50,51}.log
  - results/research_loop/eval_v4_baseline/sac_norm_h64_s{49,50,51}_load_step_{1,2}.json
  - scripts/_r51_score_sac_h64.py
  - memory/rounds/R51/verdict.md
tags: [finding, sac, hidden-64, normalized, capacity-confound-refuted, deterministic-eval-setpoint, structural-bottleneck]
metric:
  name: 6_axis
  value: 0.107
archived_provenance: true
---
