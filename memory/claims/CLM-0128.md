---
id: CLM-0128
type: decision
trust: S
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R74 — score_run.py upgraded to **dual-eval default**: outputs cum_rf
  (paper-metric) alongside 6-axis v3.1 geo per scenario AND in aggregate
  summary. Implemented via TDD with 3 new unit tests; 7/7 tests pass.

  ### Motivation

  Prior workflow required 2 separate scripts:
  - `score_run.py` → 6-axis v3.1 only (LSTM canonical eval)
  - `_r58_paper_strict_eval.py` → cum_rf paper-metric only (TD3/SAC SOTA)
  - `_r70_eval_matrix.py` → both, but manual invocation

  Each ckpt eval requires both numbers for paper writing. Dual-script was
  error-prone (sometimes 1 forgotten).

  ### Implementation

  `scripts/score_run.py`:
  - `aggregate_scores`: +3 optional keys (`mean_cum_rf`, `min_cum_rf`,
    `max_cum_rf`) when records contain `cum_rf`. Backwards-compat preserved
    via `if cum_rfs:` guard.
  - `score_seed`: also calls `compute_global_cum_rf(rec)` per scenario,
    returns `cum_rf` (total) + `cum_rf_LS1` + `cum_rf_LS2`.
  - `_main` CLI: prints cum_rf alongside geo per seed + per summary.

  ### TDD cycle (3 tracer bullets)

  1. RED: `test_aggregate_scores_computes_mean_cum_rf_when_present` (KeyError)
     GREEN: add `cum_rf` aggregation guarded by `if cum_rfs:`
  2. RED: `test_aggregate_scores_omits_cum_rf_keys_when_records_lack_field`
     GREEN: already covered by guard
  3. RED: `test_aggregate_scores_partial_cum_rf_records_use_only_present`
     GREEN: already covered (uses `if "cum_rf" in rec`)

  ### Smoke test (R74 W4 s57 ckpt)

  CLI output:
  ```
  [score_run] scoring results/r74_w4_lstm_tau001_warmup20_s57 -> r74_w4_..._s57
    LS1=0.0000  LS2=0.4217  geo=0.0649  cum_rf=-0.0875

  === r74_w4_lstm_tau001_warmup20_s57 (1-seed) ===
    geo: mean=0.0649  range=[0.0649, 0.0649]  std=0.0000
    cum_rf: mean=-0.0875  range=[-0.0875, -0.0875]
  ```

  Both metrics in single invocation. ✓

  ### Test results

  - 7/7 score_run unit tests pass
  - 21/21 v3+v3.1 paper_grade_axes tests pass
  - All prior 150 tests still pass (zero regression)

round: R74
provenance:
  - scripts/score_run.py
  - tests/test_score_run.py
  - memory/rounds/R74/verdict.md
tags: [decision, dual-eval-default, tdd, score-run-enhancement, paper-metric-and-6axis-together]
---
