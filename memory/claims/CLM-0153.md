---
id: CLM-0153
type: finding
trust: V
status: current
statement: |
  R84-W2 (D2 Q-landscape diagnostic, zero-ANDES forensics on the
  r72_w4_lstm_tau001_warmup5_s54 SOTA ckpt): the centralised double-Q
  critic is **decoupled from the deterministic actor** on synthetic
  obs drawn from a N(0, I) prior. Across 4 agents × 200 prior obs ×
  100 random action samples:
  SOTA-action advantage median A(s)=Q(s,a_sota)-mean_a~U Q(s,a) =
  **+0.0057** (q_sota mean magnitude reference 0.0634, so advantage
  is ~9% of |Q|), positive-fraction 91.1% — actor's action is
  **barely** above the random baseline.
  argmax_a Q distance to a_sota = **1.12 L2** (action ∈ [-1, 1]^2,
  max possible distance ≈ 2.83) — argmax is ~40% of action diameter
  away from a_sota.
  Q1/Q2 disagreement median = 0.046 ≈ **73% of |Q_sota|** —
  twin-critic epistemic uncertainty is on the same scale as Q itself.
  best-random Q − sota Q = 0.043 — a sampled random action beats
  a_sota by 68% of |Q_sota|.
  1-D action-axis sweep visualisation (action_sweep.png) confirms
  Q(s, a) is **near-linear monotone** along both action dimensions
  over [-1, 1] for all 4 agents — critic argmax saturates at the
  boundary ±1, never at the actor's interior a_sota.
  Pass criterion (plateau NOT from critic): **FAIL** (advantage
  marginal, argmax_dist > 0.5, Q landscape monotone). Caveat:
  prior obs is OOD vs SOTA training distribution; W3 follow-up
  uses trajectory-scale proxy obs (σ=0.77) and finds identical
  pathology, but on-manifold ANDES-trajectory variant (Q-0018)
  still required for final closure. Prior version of this claim
  was lost during a R79/R84 parallel-session CLM-ID race; current
  ID is the canonical record.
round: R84
provenance:
  - scripts/r84_d2_q_landscape.py
  - scripts/r84_d2_sweep_viz.py
  - results/r84_d2_q_landscape/summary.json
  - results/r84_d2_q_landscape/action_sweep.png
  - results/r72_w4_lstm_tau001_warmup5_s54/agent_0_best.pt
  - memory/rounds/R84/verdict.md
tags: [plateau, critic, actor-critic-decoupling, r72_w4, sota, diagnostic, q-landscape]
metric:
  name: q_landscape_advantage_median
  value: 0.0057
---
