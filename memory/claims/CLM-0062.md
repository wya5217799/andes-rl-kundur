---
id: CLM-0062
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R55-α: Windowed-horizon anti-smoothness reward (SMOOTHNESS_WINDOW=10,
  telescoping diff `(a[t] - a[t-10])²` instead of per-step `(a[t] -
  a[t-1])²`) at TD3 norm 75ep h=64 LAMBDA_SMOOTH=-100 **does NOT
  break the exploration-noise hijack** documented in CLM-0058 (R50).
  3-seed sweep, same seeds (49/50/51):
  - mean 6-axis = **0.1104** (range [0.105, 0.118]) — **identical
    to R50 (W=1) result 0.110**
  - train reward: +89 to +125 per agent (R50: +91; same hijack
    pattern)
  - dM_span at eval: 0.5 % (R50: 0.5 %)
  - dD_util: 0.001 (R50: 0.001)
  - max_df axis 0.45 mean (vs baseline 0.97)
  - settling axis 0.00 (frequency control completely failed)
  - dH/dD smoothness ~0.99 (actor near-constant, max smooth)
  The hijack-resistance hypothesis (R50 verdict point (b))
  REFUTED. Mechanism corrected:
  - Per-step noise variance contribution to (a[t] - a[t-W])² is
    **2σ² REGARDLESS of W** because both noise terms are
    independent draws.
  - Policy-driven signal would scale with W only IF the deterministic
    policy actually drifted over W steps. It doesn't — the actor
    converges to a constant deterministic output during training
    because the noise-induced reward channel makes any policy
    output yield similar Q-value, collapsing the gradient signal
    to actor.
  - Window size W is therefore irrelevant; the hijack is structural
    to per-step action-change reward terms in the presence of
    Gaussian exploration noise.
  Together with CLM-0057/0058/0059/0060/0061 this completes the
  SIX-failure hexagon on temporal-flatness bottleneck. The
  remaining theoretical levers (none cheap, all require
  architectural changes):
  - Smoothness reward computed on deterministic policy MEAN, not
    noise-augmented executed action (~1-2 hr actor-env interface
    refactor)
  - Sparse end-of-episode reward = std(action over episode)
    (~30 min impl but TD3 sparse-reward learning is unreliable)
  - LSTM recurrent actor — policy structurally time-varying
    (~1 day impl)
  - Curriculum disturbance — force action via harder env (~2-3 hr)
  - True parameter sharing across agents (~1-2 hr actor wrapper)
round: R55
provenance:
  - results/research_loop/r55_alpha_winsm.json
  - results/td3_norm_h64_winsm_s{49,50,51}/agent_*_best.pt
  - logs/r55_winsm_s{49,50,51}.log (train reward magnitudes documented)
  - results/research_loop/eval_v4_baseline/td3_norm_h64_winsm_s{49,50,51}_load_step_{1,2}.json
  - src/andes_rl_kundur/env/andes/base_env.py (telescoping diff in r_smooth section, deque init)
  - src/andes_rl_kundur/env/andes/andes_vsg_env_v4.py (cfg.smoothness_window + cfg.lambda_smooth conditional override)
  - src/andes_rl_kundur/env/andes/v4_config.py (smoothness_window field)
  - memory/rounds/R55/verdict.md
tags: [finding, windowed-horizon, smoothness-reward, td3, h64, hijack-resistance-refuted, structural-bottleneck-hexagon, negative-result]
metric:
  name: 6_axis
  value: 0.110
---
