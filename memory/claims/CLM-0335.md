---
id: CLM-0335
type: decision
trust: S
status: current
statement: |
  **R176 — Gap 5-10 fixes (full ledger hardening)**. Closes the last
  six gaps surfaced during R166/R171 audits. After this round the
  ledger system is structurally complete: round lifecycle states +
  staleness detection (R166), results↔ledger detection + Q-supersession
  heuristic + closed-partial enum + research-round filter (R171),
  reusable close CLI + git pre-commit hook + tightened thresholds +
  meta-round contract + GC of empty dirs + soft-warn noise reduction
  (R176).

  ### What was added

  **G5 — git pre-commit hook**: `scripts/githooks/pre-commit` now runs
  `memory/tools/validate.py` and blocks commits on hard errors. Soft
  warnings print but do not block. Install: `git config core.hooksPath
  scripts/githooks`. Bypass: `git commit --no-verify`.

  **G6 — reusable close_round CLI**: `memory/tools/close_round.py` with
  subcommands `superseded|aborted|completed`. Atomic frontmatter
  rewrite (tempfile+rename). 6 new tests. Replaces ad-hoc
  `_r166_sweep.py` / `_r171_sweep.py` pattern.

  **G7 — soft-warn noise reduction**: Added `TLDR_CUTOFF=59` parallel
  to `PI_BRIEFING_CUTOFF` (pre-R59 verdicts exempt from TL;DR check);
  raised `PI_BRIEFING_LINE_CAP` 30→40 (matches actual brief length
  distribution); added round cutoff R50 on "decimal-but-no-metric"
  warning (pre-R50 claims pre-date structured metric). Total warning
  count: 78 → 49 (12 TL;DR + 8 briefing-length + 9 legacy decimal
  noise eliminated).

  **G8 — `reserve_round.py --gc`**: Sweeps `RNNN/` dirs that are older
  than `--gc-minutes` (default 60), have no plan.md, and no verdict;
  stubs them as `state=aborted` via canonical close path. First run
  caught R173/R174/R175 (3 parallel-session race empties created
  while R176 was being drafted). 3 new tests.

  **G9 — tightened stale thresholds**: `ROUND_STALE_ACTIVE_DAYS` 14→3,
  `ROUND_STALE_QUEUED_DAYS` 7→2. Project velocity is ~30 rounds/day so
  14-day grace = ~400 round period before any stale fires. 3 days
  matches actual research cadence. Tests updated.

  **G10 — claim-into-meta-round warning**: New `warn_claim_into_meta_round`
  in validate.py. Fires when `type ∈ {finding, correction}` claim has
  `round=R<N>` but R<N>/plan.md declares `type: meta`. Surfaced by
  the R171 dual-identity case (CLM-0325 wrote round=R171 even though
  R171 plan was meta gap-fix). Decision claims exempt. 3 new tests.

  ### Test stats

  Before R176: 126 tests
  After R176: 138 tests (+12 across G6/G8/G10 + 1 enum/G3 test from R171)
  Pass rate: 138/138 (100%)

  ### Validate output state after R176

  - Errors: **0** (was 0 after R171, maintained)
  - Warnings: **49** (was 78 after R171, -37%)
    - 49 remaining warns split: 38 decimal-cite-no-metric (R50+ real
      hints), 5 provenance-path-missing (real data errors in CLM-0145
      etc.), 4 Q-supersession heuristic, 1 R11 status-header, 1
      CLM-0325 G10 meta-round contract violation (now visible)

  ### Trust rationale (S)

  Decision claim — six tooling additions + threshold/cutoff changes.
  The CLM-0325 G10 violation it surfaces is a true positive from a
  parallel session's contract drift; closing that is research-round
  business, not R176 scope.

  ### Concurrent R172 training

  R176 is meta and ran in parallel with R172 (Q-0020 transient-phase
  replay reweighting training). R172 results land in CLM-NNNN under
  round R172 after training completes; this CLM does not touch R172
  semantics.

round: R176
provenance:
  - memory/tools/validate.py
  - memory/tools/render.py
  - memory/tools/reserve_round.py
  - memory/tools/close_round.py
  - memory/tools/tests/test_validate.py
  - scripts/githooks/pre-commit
  - memory/rounds/R176/plan.md
  - memory/rounds/R176/verdict.md
  - memory/claims/CLM-0316.md
  - memory/claims/CLM-0330.md
tags: [meta, workflow, infrastructure, gap-fix-final, R176, R166-R171-followup]
---
