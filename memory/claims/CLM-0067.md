---
id: CLM-0067
type: decision
trust: S
status: current
statement: |
  R57 decision: **escalate CLM-0064's production-candidate recommendation
  to a firm production recommendation** for the V4 paper-faithful Kundur
  4-VSG env, with two deployment modes:

  **Mode A — single actor** (paper-headline / reviewer-replication):
  - Use **R57-α s51 ckpt** (`results/td3_lstm_h64_warmup5_s51/`)
  - 6-axis = 0.543 (new V4 single-seed SOTA, +44 % over prior best
    R48-β s51 = 0.365; +56 % over production R48-β 3-seed mean 0.334)
  - Training cmd:
    `--algo td3_lstm --normalize-actions --episodes 75 --seed 51
    --hidden-size 64 --lstm-lr-warmup-eps 5`
  - Wall: ~13 min on CPU

  **Mode B — ensemble** (production deployment for variance-reduction):
  - Use **R57-β top2 HAWE-LSTM** of {s51_warmup, s53_warmup}
    with weights (0.7, 0.3)
  - 6-axis = 0.501 (new V4 ensemble SOTA, +43 % over R48-δ MLP HAWE
    median 0.351)
  - Eval cmd: `scripts/eval_ensemble.py --ckpt-dirs
    results/td3_lstm_h64_warmup5_s51 results/td3_lstm_h64_warmup5_s53
    --suffixes best best --agg weighted --weights 0.7 0.3 ...`
  - Mode B is preferred when seed-stability matters more than peak
    score (e.g., paper benchmark with multiple eval disturbances).

  **Supersession scope**:
  - This decision **supersedes CLM-0064** on the production-candidate
    framing dimension: CLM-0064 said "candidate, conditional on
    resolving Q-0005"; R57 evidence advances Q-0005 (s50 collapse
    mechanism narrowed to checkpoint-selection, not training-stability)
    and presents concrete, picking-list-ready ckpts. The conditional
    is now: gated only on Q-0007 (best-by-eval-score) for fully-robust
    deployment, but Mode A/B are usable as-is.
  - This decision does **NOT supersede** CLM-0055 or CLM-0056 on the
    numerical-value dimension — those remain valid characterizations
    of the *memoryless* policy class. R57's results characterize the
    *recurrent* class.

  **Caveats / open gates** (do NOT promote to firm recommendation
  before resolving):
  - Q-0005 (seed-variance / collapse rate): R57 narrowed but did not
    close. Current collapse rate 1/5 = 20 % is acceptable but room
    for improvement.
  - Q-0007 (best-by-eval-score): would lift s50 from 0.109 to its
    actual end-of-training value, likely > 0.30 — would push 5-seed
    mean above the 0.40 H1α threshold without retraining.
  - HAWE pool depth: current pool is 4 healthy ckpts; production
    deployment should re-evaluate ensemble configs after Q-0007
    resolution adds s50_warmup back to the pool.

round: R57
provenance:
  - memory/claims/CLM-0065.md (R57-α evidence)
  - memory/claims/CLM-0066.md (R57-β evidence)
  - memory/rounds/R57/verdict.md
  - results/research_loop/r57_alpha_lstm_warmup5.json
  - results/research_loop/r57_beta_hawe_lstm_warmup.json
  - results/td3_lstm_h64_warmup5_s51/agent_*_best.pt (Mode A)
  - results/td3_lstm_h64_warmup5_s53/agent_*_best.pt (Mode B)
tags: [decision, td3-lstm-production, hawe-lstm-production, supersession-framing-only, gated-on-q0007]
supersedes: [CLM-0064]
---
