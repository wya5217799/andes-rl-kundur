---
id: CLM-0068
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R58 Phase 0 audit findings: line-by-line audit of `V4Config` +
  `_compute_rewards` vs Yang et al., IEEE TPWRS 2023 Eq.11/15-18
  surfaces **2 layers** of deviations beyond the engineering hacks
  documented in CLM-0040 (PHI_ABS, PHI_H/D rescale).

  **Layer 1** — `critic`-agent verdict on V4 codebase as "paper-faithful":
  3 CRITICAL + 4 HIGH + 4 MEDIUM. Salient items:
  - **C1** PHI_ABS=50 reward term changes objective from sync to restoration
  - **C2** 6-axis eval composite incommensurable with paper's 50-scen cum_rf
  - **C3** 75-ep training horizon vs paper's 500-ep stabilisation
  - **H1** PHI_H/D=0.0056 rescale nullifies parameter-conservation
  - **H2** G4 GENROU M=0.1 vs paper's REGCA1 wind farm
  - **H3** rad/s obs vs Hz reward (precursor to A3 below)

  **Layer 2** — R58 audit-A (this round): 5 additional deviations from
  line-by-line read of paper Eq.11/15-18:

  | # | Severity | Status |
  |---|---|---|
  | A1 | LOW (dormant) | **Fixed**: delay-mode reward consumes `self._delayed_omega` cache when delay>0 |
  | A2 | MEDIUM (paper-ambiguous) | **Exposed**: `h_paper_interpretation ∈ {mechanical_H, andes_M}` flag (default preserves R56/R57) |
  | A3 | **HIGH** | **Fixed + new config**: `r_f_freq_units ∈ {hz, rad_per_s}` flag + `paper_strict_pure_radsec()` classmethod; r^f in rad/s ≈ 39.5× Hz value |
  | A4 | LOW (equivalent) | No fix: Eq.15 `× η_j` ≡ `if η==1: subtract` |
  | A5 | LOW (paper-ambiguous) | **Exposed**: `r_avg_scope ∈ {global, neighbor}` flag (default preserves R56/R57) |

  All defaults preserve R30..R57 behaviour bit-identically. Test
  coverage: `test_r58_audit_a_fixes.py` (8 tests) +
  `test_paper_strict_config.py` (config matrix invariants) +
  `test_paper_strict_eval.py` (Sec.IV-C formula + scenario generator).

  **Audit A3 is the materially-impactful finding**: empirically
  validated in CLM-0070 — under rad/s the r_h/r_f ratio drops from
  ~3600:1 (Hz) to ~91:1, making paper-faithful PHI=1.0 trainable for
  all 3 algorithms. R18 verdict's "PHI=1 cannot be trained" was a
  Hz-unit artifact, not a mechanism-true statement.

  **Audit A1 fix** also restores delay-mode reward consistency that
  was dormant in R56/R57 (delay=0); a future round using delay>0
  would have observed a silent reward bug otherwise.

round: R58
provenance:
  - src/andes_rl_kundur/env/andes/v4_config.py
  - src/andes_rl_kundur/env/andes/base_env.py
  - src/andes_rl_kundur/env/andes/andes_vsg_env_v4.py
  - tests/test_paper_strict_config.py
  - tests/test_paper_strict_eval.py
  - tests/test_r58_audit_a_fixes.py
  - docs/adr/0002-paper-strict-vs-paper-faithful.md
  - memory/rounds/R58/verdict.md
tags: [finding, audit, paper-strict, audit-a, hz-vs-radsec, config-flags]
metric:
  name: r58_audit_a_additional_deviation_count
  value: 5
  kind: count
---
