---
id: CLM-0110
type: finding
trust: V
status: current
statement: |
  R68 W2 — LSTM + tau=0.001 + paper_faithful (default reward) s51 single-seed pilot:
  v2 8-axis = **0.4226** (vs R57-α today 0.4259 = -0.8% noise band).

  First impression: tau alone is **noise on v2 ranker** for LSTM.

  ### v3 11-axis re-rank (R69 ranker upgrade) reveals real story

  | label | v2 | **v3** | %Δ |
  |---|---|---|---|
  | R68 W2 LSTM tau=0.001 s51 | 0.4226 | **0.5329** | **+26.1%** |

  tau=0.001 IS effective for LSTM — but on **per-agent balance + late oscillation**
  dimensions (axes 9-11), NOT on v2 8-axis (cross-agent mean).

  ### Mechanism

  - v2 axes 1-3 (frequency alignment) use cross-agent mean Δf → tau small effect
  - v2 axes 4-5 (smoothness) use cross-agent mean ΔH/ΔD std → tau small effect
  - v2 axes 6-7 (utilization) use cross-agent mean span → tau small effect
  - **v3 axes 11 (agent_P_balance) uses PER-AGENT ΔP_final** → tau enables 4-agent balance

  ### Implication for R68 design

  Round 68 used v2 as evaluation criterion. Concluded "tau=noise for LSTM" was wrong.
  R69 added v3 axes specifically to catch this class of mistake. CLM-0110 is the
  flag value that motivated ranker upgrade.

round: R68
provenance:
  - results/r68_w2_lstm_tau001_s51/agent_*_best.pt
  - results/research_loop/eval_v4_baseline/r68_w2_lstm_tau001_6axis_s51_load_step_*.json
  - memory/claims/CLM-0113.md (R69 ranker v3.0)
  - memory/rounds/R68/verdict.md
tags: [finding, lstm-tau-v2-hidden, v3-reveals-sota, ranker-upgrade-motivator]
metric:
  name: r68_w2_lstm_tau001_v3_geo
  value: 0.5329
  kind: geo
---
