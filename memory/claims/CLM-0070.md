---
id: CLM-0070
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  R58 Phase 1 training-stability result for `paper_strict_pure_radsec`
  config (PHI_ABS=0, PHI_H=PHI_D=1.0, r^f in **rad/s** — audit A3
  supplement) at 75 ep × 3 algos × 3 seeds (49/50/51):

  | Algo | s49 critic loss | s50 | s51 | Outcome |
  |---|---|---|---|---|
  | SAC | 271→131 | 259→138 | 339→119 | **all decreasing 3/3** |
  | TD3 | 178→75 | 168→63 | 364→113 | **all decreasing 3/3** |
  | TD3+LSTM | 205→127 | 108→43 | 341→128 | **all decreasing 3/3** |

  Absolute critic loss is ~40²× larger than Hz mode (driven by
  (2π·FN)² = 39.5× r^f scaling) but the **relative ratio r_h / r_f
  drops from ~3600:1 (Hz) to ~91:1 (rad/s)** — within trainable range
  for all 3 algorithms.

  **MAJOR FINDING — audit A3 empirically confirmed**: R18 verdict's
  "PHI=1 diverges" conclusion was a Hz-unit artifact, not a mechanism
  truth. Under physics-correct rad/s (matching paper §III-A's Δω
  notation), paper-faithful PHI=1.0 is trainable for SAC and TD3-MLP,
  not just TD3+LSTM. This **opens** the possibility of reproducing
  paper Sec.IV-C exactly: paper's SAC + PHI=1 + auto-α config is
  trainable in our env if we use rad/s frequency units.

  **Eval consequence** (CLM-0071): under paper metric, strict_radsec
  SAC -0.518 wins over LSTM -0.645 and TD3 -0.699 — **SAC ranking
  preserved from paper at paper-faithful conditions**.

  **Implication for project history**: the V4 historical choice
  (PHI_ABS=50 + Hz r^f) is a *different engineering solution* to the
  same r_h/r_f imbalance the paper's rad/s units already solve.
  PHI_ABS adds a non-paper restoration objective that prevents the
  actor from sitting at static setpoint (the symptom) without
  addressing the unit choice (the cause). Both are valid solutions
  on different benchmarks, but the rad/s solution is paper-faithful.

  **Note on absolute loss magnitude**: ~100-300 critic loss is large
  relative to typical RL training (~1-10). All 3 algorithms are still
  decreasing at ep 75; 500-ep training would likely reach a more
  stable regime (Q-0008 opened).

round: R58
provenance:
  - results/r58_paper_strict_pure_radsec_sac_s{49,50,51}/stdout.log
  - results/r58_paper_strict_pure_radsec_td3_s{49,50,51}/stdout.log
  - results/r58_paper_strict_pure_radsec_td3_lstm_s{49,50,51}/stdout.log
  - results/research_loop/r58_paper_metric_r58_paper_strict_pure_radsec_*.json
  - scripts/_r58_train_radsec.sh
  - src/andes_rl_kundur/env/andes/v4_config.py (paper_strict_pure_radsec)
  - tests/test_r58_audit_a_fixes.py
  - memory/rounds/R58/verdict.md
tags: [finding, paper-strict-radsec, audit-a3, radsec, training-stability, r18-unit-artifact, sac-trainable]
metric:
  name: total_cum_rf_radsec_best
  value: -0.397
---
