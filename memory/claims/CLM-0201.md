---
id: CLM-0201
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  **R109-W1 (TD3LSTMWarmH0Agent drop-in, zero ANDES, code-only)**.
  CLM-0193 (R107) provided the actor module (`WarmH0RecurrentActor`)
  but not the agent integration. R109 adds
  `src/andes_rl_kundur/agents/td3_lstm_warmh0.py::TD3LSTMWarmH0Agent`
  — extends `TD3LSTMAgent` with 4 override methods (`__init__`,
  `select_action`, `select_action_recurrent`, `update`) that thread
  `obs_for_warm` to `actor.init_hidden(...)`. Total diff: 0 lines
  changed in existing files; 1 new file (~230 lines).

  Override scope:
  - `__init__`: super() builds vanilla actor with RecurrentActor;
    then replace `self.actor` / `self.actor_target` with WarmH0
    variants + rebuild `actor_optimizer`. Critics untouched.
  - `select_action`: pass `obs_t` to init_hidden when episode starts
    (when `_h_rollout is None`).
  - `select_action_recurrent`: same for stateless eval scripts.
  - `update`: at burn-in start, pass `obs[:, 0]` to actor init_hidden,
    `next_obs[:, 0]` to actor_target init_hidden. Critic init still
    uses zeros (matches CLM-0183 / CLM-0188 forensics scope:
    actor-side warm-up only).

  Smoke test: `TD3LSTMWarmH0Agent(obs_dim=7, action_dim=2,
  hidden_sizes=64)` instantiates correctly; algo_name = `td3_lstm_warmh0`;
  actor type = `WarmH0RecurrentActor` with `h_init` MLP head; 6-step
  rollout produces non-NaN actions (||a|| ~ 0.13 at random init, will
  rise after training).

  Integration surface for R96 launch (NOT applied yet):

  | File | Change | LOC |
  |---|---|---|
  | `scripts/train.py` | `--algo td3_lstm_warmh0` dispatch in agent factory | ~5 |
  | `src/andes_rl_kundur/agents/checkpoint_loader.py` | `elif algo == "td3_lstm_warmh0":` | ~5 |
  | (everything else) | unchanged | 0 |

  Both edits are mechanical 5-line dispatches identical to the existing
  td3_lstm / td3_transformer branches.

  **R96 launch readiness checklist**:
  - [x] Architectural feasibility confirmed (R99, R104, R107-W2)
  - [x] Actor module implemented + unit-tested (R107-W1, CLM-0193)
  - [x] Agent class implemented + smoke-tested (R109-W1, this claim)
  - [ ] train.py dispatch (5-line edit, R96 phase)
  - [ ] checkpoint_loader.py dispatch (5-line edit, R96 phase)
  - [ ] WSL training run (1 seed × 75 ep, ~30 min)
  - [ ] 11-axis eval vs R72_w4 baseline

  Parallel-write race note: an in-flight session reserved CLM-0194
  before this write landed. CLM-0201 replaces the originally-intended
  CLM-0194 slot for this R109 claim. No content overlap; verified
  CLM-0194 belongs to R110.
round: R109
provenance:
  - src/andes_rl_kundur/agents/td3_lstm_warmh0.py
  - src/andes_rl_kundur/agents/networks_warmh0.py
  - memory/claims/CLM-0193.md
  - memory/claims/CLM-0188.md
  - memory/claims/CLM-0183.md
  - memory/rounds/R109/verdict.md
tags: [code, q-0022, warm-h0, drop-in-ready, agent-integration, r96-prep]
metric:
  name: r109_warmh0_agent_smoke_steps
  value: 6
  kind: count
---
