---
id: CLM-0085
type: decision
trust: S
status: current
statement: |
  R63 — Autonomous hyper sweep (user grant). 4 waves × ~15 min wall
  each = ~3 hr total. 12 hyper variants explored on TD3 baseline.

  ### Infrastructure added

  Two env var overrides (matching `LAMBDA_SMOOTH` pattern):
  - `N_SUBSTEPS` (base_env.py): integer ≥ 1, overrides default 5
  - `MAX_GRAD_NORM` (sac_base.py): float, overrides hardcoded 1.0

  Both opt-in (default = pre-R63 behaviour). 12 existing regression
  tests still pass.

  ### Sweep results summary

  | axis | tested values | winner | lift |
  |---|---|---|---|
  | N_SUBSTEPS | 1, 3, 5, 10 | **3** | +3.6 % (+ 2× faster train) |
  | MAX_GRAD_NORM | 0.5, 1.0, 5, 10 | **0.5** | +8.4 % |
  | batch_size | 128, 256, 512, 1024 | **512** | +6.6 % |
  | combo (3 winners) | 3-seed s49/s50/s51 | combo | +24.4 % 3-seed mean |

  ### Decision rule

  Single-seed kill-switch:
  - >+5 % vs baseline → expand to 3-seed
  - within noise → discard or note for combo
  - <baseline → discard

  All 3 single-axis winners advanced to combo W4. Combo 3-seed shows
  +24.4 % over R62 baseline.

  ### Implication

  Pre-R63 default hyper config (N_SUBSTEPS=5, MAX_GRAD_NORM=1.0,
  batch=256) was **sub-optimal**. All R56–R62 trainings used inferior
  hyper. New optimal: `N_SUBSTEPS=3 MAX_GRAD_NORM=0.5 --batch-size 512`.

  ### Next-round candidates (R64+)

  Axes not yet swept:
  - lr (default 3e-4): try 1e-4, 1e-3
  - explore_noise (TD3 default 0.1): try 0.05, 0.2
  - lr-warmup on TD3 (only LSTM had warmup-5): try 5, 10
  - policy_noise (TD3 default 0.2): try 0.1, 0.4
  - SAC fixed-α vs auto-α
  - smaller hidden sizes h=32, 48

round: R63
provenance:
  - src/andes_rl_kundur/env/andes/base_env.py (N_SUBSTEPS env var)
  - src/andes_rl_kundur/agents/sac_base.py (MAX_GRAD_NORM env var)
  - results/r63_w1_*/agent_*_best_eval.pt
  - results/r63_w2_*/agent_*_best_eval.pt
  - results/r63_w3_*/agent_*_best_eval.pt
  - results/r63_w4_*/agent_*_best_eval.pt
  - memory/rounds/R63/verdict.md
tags: [decision, hyper-sweep, autonomous-mode, n-substeps-3, gc-0.5, bs-512, combo-3-seed]
---
