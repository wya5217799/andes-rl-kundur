---
id: CLM-0615
type: finding
trust: V
status: current
statement: |
  R281 (Q-0042, MECHANISM-PARTIAL): on the frozen R279/R280 V4 modified
  Kundur plant, static differential VSG inertia allocation along the frozen
  zero-sum mode q*[1,1,-1,-1] (M_i = 200+600*(0.25+q*pattern_i), q in
  [-0.25,+0.25]) materially changes the classical GENROU inter-area mode's
  small-signal damping ratio, and the increase follows the direction the
  learned controllers actually exploited.

  The tracked inter-area mode (0.52--0.56 Hz, dominated by GENROU 3+4 omega
  participation ~1.7 across the grid) has zeta(q=0)=0.01947,
  zeta(q=-0.25)=0.03023 (+55.3% vs q=0) and zeta(q=+0.25)=0.02132 (+9.5%).
  Span S = 45.8%, far above the pre-registered 5% materiality threshold.
  zeta rises monotonically as q decreases over q in [-0.25, +0.125]
  (0.01880 -> 0.03023); global monotonicity is broken only by a U-shaped
  uptick at the q=+0.25 extreme. The beneficial direction b=-1 (measured
  from mean executed q over the 144 learned-arm R279 formal trajectories,
  all-negative) matches the direction of stronger damping increase.

  The VSG-pair local modes (~0.24--0.40 Hz) split and merge across q and
  fail consistent identification (pre-registered flag); their damping
  follows the expected zeta ~ 1/sqrt(M) scaling with D fixed (area-2 pair
  at M=200: zeta=0.112; coherent mode at M=500: zeta=0.080), documenting
  the inertia-damping trade-off rather than an allocation-created damping.

  Development-only heterogeneity probe (scaling executed M by M0/200):
  allocation sensitivity grows sharply as aggregate VSG inertia shrinks
  (M0=100: zeta(0)=0.0338 -> zeta(+0.25)=0.0518, +53%; M0=300: effect
  ~0--20%; M0=100 q=-0.25 identification broke). Read: placement matters
  more in lower-inertia systems, consistent with the weak-grid axis.

  Verdict class MECHANISM-PARTIAL per the pre-registered decision tree
  (S>=5% but monotonicity broken). The SCI mechanism section is downgraded
  to a bounded empirical statement: allocation materially modulates
  classical inter-area mode damping in the learned beneficial direction,
  but the mapping is not globally monotone and VSG local modes follow
  1/sqrt(M) trade-off physics. All guards passed (G4 M=0.1/D=0 contract,
  zero-sum 1400, PFlow convergence, contract hash 11a4800123f48a33).
round: R281
provenance:
  - memory/rounds/R281/plan.md
  - memory/rounds/R281/execution_amendment_20260729.md
  - results/r281_eig_mechanism/summary.json
  - results/r281_eig_mechanism/provenance.json
tags:
  - mechanism
  - inertia-placement
  - eigenvalue
  - icems2026
  - sci-upgrade
---
