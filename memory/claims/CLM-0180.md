---
id: CLM-0180
type: finding
trust: V
status: current
suspect: true
suspect_round: R478
suspect_reason: "SUSPECT (2026-08-24): evidence produced on the pre-R478 ANDES V4 object (GENCLS M/D device/system base mix-up: runtime M/D halved, zero-action drift, halved action authority). Numbers invalid as evidence until corrected revalidation replaces them. Flags the evidence object, not the conclusion direction."
statement: |
  **R93-W0 (actor pre-tanh logit + fc_out weight forensics, zero ANDES,
  on R72_w4 SOTA) — actor weights are NOT push-the-boundary trained**;
  on N=200 prior obs ~ N(0, I) with h_actor = 0, the pre-tanh logits
  cluster deep in the tanh-linear regime, refuting "fc_out is large"
  as the cause of CLM-0170's 76% saturation finding.

  Per-agent pre-tanh logit |z| statistics (h_actor=0, prior obs):

  | Agent | median |z| | P90 |z| | frac |z|>2 | frac |z|>5 | fc_out_max_W |
  |-------|-----------|----------|------------|------------|--------------|
  | 0     | 0.121     | 0.189    | 0%         | 0%         | 0.156        |
  | 1     | 0.084     | 0.163    | 0%         | 0%         | 0.156        |
  | 2     | 0.194     | 0.265    | 0%         | 0%         | 0.153        |
  | 3     | 0.051     | 0.134    | 0%         | 0%         | 0.149        |

  Cross-agent median |z| = **0.102** (deep in linear tanh regime;
  tanh(0.1) ≈ 0.10). fc_out weight max_abs median = **0.154** (modest,
  not exploded). fc_out spectral norm median = 0.781 (well below 10×
  norm threshold). No actor agent in any prior obs sample has |z| > 2.

  **Implication**: at episode reset (h_actor = 0), the actor naturally
  outputs interior actions ≈ 0. The 76% saturation observed by R92-W1
  / CLM-0170 in real ANDES rollouts is NOT caused by fc_out projecting
  large logits — fc_out is small and outputs interior. The saturation
  must arise from **LSTM hidden state h evolution** across the
  episode, which is exactly what R93-W0b tests.

  Caveat from CLM-0160: prior obs is off-manifold. But unlike critic
  Q-landscape forensics (R84-W2/W3), this measurement is far more
  defensible: actor logits depend only on (obs, h_actor); we set
  h_actor = 0 explicitly; obs distribution affects logit *via*
  fc_out(lstm(obs, h=0)), which for a single forward step with h=0
  is well-defined and consistent across reasonable obs scales.
round: R93
provenance:
  - scripts/r93_w0_actor_logit_forensics.py
  - results/r93_w0_logit_forensics/summary.json
  - results/r93_w0_logit_forensics/logit_histogram.png
  - results/r72_w4_lstm_tau001_warmup5_s54/agent_0_best.pt
  - memory/claims/CLM-0170.md
  - memory/rounds/R93/verdict.md
tags: [plateau, mechanism, actor-fc-out, refute-actor-weight-push, R72_w4, zero-andes]
metric:
  name: r93_w0_actor_logit_median_abs
  value: 0.102
---
