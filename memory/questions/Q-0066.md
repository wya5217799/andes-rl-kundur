---
id: Q-0066
status: closed-negative
title: Can a fresh two-phase-solver Stage-1 bank execute all signed common and
  edge active-power probes across OP0--OP2 with valid authority, local
  linearity, and measured common-differential coupling?
opened_round: R309
closed_round: R310
closed_by: CLM-0760
---

## Why it matters

R309 validates the numerical execution seam only on the zero baseline and the
prior worst OP1 edge-2-negative trace. The conference title still lacks valid
evidence for signed independent edge actions and measured retained coupling,
because R307's full bank was invalid and cannot be repaired post hoc. A fresh
prospectively sealed Stage-1 bank is required before any predictor, controller,
distributed-agent comparison, or learning step.

## Candidates

- Recreate the exact R307 OP0--OP2 bank from new executions under the R309
  two-phase TDS contract: one zero trace plus paired signs for the common and
  three source-positive edge coordinates at each operating point.
- Preserve the 0.05 system-p.u. pulse, five active samples, twenty recovery
  samples, plant, graph, SOC points, all-zero M/D actions, authority/sign/SOC/
  neutrality/linearity gates, and `max(abs(g)) <= 1e-8` ceiling.
- Run EVAL-v2 on the compatible fresh edge records with the predeclared 1-s
  `vector_power` profile and `EXTERNAL_AUTHORITY_REQUIRED` status.
- Stop after fresh Stage-1 PASS, authority NO-GO, or invalid execution. A pass
  may authorize predictor construction in a later round, never training here.

## Log

- R309: opened after CLM-0755; R307 and R308 traces remain historical invalid
  diagnostics and cannot enter the fresh bank.
- R310: closed negative by CLM-0760. The 27 fresh source executions completed
  and every non-EVAL guard was true, but all 18 EVAL views lacked the required
  record-level `guards` synthesis. The conjunctive formal result is invalid, so no
  authority, coupling, decoupling, predictor, controller, agent, or training
  gate opens.
