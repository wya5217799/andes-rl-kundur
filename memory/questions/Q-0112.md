---
id: Q-0112
status: open
title: Does the finite-bank information-level margin program (shared action variables per non-anticipative info class, solution section 5.4) certify or refute INFORMATION-LIMITED for the 2% joint target under the exact R352/R353 observation histories?
opened_round: R445
closed_round: null
closed_by: null
---

## Why it matters

R445 (CLM-1385) verifies ACTION-BASIS-LIMITED: the zero-sum edge basis B_e
fails the 2% joint target in 6/16 scenarios even in the relaxed problem,
while any basis containing the fleet-equal direction (I_4, B_+) reaches
eps* >= 0.999. The external answer's proposition P5 (info aliasing
impossibility) stays design aid because the exposed bank has no exact
aliasing pairs; the representative same-channel check reports delta > rho
on all 24 pairs (min gap 0.031), which is suggestive but not a certificate.
The information-level margin program of the solution section 5.4 (exact
non-anticipative equivalence classes with shared action variables, plus the
measurement-error/Lipschitz relaxations) is the only missing computation
between the current state and an INFORMATION-LIMITED / TRAINABLE-RESIDUAL
classification for a successor line.

## Prospective contract

- Reuse the R445 extracted matrices (results/r445_gpt_pro_intake_verify/
  matrices/*.npz: G_s, H_e, H_plus, y0) and the sealed R352 observation
  histories (causal_edge_features at each edge, per R353 construction).
- Build exact info classes from identical histories; solve the shared-action
  max-eps cone program per section 5.4 of the external solution on the
  relaxed problem first, then with the registered physical contract.
- Report per-class action-set distances and the class-constrained eps*
  versus the eps*(I_4)/eps*(B_+) values; classify per the section 6.5
  layered flow. Keep holdout untouched; no training, simulation, or ANDES.
- A valid pass authorizes only a successor-line decision; it does not
  authorize training.

## Log

- R445: opened. P5 repo-side proof incomplete (exact aliasing absent on the
  bank; set-distance program not implemented). The representative check
  (24/24 same-channel pairs with delta > rho) is recorded in
  results/r445_gpt_pro_intake_verify/analysis.json#/propositions/P5.
