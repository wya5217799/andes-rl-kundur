---
id: Q-0018
status: closed-negative
opened_round: R84
closed_round: R84
closed_by: CLM-0160
title: |
  R84-D2 Q-landscape monotone-in-action pathology on prior obs —
  does it persist on the SOTA state distribution (ANDES trajectory)?
body: |
  R84-W2 (CLM-0153) measured actor-critic decoupling using synthetic
  obs ~ N(0, I). Pass criterion failed: critic argmax at action
  boundary ±1, actor a_sota interior, advantage ≈ 0. R84-W3 (CLM-0154)
  refined this with local Q curvature: d²Q/da² ≈ 0 (~2% of |Q|),
  spectral norms normal, obs-scale invariant. Caveat remains: all
  prior obs is OOD vs SOTA training distribution.

  Q-0018: does the same monotone-in-action Q landscape persist when
  obs is drawn from the actual SOTA trajectory distribution? If yes,
  R84-D2 finding upgrades to "the SOTA actor truly disagrees with its
  own critic on the on-manifold state distribution" → strong
  mechanism evidence for the R57-R82 91-round plateau. If no (Q
  landscape becomes concave around a_sota on real obs), then synthetic
  obs is unrepresentative and we need a different diagnostic.

  Resolution path (R84-W2-followup or R85-W1):
  1. Collect ~50-episode SOTA trajectory via ANDES (LS1 + LS2 each
     deterministic, no exploration noise) — saves (obs_t, h_t, a_sota_t)
     per step.
  2. Re-run r84_d2_q_landscape.py with obs sampled from this
     trajectory pool (not N(0, I)).
  3. Compare advantage / argmax_dist / grad_norm vs synthetic
     baseline. Threshold same as R84-D2.

  Blocked on: R83 ANDES WSL session release (R83-W2 in-flight as of
  2026-05-19 05:31).
tags: [plateau, critic, actor-critic-decoupling, diagnostic-followup, andes-trajectory]
---

## Log

- **R84-W2 opened**: synthetic-obs D2 (CLM-0153) flagged monotone-in-action Q
  at h=0, caveat: real-trajectory variant needed for definitiveness.
- **R84-W3 (synthetic, CLM-0154)**: deepened with high-res local curvature
  (d²Q/da² ≈ 0) and obs-scale invariance (N(0,1) vs N(0,0.77²) same result).
  Off-manifold conclusion: Q is affine in action; R85 distributional critic
  PRIORITY 1.
- **R84-W3-traj closed (2026-05-19 ~05:43, CLM-0160)**: D2b ANDES on-manifold
  variant (`scripts/r84_d2b_q_landscape_trajectory.py`, wall 20.6s, 400 probes
  across 4 agents × LS1+LS2 × 50 steps with critic h advanced along the
  actual SOTA rollout). Gate: PASS_CRITIC_CONFIDENT_ON_REAL_TRAJ. Synthetic
  monotonicity does NOT persist — argmax_dist = 13% of action diagonal,
  advantage = +120% of |Q|, best random loses to a* by −8% of |Q|, 99.75%
  positive-advantage frac. The off-manifold affine-Q pathology is an
  artefact of h_critic=0 + off-manifold obs; the LSTM hidden state does
  substantial heavy lifting in shaping the action-preference landscape.
  R85 priority pivots from critic-side (CLM-0154 ordering) to D3 obs
  sufficiency / D4 env floor / reward-shape ablation (CLM-0160 ordering).
