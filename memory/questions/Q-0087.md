---
id: Q-0087
status: open
title: Which location-dependent input dynamics explain the upstream-load
  mismatch before any bridge repair?
opened_round: R336
closed_round: null
closed_by: null
---

## Why it matters

CLM-0885 shows a valid, repeatable split: the immutable model matches both
co-located Bus14/Bus15 load channels but misses both upstream Bus7/Bus8 channels
under the same signed waveforms at development and untouched points. Numerical
rank and conservation pass, so adding more static columns without diagnosing
the waveform mismatch would preserve the wrong input meaning.

## Candidates

- Keep R316 and R336 immutable. Use R336 development records first to test
  falsifiable alternatives such as missing algebraic feedthrough,
  location-specific dynamic entrance, timing alignment, or insufficient
  reduction order. Do not select among alternatives on HS1.
- Compare each alternative through one public pure prediction seam with frozen
  units, signs, output coordinates, and waveform metrics. Require a distinct
  signature that can reject alternatives rather than another flexible fit.
- Use scratch-only development diagnosis until one mechanism question and one
  minimal repair are frozen prospectively. No new physical holdout is opened
  merely to tune the repair.
- Stop after causal discrimination or an explicit unresolved result. Do not
  execute a controller, distributed runtime, reward design, agent, training,
  EVAL, topology generalization, stability, safety, or title-result test.

## Log

- R336: opened after CLM-0885 blocked the complete physical package and bounded
  the observed split to a location association rather than a root-cause claim.
