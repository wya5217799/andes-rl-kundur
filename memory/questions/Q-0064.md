---
id: Q-0064
status: closed-negative
title: Does the R307 active-pulse algebraic-residual breach come from the TDS
  solve/readback contract, and can a prospective worst-case canary meet the
  unchanged 1e-8 gate without changing the plant or pulse?
opened_round: R307
closed_round: R308
closed_by: CLM-0750
---

## Why it matters

R307 completed all 27 traces and passed every registered authority, sign, SOC,
linearity, coupling, and EVAL diagnostic guard except execution integrity. All
24 nonzero traces exceeded the prospectively frozen `max(abs(g)) <= 1e-8`
ceiling, while all three zero baselines passed. This invalidates Stage 1 but
does not identify whether the cause is the physical actuator path, the TDS
Newton tolerance, or a stale/post-step algebraic-residual readback.

## Candidates

- Inspect the installed ANDES 2.0.0 TDS convergence and residual-update path;
  record the configured tolerance and the exact meaning/timing of `dae.g`.
- Freeze one small canary using the worst R307 condition, OP1 edge-2 negative,
  plus its zero baseline. Keep the plant, 0.05 system-p.u. pulse, 1-s active
  window, 4-s recovery, and `1e-8` algebraic gate unchanged.
- Test one implementation-faithful solver/readback contract selected before
  the canary. Do not compare multiple outcome-driven tolerances.
- If the canary passes, authorize a separate fresh full Stage-1 round. If it
  fails, retain the model/execution NO-GO and stop this path.

## Log

- R307: opened after `INVALID-STAGE1-EXECUTION`; no R307 trace, threshold, or
  classifier is amended, and the blocked coupling diagnostics remain unusable.
- R308: closed negative for progression by CLM-0750. The strict dynamic Newton
  setting reduced post-step `max(abs(g))` below `1e-8`, but applying the same
  `1e-10` tolerance to TDS initialization made both traces fail initialization
  and retain exit code 1. No fresh Stage 1 is authorized.
