---
round: R486
state: completed
manuscript_line: yang-md-decoupling-marl
opened: '2026-08-31'
closed: '2026-08-31'
supersedes_rounds: []
superseded_by_round: null
abort_reason: null
superseded_note: null
---
# R486 plan — durable post-hoc intake for R485 paper evidence

**Opened**: 2026-08-31
**Driver**: Preserve the useful sealed-data analyses produced in recent project conversations so the R485 writer can find them without treating chat or `tmp/` as authority.
**Parent**: CLM-1525; R485 remains closed and unchanged.

## TL;DR

No new ANDES, training, evaluation, seed, threshold, or manuscript prose. Verify,
package, and register selected R485 post-hoc derivatives in a separate feed. Keep
precision diagnostics supplemental and mechanism probes exploratory.

## Snapshot at plan-time (oracle as of 2026-08-31)

<!-- Auto-injected by reserve_round.py (F4 audit 2026-05-19). -->
<!-- Do not delete — re-render-render.py STATE.md if you want to -->
<!-- refresh, but keep this block as the plan-time snapshot. -->

## Open Questions

- Q-0112 [opened R445] Does the finite-bank information-level margin program (shared action variables per non-anticipative info class, solution section 5.4) certify or refute INFORMATION-LIMITED for the 2% joint target under the exact R352/R353 observation histories?

## Recently Closed (last 3)

- Q-0026 closed-negative @ R443, by CLM-1375 — Will the Archive Index actually be queried (lazy-extraction loop signal)?
- Q-0004 closed-negative @ R442, by CLM-1370 — AndesBaseEnv absorb-into-V4 (complete AD-01) — verify 1e-9 bit-identical from WSL before landing
- Q-0111 closed-negative @ R397, by CLM-1130 — Do one-device-at-a-time signed Pref and Qref steps on the two-unit PPVSM1 diagnostic cell produce correctly applied, signed, target-attributed active and reactive power responses without solver or electrical-guard failure, thereby opening only a separately registered droop-slope matching verification?

## Methodology

### Frozen inputs

- R485 formal decision, sidecar, training manifests, and sealed traces remain
  read-only under `results/research_loop/r485_60hz_source_factorial/`.
- Candidate derivatives predate R486 and are explicitly post-hoc:
  `r485_offline_extension`, `r485_prepaper_audit`, root-cause probes 01--10,
  and `r485_root_cause_synthesis` under `tmp/yang-md-decoupling-marl/`.
- Conversation text is discovery metadata only. Numeric intake requires a
  repository artifact, reproducible script/result, hash check, and R485 source
  locator.

### Intake slices

1. Promote the offline precision/Pareto/M-D package with its script, result JSON,
   CSVs, figures, report, and hashes. Preserve the exact post-completion status;
   do not call it preregistered equivalence.
2. Promote the paper-audit recomputation package as validation provenance, not
   as a second R485 verdict.
3. Promote the root-cause scripts/results and one synthesis. Only the synthesis's
   narrowed statements may enter the feed; probe 03's
   `CHANNEL_SCALING_PRIMARY_CAUSE_SUPPORTED` label is excluded as overstrong.
4. Aggregate only stable paper-useful facts: confidence-bound disposition,
   Pareto/non-action-clean count, channel-level command-activity scale,
   threshold distance, training alpha-floor disclosure, and bounded mechanism
   diagnostics.
5. Preserve the negative-results literature note separately as non-authoritative
   writing leads; every paper citation still requires source recheck.

### Verification

- Recompute SHA-256 for every promoted file and create sidecars for every
  conclusion-carrying JSON.
- Re-run the durable intake checker against R485 and the promoted packages;
  reject missing files, sidecar mismatch, non-finite values, count drift, or a
  stronger causal/equivalence/hardware label.
- Recheck 208 training manifests for `alpha_at_floor`, validity, and TDS count.
- Audit the same canonical R486 feed first for evidence binding and then for
  power-system/statistical scope before finalizing the claim.

## Gate

- `ENTER-POSTHOC`: all hashes and independent recomputations agree; the feed
  clearly separates R485 formal results, post-hoc precision, and exploratory
  mechanism diagnostics.
- `QUALIFY`: a useful result is reproducible but only as a narrower supplemental
  or Discussion statement; record the narrower wording and stay-out boundary.
- `STAY-OUT`: unresolved hash/schema/recompute disagreement, label overreach, or
  a result whose only source is conversation prose.
- Any new trajectory, changed threshold, equivalence claim, closed-loop causal
  claim, physical-harm claim, or modification of R485 authority aborts this
  intake rather than expanding it.

## 资产保护契约

- Unchanged: R485 plan/seal/formal root/feed/claim/verdict; R478--R485 evidence;
  all manuscript files; title; no process control or external writes.
- Added only: R486 plan/results/probes/feed/claim/verdict, MANIFEST row, current
  line pointer, `WRITING.md` intake pointer, artifact hashes, and gate log row.
- Existing owner/uncommitted `LINE.md`, `ARTIFACTS.json`, and `WRITING.md` edits
  are preserved and extended; no unrelated dirty files are reverted.

## Cross-references

- Parent: CLM-1525 -> `paper/yang_md_decoupling_marl/reports/R485.md`.
- R486 does not supersede CLM-1525; it registers derivative precision and
  exploratory diagnostics with a lower claim ceiling.
