# R278 analysis-only numerical repair

The first sealed analysis remains immutable and classified `INVALID` with
summary SHA-256
`d0abf23e9d8fb6b69f98970272ed2a476b64f501b4270e0a3a4dc7097230e056`.
Its sole failed validity guard was `physical_zero_sum`.

The executed normalized actions are float32. At the maximum physical inertia
of 500, one float32 ULP is `3.0517578125e-5`, while the original action audit
used an absolute threshold of `1e-8`. The observed maximum absolute four-device
physical residual sum was exactly one ULP, so the old threshold tested
representation noise rather than the zero-sum control contract.

The repair is restricted to a four-ULP absolute sum tolerance:

`4 * spacing(float32(500)) = 1.220703125e-4`.

No trajectory, endpoint, paired bootstrap result, storage guard, tail guard,
checkpoint, training setting, or decision threshold may change. The original
`INVALID` artifact is retained. The amended decision must be generated by
`scripts/repair_r278_physical_zero_sum_audit.py`; it is acceptable only if the
same frozen endpoint contrast classifies `PILOT-NO-GO`. A positive
reclassification is forbidden and would require a new prospective round.
