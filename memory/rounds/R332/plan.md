---
round: R332
state: completed
opened: '2026-08-04'
closed: '2026-08-04'
supersedes_rounds:
- R331
superseded_by_round: null
abort_reason: null
superseded_note: null
---
# R332 plan - publication-safe ANDES bridge reconciliation correction

## TL;DR

Repeat only the static Q-0084 reconciliation after R331 failed publication
review. Preserve R331 and all earlier sealed assets. Correct the evidence
checker, exact source locators, red-test inventory, and deterministic artifact
reproduction; then reissue one source-bound `ALLOW`, `QUALIFY`, or `BLOCK`
judgment. Run no ANDES trajectory, controller, distributed runtime, training, or
EVAL.

## Authority and workload

- Direct question: `memory/questions/Q-0084.md`; R331 remains an aborted
  diagnostic and cannot answer it.
- Scientific authority: the selected manuscript `LINE.md`, model contract,
  CLM-0860, R330 feed/results, and official ANDES 2.0.0 definitions.
- Workload: `evidence`, because the corrected result may dispose Q-0084 and
  authorize or block a later physical experiment.
- Research Supervisor route: reuse the bounded official-source findings already
  obtained for the same question; no new literature landscape is needed.
- Ask Matt route: `diagnosing-bugs`; use the R331 publication-audit failures as
  exact red tests before implementing the correction.

## Methodology

### Frozen correction scope

- Create R332-specific checker, adapter, tests, seal, and result paths. Do not
  edit any R331 sealed source or artifact.
- Retain the same ten mapping categories and the same prospective decision tree
  from R331. No scientific threshold, model, algorithm, case, title, or
  classification rule may change.
- Correct every source-line locator identified by the R331 evidence audit.
  Resolve repository files and declared lines, traverse every JSON pointer to a
  real target, require syntactically versioned primary-source URLs, pin the
  installed GENCLS, ESD1, PVD1, TDS, limiter, and group-API source hashes, and
  independently audit the semantic relevance of the cited locations.
- Preserve the domain corrections: wrapper-owned exact-event sampling and
  one-sample delay; estimator return input derived from the externally projected
  node command; internal system-base `Psum > -pmx + epsilon` or exact lower-bound
  behavior; no assumption of an internal ramp, one-step-energy projector, or
  hard SOC clamp; achieved power from `v * Ipout_y`; and no transfer of R329 to a
  distinct disturbance channel without prospective equivalence.

### Mandatory red tests

- Demonstrate failure for each missing inventory row; blank meaning, unit,
  base, sign, sample time, or claim consequence; unresolved repository file or
  line; unresolved JSON pointer; unversioned/nonprimary official URL; unpinned
  installed source; false installed semantic fact; hidden M/D write; requested
  value used as achieved power; wrong action sign; over-ceiling platform claim;
  any forbidden execution/training flag; and nondeterministic replay.
- Exercise the detector inputs, not only classifier booleans: source mutation
  or monkeypatch tests must make the static detector reject a hidden M/D write,
  requested-as-achieved substitution, reversed incidence, and over-ceiling
  platform wording. Mutate each newly pinned installed source/fact family and
  require identity rejection.
- Run two independent temporary prepare/analyse passes with the same injected
  installed identity and one prospectively frozen creation timestamp as an
  explicit test input. Seals, analyses, provenance, and manifests must then be
  byte-identical and payload hashes must match. Create-only behavior must reject
  both seal and result overwrites.
- Tests must be red because the R332 modules do not yet exist, then green after
  the smallest implementation. No simulator run is needed.

## Prospective decision tree

- `INVALID-BRIDGE-RECONCILIATION`: any identity, inventory, field, locator,
  JSON-pointer, installed-source, official-source, replay, or scope guard fails.
- `BLOCK`: any load-bearing endpoint, unit/base, sign, timing, disturbance,
  initialization, or feasibility mapping remains unsupported.
- `QUALIFY`: all load-bearing mappings are implementable but one or more
  declared assumptions or platform omissions require a narrower claim and a
  frozen later executable guard.
- `ALLOW`: every load-bearing mapping is exact or guarded with no unresolved
  material assumption beyond the declared phasor-domain scope.

The maximum claim is a static platform/interface judgment for prospective
phasor-domain electromechanical bridge design. No performance, EMT, hardware,
stability, safety, topology, distributed-agent, coordination, learning, reward,
or title-result inference is permitted.

## Verification and stopping conditions

- Run preflight before implementation; run focused tests during the repair.
- After sealing, independently recompute source hashes, pointer resolution,
  classification, blocker/qualification inventories, and normalized two-pass
  artifact equivalence.
- Complete the evidence and power-system publication audits before registering
  a claim. Then run feed check, ledger validation, render, repository health,
  whitespace check, and the complete test suite once.
- Stop after the static judgment. A physical trajectory requires a new question
  and round. Distributed runtime, reward, agents, training, and EVAL remain
  blocked. The conference title remains unchanged.

## Asset protection

- Preserve R306-R331 plans, seals, results, feeds, claims, questions, verdicts,
  protected environments, installed ANDES, and the R329 package byte-for-byte.
- New durable assets are limited to the R332 plan, checker, adapter, focused
  tests, create-only seal/results, feed, claim, verdict, Q-0084 disposition, one
  follow-up question if justified, and selected-line navigation refresh.

## Cross-references

- `memory/questions/Q-0084.md`
- `memory/rounds/R331/verdict.md`
- `paper/decoupling_marl_model_first/reports/R331.md`
- `paper/decoupling_marl_model_first/working/model_contract.md`
- `memory/claims/CLM-0860.md`
