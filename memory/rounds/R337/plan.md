---
round: R337
state: aborted
opened: '2026-08-04'
closed: '2026-08-04'
supersedes_rounds:
- R293
superseded_by_round: null
abort_reason: User terminated the sealed run after 65 of 264 new formal traces because
  serial shard execution violated the requested small-step CPU-parallel workflow;
  partial matrix is not interpretable.
superseded_note: null
---
# R337 plan - ICEMS genuine distributed-versus-single-agent comparison

**Opened**: 2026-08-04
**Driver**: Repair the ICEMS manuscript's largest validity gap without changing
its title.
**Question**: Q-0088
**Parents**: R280/CLM-0610 scalar boundary, R292/CLM-0675 invalid genuine
distributed matrix, and aborted R293/Q-0050.

## TL;DR

Use the already implemented three-edge execution object, not a new algorithm
sweep. Compare a neighbour-only parameter-sharing edge actor against one
joint-observation actor that emits the identical three edge actions. Both use
the same selected causal edge prior, physical projection, limits, reward,
training budget, new seeds, and new formal bank. R293 code and classical
development may be reused after hash verification; every R293 learned object
and formal endpoint is excluded. The manuscript title remains byte-for-byte
unchanged.

## Snapshot at plan-time (oracle as of 2026-08-04)

<!-- Auto-injected by reserve_round.py (F4 audit 2026-05-19). -->
<!-- Kept as the immutable plan-time snapshot. -->

## Open Questions

- Q-0004 [opened R46] AndesBaseEnv absorb-into-V4 (complete AD-01) - verify
  1e-9 bit-identical from WSL before landing.
- Q-0026 [opened R260] Will the Archive Index actually be queried?
- Q-0087 [opened R336] Which location-dependent input dynamics explain the
  upstream-load mismatch before any bridge repair?
- Q-0088 [opened R337] Does the fixed-path neighbour-only distributed residual
  add guarded value and remain non-inferior to the matched single actor?

## Recently Closed (last 3)

- Q-0086 closed-negative at R336 by CLM-0885.
- Q-0085 closed-positive at R334 by CLM-0880.
- Q-0084 closed-negative at R332 by CLM-0870.

## Authority and workload

- Selected line: `paper/icems2026`. The exact protected title is
  `Decoupling-Oriented Coordination of Paralleled VSGs With Multi-Agent
  Reinforcement Learning`.
- Lane: `evidence`. R337 creates new physical training and formal trajectories
  that may change a paper claim. No manuscript LaTeX may be changed before the
  feed publication gate.
- Ask Matt work order: academic goal = make the title's multi-agent comparison
  identifiable; scientific acceptance = neighbour-only independent edge
  execution against a matched single actor; engineering blocker = R280's
  scalar action seam; authority = R337 code, tests, seals, results, feed, and
  selected-line navigation only; verification = public-seam TDD, preflight,
  formal-entry rehearsal, seals, WSL execution, publication audits, and repo
  checks; deliverable = one thin successor entry plus sealed evidence; return
  gate = formal classification, not tests alone.

## Methodology

Run one prospective matched-architecture study. First verify the existing
three-edge public interfaces and build the thin R337 successor entry. Then run
that entry's same-path rehearsal before any seal. The single formal attempt
seals and executes matched training, generates and screens a controller-blind
fresh bank, freezes the formal matrix, executes all controller cells, and
applies the unchanged R293 decision procedure once. Classical development is a
declared viewed parent; learned checkpoints and formal scenarios are new.

## Frozen causal and comparison objects

- Plant and physical layer remain the R274/R275 object used by R293: validated
  droop--PI storage, a fixed 3 s common-inertia pulse, four VSGs, and path edges
  `[(0,1),(1,2),(2,3)]`. No topology, actuator, reward, or environment change is
  authorized.
- Common prior and classical comparator are the already viewed R293 selection
  `classical_edge_full_k1`: endpoint-only absolute normalized frequency,
  rate-of-change, and power features with equal one-third weights, gain 1.0,
  and residual scale 0.5. R337 verifies but does not rerun or retune this
  development choice.
- Distributed runtime: one shared `10 -> 64 -> 64 -> 1` actor is called once
  per edge using only its two endpoint observations. The three scalar calls are
  executed directly as the three edge actions. Joint state may enter the
  centralized critic during training only. No runtime server sees the joint
  observation, combines votes, or changes the edge outputs.
- Single-agent runtime: one `20 -> 59 -> 59 -> 3` actor sees the joint
  observation and emits the same three oriented edge actions. Its actor has
  4,959 parameters versus 4,929 for the distributed actor, a difference below
  one percent.
- Both learned arms add only a bounded magnitude residual along the same fixed
  classical severity direction. They share the same three-edge-to-four-node
  zero-sum projection, common inertia, limits, slew, active window, reward,
  critic, replay, optimizer, exploration, batch, and update schedule.
- The reward and thresholds are exactly R293's precommitted values: R292 sync,
  inter-area, and edge-variation terms plus weight 0.25 on squared maximum
  rate-of-change scaled by 0.5 Hz/s. No sweep is permitted.

## Prospective independence and budgets

- R293 classical development and guard files are viewed parent development and
  must match their recorded hashes. R293 checkpoints, replay buffers, training
  diagnostics, candidate bank, screen records, formal traces, logs, and any
  partial outcome are prohibited R337 inputs.
- Freeze five new seeds `[421, 463, 509, 557, 601]`; run every seed for each
  learned architecture. No seed replacement or seed selection is permitted.
- Run 300 episodes x 15 physical steps for every architecture/seed, warmup 512,
  final checkpoint only, giving 4,500 steps per checkpoint and 45,000 matched
  learned steps total.
- Generate a new candidate bank with seed `2026080401`, environment seed 42,
  screen q0 before every controller formal trace, retain exactly 24 cases, and
  forbid redraw. Bootstrap uses 20,000 resamples and seed `2026080402`.
- Formal arms are q0, the selected classical edge controller, five centralized
  seeds, and five distributed seeds on the same 24-case bank: 288 matrix cells,
  with the screened q0 records reused only after hash verification.

## Public test seams and engineering route

- Public seam 1: the distributed controller's `select_edge_actions` leaves an
  edge output bit-identical when non-endpoint observations change, while a
  changed endpoint may change it.
- Public seam 2: distributed and centralized controllers both return three
  finite edge actions and train on the three actually executed edge actions;
  actor-capacity difference stays below one percent.
- Public seam 3: the environment accepts three edge actions and records the
  three-edge and four-node zero-sum execution, limits, slew, inactivity, and
  absence of central aggregation.
- Public seam 4: the R337 formal entry rejects old seeds/results, source or
  parent drift, installed package/case drift, pre-existing outputs, excessive
  WSL processes, or native thread count other than one before any attempt.
- Implement only a thin R337 adapter/orchestrator over immutable R293 reusable
  implementation. Do not edit R280-R293 plans, seals, results, scripts, probes,
  claims, or verdicts. Promote only genuinely reusable verification if a thin
  adapter cannot express the contract.
- Use vertical red-green slices. Expected test values come from the contract,
  not by recomputing them through the implementation.

## Formal launch contract

- `formal_entry`: `/home/wya/andes_venv/bin/python scripts/andes_scratch.py scripts/run_r337_icems_comparison.py execute`
- `rehearsal_command`: `/home/wya/andes_venv/bin/python scripts/andes_scratch.py scripts/run_r337_icems_comparison.py rehearse`
- `rehearsal_scope`: `same-pre-attempt-path`; it executes the exact validation
  path used by `execute` and returns before any attempt marker, seal, training
  output, candidate bank, controller trace, or formal result is created.
- `rehearsal_checks`: `source_hash`, `parent_hash`, `installed_package`,
  `installed_case`, `output_absence`.
- `wsl_python_processes`: 3 maximum across R337; no R337 launch starts if the
  repository cap would be exceeded.
- `native_threads_per_process`: 1, enforced for OpenMP, OpenBLAS, MKL,
  NumExpr, and VECLIB before importing numerical libraries in workers.
- Rehearsal runs after implementation and tests, before every R337 seal. The
  actual `execute` reruns the same checks, atomically creates one attempt
  marker, then seals and executes the stages in order. An interrupted or
  failed formal attempt is retained and never automatically retried.

## Formal outcome tree

1. Source/seal/provenance/budget/bank/order/action/statistical implementation
   failure is `INTEGRITY-INVALID`; interpret no performance endpoint.
2. Simulator nonconvergence, controller failure, endpoint failure, or no-harm
   failure is retained as a scientific outcome, not relabelled as integrity
   failure.
3. Distributed versus classical must improve both co-primary endpoints by at
   least 2%, have a two-sided 95% upper bound below zero, and show both-endpoint
   improvement in at least 3/5 seeds. Its positive-claim guards must all pass.
4. If step 3 passes and distributed materially beats centralized on both
   endpoints, classify `DISTRIBUTED-SUPERIOR`. If it instead has one-sided 95%
   upper bounds below the unchanged +5% margin on both endpoints and at least
   3/5 paired seeds meet that margin, classify
   `DISTRIBUTED-NONINFERIOR-LOCAL`.
5. Otherwise distinguish `CENTRALIZED-SUPERIOR`,
   `DISTRIBUTED-EFFECTIVE-CENTRALIZED-SUPERIOR`, `NO-NEURAL-INCREMENT`, and the
   corresponding valid `*-GUARD-FAIL` outcomes. Do not repair a negative result.

The co-primary endpoints are normalized synchronization loss and first-3-s
inter-area absolute-error integral; lower is better. Retain common/fast/slow
mean and tail guards, per-device inertia/damping, zero-sum, action variation,
storage power/state, every failure, and every nonconverged cell.

## Claim ceiling and stopping condition

- Maximum positive claim: on one modified-Kundur plant and one fixed four-node
  path, this neighbour-only parameter-sharing edge policy adds guarded value
  beyond the selected causal edge controller and is superior or locally
  non-inferior to the matched joint-observation single actor, without a joint
  runtime action server.
- Do not claim four independent device agents, generic MARL advantage,
  topology generalization, communication robustness, formal stability/safety,
  cross-simulator transfer, hardware readiness, or deployment.
- Stop after the one sealed outcome and publication decision. Do not change
  title, controller family, architecture, reward, seeds, bank, margins, or
  thresholds after any R337 physical endpoint becomes visible.

## Asset and close-out contract

- New assets are confined to `memory/rounds/R337`, `memory/questions/Q-0088.md`,
  `results/r337_*`, an R337 script/probe if needed, focused tests, and after feed
  publication `paper/icems2026/reports/R337.md`, the selected line's
  `LINE.md`/`ARTIFACTS.json`, and evidence-bounded manuscript repairs.
- Finish with hashes and manifest, evidence and power-system feed audits,
  atomic claim reservation/finalization, feed check, verdict, Q-0088 update,
  selected-line reconciliation, `repo_health.py check --no-baseline`,
  `validate.py`, `render.py`, focused/full tests, and the exact three-part
  plain-Chinese PI report.

## Cross-references

- Direct question: `memory/questions/Q-0088.md`.
- Current manuscript line: `paper/icems2026/LINE.md`.
- Scalar comparison boundary: R280/CLM-0610.
- Invalid genuine-distributed comparison: R292/CLM-0675.
- Aborted engineering parent: R293/Q-0050.
