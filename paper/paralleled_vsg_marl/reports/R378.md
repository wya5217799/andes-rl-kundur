# R378 settling-rule correction and held-out gate feed

## Identity

- Round/date/claim: `R378`, 2026-08-12, `CLM-1035`.
- Formal runner: `scripts/run_r378_gate_b2_correction.py`; correction
  module: `src/andes_rl_kundur/evaluation/gate_b2_correction.py`; unchanged
  R377 summarizer and runtime reused.
- Formal seal: `memory/rounds/R378/formal_seal.json`, SHA-256
  `2cb0613eb25464b2e06cdb5dae2c5460e467925ff0fd4044ec2ee20fee2985e7`.
- Decision artifacts:
  `results/research_loop/r378_gate_b2_correction/development_analysis.json`,
  `evaluation_execution.json`, `formal_analysis.json`, all with matching
  `.sha256` sidecars and a create-only manifest.
- Archive class: `LOCAL-ONLY`.

## Frozen setup

- R378 corrects exactly one rule of the sealed R377 contract: development
  settling improvement changes from "at least one dt below the local arm" to
  "no worse than the local arm", in both selection and classification. An
  independent validator proves the corrected contract differs from the R377
  contract only in round id. All banks, endpoints, thresholds, seeds, and
  execution discipline are unchanged.
- The R377 60-record development execution is read-only input, verified by
  its SHA-256 sidecars, and is re-summarized with the unchanged R377
  summarizer under the corrected selection rule.
- Held-out bank (30 records, selection-blinded): probe `PQ_0 +0.40`;
  disturbances `PQ_Bus14 -0.55`, `PQ_1 -0.70`.
- Primary: mean differential-frequency energy and settling (band 0.01 Hz);
  no-harm: common IAE <= 1.05, peak and RoCoF <= 1.10, probe cross
  <= 1.10; held-out primary ratio threshold 0.95 vs both baselines.

## Observations

- [CLM-1035] The correction validator passes and the corrected development
  analysis selects `distributed_hp_damping_ks1_kc0p5_alpha0p6`
  (differential-energy ratio 0.9606, settling 1.2 s no worse than local,
  probe cross 0.78 no-harm within ceiling). Source:
  `development_analysis.json#/selection`.
- [CLM-1035] All 30 held-out trajectories complete 50 steps with no TDS
  failure, no execution exception, and identity projection on every step;
  all physical guards pass for the selected arm and both baselines. Source:
  `evaluation_execution.json#/records` and `formal_analysis.json#/checks`.
- [CLM-1035] On the held-out bank the selected candidate reduces probe
  cross-response to 0.79x the local arm (off-diagonal energy ratio 0.7875,
  normalized cross ratio 0.7909), passing the no-harm ceiling and improving
  on the R376 amplification. Source: `formal_analysis.json#/probe_cross_ratios_vs_zero_and_local`.
- [CLM-1035] The selected candidate reduces mean differential-frequency
  energy to 0.962x the local arm and 0.800x zero feedback. The frozen
  primary threshold requires 0.95x or below relative to the local arm, so
  the differential-oscillation reduction check fails by a 1.3% margin;
  common-mode no-harm and all physical guards pass. Source:
  `formal_analysis.json#/differential_mean_ratios_vs_zero_and_local` and
  `#/checks`.
- [CLM-1035] The formal analysis retains `training_authorized=false`,
  `retry_authorized=false`, and `next_gate=null`. Source:
  `formal_analysis.json`.

## Conclusions

- **Primary judgment — STOP-NO-DIFFERENTIAL-BENEFIT**: after the single
  registered rule correction, the high-pass damping candidate passes every
  physical guard and no-harm ceiling and directionally reduces differential
  oscillation and probe cross-response on the held-out bank, but its mean
  differential-frequency energy improvement over the local arm (0.962x)
  does not clear the frozen 0.95 primary threshold. The registered Gate B-2
  gate therefore stops.
- **Boundary**: the held-out result is valid and guard-first; the 1.3%
  margin is not a tuning failure and authorizes no retry, gain change, or
  threshold adjustment.
- **Formulation decision**: the high-pass mutual-damping successor law stops
  at Gate B-2. Per the contract, no random arm, non-learning headroom gate,
  or MARL training is authorized. The route decision authority for any
  further successor remains with ROUTE.md#current-gate.

## Limits

- One controller family (high-pass mutual damping, alpha 0.60, ks1_kc0p5
  selected), one modified-Kundur topology, and the registered development
  and held-out banks.
- Identity guards prove no external repair, not stability or safety
  certificates. Arithmetic coordinates are projections, not eigenmodes.
- No communication ablation, learner, MARL method, neural residual, seed,
  checkpoint, unseen topology, or hardware platform is present.

## Publication gate

- **Evidence audit**: PASS — observations bind to the sealed correction
  contract, the immutable parent execution, the hashed development
  reanalysis, the new held-out execution, and the final guard-first
  classification; every formal JSON sidecar matches its whole-file SHA-256.
- **Domain audit**: PASS — the review preserves the four-VSG object,
  feasibility-native action seam, identity-only projection, 60-Hz
  coordinates, 0.2-s timing, achieved-power accounting, and guard-first
  precedence. The near-miss margin is treated as a frozen-threshold outcome,
  not a stability or safety result.
- **External context**: NOT-APPLICABLE — no novelty or related-work claim.
- **Claim disposition**: STAY-OUT for efficacy, coordination, and learning
  consequences; register the stop and the descriptive diagnostics only.
- **Allowed claim**: After the single registered settling-rule correction,
  the high-pass damping candidate selected on immutable R377 development
  data clears all physical guards and the probe cross-response no-harm
  ceiling on the held-out bank, but its mean differential-frequency energy
  ratio of 0.962 versus the local arm does not clear the frozen 0.95
  primary threshold, so the registered Gate B-2 stops as
  `STOP-NO-DIFFERENTIAL-BENEFIT` without authorizing learning.
- **Stay-out**: deterministic decoupling efficacy; safe coordination;
  residual headroom; MARL value; stability or safety certification;
  robustness; generalization; publication readiness.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| Rule correction validator | project validity record only | none |
| All guards and no-harm pass on held-out | limitations / implementation record | positive engineering boundary |
| Differential energy 0.962x vs local | none | stay out; near-miss diagnostic |
| Probe cross 0.79x vs local | none | stay out; descriptive |
| 0.95 primary threshold not cleared | formulation-stop record | negative boundary only |
| No training, retry, or next-gate authority | project navigation only | not a paper result |

## Follow-up pointers

- Close the high-pass deterministic comparison; keep training unauthorized.
- Any successor must state a genuinely different physical/control hypothesis
  beyond high-pass damping (R377/R378 provide no gain-change or threshold-
  change authority), or the no-differential-benefit outcome closes this
  coordination direction at Gate B-2.
- The identity and no-harm guards from R376-R378 remain the registered
  feasibility-native seam quality gate.
