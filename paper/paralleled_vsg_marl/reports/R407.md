# R407 feed — candidate-B 0.4 Hz ring-edge bandpass gate on the energy ports

## Identity

- Round/date/claim: `R407`, 2026-08-16, `CLM-1190`.
- Authority: `route_decision_bandpass_b_2026-08-16.md` (owner approved
  2026-08-16); frozen K grid {0.10, 0.25, 0.50, 1.00, 2.00}.
- Seal: `memory/rounds/R407/formal_seal.json`.
- Decision artifacts:
  `results/research_loop/r407_bandpass_gate/formal_analysis.json`
  (sha256 `b5f698b8c838795faac263c6e66d777456b98672778ed2881d4f91f52058d460`),
  `formal_execution.json`
  (sha256 `fd7ca4f0beca8bd2e900fac0863295c6492bd9847cdd4044b84d1d9817e8d7ec`),
  each with a `.sha256` sidecar. Archive class `LOCAL-ONLY`.
- Execution amendments (R402 precedent, registered before use): A-1 (bandpass
  arm registration in the forked job loop), A-2 (owner-approved parallel
  execution: 8 workers, budget re-frozen at 9 processes with the rung-8
  capacity ladder), A-3 (row-field completeness repair in the forked loop).
  Pre-repair attempts preserved under
  `results/research_loop/r407_bandpass_gate_pre_repair/`,
  `results/research_loop/r407_bandpass_gate_pre_repair_2/`, and
  `results/research_loop/r407_bandpass_gate_pre_repair_3/`.

## Frozen setup

- R376-R379 object, 60-record development bank, feasibility-native energy
  ports, seed 42, 0.2 s x 50 steps.
- Candidate B: F(s) = K*2*zeta*wm*s / (s^2 + 2*zeta*wm*s + wm^2),
  wm = 2*pi*0.4 rad/s, zeta = 0.35 frozen, bilinear with 0.4 Hz gain
  correction; ring-edge frequency differences, output zero-sum by
  construction (1^T v = 0); normalized action clip +/-0.70 through the
  feasibility-native map. Only K varies over the frozen grid.
- Thresholds: disturbance differential ratio <= 0.95, probe-cross ratio
  <= 1.10, all R379 guards; no held-out access; no training; the legacy M/D
  path stays pinned to zero.

## Observations

- [CLM-1190] All five grid points complete their 18-record banks with every
  guard passing (zero guard errors) and every estimator-valid record.
  Source: `formal_execution.json#/grid_results`.
- [CLM-1190] The probe-cross ratio improves monotonically with K from
  `1.0246` (K=0.10) to `0.6778` (K=2.00), passing the 1.10 ceiling at
  K >= 0.25; the bandpass's common-transparent spatial projection does
  suppress the probe cross channel as designed.
  Source: `formal_execution.json#/grid_results`.
- [CLM-1190] The disturbance differential ratio moves in the opposite
  direction: `1.1930` (K=0.10) down to `1.0346` (K=2.00), above the
  0.95 threshold at every grid point. No K satisfies both endpoints, and
  the immutable decision returns `BAND-FAIL` with `next_gate=none`.
  Source: `formal_execution.json#/grid_results` and
  `formal_analysis.json#/classification`.

## Conclusions

- **Primary judgment — BAND-FAIL**: the frozen candidate-B bandpass stage has
  no K in the grid that passes both registered thresholds on the disclosed
  development bank; the stage stops without retry.
- **Mechanism boundary**: the bandpass trades in the opposite direction of
  the closed first-order family (R406/CLM-1185): it suppresses the probe
  cross channel (down to 0.68 at K=2) but degrades the disturbance-driven
  differential energy at every tested gain (1.03-1.19). The spatial
  projection achieves its cross-channel goal while the 0.4 Hz channel does
  not reduce the registered disturbance differential endpoint on this
  object.
- **Authority boundary**: no held-out bank was touched, no zeta/structure
  change occurred, and no title-positive, impossibility, or MARL claim is
  made.

## Limits

- Covers one bandpass structure (frozen zeta, frozen K grid), one 60-record
  development bank, and one topology; no held-out evaluation occurred.
- This is not a statement about other bandpass designs, the M/D line, or any
  trained policy.

## Publication gate

- **Evidence audit**: PASS — every observation resolves to the sealed
  artifacts; sidecars match; the per-K ratios recompute from the execution
  payload; all three pre-repair attempts are preserved with their registered
  amendments.
- **Domain audit**: PASS — the R376-R379 object/estimator/threshold identity,
  the zero-sum bandpass channel, the guard completeness after A-3, and the
  opposite trade direction versus R406 are consistent across plan, seal,
  execution, and analysis.
- **External context**: NOT-APPLICABLE — bounded route-gate evidence.
- **Claim disposition**: ENTER — CLM-1190 registers the bounded BAND-FAIL
  route disposition.
- **Allowed claim**: On the registered 60-record development bank of the
  four-VSG energy-port object, the frozen 0.4 Hz ring-edge bandpass stage
  shows a monotone trade against K: the probe-cross ratio improves from
  1.0246 to 0.6778 (passing 1.10 at K >= 0.25) while the disturbance
  differential ratio stays above 0.95 at every grid point (1.0346-1.1930);
  no K passes both frozen thresholds, so the gate classifies BAND-FAIL and
  the bandpass stage stops within the frozen grid.
- **Stay-out**: bandpass impossibility beyond the grid, held-out claims,
  M/D-line value, title support, MARL value.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| Full grid with all guards passing | Methods / evidence validity | bounded physical evidence |
| Cross improves, differential degrades | Results / mechanism boundary | no title claim |
| Opposite trade vs the first-order family | Discussion / channel-structure contrast | bounded |

## Follow-up pointers

- The bandpass stage is closed within the frozen grid. All three
  owner-approved route steps (R405 candidate A, R406 first-order sweep,
  R407 bandpass) are now terminal; the next eligible activity is manuscript
  closure from the accumulated bounded evidence.
