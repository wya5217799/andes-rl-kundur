# R370 successor-mechanism direction feed

## Identity

- Round/date/claim: `R370`, 2026-08-12, `CLM-0995`.
- Decision surfaces: `paper/paralleled_vsg_marl/LINE.md` and
  `paper/paralleled_vsg_marl/ROUTE.md`.
- Parent title-object decision: `CLM-0970`; stopped direct M/D formulation:
  `CLM-0990`.
- Reusable-asset inputs: `CLM-0580`, `CLM-0965`,
  `paper/decoupling_marl_model_first/working/implemented_control_and_topology.md`,
  `src/andes_rl_kundur/env/andes/andes_vsg_storage_env.py`, and
  `src/andes_rl_kundur/control/active_power.py`.
- External direction input: Desktop Deep Research report, SHA-256
  `d2dd21c5fec731f6886393f6af106dc677b496494ae468c1c79182d6217c2e76`;
  Yang fact base, SHA-256
  `8284cd4b07d2c19fdc1c41088ae3f378b2f04df8be181e77d46f5e809dc7737f`.

## Frozen setup

- Scope is a Direction gate over successor actuator/mechanism families.
- The fixed title, four per-VSG actor identities, differential-oscillation
  endpoint scope, and evidence non-transfer rule remain fixed.
- No controller, environment, agent, evaluator, simulation, training,
  checkpoint, or performance result is created or changed.
- Candidate mechanisms are judged on title-object identity, material actuator
  difference, existing implementation leverage, and a falsifiable next gate.

## Observations

- [CLM-0995] Replacing the learner, reward, messages, seeds, or network while
  retaining direct per-VSG M/D actions does not create a successor mechanism;
  that registered formulation remains stopped by `CLM-0990`. Source:
  `memory/claims/CLM-0990.md#statement`.
- [CLM-0995] The existing storage environment provides four independently
  commanded, energy-constrained ESD1 devices co-located with four VSG proxies,
  but the code and implementation inventory identify them as separate models
  and explicitly state that the VSG controller is not embedded in ESD1.
  Source: `src/andes_rl_kundur/env/andes/andes_vsg_storage_env.py` class
  contract and
  `paper/decoupling_marl_model_first/working/implemented_control_and_topology.md`
  lines 25-52.
- [CLM-0995] The storage line contains reusable implementation mechanisms for
  per-device power projection, ramp, current capability, SOC, energy,
  common/differential power coordinates, deterministic coordination, and
  failure-aware evaluation; its prior outcomes remain evidence only for their
  registered hybrid proxy. Source:
  `paper/decoupling_marl_model_first/working/implemented_control_and_topology.md`
  lines 153-207, 243-293, and 645-670; `CLM-0580`; `CLM-0965`.
- [CLM-0995] The only selected successor is a composite VSG energy unit in
  which each runtime actor owns one bounded active-power-reference port backed
  by an explicit energy state. The current independent GFL ESD1 action is an
  implementation donor, not the selected acting object. Source:
  `paper/paralleled_vsg_marl/ROUTE.md#current-gate`.

## Conclusions

- **Direction decision — DIRECTION-SELECTED-WITH-OBJECT-REPAIR**: select
  energy-constrained per-VSG active-power-reference coordination as the sole
  successor to the stopped direct M/D formulation.
- **Object boundary**: a valid implementation must place the commanded power
  reference inside each VSG unit's power balance and bind its feasible set to
  that unit's achieved power and energy state. A separate co-located GFL
  storage action cannot be relabelled as per-VSG actuation.
- **Reuse boundary**: reuse energy and action governors, common/differential
  analysis, deterministic control ideas, and evaluation infrastructure only
  after prospective rebinding. Do not transfer old trajectories, performance
  values, claims, checkpoints, or actor semantics.
- **Next gate**: verify one-to-one actor/VSG/port identity, units, sign, timing,
  zero-action equivalence, independent intervention, and achieved-power energy
  accounting. Failure stops this direction before authority or headroom tests.

## Limits

- This is a trust-S programme direction decision; it is not a physical,
  numerical, comparative, causal, stability, safety, or learning result.
- No current ANDES model is claimed to expose the selected VSG-owned energy
  port. GENCLS setpoint availability and a physically valid energy coupling
  remain implementation and object-contract questions.
- Existing ESD1 authority and common-channel headroom cover an independent GFL
  storage proxy and an information-unconstrained finite development bank,
  respectively; neither establishes this successor object or a controller.
- Deterministic efficacy, non-learning headroom, MARL increment, held-out
  behavior, topology generalization, and hardware evidence remain untested.

## Publication gate

- **Evidence audit**: PASS — the four material observations resolve to current
  `CLM-0990`, the named environment and implementation inventory, current
  `CLM-0580`/`CLM-0965`, and the updated route. The wording transfers no old
  outcome into the selected object and states no new measured result.
- **Domain audit**: QUALIFIED — the review distinguishes parameter modulation,
  independent GFL storage injection, and a prospective VSG-owned power port;
  it also preserves per-device actuation, energy feasibility, common versus
  differential dynamics, and information-pattern boundaries. The selected
  port remains unimplemented and must pass the stated object, unit, actuator,
  and energy-accounting checks before it can support a physical claim.
- **External context**: CURRENT — the verified Deep Research report with a
  2026-08-11 cutoff and the Yang fact base support the taxonomy and fixed-title
  object constraints only; neither is used as experimental evidence.
- **Claim disposition**: QUALIFY — register as a programme direction decision,
  not a manuscript result or implementation claim.
- **Allowed claim**: after the registered direct per-VSG M/D formulation stops,
  the project selects an energy-constrained, VSG-owned active-power-reference
  mechanism as its sole conditional successor; existing ESD1 and model-first
  assets are implementation donors only, and the next gate must establish the
  VSG/port/energy object before any execution or training.
- **Stay-out**: the current ESD1 plant already represents unified VSG-BESS;
  the selected port is physically valid or authoritative; storage or common-
  channel results transfer; deterministic or MARL value exists; the method is
  stable, safe, robust, topology-general, novel, or publication-ready.

## Manuscript mapping

| Observation | Destination | Status |
|---|---|---|
| Direct M/D remains stopped | Methods route and limitation | project decision only |
| Independent ESD1 is not a VSG action | System model boundary | required before drafting |
| Energy/action assets are reusable after rebinding | Future Methods provenance | prospective |
| VSG-owned energy-port successor | Future system and control design | conditional direction |
| Performance, learning, safety, and generalization | none | stay out; untested |

## Follow-up pointers

- The active next gate is `paper/paralleled_vsg_marl/LINE.md#verification`.
- No implementation, simulation, or training is authorized by R370.
