# R374 deterministic cross-coordinate decoupling invalid-analysis feed

## Identity

- Round/date/claim: `R374`, 2026-08-12, `CLM-1015`.
- Repository head at execution: `a20559986856d2ca3bafeb1623ecb9781e61d3bd`.
- Formal runner: `scripts/run_r374_deterministic_decoupling.py`; classifier:
  `src/andes_rl_kundur/evaluation/deterministic_decoupling.py`.
- Formal seal: `memory/rounds/R374/formal_seal.json`, SHA-256
  `2c2b6493b9341355582304e65f0d30f4304cb3bb9885d333383597a5ba4250d7`.
- Decision artifact:
  `results/research_loop/r374_deterministic_decoupling/formal_failure.json`,
  SHA-256
  `61c32ace05929d55740539d9e4ca2a94e2db9153c189c0eabb4d97e387acc168`.
- Archive class: `LOCAL-ONLY`; no private second copy or public push is
  registered.

## Frozen setup

- The sealed development bank compares zero feedback, local diagonal PI, and
  four distributed common/differential candidates on the same four
  energy-constrained VSG-owned active-power-reference ports.
- The plan fixes the runtime object as `VSG_1..VSG_4` at buses
  `[12,16,14,15]`; ESD1, legacy M/D action, reward use, and training are
  excluded.
- Development must classify before any held-out trajectory may be
  instantiated; every terminal branch keeps `training_authorized=false`.
- The formal attempt is create-only, seal-bound, and has no authorized retry.

## Observations

- [CLM-1015] The immutable development execution contains all 60 registered
  trajectories.  Every trajectory completed all registered steps, none
  reports a TDS failure, and no reward or training process entered the gate.
  Source:
  `results/research_loop/r374_deterministic_decoupling/development_execution.json#/record_count`,
  `#/records`, `#/reward_used_for_gate`, and `#/training_executed`.
- [CLM-1015] Every execution record reports the same physical identity:
  four agents named `VSG_1..VSG_4` at buses `[12,16,14,15]`, matching the
  frozen plan and the R373 parent object.  The sealed classifier contract
  instead expects `ES1..ES4` while retaining the same buses.  Source:
  `development_execution.json#/records/0/identity`,
  `memory/rounds/R374/plan.md`, and
  `src/andes_rl_kundur/evaluation/deterministic_decoupling.py`.
- [CLM-1015] The formal terminal classification is `ANALYSIS-INVALID` with
  error `development VSG identity drift`.  Because validation stops before
  candidate selection, no held-out trajectory or held-out endpoint exists.
  Source:
  `results/research_loop/r374_deterministic_decoupling/formal_failure.json#/classification`
  and `#/error`.
- [CLM-1015] The attempt and failure artifacts both retain
  `training_authorized=false` and `retry_authorized=false`; all six formal
  JSON and sidecar files match their recorded SHA-256 values.  Source:
  `formal_attempt.json`, `formal_failure.json`, and their sidecars.

## Conclusions

- **Primary judgment — ANALYSIS-INVALID**: R374 supports no deterministic
  decoupling, candidate-selection, differential-benefit, common-mode no-harm,
  coordination, communication, or learning conclusion.
- **Cause boundary**: the invalidity is an identifier-contract defect in the
  sealed classifier, not observed VSG object drift and not a negative
  controller result.  Performance fields in the completed development records
  remain quarantined.
- **Eligible successor**: one separately registered, outcome-blind successor
  may correct the expected identifiers to the plan- and environment-bound
  `VSG_1..VSG_4`, add an independent regression assertion, and reuse the
  immutable R374 development records for reanalysis.  Only a valid eligible
  development result may authorize a new create-only held-out execution; R374
  itself may not be retried.

## Limits

- Covers one invalid development attempt on one modified-Kundur topology; no
  efficacy estimate is usable.
- Completion of the physical trajectories does not override the failed
  identity-validity guard.
- No held-out, stability, safety, robustness, topology-generalization,
  storage-device, hardware, novelty, MARL, or publication-readiness claim is
  supported.
- The fixed working title remains a prospective programme target; R374 does
  not validate any title term.

## Publication gate

- **Evidence audit**: PASS — the complete development count, completion and
  no-training fields, runtime identity, classifier identity mismatch,
  terminal invalid classification, absent held-out output, no-retry boundary,
  and all result sidecars were reconciled to the sealed plan, formal records,
  failure artifact, source, and manifest.  Performance fields remain
  quarantined.
- **Domain audit**: PASS — the review checked the four named VSG-owned power
  ports, bus mapping, active-power-reference actuator path, 60-Hz physical
  reporting, energy projection, local/ring information pattern, common versus
  differential endpoint roles, and validity precedence.  It accepts only the
  identifier-contract invalidity boundary and no controller result;
  presentation-only checks are not applicable.
- **External context**: NOT-APPLICABLE — the invalid result stays out of the
  paper and makes no novelty or related-work claim.
- **Claim disposition**: STAY-OUT — register only the invalidity boundary.
- **Allowed claim**: R374 completed its development trajectories but its
  formal analysis is invalid because the sealed classifier expected device
  identifiers inconsistent with the frozen plan and runtime object; no
  controller or learning conclusion is supported.
- **Stay-out**: deterministic decoupling efficacy; candidate selection;
  differential or common-mode benefit; coordination, communication, or MARL
  value; stability; safety; robustness; topology generalization; storage or
  hardware feasibility; novelty; and publication readiness.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| Complete development bank with failed identity-validity guard | none | stay out; invalid evidence |
| Controller performance fields | none | quarantined; invalid analysis |
| No-training and no-retry boundary | project navigation only | not a paper result |

## Follow-up pointers

- The current manuscript line may register one outcome-blind successor that
  corrects the identifier contract and reuses the immutable development
  records; training and an R374 retry remain unauthorized.
