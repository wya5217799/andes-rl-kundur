# FEED — R282: U-upturn authenticity confirmation (4-point densification)

> Feed report per the `experiment-report` skill: raw material for paper
> drafting, not prose, not the ledger. Ledger entry: CLM-0625; round record:
> `memory/rounds/R282/verdict.md`. Parent: R281 / CLM-0615.

## Identity

- Round R282, closed 2026-07-29, verdict `UPTURN-REAL`; claim CLM-0625;
  strengthens CLM-0615 (no wording change).
- Code: `probes/r282_eig_upturn.py` (ANDES 2.0.0 EIG, WSL
  `/home/wya/andes_venv/bin/python`); plant built via
  `AndesMultiVSGEnvV4._build_system()`; R282 contract hash
  `6d0da4e1da39fc47`; parent R281 contract `11a4800123f48a33` (cited, not
  recomputed).
- Data: `results/r282_eig_upturn/summary.json`,
  `results/r282_eig_upturn/provenance.json`; R281 endpoints re-identified
  in-script from `results/r281_eig_mechanism/summary.json` (sha256
  `06344bfa07e014b69fa68785172bd08c6f5c957c2afda3230612790e8d27a559`).
- Contract pointer: `memory/rounds/R282/plan.md`.

## Frozen setup

Plant, mapping M_i(q)=200+600*(0.25+q*[1,1,-1,-1]), guards (G4 M=0.1/D=0,
zero-sum 1400, PFlow convergence), and identification rule
(conjugate-pair merge, max |P_area1-P_area2|) identical to R281 — only the
grid changed: q in {0.2000, 0.2125, 0.2250, 0.2375}. Identification moved
in-script (process fix; R281 identified offline). Pre-registered continuity:
participation cosine >= 0.9 and |df| < 0.05 Hz for every consecutive pair of
the chain R281 q=0.1875 -> 4 new points -> R281 q=0.25.

## Observations

- O1 Upturn is authentic same-branch behavior: all 5 consecutive pairs
  continuous (participation cosine = 1.0; |df| <= 0.0009 Hz vs the 0.05 Hz
  threshold); the tracked mode stays genrou3+genrou4 dominated (area
  contrast 0.8638 -> 0.8648). [CLM-0625;
  `results/r282_eig_upturn/summary.json` field `continuity`]
- O2 Damping rises smoothly and monotonically across the upturn: zeta
  0.01939 (q=+0.1875) -> 0.02132 (q=+0.25), increments +0.0002..+0.0006 per
  0.0125 step; no jump. [CLM-0625; `summary.json` field `chain`]
- O3 Frequency rises smoothly 0.5243 -> 0.5273 Hz, continuing the R281
  trend — no branch-swap signature. [CLM-0625; `summary.json` field `chain`]
- O4 In-script re-identification of the two R281 endpoints reproduces
  R281's offline-identified f/zeta exactly — retro-validates R281's offline
  identification; in-script identification is now the reproducible standard.
  [`summary.json` chain[0], chain[-1] vs `results/r281_eig_mechanism/summary.json`]

## Conclusions (bounded)

- C1 [linearized small-signal]: the U-shaped upturn at q=+0.25 is confirmed
  structure of the allocation-to-damping map, not a mode-hybridization
  artifact. CLM-0615's bounded non-monotone wording stands unchanged; both
  extremes raise damping, the b=-1 side much stronger.
- C2 [linearized small-signal]: the physical origin of the upturn is not
  explained by this round — shape confirmed, cause unattributed. No
  mechanism claim beyond the shape is licensed.

## Limits (scope)

- Covers only q in [+0.1875, +0.25]; the left extreme (q -> -0.25) was not
  densified — R281's 5-point left flank was monotone, no anomaly suspected.
- Same scope as R281: plant plus ANDES-resident dynamics only, small-signal
  at the pre-pulse equilibrium; no transient, topology, or cross-simulator
  evidence.

## Manuscript mapping (SCI line, C1 spine)

- O1+O2 -> Mechanism section: upgrades the U-shape from "suspected artifact"
  to confirmed non-monotone structure; wording per CLM-0615 bounded
  statement unchanged, CLM-0625 cited as the authenticity evidence.
- O2+O3 -> Mechanism figure (zeta and f vs q): the densified right flank can
  be drawn as a smooth curve; figure data pointer
  `results/r282_eig_upturn/summary.json` fields `chain`, `continuity`.
- O4 -> Methods/reproducibility paragraph: mode identification in-script.
- Stay-out: physical cause of the upturn (C2) — reported as an open limit,
  not a result.

## Open threads

- R283 (Q-0043 strength sweep, authorized rank 130) is next: inertia axis
  M0 x q (15 points, includes repair of the R281 M0=100/q=-0.25
  identification breakpoint) and electrical axis tie-line reactance k x q
  (9 points).
- Optional: left-flank densification q in [-0.25,-0.1875] if a reviewer
  questions symmetry — script-ready, grid change only.
