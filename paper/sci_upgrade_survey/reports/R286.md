# FEED — R286: weak-tie time-domain survival + disturbance-location read (Q-0045)

> Feed report under `skills/kundur-round/SKILL.md` §3: raw material for paper
> drafting, not prose, not the ledger. Ledger entry: CLM-0645; round record:
> `memory/rounds/R286/verdict.md`. Question Q-0045 -> closed-positive.

## Identity

- Round R286, closed 2026-07-29, verdict `SURVIVES`; claim CLM-0645; parent
  CLM-0610 (R279/R280 nominal gain), CLM-0630 (R283 linearization gradient).
- Code: `scripts/run_r286_weak_grid_td.py` (prepare/run/analyse, seal-first),
  new env subclass `src/andes_rl_kundur/env/andes/andes_vsg_env_v4_weak_tie.py`
  (V4+storage intact; only Line_4/5/6 r/x scaled, post-setup pre-PFlow, same
  injection timing as `probes/eig_alloc_common.py`); launcher
  `scripts/run_r286_weak_grid_td.sh`; ANDES 2.0.0 TDS, WSL.
- Data: `results/r286_weak_grid_td/weak_tie_summary.json` (decision +
  contrasts + location read), `results/r286_weak_grid_td/traces/` (192
  immutable records + .sha256),
  `results/r286_weak_grid_td/provenance.json`, seal
  `memory/rounds/R286/weak_tie_seal.json`; nominal
  k=1 reference from sealed `results/r279_formal_evaluation/formal_summary.json`
  (hash-verified) and its 96 read-only traces.
- Contract pointer: `memory/rounds/R286/plan.md`; question
  `memory/questions/Q-0045.md`.

## Frozen setup

Zero-training transfer: frozen R279 arms q0 + centralized TD3 s17/s53/s89
(checkpoints hash-verified against the sealed training matrix; metadata
checked) rerun on the same sealed 24-scenario bank (hash-verified), with the
7<->8 triple-circuit inter-area corridor r and x multiplied by k in {1.5,
2.0}. 24 x 4 x 2 = 192 trajectories, 8 parallel shards, seal hash
`524c2cbd…78c4`. Endpoints/statistics identical to R279:
`normalized_sync_loss_hz2` + `fast_inter_area_iae_hz_s`, hierarchical
seed-then-scenario ratio bootstrap (10000 resamples, seed 2026072901).
Decision tree pre-registered (plan Outcomes). No retraining, no new
controller, no plant change beyond the corridor scaling.

## Observations

- O1 Verdict `SURVIVES`: both primary endpoints keep material improvement
  (point <= -2%, CI95 upper < 0) with >=50% retention of the nominal gain at
  every k level. [CLM-0645; `weak_tie_summary.json` field `decision`]
- O2 Per-level effects (centralized vs q0, nominal k=1 in parentheses):
  k=1.5 sync loss -23.964% [-29.253, -17.362] (-24.345%), inter-area IAE
  -17.550% [-24.949, -9.728] (-17.041%); k=2.0 sync loss -22.333% [-27.066,
  -15.686], inter-area IAE -17.345% [-24.307, -9.795]. Retention vs nominal:
  0.984 / 1.030 at k=1.5; 0.917 / 1.018 at k=2.0. [CLM-0645; field
  `decision.per_k`]
- O3 Doubling the corridor reactance (x 0.22 -> 0.44 per circuit) costs the
  learned allocation ~2 points of sync-loss gain (from -24.3% to -22.3%) and
  nothing measurable on the fast inter-area endpoint — the gain is flat-to-
  mildly-declining, not collapsing. [CLM-0645]
- O4 Guards: 192/192 completed, zero TDS failures, action audits pass,
  absolute storage guards pass at every arm x k (command/actual <= 0.36 pu,
  SoC in [0.20, 0.80], zero constraint violations, zero saturation reasons),
  injection consistency verified on every record (tie r/x = k x nominal,
  nominal x = {0.22001, 0.22002, 0.22000} across all 192). [CLM-0645; field
  `guards`]
- O5 Location read (ratio-of-means %, seeds pooled, n=6 per group, k=1/1.5/
  2.0): sync loss — PQ_0 -36.42/-28.65/-20.00; PQ_1 -37.86/-33.98/-30.45;
  PQ_Bus14 -19.47/-21.54/-22.37; PQ_Bus15 -21.06/-21.35/-21.06. Inter-area
  IAE — PQ_0 -27.46/-27.82/-24.03; PQ_1 -13.87/-14.08/-15.24; PQ_Bus14
  -14.47/-14.91/-15.55; PQ_Bus15 -14.28/-14.64/-15.18. Every location keeps
  double-digit improvement on both endpoints at k=2.0. [CLM-0645; field
  `location_read`]
- O6 Descriptive location pattern: the nominal sync-loss advantage is
  largest for the Kundur base-case load locations (PQ_0/PQ_1, ~-36 to -38%)
  and smaller for the new-bus loads (Bus14/15, ~-20%); under corridor
  weakening the two families converge (PQ_0 falls to -20%, Bus14/15 flat).
  The fast inter-area endpoint shows no such split (~-14 to -16% everywhere
  except PQ_0 ~-24 to -28%). [CLM-0645, descriptive only]

## Conclusions (bounded)

- C1 [time-domain, frozen controllers, this bank]: the centralized
  differential-allocation gain measured at nominal tie strength survives a
  1.5x-2.0x weakened inter-area corridor with >=92% retention on both
  primary endpoints — the C2 weak-grid section now has a time-domain
  survival statement, not only a linearization gradient.
- C2 [time-domain, descriptive]: the gain is not a disturbance-location
  artifact — it persists at all four bank locations under weakening; the
  location-dependent size split (base-case loads vs new-bus loads) is
  recorded for the discussion section.
- C3 [consistency]: time-domain survival is directionally consistent with
  the R283 small-signal gradient (weak grid makes allocation matter more,
  not less) — stated as consistency, not as a mechanism identification.

## Limits (scope)

- Two k levels on one corridor definition (7<->8 triple circuit r/x); the
  8<->9 pair and all other parameters untouched; k beyond 2.0 unexplored.
- Frozen controllers only — no weak-grid retraining measured (the tree did
  not open it); generalization to retrained policies is a separate gated
  question.
- Location read is descriptive (n=6 per group, no significance claim);
  single plant, single disturbance family (signed PQ load steps).

## Publication gate

- **Evidence audit**: PASS - O1-O6 and C1-C3 bind to current CLM-0645,
  the final decision/guards/location read, seal, provenance, immutable trace
  inventory, current CLM-0610 correction, and the valid R286 verdict. The
  inventory's R279 invalid warning is resolved only through CLM-0610/R280.
- **Domain audit**: PASS - checked one-topology proxy scope, frozen-controller
  information pattern, 50-Hz controller semantics versus 60-Hz physical
  endpoints, endpoint signs/windows, paired bank, hierarchical uncertainty,
  actuator/storage guards, and transient-versus-restoration semantics.
  Presentation-only checks are not applicable.
- **External context**: CURRENT - the registered C2 landscape in
  `paper/sci_upgrade_survey/REPORT.md` and corpus has research date
  2026-07-29 and review after 2026-10-29; this historical remediation adds no
  novelty axis.
- **Claim disposition**: ENTER - use only the bounded frozen-controller
  survival statement.
- **Allowed claim**: On the sealed 24-scenario modified-Kundur bank, the
  frozen centralized differential-allocation controller retained
  interval-supported improvements on both registered transient endpoints
  through the declared Line_4/5/6 multiplier k=2.0 without retraining.
- **Stay-out**: measured SCR; collapse threshold; unseen-topology
  generalization; sustained common-frequency restoration; formal
  stability/safety; decentralized or MARL value; weak-grid retraining;
  cross-simulator, HIL, or deployment claims.

## Manuscript mapping (SCI line, C2 validation)

- O1 + O2 -> C2 core survival sentence + results table (k x endpoint effect
  matrix with CIs; data: `weak_tie_summary.json` field `decision.per_k`).
- O3 -> C2 bounded discussion of flat-to-mildly-declining gain through k=2.0.
- O5 + O6 -> C2 robustness paragraph (location grouping table; descriptive
  wording only, n=6 flagged).
- O4 -> Methods/reproducibility paragraph (seal-first, hash chain, guard
  battery identical to the nominal formal evaluation).
- Stay-out: any claim about retrained controllers; SCR-unit conversion
  (corridor scaling stays declared, per LINE.md C2 wording rule); any
  mechanism identification from the consistency noted in C3.

## Open threads

- Weak-grid retraining gate: tree returned SURVIVES, so retraining is NOT
  opened (per plan). R287 extended the same declared proxy to k=3.0 and kept
  this gate closed (CLM-0650; `paper/sci_upgrade_survey/reports/R287.md`).
- ~~k > 2.0 sweep~~ — resolved by R287 (k in {2.5, 3.0}, CLM-0650). A second
  corridor definition remains unmeasured and unauthorized.
- Branch-continuation identification (from R285) remains the candidate
  remedy for the low-inertia linearization blind spot — untouched here.
