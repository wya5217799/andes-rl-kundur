# FEED — R284: left-flank densification (R282 symmetric completion)

> Feed report per the `experiment-report` skill: raw material for paper
> drafting, not prose, not the ledger. Ledger entry: CLM-0635; round record:
> `memory/rounds/R284/verdict.md`. Parent: R281 / CLM-0615, R282 / CLM-0625.

## Identity

- Round R284, closed 2026-07-29, verdict `LEFT-FLANK-SMOOTH`; claim CLM-0635.
- Code: `probes/r284_eig_left_flank.py` importing
  `probes/eig_alloc_common.py` (ANDES 2.0.0 EIG, WSL); plant via
  `AndesMultiVSGEnvV4._build_system()`; contract hash `ff3520fa2ba4dc0f`.
- Data: `results/r284_eig_left_flank/summary.json`,
  `results/r284_eig_left_flank/provenance.json`; R281 endpoints
  re-identified read-only from `results/r281_eig_mechanism/summary.json`.
- Contract pointer: `memory/rounds/R284/plan.md`.

## Frozen setup

Identical to R282 except the grid: q in {-0.2000, -0.2125, -0.2250,
-0.2375}, chaining R281 q=-0.25 -> 4 new points -> R281 q=-0.1875; same
identification rule (in-script), same continuity criteria (cosine >= 0.9,
|df| < 0.05 Hz), same guards.

## Observations

- O1 Left flank is smooth and monotone: all 5 consecutive pairs continuous
  (participation cosine = 1.0; |df| <= 0.003 Hz), damping falling smoothly
  0.03023 (q=-0.25) -> 0.02528 (q=-0.1875) — no hidden structure on the
  beneficial side. [CLM-0635; `summary.json` fields `chain`, `continuity`]
- O2 The zeta-vs-q figure is now symmetric in resolution: both flanks of
  the R281 sweep carry 0.0125-spacing densification between the outermost
  R281 points. [CLM-0635 vs CLM-0625]

## Conclusions (bounded)

- C1 [linearized small-signal]: the monotone rise of inter-area damping
  toward the beneficial extreme (q -> -0.25) is genuine smooth same-branch
  behavior at 0.0125 resolution; no left-flank caveat is needed in the
  mechanism section. CLM-0615's monotone interval [-0.25, +0.125] stands
  with densified evidence on both ends of the non-monotone right extreme.

## Limits (scope)

- Covers q in [-0.25, -0.1875] only; same plant/small-signal scope as
  R281-R283. No new physics expected or found.

## Manuscript mapping (SCI line, C1 spine)

- O1 -> Mechanism figure M1: left flank drawn at the same density as the
  right; caption notes symmetric densification (data:
  `results/r284_eig_left_flank/summary.json`).
- O1 -> Mechanism section P4 (structure paragraph): one clause confirming
  the beneficial-side monotonicity is smooth at densified resolution.
- Stay-out: nothing — no new claims beyond figure resolution.

## Open threads

- (none)
