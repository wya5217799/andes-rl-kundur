# FEED — R285: hybridization-zone map at low aggregate inertia (Q-0044)

> Feed report per the `experiment-report` skill: raw material for paper
> drafting, not prose, not the ledger. Ledger entry: CLM-0640; round record:
> `memory/rounds/R285/verdict.md`. Question Q-0044 -> closed-positive.

## Identity

- Round R285, closed 2026-07-29, verdict `ZONE-CHARTED`; claim CLM-0640;
  parent CLM-0630 (R283 flags).
- Code: `probes/r285_hybridization_map.py` + `probes/r285_zone_analysis.py`
  on `probes/eig_alloc_common.py` (backward-compatible `keep_modes`
  extension); ANDES 2.0.0 EIG, WSL; contract hash `6bf89096bfce84ca`.
- Data: `results/r285_hybridization_map/summary.json` (20-cell map),
  `results/r285_hybridization_map/zone_analysis.json` (boundary +
  attribution), `results/r285_hybridization_map/provenance.json`; 6 cells
  reused read-only from `results/r283_strength_sweep/summary.json`.
- Contract pointer: `memory/rounds/R285/plan.md`.

## Frozen setup

Plant/mapping/guards/identification/branch-screen identical to R283
(zero-sum re-declared per level as 1400*M0/200). Grid frozen before the
first eigenvalue: M0 in {100,125,150,175} x q in {-0.25,-0.125,0,+0.125,
+0.25}. 14 cells new, 6 read-only from R283, 3 flagged R283 cells
re-computed for attribution recording only (all reproduce R283 within
|dzeta| < 1e-6). Attribution classifier pre-registered (plan contract 4).

## Observations

- O1 Boundary charted (valid V / flagged F across q = -0.25,-0.125,+0.125,
  +0.25): M0=100 → F F F V; M0=125 → F V V F; M0=150 → F V V F; M0=175 →
  V V V V. [CLM-0640; `zone_analysis.json` field `boundary`]
- O2 Attribution: 6 of 7 flagged picks are VSG-local-leaning modes
  dominated by the inertia-squeezed area-1 units (top-2 participation
  vsg12/vsg16); one is non-area-contrasted (M0=150/q=-0.25, contrast
  0.0000, genrou1/genrou2 top, zeta 0.653); one is GENROU-area-leaning
  (M0=100/q=-0.125). At low aggregate inertia and large |q|, VSG local
  modes overtake the inter-area branch in area contrast — the max-contrast
  rule loses the branch, which is why these cells are unmeasurable, not
  anomalous. [CLM-0640; `zone_analysis.json` field `attributions`]
- O3 Measurable region stated precisely: the inertia-axis sensitivity
  gradient is measurable at M0 >= 175 across the full q range, and at
  M0 in {125,150} only for |q| <= 0.125 — consistent with R283's clean
  M0 = 200..300 rows and localizing the blind spot to M0 < 200 at large
  |q|. [CLM-0640]
- O4 Boundary is not a clean frontier: at M0=100 the rule re-locks onto an
  on-branch mode at q=+0.25 (the R283-validated valid cell) while both
  nearer cells q=+/-0.125 are flagged. [CLM-0640; `zone_analysis.json`
  boundary + R283 branch_analysis cross-check]
- O5 Process: the three R283 flags are reproduced and attributed fully
  in-script (rerun |dzeta| < 1e-6; keep_modes records the merged mode
  list) — no offline identification anywhere in the chain. [CLM-0640;
  `summary.json` field `attribution_reruns`]

## Conclusions (bounded)

- C1 [linearized small-signal]: below M0 ≈ 200 the inter-area branch
  hybridizes with VSG-local modes at large |q|; the inertia-axis gradient
  is measurable at M0 >= 175 (full range) and M0 in {125,150} (|q| <=
  0.125) — the C2 section can now state exactly where the gradient is
  measured versus unmeasured.
- C2 [linearized small-signal, descriptive]: the hybridization is a
  mode-family overtaking (VSG-local over inter-area contrast), not a
  numerical failure; a branch-continuation identification rule could in
  principle recover the inter-area branch through the zone — candidate
  follow-up, not measured here.

## Limits (scope)

- Attribution is descriptive (declared coarse classifier), not a
  mechanism explanation; no participation-dynamics claim beyond the
  recorded vectors.
- Single plant, small-signal; q subset {-0.25,-0.125,0,+0.125,+0.25} and
  M0 <= 175 only — the zone boundary beyond the grid (e.g., M0 < 100) is
  unexplored.

## Manuscript mapping (SCI line, C2 validation)

- O1 + O3 -> C2 blind-spot sentence (P4 of the C2 skeleton) upgraded from
  "M0 in [100,200)" to the exact measurable-region statement; figure or
  table: 4x5 valid/flag boundary map (data: `zone_analysis.json`).
- O2 -> C2 limits paragraph + future-work sentence (branch-continuation
  identification as the candidate remedy).
- O5 -> Methods/reproducibility paragraph.
- Stay-out: any physical explanation of *why* the overtaking happens;
  boundary outside the frozen grid.

## Open threads

- Branch-continuation (eigenvector-continuation) identification rule —
  candidate to recover the gradient through the zone; requires a
  prospective rule amendment before any use.
- Zone boundary outside the frozen grid (M0 < 100, finer q) — only if the
  boundary statement is challenged.
