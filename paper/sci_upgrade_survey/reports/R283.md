# FEED — R283: grid-strength sweep of allocation damping sensitivity (Q-0043)

> Feed report under `skills/kundur-round/SKILL.md` §3: raw material for paper
> drafting, not prose, not the ledger. Ledger entry: CLM-0630; round record:
> `memory/rounds/R283/verdict.md`. Question Q-0043 -> closed-positive.

## Identity

- Round R283, closed 2026-07-29, verdict `STRENGTH-GRADIENT-CONFIRMED`;
  claim CLM-0630; anchors CLM-0615 / CLM-0625.
- Code: `probes/r283_strength_sweep.py` (sweep, ANDES 2.0.0 EIG, WSL
  `/home/wya/andes_venv/bin/python`), `probes/eig_alloc_common.py` (shared
  core extracted from the R281/R282 pattern),
  `probes/r283_branch_analysis.py` (branch-validity screen, reads the sealed
  sweep); plant via `AndesMultiVSGEnvV4._build_system()`; contract hash
  `45d28fcd9531bfe6`.
- Data: `results/r283_strength_sweep/summary.json` (24 EIG points),
  `results/r283_strength_sweep/branch_analysis.json` (flags + verdict
  classification), `results/r283_strength_sweep/provenance.json`; R281
  anchor re-identified read-only from
  `results/r281_eig_mechanism/summary.json`.
- Execution amendment: `memory/rounds/R283/execution_amendment_20260729.md`
  (branch-validity detector: within-level endpoints vs q=0 at cosine >= 0.9
  and |df| < 0.05 Hz; cross-level q=0 chain cosine >= 0.9, frequency drift
  physical). Contract pointer: `memory/rounds/R283/plan.md`.

## Frozen setup

Plant, mapping, guards, and identification rule identical to R281/R282.
Two pre-registered strength axes, q in {0, +/-0.25} on both: axis A scales
the executed M vector by M0/200, M0 in {100,150,200,250,300} (zero-sum
total re-declared per level as 1400*M0/200); axis B scales r and x of the
7<->8 tie corridor (Line_4/5/6, the declared SCR proxy) by k in
{1.0,1.5,2.0}, charging b untouched, PFlow re-run per k. Sensitivity per
level S = |zeta(+0.25)-zeta(-0.25)|/|zeta(0)|. Anchors M0=200 and k=1.0
reproduce R281 within |dzeta| < 1e-6 (both pass).

## Observations

- O1 Electrical-axis gradient CONFIRMED: S = 0.458 (k=1.0) -> 1.050 (k=1.5)
  -> 2.053 (k=2.0), ratio 4.48, monotone; all 9 points on-branch. The
  weaker the tie, the more allocation matters. [CLM-0630;
  `branch_analysis.json` axis_b]
- O2 Inertia-axis gradient PARTIAL: valid levels give S = 0.458 (M0=200)
  -> 0.304 (M0=250) -> 0.199 (M0=300), ratio 2.30, monotone decreasing
  with inertia; M0=100/q=-0.25 and M0=150/q=+/-0.25 flagged off-branch
  (hybridization); the valid M0=100 q=0 -> +0.25 pair corroborates (+53%,
  matching the R281 dev probe). [CLM-0630; `branch_analysis.json` axis_a]
- O3 Weak-grid structure change: the R281/R282 U-upturn disappears at
  k >= 1.5 — zeta(+0.25) < zeta(0) (0.0148 < 0.0161 at k=1.5; 0.0069 <
  0.0129 at k=2.0); the mapping becomes monotone over the measured range
  and the b=-1-side gain grows to +159% at k=2.0 (vs +55% at k=1.0).
  [CLM-0630; `summary.json` axis_b runs]
- O4 Baseline mode degrades with weaker tie: zeta(0) 0.0195 -> 0.0129,
  f 0.527 -> 0.482 Hz across k = 1.0 -> 2.0 — physically consistent
  inter-area weakening. [CLM-0630; `summary.json` axis_b runs]
- O5 Process: R281's M0=100/q=-0.25 offline identification breakpoint is
  reproduced as an in-script flag (cosine 0.0000, |df| = 0.186 Hz vs q=0) —
  identification-failure detection is now reproducible, not judgement.
  [CLM-0630; `branch_analysis.json` axis_a levels]

## Conclusions (bounded)

- C1 [linearized small-signal]: differential-allocation damping authority
  grows materially as grid strength falls — confirmed on the declared SCR
  proxy axis (tie reactance, ratio 4.48 over k in [1.0, 2.0]), partially
  supported on the inertia axis (ratio 2.30 over the valid levels M0 in
  [200, 300], hybridization flags below). "Placement matters more in weaker
  grids" is now a measured gradient, not a dev-probe hint.
- C2 [linearized small-signal]: under a weakened tie the mapping becomes
  monotone in the measured range and the learned-direction authority
  increases (+159% at k=2.0) — the C2 weak-grid section may state this
  bounded form. "Allocation creates damping" and any unmeasured-range
  extrapolation remain out of bounds.

## Limits (scope)

- Single plant (modified Kundur two-area), small-signal at the per-level
  PFlow equilibrium; no transient, topology, or cross-simulator evidence.
- The SCR proxy is declared, not canonical: 7<->8 tie-corridor r,x scaling
  only; real SCR variation involves more than one impedance group.
- Axis A hybridization zone (M0 in [100,200), q = +/-0.25) is flagged, not
  characterized; gradient there is unmeasured, not absent.
- U-upturn physical origin remains unattributed (carried from R282).

## Publication gate

- **Evidence audit**: PASS - O1-O5 and C1-C2 bind to current CLM-0630,
  the sealed sweep, branch analysis, provenance, anchors, amendment, and valid
  final verdict; flagged cells remain excluded from the measured gradient.
- **Domain audit**: PASS - checked the declared Line_4/5/6 r/x-scaling proxy,
  per-level PFlow, modal branch tracking, damping/frequency signs, zero-sum
  mapping, hybridization flags, and the distinction between local
  small-signal evidence and time-domain or SCR claims. Presentation-only
  checks are not applicable.
- **External context**: CURRENT - the active weak-grid/placement landscape in
  `paper/sci_upgrade_survey/REPORT.md` and its registered corpus has research
  date 2026-07-29 and review after 2026-10-29; no additional novelty search is
  required for this historical feed.
- **Claim disposition**: QUALIFY - enter the measured gradient with proxy and
  identification-zone limits.
- **Allowed claim**: Within the scanned modified-Kundur ranges, differential
  inertia-allocation sensitivity increased as the declared tie-impedance proxy
  weakened, while low-inertia off-branch cells remained unmeasured.
- **Stay-out**: measured SCR; topology generalization; a universal monotone
  law; allocation-created damping; transient stability; sustained restoration;
  formal safety/stability; cross-simulator, HIL, or deployment claims.

## Manuscript mapping (SCI line, C1 spine + C2 validation)

- O1 + O3 -> C2 weak-grid validation section core result: measured
  strength-sensitivity gradient plus the monotone-with-growing-authority
  structure change; figure = S vs k and zeta vs q per k (data:
  `summary.json` axis_b, `branch_analysis.json` axis_b.valid_S).
- O2 -> C2 complement paragraph: inertia-axis gradient with the
  hybridization caveat stated exactly as in Limits.
- O4 -> C2 setup paragraph (proxy sanity: baseline mode weakens with k).
- O5 -> Methods/reproducibility paragraph (in-script identification +
  branch-validity screen).
- Stay-out: hybridization-zone physics; any SCR value in Hz/kA units — the
  proxy is reported as a declared reactance scaling, not converted.

## Open threads

- ~~Hybridization-zone characterization~~ — resolved by R285
  (`ZONE-CHARTED`, CLM-0640). Feed:
  `paper/sci_upgrade_survey/reports/R285.md`.
- ~~Left-flank densification~~ — resolved by R284
  (`LEFT-FLANK-SMOOTH`, CLM-0635). Feed:
  `paper/sci_upgrade_survey/reports/R284.md`.
- ~~Time-domain survival check on a sealed disturbance bank~~ — resolved by
  R286 (`SURVIVES`, CLM-0645). Feed:
  `paper/sci_upgrade_survey/reports/R286.md`.
