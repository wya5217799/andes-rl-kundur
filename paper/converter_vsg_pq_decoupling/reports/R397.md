# R397 feed — PPVSM1 two-unit signed P/Q authority gate stop

## Identity

- Round: R397. Claim: CLM-1130. Question: Q-0111. Date: 2026-08-14.
- Runner: `scripts/run_r397_ppvsm1_signed_authority_gate.py`; classifier:
  `src/andes_rl_kundur/evaluation/ppvsm1_signed_authority_gate.py`.
- Seal: `memory/rounds/R397/formal_seal.json` (sha256
  `9cf0fae97dd304c8d3ae8dd0eccb569d423855d039b179e920fdd90bf3b71e8a`).
- Formal attempt/execution/analysis/manifest under
  `results/research_loop/r397_ppvsm1_signed_authority_gate/` (sha256
  `a15e3df1ea0077a35369533b2db745f53924b29df618befdb92b04de80811668`,
  `6be6611c5abbc66dccb111eeff5235044a51a1f59d7a88e521adc653338e8f21`,
  `98f5afd48f8f2d5bd5743f5065ae7c1104298aa2bcb71fecdc223ae6966d5e6f`,
  `cf2cb8459554d6ef4dc5a3f36796b1af83e18b588853a16db9087be8a58d5b33`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator is registered.

## Frozen setup

- Two-unit PPVSM1 cell on the unchanged Kundur static case (buses 1-2;
  StaticGen 3-4 static anchors; 900-MVA rating; frozen R396 card; no PLL;
  forbidden models absent).
- Nine-arm bank: one zero arm plus two devices times two channels (pref->Pe,
  qref->Qe) times two signs. Post-init absolute reference-service step of
  +/-0.09 system pu; 2.0-s horizon; native TDS tolerance 1e-4.
- Intervention: direct write to the frozen `PPVSM1.Pref.v`/`Qref.v`
  service array element (the ANDES RenGen set_setpoint array-write
  mechanism); four-channel pre/post receipts; StaticGen tables never written.
- Gates: requested/applied 1e-12; signed and target-attribution floors 2e-4;
  paired separation 4e-4; bus voltage [0.9, 1.1] pu; current <= 10 pu;
  apparent power <= 9 system pu; virtual frequency [0.95, 1.05] pu.
- Lifecycle: one WSL Python process, one native thread, one create-only
  formal attempt; the rehearsal ran the complete bank as a canary.

## Observations

- O1 (CLM-1130): all nine arms complete the 2.0-s horizon with native
  convergence, no execution error, zero initialization residuals, reference
  deviation 0.0, finite values, and the full electrical envelope (see
  `formal_execution.json` /arms and /checks).
- O2 (CLM-1130): every action receipt is exact — requested/applied equality
  within 1e-12 with no non-commanded channel change (see
  `formal_execution.json` /arms/*/action).
- O3 (CLM-1130): all eight action arms achieve correctly signed terminal
  responses between 0.0355 and 0.0466 system pu against the 0.09-pu step,
  and all four paired separations pass (0.0712, 0.0864, 0.0931, 0.0924; see
  `formal_analysis.json` /responses and /paired_separations).
- O4 (CLM-1130): target attribution fails on exactly the two PPVSM1_1 Pref
  arms: the non-target PPVSM1_2 Pe response magnitude (0.04663 and 0.04644)
  exceeds the target's own (0.04309 and 0.04329), giving margins -0.00354
  and -0.00315 below the 2e-4 floor. The PPVSM1_2 Pref arms pass with
  margins 0.00315 and 0.00354 (see `formal_analysis.json` /responses).
- O5 (CLM-1130): the immutable classifier returns
  `STOP-PPVSM1-SIGNED-AUTHORITY` with exactly one failed check
  (`target_attribution`); record integrity and the other nine checks pass
  (see `formal_analysis.json` /checks and /classification).

## Conclusions

- C1 (sealed-bank time-domain): the frozen two-unit PPVSM1 cell fails the
  signed authority gate on the target-attribution requirement. A signed
  0.09-pu Pref step at PPVSM1_1 moves PPVSM1_2's achieved Pe in the opposite
  direction with at least as large a magnitude as the target device's own
  response, so per-device attribution of the active-power response cannot be
  established at this operating point.
- C2 (sealed-bank time-domain): every other registered guard passes —
  receipts, signed responses, paired separation, native solver, finite
  values, electrical envelope, references, and diagnostics — so the failure
  is attribution-specific, not an integrity, convergence, or envelope
  failure.
- C3 (analytic boundary): the pre-registered stop applies: the PPVSM1
  formulation stops before any droop-slope verification, controller,
  decoupling, or learning work. Q-0111 closes negative.

## Limits

- One simulator, cell, operating point, step size, and 2-s horizon were
  tested. The result is a formulation-level attribution failure, not a
  stability theorem or technology-class rejection.
- The attribution failure is a network-mediated redistribution observation
  (opposite-sign response at the non-target device of at least equal
  magnitude); it contains no eigenvalue, modal, causal-identification, or
  mechanism proof.
- The run supports no authority-pass consequence, controller, decoupling,
  learning, topology, EMT, HIL, or deployment claim.

## Publication gate

- **Evidence audit**: PASS — every inline number was recomputed from
  `formal_execution.json` /arms and `formal_analysis.json` /responses,
  /paired_separations, /checks; the classification matches the sealed
  classifier output.
- **Domain audit**: PASS — 60-Hz card, system/device base separation,
  per-device local direct reference writes with no inter-device
  communication, projected P/Q droop commands retained, phasor-domain
  boundary respected (research-skill-adapter mandatory checks).
- **External context**: NOT-APPLICABLE — all statements are internal to the
  installed ANDES model at one operating point.
- **Claim disposition**: ENTER — CLM-1130 registers the bounded negative
  authority stop.
- **Allowed claim**: the CLM-1130 statement.
- **Stay-out**: controller value, decoupling value, instability, safety,
  technology-class rejection, learning necessity, robustness, and hardware
  claims.

## Manuscript mapping

- O1, O2 -> candidate signed-authority section: bank integrity and receipt
  evidence.
- O3 -> same section: signed response and paired-separation evidence.
- O4 -> same section: the attribution failure that stops the route.
- O5, C1-C3, Limits -> claim-boundary paragraph (prospective; only after the
  manuscript stage reopens).

## Follow-up pointers

- Q-0111 closed-negative by CLM-1130 (this round).
- pending: none — the route stops before droop-slope matching; any successor
  requires a new prospective route decision.
