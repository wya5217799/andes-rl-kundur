# R388 integrity-corrected signed REGCV1 authority feed

## Identity

- Round/date/claim: `R388`, 2026-08-14, `CLM-1085`.
- Formal seal: `memory/rounds/R388/formal_seal.json`, SHA-256
  `f10d9add6746dda3fd8e0728d205bed17ab39fa9e9bb9be990e23bfc697b73b4`.
- Formal attempt:
  `results/research_loop/r388_regcv1_signed_authority_correction_gate/formal_attempt.json`,
  SHA-256
  `8bffc734f6ede5ef0b087eded4118a842dad1f3232acd2f43dde8f48fd34050a`.
- Formal execution:
  `results/research_loop/r388_regcv1_signed_authority_correction_gate/formal_execution.json`,
  SHA-256
  `696602dfdff62898745260eb7c7f75061a8a78d124001a741f50e69be5c479e4`.
- Formal analysis:
  `results/research_loop/r388_regcv1_signed_authority_correction_gate/formal_analysis.json`,
  SHA-256
  `466296010670018e05619e2bd98a378c46f21d04cf74d0b01a2f4042215a5c39`.
- Formal manifest:
  `results/research_loop/r388_regcv1_signed_authority_correction_gate/formal_manifest.json`,
  SHA-256
  `bb925adffe684c9a5e69f117bf8d249d48e17579da4499c463616a364d5937df`.
- Strict diagnosis:
  `paper/converter_vsg_pq_decoupling/working/R388_diagnosis.md`.
- Archive status: `LOCAL-ONLY`; no private second-copy locator is registered.

## Frozen setup

- Runtime and plant: WSL Python 3.12.3, ANDES 2.0.0, exact packaged Kundur
  static tables and unchanged 10-bus/15-line connectivity, four structurally
  clean REGCV1 devices mapped to static generators and buses 1--4, 900-MVA
  device ratings, the R386/R387 parameter card, and zero legacy dynamic/event
  records.
- Bank: one fresh zero-step reference plus four devices times two input
  channels times two signs, exactly 17 independent trajectories. `Pref` was
  prospectively mapped to achieved `Pe`; `Qref` to achieved `Qe`.
- Intervention: one post-init absolute setpoint increment of `+/-0.09` system
  pu, equal to 9 MW or 9 Mvar and one percent of the registered device rating.
  Each arm had `tf=2.0 s` and TDS tolerance `1e-4`.
- Gates: requested/applied tolerance `1e-12`; signed and target-attribution
  floors `2e-4` system pu; paired-separation floor `4e-4`; bus voltage
  `[0.9,1.1] pu`; current magnitude at most `10 pu`; apparent power at most
  `9` system pu; virtual speed `[0.95,1.05] pu`.
- Integrity correction only: exact bus identities are order-independent; a
  full explicit initial snapshot precedes the native post-start samples; an
  advanced, short, native-nonconverged trace has a strict scientific STOP
  schema. Every scientific object, arm, threshold, and resource field equals
  R387.
- Lifecycle: one WSL process, one native thread, one create-only formal
  attempt, no retry, controller, feedback, reward, training, topology/model/
  card/threshold change, or overwrite.

## Observations

- [CLM-1085] The corrected compound record passes integrity. The sealed source
  and contract digests, exact arm metadata/order, source/static identity,
  initialization references/diagnostics, initial snapshots, native sample
  grids, action receipts, and partial-trace schemas are admissible. Exactly 17
  arms were attempted and captured with no execution exception; all stored
  values are finite.
- [CLM-1085] The zero arm completes 2.0 s with native convergence and stays
  inside every registered electrical guard. Every one of the 16 action arms
  crosses the electrical envelope: 16 cross voltage, 10 current, 13 apparent
  power, and six virtual speed. Eight action arms terminate early with native
  nonconvergence between `1.026536619317691 s` and
  `1.990480459866156 s`.
- [CLM-1085] Independent receipt replay finds exact requested/applied target
  equality and no non-commanded setpoint change in all 16 interventions. The
  aggregate formal action-identity check is false because its compound
  implementation rejects any arm carrying a scientific-error sentinel, not
  because a receipt shows cross-channel write leakage.
- [CLM-1085] The immutable classifier returns
  `STOP-REGCV1-SIGNED-AUTHORITY`: record integrity, reference preservation,
  zero initialization diagnostics, and finite values pass; native solver and
  electrical-envelope checks fail. Because the bank contains valid advanced
  partial failures, no terminal response or paired-separation table is issued.
- [CLM-1085] Diagnostic-only zero-arm subtraction shows that the target-device
  `Qe` change has the opposite sign to the requested `Qref` step in all eight
  `Qref` arms at the first native sample and 0.5 s. For `Pref`, four of eight
  arms have a target-device `Pe` change with the requested sign at both
  checkpoints. This is a sign-only transient diagnostic, not the registered
  terminal magnitude-floor test. The sealed equations route `Qref` through
  voltage droop rather than a direct post-init `Qe` tracking equation; the
  frozen active loop has `kw=0` (no frequency-droop feedback) and `D=0`, and
  neither loop enforces the registered electrical guards as saturations. These
  structural observations do not establish the cause of the guard/solver
  failures.

## Conclusions

- **Valid bounded negative**: the exact four-device REGCV1/card/port
  formulation fails the pre-registered signed authority gate. Correct
  setpoint writes do not preserve the required electrical/solver envelope,
  and the full bank cannot support valid terminal signed, target-attributed,
  paired authority.
- **Failure boundary**: initialization and zero-input validity from R386 remain
  intact; R387 remains analysis-invalid. R388 establishes failure only after
  nonzero direct reference steps on this one phasor-domain Kundur operating
  point and exact parameter card. It does not reject every converter model or
  every possible REGCV1 parameterization.
- **Route decision**: Q-0106 closes negative. Stop this REGCV1 formulation
  before deterministic decoupling, headroom, information, controller, or MARL
  work. A different model, card, or physically defined power port requires a
  new prospective route decision and repeats object/initialization/authority
  qualification from the beginning.

## Limits

- One simulator, topology, operating point, device card, disturbance size,
  and 2-s horizon were tested. The result is a formulation rejection, not a
  general stability theorem or technology-class rejection.
- Guard violation means the registered experimental envelope was exceeded; it
  is not a hardware safety or protection certification.
- Source-and-trace diagnosis is consistent with insufficient damping and
  unconstrained loop amplification but contains no eigenvalue, modal,
  causal-identification, robustness, EMT, HIL, or field validation.
- The run supports no P/Q-decoupling benefit, controller comparison, learning
  necessity, robustness, topology/VSG-count generalization, real-time,
  hardware, or deployment claim.

## Publication gate

- **Evidence audit**: PASS -- all five reported artifact hashes and sidecars,
  exact arm order/count, finite values, zero/action guards, violation counts,
  partial-terminal range, raw action receipts, formal checks, and source
  equations were independently recomputed. The transient sign observation is
  explicitly zero-arm-subtracted and excluded from the terminal floor test.
- **Domain audit**: PASS -- system/device bases, units, requested/applied/
  achieved separation, native solver failure, electrical-envelope meaning,
  indirect `Qref` semantics, and exact-formulation inference boundary are
  physically consistent. Mechanism wording remains non-causal and no
  stability, safety, decoupling, or learning conclusion is admitted.
- **External context**: NOT-APPLICABLE -- no literature, novelty, comparative,
  venue, or related-work claim is introduced.
- **Claim disposition**: QUALIFY -- admit only the bounded negative for this
  exact formulation and the no-controller/no-training stopping decision.
- **Allowed claim**: one sealed integrity-corrected bank rejects the exact
  ANDES 2.0.0/Kundur/four-REGCV1/card/port formulation at signed authority:
  direct writes are exact, the zero arm is admissible, all nonzero arms leave
  the registered envelope, and eight end with native nonconvergence.
- **Stay-out**: every-controller or every-converter failure; certified
  instability/safety; decoupling, learning, generalization, EMT, HIL, or
  deployment claims.

## Evidence audit table

| Claim | Canonical locator | Pre-audit status | Safe wording |
|---|---|---|---|
| Formal classification is a valid scientific STOP | `formal_analysis.json#/classification` and `#/checks` | VERIFIED | The immutable corrected classifier returns STOP with record integrity true. |
| All 17 arms are captured; zero is clean; all 16 actions leave the envelope | `formal_execution.json#/arms`, `#/trajectory_attempted_count`, `#/trajectory_executed_count` | VERIFIED | The zero/action contrast rejects this exact nonzero-step formulation. |
| Eight advanced partial traces are native solver failures | `formal_execution.json#/arms/*/scientific_error`, `#/solver` | VERIFIED | Typed partial traces are retained and force the solver gate to fail. |
| All direct writes are exact and isolated | `formal_execution.json#/arms/*/action` | VERIFIED | Receipt replay excludes software write leakage as the primary failure. |
| The tested model class is universally unstable | no such evidence | UNSUPPORTED | The result is bounded to one model/card/network/operating point and intervention contract. |

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| Clean zero-input object but failed nonzero signed-authority gate | future problem/method motivation | qualified; exact formulation only |
| Guard and native-convergence failures | model-qualification negative result | qualified; no hardware-safety wording |
| Early directional/source-equation diagnosis | future route design rationale | diagnostic only, not a causal theorem |
| Deterministic decoupling, controller, learning | none | stay out; gate never opened |

## Follow-up pointers

- Close Q-0106 negative and leave the programme with no authorized controller,
  decoupling, or training experiment on this formulation.
- Treat REGCV2, REGF2, another converter object, a changed card, or a separately
  modeled physical power port as a new route decision, not a retry.
- Do not reduce the step, relax guards, tune gains, or continue algorithms
  against the failed formulation under R388 authority.
