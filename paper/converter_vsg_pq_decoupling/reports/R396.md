# R396 feed — PPVSM1 two-unit object gate pass

## Identity

- Round: R396. Claim: CLM-1125. Question: Q-0110. Date: 2026-08-14.
- Runner: `scripts/run_r396_ppvsm1_object_gate.py`; classifier:
  `src/andes_rl_kundur/evaluation/ppvsm1_object_gate.py`.
- Seal: `memory/rounds/R396/formal_seal.json` (sha256
  `ce4cfe81d6c17dcd84d487ebac58a65890e955402b6db27c8d24b75bafb4383d`).
- Results root: `results/research_loop/r396_ppvsm1_object_gate/`.

## Frozen setup

- Two-unit PPVSM1 cell on the unchanged Kundur static case (buses 1-2;
  StaticGen 3-4 anchors; 900-MVA rating; frozen card; no PLL in the main
  loop). One serial arm: power flow, native init, equilibrium EIG, frozen
  0.2-second zero-input trajectory. Drift ceiling 2e-4 system pu;
  positive-real threshold 1e-7; allowed zero modes 1.

## Observations

- O1 (CLM-1125): all eleven registered checks pass; references deviation 0.0;
  residual count 0; no clamps; the 0.2-second trajectory converges.
- O2 (CLM-1125): the 18-by-18 reduced spectrum has no eigenvalue with
  Re > 1e-7 and exactly one near-zero root (+3.6095684200836956e-13, the
  network common-angle degeneracy); the next modes are -1.131214,
  -1.134297 +/- j0.402273, -3.630964 (see
  `formal_analysis.json` /spectrum_summary).
- O3 (CLM-1125): by construction the stopped object's eight conserved
  integrator directions are absent (no Psen/Psig/limit-PI states), and the
  two positive-real local modes of the stock REGF2 object do not appear.

## Conclusions

- C1 (sealed-bank time-domain + linearized small-signal): the redesigned
  two-unit object initializes cleanly, stays inside the zero-input
  stationarity envelope, and its reduced spectrum shows no positive-real
  mode and no neutral degeneracy beyond the network common angle.
- C2 (analytic boundary): the object pass opens only a separately registered
  signed P/Q authority gate; it establishes no authority, controller,
  decoupling, learning, robustness, or hardware claim.

## Limits

- Covers only the frozen two-unit cell at the registered operating point;
  no perturbation, authority, action, controller, decoupling, MARL,
  topology, EMT, HIL, or deployment work. Exact steady-state droop-slope
  matching against the stopped object is a later verification, not this
  gate.

## Publication gate

- **Evidence audit**: PASS — every inline number traces to
  `formal_analysis.json` /checks and /spectrum_summary; the classification
  matches the classifier output.
- **Domain audit**: PASS — units per second, 60-Hz card, per-device local
  control with no inter-device communication, projected P/Q limits retained,
  phasor-domain boundary respected (research-skill-adapter mandatory checks).
- **External context**: NOT-APPLICABLE — all statements are internal to the
  installed ANDES model at one operating point.
- **Claim disposition**: ENTER — CLM-1125 registers the bounded object pass.
- **Allowed claim**: the CLM-1125 statement.
- **Stay-out**: signed authority, controller value, decoupling value,
  learning necessity, robustness, and hardware claims.

## Manuscript mapping

- O1 -> candidate-object qualification section: initialization and
  stationarity evidence.
- O2 -> same section: reduced-spectrum guard evidence.
- O3 -> same section: conserved-direction removal by construction.
- C1 -> same section conclusion (prospective; only after the manuscript
  stage reopens).
- C2, Limits -> claim-boundary paragraph.

## Follow-up pointers

- Q-0110 closed-positive by CLM-1125 (this round).
- pending: the signed P/Q authority gate requires a separately registered
  round (see route contract).
