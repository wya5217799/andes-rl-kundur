# Prospective Yang direct-M/D successor decision (2026-08-25)

## Status

OWNER-APPROVED (2026-08-25). The owner explicitly accepted abandonment of the
bounded positive energy-port claim and selected the direct-M/D main-line
re-verification route (condition 1 below satisfied). This document is now the
route decision for the successor generation; it is not an execution authority
by itself — conditions 2-7 remain binding prerequisites for any formal bank.

## Evidence boundary

The repair3 zero-family capacity and semantic rehearsal are authority-valid.
All subsequent direct-M/D and energy-port runs are preserved as quarantined,
development-visible diagnostics. Their consistent patterns can motivate this
proposal but cannot enter a formal claim. Viewing `eval_a`--`eval_d` consumed
their holdout status permanently.

## Recommended route decision

Abandon the paper's bounded positive energy-port claim instead of spending the
remaining time on its formal deterministic bank. Retain only the direct-M/D
mechanism as the candidate core. This is the recommended judgment because two
separate diagnostic energy banks missed the 5% differential-improvement gate,
while all six diagnostic direct profiles passed their paired response gates.

## Conditions before any execution

1. Owner explicitly accepts abandonment of the energy-port positive claim.
2. Create a successor round/authority generation; do not reuse repair3.
3. Prospectively generate and freeze genuinely fresh direct-M/D holdout
   profiles. Exclude `eval_a`--`eval_d` and every viewed condition.
4. Register exactly one formal deterministic direct-M/D bank, its stop rule,
   profiles, seeds, metrics, thresholds, output root, and report path.
5. Bind every physical command to matching pre/post transitive Python-source
   manifests, installed ANDES module/distribution, and Kundur case hashes.
6. Require WSL/POSIX and `scripts/andes_scratch.py` fail-closed at each physical
   entrypoint.
7. Keep training, MARL attribution, topology expansion, and manuscript-number
   replacement closed until that formal deterministic bank passes.

## Alternative

If the owner does not accept abandonment, follow the frozen route and run only
the formally authorized deterministic energy-port bank next. Do not open a
direct formal bank in parallel.
