# GPT Pro additional-data request — project intake

## Integrity and role

- Imported from
  `C:\Users\27443\Downloads\gpt_pro_additional_data_request_20260821 (1)`
  on 2026-08-21.
- The 12 declared source hashes pass 12/12. The source files are preserved
  unchanged beside this note.
- Role: external experiment-design request. It is not experimental authority,
  a frozen project plan, or evidence of any requested result.

## State reconciliation

- The request's R458 snapshot is stale. R458 is already completed and sealed as
  `GUARD-CLEAN-TRANSFER`; it must not be rerun. U9 enters this programme only as
  independent verification, provenance export, and packaging of the immutable
  R458 artifacts.
- R451 remains `CANARY-INVALID`. U2 requires a new successor round and cannot
  reuse or repair R451 outcomes.
- Historical R431 checkpoint files exist, but no archived replay-buffer file has
  been located. Exact historical Bellman-bias reconstruction therefore remains
  conditional on a later inventory finding the original transitions.
- The working tree is dirty and contains extensive pre-existing user work.
  Provenance must preserve the tracked diff plus content hashes of every actual
  source/input; it must not claim that one git commit alone recreates the run.

## Project execution disposition

The owner's instruction to run all requested experiments is accepted under the
existing failure-mathematics advisory programme, with project-native evidence
rounds and these boundaries:

1. WP0/WP1 shared provenance, object, model, unit, profile, reference, and
   execution-contract exports land first in one create-only evidence round.
2. U3 execution/replay/target semantics and U4 independent guard recomputation
   precede any new training.
3. U1 and U5--U8 each receive a bounded successor round; certificate or local
   Taylor failure states are valid terminal results and are never filled with
   invented values.
4. U9 is independently checked from sealed R458 without rewriting it.
5. U2's 18-cell factorial runs last, after a prospective power/materiality
   contract and a passed U3 semantics gate.
6. Distributional transfer, HIL/EMT, deployment safety, larger grids, and a
   universal controller or message-value claim remain outside this request.

## Stop rules

- A source/seal/hash mismatch, output pre-existence, invalid DAE equilibrium,
  singular required reduction, failed fixed-mode premise, or incomplete raw
  record makes the owning round invalid or not identifiable; it does not permit
  an in-place repair and retry.
- Object A and Object B retain separate actuator, reference, estimator, bank,
  timing, and unit identities. Their numerical ratios are never pooled.
- Each formal result is create-only, hash-sidecarred, independently recomputed,
  bound into a feed and claim, and closed before the next item starts.
