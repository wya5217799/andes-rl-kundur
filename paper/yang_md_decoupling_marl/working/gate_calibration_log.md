# Gate calibration log — yang-md-decoupling-marl

Self-evolution record (SKILL.md j2, R411+). One entry per round: which gate
ran too hard / too soft / right, the observed evidence, and the concrete
adjustment. Gates are reminders with pause checkpoints; the owner decides at
pauses; adjustments land via CLAUDE.md/SKILL.md edits or an issue/ADR,
never improvised mid-round.

Seed entries from the R410 session (2026-08-16):

| Round | Gate | Verdict | Evidence | Adjustment (status) |
|---|---|---|---|
| R473 | Hardlink reuse gate (frozen inventory + on-disk re-verification + NTFS hardlink import) | right | 96/108 shards re-verified and imported as 816 hardlinks with zero additional bytes (all_same_inode=true); the 12 missing cells trained fresh; evaluation/aggregate ran byte-identical under the sealed R470 parent and classified U2-SOURCE-EFFECT-SUPPORTED | Keep: successor reuse stays gated on frozen inventory + re-verification + hardlink import with provenance manifest |
| R473 | Log-scale CI quoting in feeds (publication-gate domain audit) | too-soft | The domain auditor caught that the log-scale CI lower bound 0.1287 was quoted as "12.9%" in the feed's Conclusions and Allowed claim; the correct geometric conversion is exp(0.1287)-1 = 13.7% (the log-scale comparison to log(1.10) was unaffected) | Consider: feeds quoting bootstrap CIs must state the scale (log vs geometric %) or convert before quoting; codify a feed_check/claim-template note if it recurs |
| R433 | Reward-shaping semantic gate (penalty gradient-direction probe) | right | The rehearsal penalty_gradient_direction_probe pinned descent semantics on the real learner (gradient aligned with -a, penalty non-positive, penalized reward <= plain) and the projection probe verified penalty exactness (4.1e-9) before seal; objective_semantics_lint passes the SAC marker path | Keep: reward-shaping rounds keep the exact-formula plan + rehearsal gradient probe + exactness check; the lint hint list could add the penalty_gradient_direction_probe marker name to silence the informational HINT |
| R433 | Dev-lambda selection rule (smallest lambda with dev action-RMS <= 0.8x baseline) | too-soft | The selection screened only mean action magnitude on development data and did not anticipate the guard-profile trade-off (action-RMS fixed but common-frequency/worst-peak regressed 0/20 -> 20/20 and endpoints degraded 0.635/0.590 -> 1.229/1.221); the formal holdout revealed the cost that the dev screen could not see | Consider: dev selection for reward shaping should also screen guard-profile drift (e.g., re-evaluate dev checkpoints against the no-harm guards) or freeze a narrower lambda; needs an issue/ADR if adopted |
| R433 | Eval-wave external interruption (20:36 UTC, both waves killed without driver_result) | right | The interrupted wave's partial records were preserved, the rerun was byte-identical (0 diffs, deterministic eval), and the bank completed cleanly; create-only guards prevented any overwrite | Keep: preserve-then-rerun + byte-diff verification for interrupted eval waves; document in the feed |
| R430 | Successor output-root probe (successor_output_root_probe) | right | R429 was aborted post-seal because an inherited default bound SAC output to R428; the successor rehearsal's output-root probe resolved all nine SAC shards to the R430 root and rejected R428/R429 roots, and the formal run's nine manifests + eval tree landed entirely under the R430 root (driver_result exit 0/0, no engineering failure) | Keep: every engineering-successor round must re-run the output-root probe in rehearsal and pin it in rehearsal_checks |
| R430 | No-slew-projection preregistered validity risk (R429 contract carried over) | right | The preregistered risk materialized exactly as declared: all 24 adapted-SAC evaluation rows fail action_slew_violation (executed per-step deltas median 1.22-1.40 vs 0.25 limit) -> CANARY-INVALID, reported as a measured bounded endpoint with zero tuning/retry while the scalar arm stayed byte-identical to R419 | Keep: no-slew-projection SAC rounds pre-register the validity risk and report, never repair in-round |
| R430 | R429 v3 capacity ladder reuse + fresh no-load check | right | Fresh host snapshot showed no other research processes and the inherited workload/source closure was identical except output routing; the sealed 17-process budget (16 workers + driver) ran the nine-shard training wave clean (all exit 0, wall 7595 s) with no memory pressure | Keep: capacity reuse stays gated on a fresh no-other-process snapshot + identical closure; never resize an active attempt |
| R430 | Objective-semantics gate | right | Rehearsal objective_semantics_probe all-true on the real learner; objective_semantics_lint.py R430 passes (SAC probe valid) — the inherited reward's directions stayed pinned even though R430 changed no objective | Keep: objective_semantics_lint runs at every SAC-family closeout even for pure-engineering successors |
| R428 | C1-SAC pre-registered R18 divergence risk | right | The exact Yang SAC interface on the matched bundle collapsed exactly as pre-registered (paper-strict reward: critic loss ~1e8, alpha ceiling 5.0, log pi +10.5, unslewed actions slew-violating every block, CANARY-INVALID) and was reported as a measured bounded negative result with zero tuning, while the scalar arm stayed byte-identical to R419 | Keep: exact-reproduction rounds pre-register the divergence risk and report (not repair) a collapsed interface |
| R428 | Objective-semantics gate (SAC form, codified additively) | right | The rehearsal sac_semantics_probe (critic-target identity, actor-form, alpha-form, reward non-positive + obs-consistent) pinned the Eq.19-23 objective on the real learner; objective_semantics_lint.py R428 passes via the new sac marker | Keep: the semantics gate now covers SAC-style exact-reproduction objectives, not just penalty/normalization rounds |
| R428 | Close-out lane single-writer (abandoned CLM-1310 stub) | right-again | The resuming session detected and removed an abandoned CLM-1310 stub (empty, from an earlier autonomous reservation) before validate, per the CLM-1260/1280 precedent; the canonical CLM-1305 card passed feed_check | Keep: check-before-write before every close-out file; handoffs name the owning session |---|
| R427 | Pre-registered divergence criterion (original-scale Q4/Q1 < 3) | right | The judgement readout used the ORIGINAL-scale reconstruction (sigma^2-rescaled), pre-registered so the rescaling cannot trivially satisfy the threshold; it fired exactly as designed: 0.32-1.75 across six runs (<3, divergence suppressed) vs R425's 4.65-6.29, with the untouched scalar arm still diverging (raw 3.5-7.0) as the internal control | Keep: any normalization/objective-rescaling round must pre-register a rescaling-invariant judgement readout (original-scale reconstruction) and an untouched internal control |
| R427 | Objective-semantics gate (normalization form, codified additively after R425) | right | The rehearsal sac/normalization probe (output-correction identity 1.06e-7, common-target bitwise 0.0, differential-gradient dot +251.9, decomposition 3.5e-7, stats convergence <1e-3) pinned the normalization semantics before seal; objective_semantics_lint.py R427 passes via the new normalization marker | Keep: the semantics gate covers normalization-style objective changes (exact positive rescale + untouched channel), not just penalty-sign rounds |
| R427 | Tier-1 arm-filter parsing (argparse REMAINDER swallows --arm) | too-soft | The first tier1 launch had arm_filter=None for all three jobs (REMAINDER put `--arm` in args.args, not args.arm); all three raced the scalar arm and two crashed on the create-only trace write — caught at launch, no sealed-artifact damage, fixed by resolving the arm from args.args | Codified: tier1 arm filtering resolves from args.args (test_run_r427 pins `_tier1_arm_from`); any future REMAINDER-based runner must test option parsing before parallel launch |
| R427 | Close-out lane single-writer | right | The R422 lesson held: the resuming session checked feed/claim/verdict existence before writing, the single writer completed the close-out, and the create-only guards protected the sealed results | Keep: check-before-write before every close-out file |
| R425 | Objective-semantics gate (codified after R424) | right | The rehearsal-side penalty-direction probe (rms_dot +0.573, tv_dot +14.8 vs defect_form_dot -3.05) plus the directed tests and the objective_semantics_lint gate pinned the penalty semantics before seal, and the formal run confirms the direction: the sign-corrected constraint pulls action stress down (action-stress guards 36 -> 12, RMS residuals 38.3-90.8x -> 1.1-10.1x) — the gate caught at rehearsal what every mechanical check missed in R424 | Keep: rehearsal-side direction probe stays mandatory for every objective-modifying round |
| R425 | Guard-aligned-constraint pre-registered decision rule (36 -> <36) | right | The key-contrast branch fired exactly as registered: both action-stress guards moved 36 -> 12 (met — first movement in the five-round family), CANARY-FAIL holds because no arm passes all guards, so the report branch applied with no pause; the mu/residual readout matched the registered "constraint binds" expectation (mu at the 10.0 ceiling in 6/6, residual collapsed ~10x) and the same-bundle critic baseline comparison (4.65-6.29 vs R424's 5.98-9.61) is reported as damped-not-stopped | None (branch applied) |
| R425 | Ladder-snapshot discipline (re-measure capacity after runner edit) | right | The capacity evidence was re-measured (old evidence deleted first) before rehearse/prepare after the runner change, per the R424 lesson; the sealed 17-process budget held through the formal run with no memory pressure | Keep: capacity -> rehearse -> prepare order stays strict after any runner edit |
| R425 | Concurrent-round budget declaration (R426 joins) | right | R425 sealed its solo ladder first (16 workers + 1 driver); R426 then joined under its own concurrent ladder (rung 8) declaring other_reserved_processes=17 — the declared change, not a silent re-budget; both rounds ran shard-complete with the total-memory accounting green | Keep: any joining round must declare the in-flight round's processes in its own plan |
| R426 | Bit-repro gate run (fresh message&#124;401 vs stored R410 checkpoint) | right | The fresh gate run's final checkpoint sha256 equals the stored R410 sha byte-for-byte (497ff3c0c50fea26...), so BIT-REPRO-CONFIRMED and the DRIFT branch was not entered; the reused 401/402/403 checkpoints were admitted on their verified sidecars | Keep: reuse precondition = byte-identity gate before any stored checkpoint enters a new reporting layer |
| R426 | Frozen-tree reproduction over stored checkpoints | right | The frozen three-seed tree over the reused checkpoints reproduced the stored R410 guard profile to the block (action-RMS 34/36, action-variation 36/36, common-frequency 24/36, RoCoF 28/36, worst-peak 35/36) and CANARY-FAIL — the reuse seam is bit-consistent at the decision level | Keep: reporting-layer rounds re-run the frozen tree untouched and register the two verdicts separately |
| R426 | Concurrent-load ladder under a live sibling round | right | The ladder was measured with R425's 16 workers live; rung 8 sealed (8 workers + 1 driver, other_reserved=17) under total-memory accounting; seven shards ran in one wave with no memory pressure | Keep: concurrent ladder + total-memory accounting + other_reserved declaration stays the joining-round protocol |
| R426 | Rehearsal scope (B2 checkpoint-source + gate-comparison seams) | right | The rehearsal exercised the stored-vs-fresh checkpoint source resolution and the gate-comparison sanity (save/reload sha equality, same-name copies judged matches — the torch.save zip-member-name fact), so the seam was verified before the formal run instead of failing in it | Keep: B2-type reuse seams stay rehearsal-checked; gate comparisons only valid between same-name checkpoint files |
| R423 | Value-estimation-repair pre-registered decision rule | right | The CANARY-FAIL branch fired as designed (36/36 blocks still violate guards, no classification flip), and the frozen critic-loss readout judged the divergence persisting (Q4/Q1 5.0-8.0 >= the 3x stopping threshold) — the damped-not-stopped result is reported with the first no-harm guard gains (RoCoF 26 -> 22, worst-peak 36 -> 35) | None (branch applied) |
| R423 | Scalar bit-identity anchor (machine-enforced) | right | All three scalar checkpoints are byte-identical to R422 and the scalar slew diagnostics match to the last decimal, certifying the repair touched only the CD critic update path | Keep: every learner-side single-factor round machine-checks the unchanged arm's checkpoint sha against the predecessor |
| R423 | Single-factor base choice (R422 bundle carries the budget confound) | right | The clip was tested on the R422 bundle whose effort term consumes the common budget (multiplier stays at the 10.0 cap in 6/6 even under the clipped critic), so the clip's effect is measured under a structural budget excess; the theory layer (GPT A1/A2, numerically verified) identifies this confound and routes the next round back to the unpenalized R419 base | Recorded decision: R424 returns to the R419 bundle; future single-factor rounds choose the confound-free base and state the inherited-bundle caveat in the plan |
| R424 | Guard-aligned-constraint pre-registered decision rule | right | The CANARY-FAIL branch fired exactly as designed: action-stress blocks stayed 36/36 (pre-registered negative on the guard-level criterion), and the frozen dual readout (all 240 multipliers at the 10.0 ceiling, RMS residuals 38.3-90.8x) plus the sealed action traces (saturation median 0.90 across all 36 blocks, saturation-budget guard 0 -> 24) located the mechanism with no retry; the incidental no-harm gains (common-frequency 36 -> 12, RoCoF 22 -> 14, worst-peak 35 -> 24) are reported with their costs | None (branch applied) |
| R424 | Rehearsal scope (constraint-term direction) | too-soft | The rehearsal exercised the guard-term actor update path but never checked the SIGN of the constraint gradient; the sealed learner enters the terms inside the actor-loss negation, turning the guard-aligned penalties into action-stress rewards (gradient probe inner product -97.6 vs the penalty direction) — the mechanism as designed was never actually tested, and the owner caught the masking chain on raw-data recheck | Codified: any round whose single factor adds objective terms must run a rehearsal-side direction check (gradient probe / finite-difference) pinning penalty-vs-reward semantics before seal; the audit script now pins the defect at the sealed source |
| R424 | Scalar bit-identity anchor (vs R419) | right | All three scalar checkpoints are byte-identical to R419 and the scalar medians match to the last digit (0.00153891 / 0.00663988), certifying the repair touched only the CD actor objective | Keep: every learner-side single-factor round machine-checks the unchanged arm's checkpoint sha against the predecessor |
| R424 | Close-out lane single-writer (R422 rule re-check) | right-again | The rule held: the resuming session checked feed/claim existence before writing, detected the previous session's empty CLM-1280 stub (reserved 23:32), completed the close-out under CLM-1285, and removed the abandoned stub per the CLM-1260 precedent; create-only guards protected the sealed results throughout | Keep: check-before-write before every close-out file; handoffs name the owning session |
| R424 | Next-round selection (registered logic) | right | The mu traces do NOT show the constraints pinning the training statistics (residuals stayed 46-90x, dual at ceiling), so the per-profile-multiplier branch is not triggered; the critic divergence persists (Q4/Q1 5.98-9.61 >= 3) -> the registered fallback selects DR-menu P1 target normalization as the next single factor | Recorded decision: next round = critic target normalization (P1), designed under the owner's short-budget/dense-probe two-tier directive |
| R423 | External theory intake (GPT Pro A1/A2 answer) | right | Ingested as a registered design aid; its algebraic claims (c_c modal decomposition, effort Parseval split, budget necessary condition) were verified numerically on sealed trajectories (residuals <= 7e-18) by a repo probe before any feed/manuscript use; no frozen run was modified | Keep: external answers are design aids only — algebraic claims get repo-side numerical verification before entering feeds or the manuscript |
| loop | Round-length and probe-density directive (owner 2026-08-17 night) | right | Owner instruction: small steps, fast runs — each simulation should carry as many probes as possible and collect more data for analysis; avoid single simulations that run so long. The binding levers for wall time are per-run interaction budget, seeds/arms, and worker count; ANDES step cost is fixed by the solver | Adopted: future round protocols prefer shorter per-run interaction budgets with denser per-episode probes (R421/B3-style instrumentation density becomes the norm); any budget reduction is a declared protocol factor — bit-anchor comparability with the R419 bundle chain (scalar checkpoint sha, deterministic reference) must be handled explicitly in the plan; in-flight sealed rounds are exempt (R424 keeps its frozen 43,200-step budget) |
| loop | Standing concurrency authority (owner 2026-08-17 night) | overridden-by-owner | Owner directive: whenever hardware has surplus, run more in parallel — "只要有硬件富裕，就可以多并行，我给你的权力，优化仓库规则". The one-active-round-per-line posture and the exclusive-round default are replaced by measured-surplus concurrency | Adopted: CLAUDE.md 并行预算 + SKILL.md evidence lane + resume-contract updated; same-line concurrent rounds are the default posture under concurrent-load ladder + total-memory accounting + other_reserved_processes declaration; frozen rounds keep their sealed budgets and their environment must not be silently changed after a new round declares them |
| loop | Research-purpose directive (owner 2026-08-17 night) | right | Owner: the current purpose is RL algorithm tuning and optimization — compete against the classical controller with the best-optimized RL algorithm; simulations should collect as much analyzable information as possible; avoid long single simulations. Owner insight: an untuned network cannot indict the algorithm class. The loop's R421-R423 evidence (critic divergence Q4/Q1 5-8x, vanishing actor gradients 5/6, budget saturation) already instantiates "not well tuned" as concrete training-dynamics defects, so the tuning programme converges with the current single-factor repair direction | Adopted: (1) two-tier round design — Tier-1 probe screening (short budget ~20% steps, 1-2 seeds, development-only, dense per-update diagnostics) feeding Tier-2 sealed evidence rounds (full protocol + anchors, paper-facing); (2) tuning = single-factor pre-registered rounds, never blind hyperparameter grid sweeps — the R86 plateau rule stays, its evidence is old-bundle so single-factor tuning on the R419+ bundle is open; (3) C1-SAC exact reproduction is the direct well-tuned-RL-vs-classical matchup once the owner lifts its execution gate |
| loop | Next-round selection (R423 outcome + GPT theory + owner order 2026-08-17) | right | The clip damps but does not stop the divergence (5-8x vs the 3x threshold), action-stress stays 36/36 across all four rounds of the family, and the verified constraint-hierarchy theory shows the action-stress statistics are absent from the training constraint entirely — so the next single factor is the guard-aligned action constraints, not further value-scale repairs | Recorded decision: R424 = R419 bundle + guard-aligned action-RMS and total-variation constraints with per-episode projected multipliers (owner-ordered algorithm optimization); candidate protocol in tmp/yang_md_decoupling_marl/r424_candidate_protocol_draft.md |
| R422 | Close-out lane single-writer | too-soft | Two sessions resumed the same handoff and raced the close-out: both reserved a claim for R422 (CLM-1260 abandoned vs CLM-1265 canonical), both launched the sealed evaluate/classify; the create-only guard rejected every duplicate write and the first writer's close-out validated green, so no artifact damage, but duplicate work plus one abandoned claim id | Recorded practice: before writing any close-out file a resuming session must verify the round state, the existing feed, and existing claim reservations — check, do not assume; handoffs name the owning session |
| R422 | B1 channel-placement pre-registered decision rule | right | The CANARY-FAIL branch fired exactly as designed: the common-channel action-effort term did not flip the classification and no arm passed all guards, so no pause; the Lagrange saturation (median 10.0 in 6/6 vs ~0 in R419/R420) and the guard profile (worst-peak 34 -> 36, RoCoF 21 -> 26) are reported as a negative result | None (branch applied) |
| R422 | Owner-concurrency ladder (rung 16, total-memory rule) | right | The concurrent-load ladder selected rung 16 with all records valid and the total-memory guard passing; nine shards ran in one wave with 9 live workers (budget 17 is a ceiling, not a quota) and no memory pressure | Keep: total-memory accounting + max-valid-rung policy under concurrent load |
| R422 | Loop next-round selection (joint with R421 readout) | right | Three-base evidence (R419 slack budget + best endpoints; R420 differential regression; R422 common-channel saturation with no guard gain) plus the R421 readout (critic divergence, vanishing actor gradients) localizes the residual to learning dynamics, so further reward-shaping rounds are stopped and the next single factor targets value-estimation stability | Recorded decision: next round = value-estimation stability repair (bounded literature check first); B2/C1-SAC stay queued |
| R421 | B3 bit-identity anchor | right | All six diagnostic reruns reproduced the R410 checkpoints byte-for-byte (R410-BIT-IDENTICAL), certifying that the read-only instrumentation never perturbs the RNG stream or the computation | Keep: every log-only rerun carries a byte-identity anchor against the sealed predecessor |
| R421 | P3 readout probe (execution amendment) | too-soft-on-first-write | First probe pass read the anchor verdict from the wrong JSON level (wrote null) before any ledger entry; caught during review and regenerated pre-ledger (R411 precedent) | Keep: derived views must resolve the exact JSON Pointer against the sealed file before running; regeneration before ledger entry is the registered recovery |
| R421 | Loop next-round selection | right | The readout shows critic divergence + vanishing actor gradients + a never-binding common-channel Lagrange budget on the R410 bundle; the in-flight R422 common-channel term may activate that budget, so the loop waits for R422 before the next single-factor decision | Recorded decision: next selection uses R422 outcome + this readout jointly (calibration-log rule) |
| loop | Owner concurrency authorization 2026-08-17 | overridden-by-owner | Owner ordered full-hardware utilization for multi-hour simulations: same-line concurrent rounds, total-memory accounting (all concurrent live-training RSS + 3 GiB OS floor <= WSL MemTotal), max-valid-memory-safe rung under concurrent-load ladders (5% marginal chain waived while another round is active), and each new round declares the live processes of the other round via other_reserved_processes | Adopted: mission file + this log record the grant; R422 runs concurrently with R421 (other_reserved=7); solo-run ladders keep the original 5% chain + headroom rule |
| loop | Capacity memory rule (owner decision 2026-08-17) | too-hard | Owner asked for more parallelism: host 32 logical cores / 31.2 GiB, WSL configured 24 GB (MemAvailable 15.8 GiB at ladder time); the half-of-available rule capped training ladders at 8 workers while rounds carry 6-9 training shards, so most cores idle and 9-shard rounds need two waves | Adopted: training ladders now offer rungs 1/2/4/8/12/16; memory rule = projected live-training RSS (900 MB floor per worker) must not exceed WSL MemAvailable minus a fixed 4 GiB OS headroom (mission file updated); applies to FUTURE rounds only — a sealed in-flight round never re-budgets |
| R420 | B1 objective-repair pre-registered decision rule | right | The CANARY-FAIL branch fired exactly as designed: the differential-channel action-effort penalty (weight 1.0) did not flip the classification and no arm passed all guards, so no pause; the endpoint regression (message arm 0.6954 -> 2.3641 off-diagonal ratio, message increment +43.45% -> -35.25%) and the worsened no-harm guard profile (common-frequency 19 -> 36) are reported as a negative result with the residual further narrowed | None (branch applied) |
| R420 | Single-factor isolation control (unchanged scalar arm) | right | The scalar arm's three-seed medians are identical to R419 (0.0015389 / 0.0066399), certifying the reward change touched only the CD differential channel and the environment/harness is drift-free across rounds | Keep: every single-factor round keeps one unchanged in-round arm as the isolation control |
| R420 | Loop next-round selection | right | The measured mechanism (differential-channel action-effort penalty trades decoupling response for action regularization and leaves the common-mode no-harm channel untouched) selects the common-channel no-harm objective on the unpenalized R419 bundle as the next single-factor target, ahead of B3 diagnostics | Recorded decision: next round = R419 bundle + common-channel no-harm term (single factor, same seeds/budgets) |
| R419 | B1 pre-registered decision rule | right | The branch fired exactly as designed: no arm passed all guards and the classification did not flip, so no pause was triggered; the endpoint flip and positive message increment are reported per evidence with the residual failure localized to the objective-to-gate gap | None (branch applied, manuscript updated) |
| R419 | Seal-vs-runtime learner bytes | too-soft | Post-seal learner additions (B3 diagnostics) drifted the sealed hash and blocked the ninth training shard at the seal check (zero training steps, no artifacts); additionally a Windows text write silently converted LF to CRLF, so the first restore still mismatched on disk | Codified: (1) diagnostic additions live in a separate module (cd_matd3_diagnostics.py), never in the sealed learner file; (2) restore-to-seal operations must verify the ON-DISK sha256 with newline-preserving writes, not the in-memory string |
| R419 | Loop next-round selection | right | The measured mechanism (objective-to-gate gap as the residual factor) selects an action-effort objective repair on the B1 bundle as the next single-factor round, ahead of B3 diagnostics | Recorded decision: next round = B1 bundle + R403-style action-effort term (weight 1.0, frozen), same seeds/budgets |
| R419 | P1 feature-usage diagnostic | right | P1 implication 3 (record whether the policy uses the added feature) was not covered by the sealed runner; a plan-registered zero-out ablation probe (`probes/r419_prev_action_ablation.py`) re-evaluates the nine checkpoints with the prev-action actor input zeroed and compares endpoints with the sealed full-feature records | Keep: literature-derived diagnostic implications must map to a registered probe or runner field before sealing |
| loop | Per-round goal-achievement audit (2026-08-17) | right | Independent audit script re-derived every headline number of R411/R413/R415/R416/R417 from the sealed records with the frozen estimators: all sidecars valid, all pre-registered criteria met, all recomputed values match to 1e-6/1e-9, all manuscript bindings present; AUDIT PASS | Keep: run the audit after every completed loop round; the audit script stays in tmp/ (reviewer working notes) |
| R418 | Rehearsal scope (store/update path) | too-soft | Rehearsal exercised act/project/save/load but not the replay store or learner update seam, so the unflattened prev-action store bug crashed all nine training shards at the first step after seal; round aborted per rules | Codified in the R419 runner: rehearsal performs one replay store + one learner update per arm; the runner test pins the flattened (8,) store shape |
| R417 | Gain breadth on held-out blocks (P2) | right | Deep research confirms fixed-gain breadth tables are defensible if the gain was disclosed before the blocks were scored (K=4.0: R408 2026-08-15 < R415 blocks 2026-08-17); a third gain on the same blocks would be test-set search | Codified: after R417 the three A4 blocks are closed to any further gain evaluation; any adaptive/scheduled-gain follow-up needs fresh blocks (feedback_loop_deep_research_2026-08-17.md) |
| loop | B1 slew-state protocol (P1) | right | Deep research confirms the registered single factor (previous executed action in the actor state + target-action alignment) as the canonical repair; augmentation alone is insufficient | Adopted: B1 keeps augmentation + target alignment together; adds slew-saturation rate / execution-mismatch / guard-failure diagnostics |
| loop | B3 diagnostic set (P3) | right | Deep research ranks per-seed full curves > Bellman residual trend > per-module gradient norms > replay coverage, with pre-registered readout rules required | Adopted: B3 pre-registers the four diagnostics as primary endpoints with a readout-to-failure-class map before launch |
| R416 | Nine-law anchor | right | All anchor deviations 0.0: the expanded-family re-execution reproduced R399's selection and oracle values bit-consistently | None |
| R416 | Analysis-step amendment (probe) | right | The sealed runner's classify mis-read the R399 JSON shape (string classification); the plan-registered probe applied the same frozen classifier on the sealed records without re-running trajectories (R411 precedent) | Keep: analysis-step fixes after sealed execution go through a plan-registered read-only probe, never a runner edit |
| R415 | Endpoint thresholds on unseen blocks | right | The pre-registered fail branch fired exactly once (relaxed-inertia block, r_d 0.9712 > 0.95, all guards pass) and was reported as a scope boundary with no retry | None |
| R415 | Serial 1-process budget | right | 32-task serial anchor matched the <=20 min plain-seam protocol decision; execution and classification completed in one process | None |
| R414 | Seal-vs-runtime contract comparison | too-soft | The seal stored the block list with in-memory tuples while the JSON read-back yields lists, so load_seal declared drift before the formal attempt; round aborted per rules, successor R415 re-runs | Codified in the R415 runner: one `_blocks_canonical()` JSON-round-tripped form used by both prepare and load_seal, plus a regression test pinning the round-trip |
| R412 | EIG hard gate failure capture | too-soft | Two init-divergent variants (VSG tie outages) crashed the sealed runner's eig_gate (NaN spectrum -> LinAlgError) instead of recording a gate failure; round aborted per rules, successor R413 re-runs with graceful capture | Codified in the R413 runner: eig_gate wraps every step (reset/TDS.init/EIG/guard) and records failure; rehearsal now exercises the failure path (one passing outage + the two known-fail variants) |
| R412 | Rehearsal scope (failure path) | too-soft | Rehearsal exercised only the nominal passing path, so the crash surfaced during formal execution | Codified: a rehearsal must exercise at least one expected-fail path of the runner's gate machinery (R413 rehearse does) |
| R413 | Base-case anchor (1e-6 relative) | right | Nominal variant reproduced R408 (r_d 0.9389, r_cross 0.5398) within 7e-7; harness identity certified across the whole variant table | None |
| R413 | EIG hard gate ordering | right | The gate pre-filtered the two unsound variants (VSG tie outages, divergent init) and kept them out of the robustness set exactly as pre-registered | None |
| R413 | Capacity ladder 5%+half-memory | right | Reproducible selection (rung 8, budget 9) across the aborted R412 and the successor; no flip-flop | None |
| R411 | Creative-mode plan decision (order/deck) | right | Default deck A1 first; B1 (training, ~7h) deferred to the extension per the ROI asymmetry verdict and night budget; recorded here per the creative-mode no-pause clause | None (decision recorded) |
| R411 | Capacity ladder 5%+half-memory | right | 32 tasks/rung measured cleanly at all six rungs; rung 8 selected, rung 12 rejected by the half-memory rule (944 MB floor), no flip-flop; confirm-window re-measure not triggered | None |
| R411 | Drift-anchor tolerance 1e-6 | right | Amplitude-1.0 re-evaluation bit-identical to R410 (0 row mismatches); environment identity certified | None |
| R411 | Amplitude-invariance spread bound 20% | right | 11/18 cells sensitive — the bound discriminated verdict robustness (invariant) from ratio magnitudes (sensitive) as designed | None |
| R411 | Derived-view amendment (probe formula) | too-soft-on-first-write | First probe pass used median-of-ratios and missed the R410 endpoint-table formula (median-then-ratio), producing -70.93% vs the headline -78.43%; caught before ledger entry, output regenerated with the R410 formula | Keep: derived views must name the exact parent formula in the amendment before running (registered in the R411 plan text) |
| R410 | Capacity ladder 5%-marginal rule | too-noisy | Two pre-seal ladders disagreed at rung 8 (+5.3% then <5%) with 4-16 tasks per rung; budget flip-flopped 9 -> 5 | Codified: >=32 tasks per rung + confirmation re-measure when marginal gain is within 5%+-2pp (SKILL.md §4) |
| R410 | Drift-anchor tolerance 1e-6 | too-strict-as-premised | Anchor premise "identical code path => bit-repro" missed the R402-registered pre-repair slew projector; deviations 1.8-23% were real environment drift, not non-determinism | Codified: comparison-identifiability pre-check — enumerate environment factors and prove single-factor before cross-round numeric comparisons (SKILL.md §4); tolerance itself stays |
| R410 | Cold-start context budget 24576 B | too-tight | Three compression rounds plus hash-snapshot stripping to fit LINE+ARTIFACTS after registering new assets | Pending structural fix: move ARTIFACTS input_hashes to a companion file (needs its own issue/ADR); interim compression accepted |
| R410 | Publication-gate domain audit | too-soft | The named domain-audit owner was unavailable; a manual adapter-checklist pass substituted | Pending: register the missing audit owner or accept the adapter fallback explicitly (issue) |
| R410 | feed_check claim-feed binding | right | Blocked one orphan claim state before close |
| R410 | validate PI-jargon ban (ADR-0011) | right | Caught a banned term in the briefing before delivery |
| R410 | repo_health NAV/ROOT checks | right | Caught one stale decision_ref anchor and one YAML colon parse bug; one root ANDES leftover |
| R410 | Eval-shard execution amendment | right | Plan-registered concurrency-only amendment worked within the sealed budget |
| R431 | validate PI-jargon ban (ADR-0011) | too-hard | PI reads “智能体” as plain language; blacklist entry removed 2026-08-19 (owner call) | Landed: “智能体” removed from PI_PROJECT_JARGON_TERMS in memory/tools/validate.py with inline record |
| R433 | objective_semantics_lint probe-form coverage | too-soft | lint expected the R425 constraint form (rms/tv/defect dots); the mean-square penalty form has no TV term by construction | Landed: lint accepts explicit tv_not_penalized declaration (memory/tools/objective_semantics_lint.py, 2026-08-19); rehearsal amendment recorded in seal |
| R433 | dev-lambda selection metric | too-narrow | screened only training action-RMS; did not anticipate the guard-profile trade-off (frequency-restoration regression) | Recorded in feed Limits; future shaping selection should screen guard profiles, not a single statistic |
| R432 | validate PI-briefing plain-language (ADR-0011) + feed-check | right | B3 reporting-only feed passed evidence/domain audits after two numeric-bound corrections (lagrange end bracket, cost population range) and one stale-wording fix; verdict TL;DR numbers aligned to the feed after audit | None |
| R434 | Nominal-variant harness anchor (rows must reproduce R433 eval byte-for-byte) | right | The nominal variant's 44 records matched the R433 eval records exactly (anchor passed=true), certifying the variant-env seam adds zero drift — the R413-style base-case anchor worked as designed | Keep: every topology/eval-successor round keeps a byte-identity nominal anchor |
| R434 | EIG soundness citation gate (10 variants from R413, never re-run) | right | The eig_soundness_reference_probe pinned EIG_SOUND_VARIANTS to the sealed R413 eig_passing_variants; the two VSG-tie variants stayed excluded by the case-level verdict | Keep: cite case-level soundness, never re-run excluded variants |
| R434 | Shard-manifest driver contract (JSON array) | too-soft | shards() first wrote a newline-delimited file; the shared driver requires a JSON list, so the first eval wave died at json.loads (caught at launch, zero artifacts, fixed + recorded as a seal execution-amendment) | Codified: any runner with a shards command must emit the driver's JSON-array format (test_run_r434/r435 pin the shape); check the driver contract before writing the generator |
| R434 | Pause condition (any arm all-guards-pass on any variant) | right | No variant triggered it (all CANARY-FAIL); the pre-registered negative branch applied without a pause | None |
| R435 | Multiplier-floor causal verdict rule (paired-vs-R432 primary, within-run secondary) | right | The floor held mechanically (6/6 lagrange_final >= 1.0) while the primary criterion failed (1/6 pairs < 0.8), yielding a clean REFUTED — the mechanism was pressing and the cost did not obey; the modest tail improvement is reported as secondary, not a pass | None |
| R435 | External-theory-intake gate on an internal hypothesis | too-soft | The plan cited the R432 hypothesis (internal feed, not external theory) without the `## Theory intake` observable list the lint now requires; the pre-registered judgement WAS the observable list — formalized post-seal via a seal execution-amendment (plan_sha256 refresh) and a recording-amendment on the analysis | Keep: mechanism-prediction rounds register the observable list in the plan before seal even when the hypothesis is internal; the lint wording ("external") is broader than the repo's external-only trigger — flagged for a lint wording review |
| R433-R435 + GPT Pro | External-theory-intake first full cycle | right | GPT Pro's 8-question answer (A1-A3/M1-M3/P1-P2) was absorbed and independently verified: algebra reproduced (A2 projector-invariant-set counterexamples RMS 0.58630; M2 absolute-threshold need 54.2-69.5% vs dev rule 20%); mechanism predictions confirmed by independent rounds (M2 total-penalty endpoint sacrifice -> R433 0.635->1.229; A3 critic-divergence lead -> R435 REFUTED the multiplier-floor hypothesis); and it caught a real R431 feed transcription error (message-arm action-stress 16/20 -> 20/20, fixed via bounded amendment 2026-08-19 with the Evidence-audit record). The R435 lint-wording flag is now landed: the trigger extended from external-theory-only to any mechanism prediction (lint + 2 new tests + CLAUDE.md + reference updated) | None |
| R441 | No-harm guard gap in R439's classifier (R439 too-soft, caught here) | right | R439's `_classify_profile` checked only `valid` + >5% improvement and stored only r_d/r_cross, so the pre-registered no-harm guards never ran. R441 re-ran the 4 winners + static reference and applied the full guard set: GUARD-VIOLATED on action-stress (action RMS +28-34%, total variation +10-17% vs the +10% budget) while all three common-mode no-harm guards stay clean. The endpoint improvement (r_d +6-14%, r_cross +7.6-12%) is confirmed | Keep: any endpoint-only classification must apply the full no-harm guard set (common-mode +3%, action-stress +10%) before claiming "no-harm"; future oracle/endpoint rounds should store full `summarise_profile` output, not just r_d/r_cross, so guards can be re-checked offline |
| R444 | Theory-intake observable-list gate on an evaluation-only round | right | The C.8 mechanism prediction (signed-pair odd response O(eps^2) vs cubic) was registered as a `## Theory intake` observable list before seal, the sealed ladder measured it (12/12 QUADRATIC), and the feed wrote the verdicts back (quadratic branch supported, cubic refuted on range); external_theory_intake_lint passed | Keep: mechanism predictions get the observable list in the plan even when the round is eval-only and the prediction comes from the line's own theory audit |
| R444 | Noise-drop rejection threshold in the order-fit probe | too-soft-adjacent | The first probe version used 4.0x per-halving as the anomaly bound, which mis-flagged a clean quadratic response (exactly 4.0x per halving) as anomalous at float precision; raised to 9.0x (beyond cubic's 8.0x + slack) with a directed test pinning the boundary | Keep: any per-halving ratio threshold must sit strictly outside the expected-order range with slack, and a directed test must pin the exact boundary case |

| R449 | Publication domain audit on full-vs-A-channel sensitivity | right | Audit caught that the sealed runner propagates dA only, so feed/claim were narrowed before close instead of presenting a full P1.1 derivative | Keep explicit derivative-channel inventory in future sensitivity plans |
| R450 | Exact-MIMO delay curve plus nonlinear zero-delay seam | right | The registered command break, return-difference singular-value check, same-bank zero-delay point, and normalized 10% curve gate separated bounded phase-delay consistency from an unsupported stability-margin claim | Keep absolute seam and normalized-curve checks together; never relabel an endpoint crossing as a stability margin |
| R452-R453 | Aggregate population-semantics audit | right | Post-aggregate independent recomputation caught that R452 silently intersected `joint_endpoint_eligible` with validity; fail-closed termination preserved the valid shards, and the R453 successor separated endpoint-only from valid-endpoint populations with a directed invalid-but-endpoint regression fixture | Keep explicit population formulas and a fixture that distinguishes every nested eligibility label before sealing |
| R454 | Local-geometry plus mechanism-separation gate | right | Symmetric three-amplitude probes separated reward-slice curvature from critic, optimizer, and projection explanations; the dominant curvature was independently recoverable from the explicit residual penalty, while the narrow 4/7 critic comparison stayed bounded in the feed | Keep analytical reward-curvature reconciliation and report the comparable-cell denominator whenever a sign-alignment threshold fires |
| R455-R456 | Shared-driver shard command rehearsal | too-soft-then-fixed | R455 sealed phase-specific `state-shard`/`eval-shard` commands although the shared driver is hard-coded to `shard`; the first launcher failed before creating the result root. R455 was aborted, and R456 added a source-sealed arity dispatcher, a directed interface test, a fresh capacity/rehearsal/seal, and then completed 6+30 shards without retry | Every shared-driver round must exercise the literal `shard <representative-id>` CLI contract before seal, not only test the phase-specific worker functions |
| R456 | M1 mechanism-separation gate | right | The 2x2 ceiling-step factorial, per-profile cell, exact tail replay, gradient Gram/cosines, actor motion, projection activity and full physical guard bank separated ceiling mechanics from deep cause: U had zero effect in the negative-gap final-policy regime, while the RMS value-vs-constraint conflict met 4/6 and all feasibility witnesses failed | Keep training-behavior and deterministic-policy constraint populations separate; report the support denominator and never promote a cap intervention to a feasibility claim |
| R457 | M2 head-selective critic causal gate | right | Output-preserving remap, unselected-row isolation, shared replay, held-out original-scale error, directional physical-return calibration, actor displacement and independent closed-loop guards separated an implementation correction from a causal-controller claim: the R427 semantic defect was real, but common-only stabilization passed 0/5 critic/calibration/mediation/physical seeds in both arms | Retain the full critic-to-gradient-to-actor-to-physics chain for future critic repairs; a loss-scale improvement alone must never authorize retraining or physical-success language |
| R458 | Development-only winner selection plus one-use fixed evaluation bank | right | The frozen priority rule selected the unique two-development-profile witness without reading evaluation artifacts, then the unchanged winner passed every registered guard on two of four fixed evaluation profiles; the two failures remained explicit and the count was not converted to a probability | Keep the branch table, immutable winner hash, exact per-profile guard readout and fixed-bank wording together; any future probability claim requires a declared profile generator and independent sampled test bank |
| R459 | Shared-model export, independent recomputation, and measured native-thread ladder | right | The create-only export bound the dirty worktree, runtime, case, full Object A/Object B arrays, units, mappings, controllers and protocols; the independent reader reproduced every Schur/ZOH identity and all declared hashes, while the 1/4/8-thread ladder selected four threads and rejected oversubscription | Keep complete-array export plus independent reader before every certificate/sensitivity successor; use measured job/thread ladders per workload and do not infer GPU or long-training capacity from this short linear-algebra task |
| R461-R463 | Strict non-finite serialization and recursive empty-ledger semantics | too-soft-then-fixed | R461 correctly assigned infinity to invalid finite-bank rows but failed strict JSON serialization; R462 encoded infinity explicitly but treated a mapping of empty trace arrays as recorded data. Both attempts were preserved and aborted. R463 added JSON-safe status fields plus a recursive leaf-data availability checker and independently confirmed 15/15 original ledgers lack actual constraint traces | Every export rehearsal must exercise both non-finite values and nested empty containers; presence means at least one real leaf datum, never schema keys alone |
| R464 | Gauge/DCF/lift/cone certificate chain plus measured native-thread selection | right | The pre-seal chain caught a missing explicit zero quadratic matrix in CVXPY's linear-objective export; the repaired rehearsal then passed gauge removal, signed Bezout identity, finite-difference columns and direct cone replay before the create-only formal run. Four native threads reused the measured R459 workload ladder without CPU oversubscription or irrelevant GPU use | Keep explicit zero-P handling and direct unscaled cone replay for future linear-objective certificates; remeasure concurrency only when the workload class changes |
| R465 | Directional quotient check, thirteen-point concurrency, and total-derivative cross-validation | right-with-naming-note | The first pre-seal rehearsal caught a reversed quotient-block check; the corrected lower-left gauge-to-retained test matched the exact transfer and the formal 13-worker run retained ample memory. Richardson, Frechet ZOH, direct loop and two energy operators all agreed. The detailed JSON key `P1_A_only_sufficiency` is semantically awkward because `SUPPORTED` refers to the plan's insufficiency prediction, although the feed and top-level P1 field are unambiguous | Keep the directional quotient fixture and 13-worker allocation for same-size point banks; future mechanism fields should name the asserted proposition verbatim before seal |
| R468 | Full tensor convergence plus nonsmooth-interface qualification | right | Forty-nine unique points completed with the measured 15-worker allocation; three-level Richardson, exact-ZOH Frechet, and independent swing-row formulas agreed. The external 1e-9 residual suggestion remained visibly unmet while the registered 1e-8 project gate passed, and the zero-bias yet nonsmooth controller/decoder correctly forced a not-applicable smooth-Taylor outcome | Keep dual reporting of external-request acceptance and project gates; never turn physical-parameter tensors into a single normalized-action derivative across unequal one-sided slopes |
| R469 | I/O projector boundary and independent lift checker | right | The workflow refused an unverified full-state projector, retained only physically labelled I/O projectors, and checked every persisted 30-step lift with a separate NumPy recursion. All effective-stiffness points were conditioned and bounded, while homogeneous intercepts prevented a heterogeneity-only interpretation | Keep explicit unavailable fields instead of arbitrary state padding; future commutator claims require a verified symmetry action on every retained state coordinate |
| R476 | Detached-pipeline driver-result handoff gate | too-soft | Seal, dual review, routing, and rehearsal were all green, yet the pipeline exited after its first training wave: the shard driver resolved a relative --log-dir against its andes_scratch working directory while the pipeline searched for driver_result.json from the repository root. No gate checked the driver/pipeline path handoff | Landed: driver anchors relative --log-dir to the repository root with a scratch-cwd regression test; memory/tools/detached_pipeline_lint.py plus its real-pipeline asset test guard the pipeline half; discipline documented in docs/repo-hygiene/executables.md (commit ef85ebf) |
| R477 | R476-fixed handoff gate + carryover identity verification | right | The repaired driver/pipeline handoff plus the pre-seal lint and regression locks carried a full execution (2 fresh training waves, 16 eval jobs, aggregate, manifest) to completion with zero mid-pipeline stops; the 16 reused R476 shards passed all six identity checks before hardlink import | Keep the lint + asset test as the frozen pipeline handoff gate; keep per-shard scientific-identity verification for any future carryover |
| 2026-08-22 intake | External-answer re-verification gate (GPT chat audited solution + machine verifier) | right | The external verifier replayed repo-side against the sealed artifacts and all checks passed (U1 dual margin 0.0599381..., U4 350-candidate sort, U5 decomposition, U6 pole scan, U7 lifts, U8 32768-row bounds, U9 selection fields); only cross-platform float noise in the last ulps of two recomputed U7 singular values, which enter no check | Keep: every external answer ships a machine verifier against sealed paths and is replayed repo-side with registered tolerances before any manuscript wording use |
| 2026-08-22 intake | Exact-certificate + design-critique absorption gate (agent-results package) | right | Package SHA256SUMS verified 8/8; the U1 exact rational support prover replayed repo-side as PROVED against the sealed R464 sidecars (t >= 0.0599381277975427... > 0 for the serialized binary64 SOCP); the U2 placebo critique was confirmed against repo code (P rows read the exogenous random-policy donor bank, N rows the contemporaneous controlled trajectory, and donor_marginal_audit is donor-internal), so the R472 verdict must use confound-aware wording | Keep: exact/rational certificates replay with sidecar checks before any paper wording upgrade; design critiques against active-round code get confirmed against source before they constrain verdict wording |
| 2026-08-22 intake | Complete-outputs absorption gate (second independent chat) | right | Packs A/B re-verified 2451/2451 + 642/642; repo-side reruns reproduced icems B3 algebra 12/12 and the advisory algebra probe 8/8; the residual crosscheck source hashes match sealed r356/r358/r363; r472_progress_audit is structural-only (32/108 hash-valid, no evaluation artifacts) and was quarantined from any partial-endpoint inference; the chat honestly labelled cvxopt-missing and pytest fixture gaps as environment limits, not mathematical counterexamples | Keep: external packages are absorbed with repo-side replay of every runnable verifier and quarantine of any active-round progress interpretation |
| 2026-08-22 intake | Deep-solutions absorption gate (third chat) | right | 699/699 package hashes verified; repo-side reruns passed custom_math_audit (icems/headroom/r402 all true) and the C1 exact conic dual checker (hypothetical example, exact lower bound 1); the chat's conservative U1/U5/U8 INCOMPLETE labels are markdown-coverage gaps superseded by sealed R464/R465/R469 and the exact-support certificate; its R472 seal-staleness note is confirmed (formal_seal.json training_executed=false never updated after launch) and recorded for the R472 close-out | Keep: each absorbed package's per-item labels are judged against sealed repo evidence, never taken verbatim; active-round seal staleness found by an external audit goes on the close-out checklist |

Working rule: an entry with verdict "right" needs no adjustment. An entry
with "too-hard" or "too-soft" is incomplete until its adjustment lands in
the ledger (status cell updated).
| R481 | Fresh-holdout exclusion + Phase-1A 4/4 gate + corrected telemetry rehearsal | right | The rehearsal summary validity check caught the system/device-base telemetry mismatch before seal (raw GENCLS readback vs corrected device-base info fields); the 4/4 gate and generator exclusions worked first-pass; the dual-audit gate needed no relaxations | Keep the rehearsal summary validity check as a hard pre-seal gate; keep validity-only capacity records documented as non-consuming in the feed Limits |
