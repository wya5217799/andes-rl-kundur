# Yang 线直接 M/D 已查看评价配置复核报告（2026-08-25）

> 治理状态：隔离的 development-visible scratch 诊断。`eval_a` 已被查看，
> 永久失去 unseen holdout 身份；本结果不得进入正式 seal、claim 或论文数字。

## 验证目的

开发配置 `dev_a` 小门槛通过后，在未见的 `eval_a` 配置上比较零动作与确定性
直接 M/D 邻域控制，检查改善是否立即消失。共 12 条轨迹，12 并发；旧记录器的
system-base M/D 只在分析副本上按冻结参数卡换算为 device base。

## 完整性

- 12/12 登记轨迹完整，无重复、无 TDS 失败。
- 映射、动作边界、变化率、饱和预算、非恒定动作和各 VSG 独立动作检查均通过。
- 原始 scratch 工件 SHA-256：
  `ef722a638fa1fb87ac9bce390d88032fa0b941d9c43f056f92677a5c2d7bffaa`。

## 结果

分类为 `DIRECT-CANARY-PASS`：

- 非对角响应能量比（候选/零动作）：`0.527965851158809`。
- 扰动差分能量比（候选/零动作）：`0.6091336203828528`。
- 共同频率 IAE：候选 `0.5945606300714186 Hz*s`，零动作
  `0.7958453855788381 Hz*s`。
- 最差峰值：候选 `0.03399585969509644 Hz`，零动作
  `0.04779739667937122 Hz`。
- 最差 RoCoF：候选 `0.037846846986653304 Hz/s`，零动作
  `0.04227725745980848 Hz/s`。

## 与旧结论的关系

`dev_a` 与 `eval_a` 两个小批都支持“直接调节 M/D 能改善该确定性对照下的解耦
响应”。这比只在开发配置通过更可信，但仍不能推出 MARL 优越、所有配置泛化或
论文旧数值有效。

## 决策

诊断上支持继续研究直接 M/D 核心路线，但不能据此停止冻结计划中的正式能量端口
银行或开启直接 M/D 正式银行。未来 formal 必须使用前瞻性登记的全新 holdout；
训练和完整因子实验继续关闭。
