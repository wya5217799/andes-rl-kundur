# Yang 线直接 M/D 小门槛复核报告（2026-08-25）

> 治理状态：隔离的 development-visible scratch 诊断。该运行未列入 repair3
> 物理授权，不得进入正式 seal、claim、论文数字或正式路线裁决。

## 问题

第一批 12 条 `dev_a` 零动作/确定性直接 M/D 对照轨迹的旧记录器把 ANDES
运行时 system-base `GENCLS.M/D` 写进了应为 device-base 的 `M_es/D_es` 字段，
导致原分类为 `ANALYSIS-INVALID`。本次只修正分析边界，不改原始轨迹、不重跑
控制器、不训练，也不形成正式论文证据。

## 输入完整性

- 12 条原始轨迹全部完成、无 TDS 失败，原始文件保持不变。
- 原始轨迹集合 SHA-256：
  `8314956b641105da638285bd315b3137f6a88dad7e47eedca1e44b00e271018d`。
- 参数卡 SHA-256：
  `316e3ebb561002847b320c96c2d699401bcabd2892f02f35b52e140bbe394922`。
- 分析器 SHA-256：
  `a85c9ce56f401b2644fe43c9f20e1c63a4f951dd17775817ed406a74f5804e7e`。
- 换算只作用于副本：`x_device = x_system * 100 / 200 = 0.5 x_system`；
  每条副本写入一次性标记，重复换算会拒绝。

## 结果

分类为 `DIRECT-CANARY-PASS`。12 条登记轨迹齐全，零动作参照为正，M/D 映射、
动作边界、变化率、饱和预算、非恒定动作和各 VSG 独立动作检查全部通过。

- 非对角响应能量比（候选/零动作）：`0.5449332658658602`。
- 扰动差分能量比（候选/零动作）：`0.47342011490584546`。
- 候选共同频率 IAE：`0.5248830914323783 Hz*s`；零动作：
  `0.6896800996728186 Hz*s`。
- 候选最差峰值：`0.03375317087256491 Hz`；零动作：
  `0.048694722855131545 Hz`。
- 候选最差 RoCoF：`0.038253546023163665 Hz/s`；零动作：
  `0.05272813040118507 Hz/s`。

## 与旧结论的关系

这一步支持继续保留“直接调节 M/D 在该开发小样本上改善解耦响应”的旧方向，
但它只是路线筛选，不支持论文数值、跨条件泛化或 MARL 优越性。第一批能量端口
held-out 小门槛仍未复现旧正结论，因此不能据此放行训练、完整因子实验、拓扑扩展
或正式论文结果更新。

## 下一步决定

遵循过去的部分：保留基础 M/D 语义修正和直接 M/D 确定性对照路线。

重新设计的部分：废弃旧记录器的隐式基准混用；后续容量与复核必须绑定完整的
运行前/运行后源码和运行时身份。先完成干净容量梯度，再对能量端口失败做一个独立
小批确认；若再次失败，撤回或重写该伴随正结论，不继续旧下游实验队列。

## 后续治理审计

本报告记录的数值仍可作为开发诊断，但其运行未列入 repair3 allowlist，不能执行
上述正式路线动作。冻结计划下的 official next gate 仍是确定性能量端口银行；只有
owner 批准放弃 energy claim 的 successor 后，才可登记全新 direct holdout。
