# R417 feed — feedback loop: K=4.0 breadth on the three A4 unseen blocks (2/3, relaxed block near-miss)

## Identity

- Round: `R417`; reserved claim: `CLM-1240`; date: `2026-08-17`.
- Authority: feedback loop (owner instruction 2026-08-17, uninterrupted
  analyze/research/experiment mechanism); deep-research protocol
  sharpening
  `paper/yang_md_decoupling_marl/working/feedback_loop_deep_research_2026-08-17.md`
  (Perspective 2: fixed-gain breadth tables are defensible when the gain
  was disclosed before the blocks were scored; a third gain on the same
  blocks is forbidden).
- Runner: `scripts/run_r417_energy_port_banks_k4.py`; frozen banks
  `soft_spot_energy_port_banks.py`; R408/R372 harness imports read-only.
- Seal `memory/rounds/R417/formal_seal.json` (sha256
  `7f35c3f6d1c778eabeaf45506268a7a19c36337db57983a2846745f9ffd65b98`).
- Result root: `results/research_loop/r417_energy_port_banks_k4/`;
  `formal_analysis.json` (sha256
  `a96864f8a1d31548f1c1603462de43bde1c1c49bc561633d4c6ce74a765596da`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- The R415 protocol verbatim (three frozen unseen blocks, R409 thresholds
  r_d <= 0.95 / r_cross <= 1.10, all R379 guards, serial single-process
  seam, budget 1) with exactly one changed factor: the candidate arm is
  `bandpass_k4` (the second R408-disclosed development Q-ENTRY gain,
  dev r_d 0.911541 / r_cross 0.515282) instead of the frozen K=3.5.
- Pre-registered: the blocks are first-use for K=4.0; the table is
  reported in full (failing cells retained); no selection and no third
  gain on these blocks.

## Observations

- **O-001 (CLM-1240) — Complete bank.** All 90 records completed with
  identity checks and the frozen estimators (per-block `records.json`).
- **O-002 (CLM-1240) — a4_conditions_b PASS.** r_d = 0.9040, r_cross =
  0.8512 (strict pass), all guards pass.
- **O-003 (CLM-1240) — a4_md_stiff PASS.** r_d = 0.8750, r_cross =
  0.6716 (strict pass), all guards pass.
- **O-004 (CLM-1240) — a4_md_relaxed near-miss FAIL.** r_d = 0.9506
  exceeds the 0.95 ceiling by 0.06% (versus 0.9712 for K=3.5, i.e. a
  2.1-percentage-point improvement); r_cross = 0.9466 passes the strict
  gate and every guard passes, so the miss is an endpoint-ceiling miss
  only (see `formal_analysis.json#/blocks/a4_md_relaxed`).
- **O-005 (CLM-1240) — Verdict BLOCKS-FAIL (2/3).** The full table is
  retained: both disclosed gains pass the conditions and stiff blocks,
  and both miss the relaxed block's r_d ceiling (K=3.5 by 2.2%, K=4.0 by
  0.06%) (see `formal_analysis.json#/verdict`).

## Conclusions

- **Finding, trust V.** The relaxed-inertia/heavy-damping block is the
  shared boundary of both R408-disclosed gains: K=4.0 improves r_d from
  0.9712 to 0.9506 but still misses the 0.95 ceiling by 0.06% with all
  guards passing; the constructive claim's scope therefore stays bounded
  to the two passing blocks for both gains.
- **Finding, trust V.** The near-miss is reported as a
  robustness-margin characterization, not a failure to repair: per the
  registered rule no third gain will be evaluated on these blocks, and
  any scheduled/adaptive follow-up needs fresh blocks.
- **Evidence boundary.** Two disclosed gains, three frozen blocks, one
  simulator, frozen thresholds and guards only.

## Limits

- The two-gain table is a breadth characterization, not a gain search;
  the relaxed block remains uncovered by the frozen fixed-gain family.
- No scheduling/adaptation claim; no population inference.

## Publication gate

- **Evidence audit**: PASS - every observation binds to the hashed
  `formal_analysis.json` and per-block hashed records; the full table
  including the failing cell is machine-readable.
- **Domain audit**: PASS - the near-miss is separated from the passing
  blocks; the disclosure order (R408 gain disclosure predates the R415
  block freeze) is recorded in the seals; no test-set tuning language is
  used.
- **External context**: NOT-APPLICABLE - bounded internal breadth
  evidence; the defensibility condition was verified by the registered
  deep-research pass (Perspective 2), no novelty claim is made.
- **Claim disposition**: ENTER - register the two-gain block table.
- **Allowed claim**: on the frozen three-block unseen bank, the second
  disclosed gain K=4.0 passes the new-conditions block (r_d 0.9040,
  r_cross 0.8512) and the stiff-plant block (r_d 0.8750, r_cross 0.6716)
  and improves the relaxed-plant block from r_d 0.9712 (K=3.5) to
  r_d 0.9506, still exceeding the 0.95 ceiling by 0.06% with every guard
  passing; both disclosed gains therefore share the relaxed block as
  their boundary.
- **Stay-out**: third-gain evaluations on these blocks, gain-search
  language, scheduling claims, and any change to the K=3.5 headline
  without the title policy's review.

## Manuscript mapping

- Section 5.4: extend the unseen-bank sentence with the K=4.0 table and
  the shared relaxed-block boundary (near-miss +0.06%).
- Section 7: note the two-gain breadth table and the block-closure rule.

## Follow-up pointers

- The three blocks are now closed to further gain evaluations (calibration
  log). The loop continues with the B1 slew-state bundle (next round).
