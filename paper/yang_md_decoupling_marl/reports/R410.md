# R410 feed — R402 message-contrast repair: CANARY-FAIL, clean negative message increment

## Identity

- Round: `R410`; reserved claim: `CLM-1215`; date: `2026-08-16`.
- Repository commit at decision time: `9b64e6e4a4f6eb5367a69a037b84bf0c2a08bee5`
  with the preserved dirty worktree; no new commit is introduced by R410.
- Runner: `scripts/run_r410_message_repair.py`; learner mask fix in
  `src/andes_rl_kundur/agents/cd_matd3.py`; capacity/seal lifecycle in the
  same runner; endpoint probe `probes/r410_endpoint_table.py`; transient
  eval-shard orchestration `tmp/andes/r410_eval_shard.py` (execution
  amendment registered in the plan).
- Authority: owner decision
  `paper/yang_md_decoupling_marl/working/route_owner_decision_message_repair_2026-08-16.md`;
  seal `memory/rounds/R410/formal_seal.json` (sha256
  `fb52fc8f0f63d24bc8fae91eb55c2144e0d580718de16bcde068a3a1cbf4a57e`);
  contract sha256 unchanged from R401/R402.
- Result root: `results/research_loop/r410_message_repair/`; classification
  `formal_analysis.json` (sha256
  `7f6244ab28d1c20a7b7fbf6eae0a8cbeed39825f1b96917bce6a845e165529b1`);
  endpoint table `endpoint_table.json` (sha256
  `2943ee87fc3e5c6c08a473ac42ee79308206a90fa8dcf7207fa5eb7a2d87e89d`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- R402 contract verbatim: three arms (`yang_scalar_td3`,
  `cd_matd3_no_message`, `cd_matd3_message`) x seeds 401/402/403, 43200
  interaction steps per run, the same eight-profile partition
  (canary_dev_a..d / canary_eval_a..d), same hyperparameters, rewards,
  estimators, guards, and checkpoint rule.
- Single-factor learner change: `cd_matd3_no_message` zeroes neighbour slots
  in every actor path (online, target, update) via
  `actor_neighbour_mask`; scalar and message arms keep the previous
  computation path.
- Budget: sealed 5 processes (4 workers + launcher) from the pre-seal
  capacity ladder (`memory/rounds/R410/capacity_evidence.json`, rung 4
  selected, rung 8 rejected on the 5-percent marginal-throughput rule);
  evaluation ran as per-(arm, seed) shards overlapped with the final
  training run inside the same budget (execution amendment, plan-registered).
- Owner-authorized profile-partition reuse: comparability of the message
  contrast requires the identical profiles and seeds; eval records land in
  the fresh R410 root.

## Observations

- **O-001 (CLM-1215) — Complete valid bank.** All nine training manifests
  record 43200 interaction steps, `convergence_diagnostics_valid=true`,
  `missing=false`, zero TDS-failed episodes; all 40 evaluation record files
  (240 learning + 24 deterministic trajectories) present with sha256
  sidecars; classifier checks `complete_bank=true`,
  `all_manifests_valid=true`, `all_rows_valid=true`,
  `budget_frozen=true`, `reward_unused=true` (see
  `formal_analysis.json` `/checks`).
- **O-002 (CLM-1215) — CANARY-FAIL.** The frozen classifier returns
  `CANARY-FAIL`: all 36 arm-seed-profile blocks violate the common no-harm
  and action-stress budgets. Worst bank-wide ratios versus the deterministic
  reference: common integral 1.4454, worst-unit peak 2.4310, RoCoF 4.1379
  (limits 1.03) and action RMS 4.7458, action total variation 8.3572
  (limits 1.10) (see `formal_analysis.json` `/guard_failures`;
  `endpoint_table.json` `/worst_guard_ratios_vs_deterministic`).
- **O-003 (CLM-1215) — Repaired no-message arm endpoints.** The repaired
  arm's three-seed median endpoint ratios versus the deterministic
  reference are 2.9463 off-diagonal and 2.0063 disturbance differential
  (aggregate energies 1.0094e-3 and 4.4438e-3) (see
  `endpoint_table.json` `/seed_medians`,
  `/median_endpoint_ratio_vs_deterministic`).
- **O-004 (CLM-1215) — Message arm endpoints.** The message arm's ratios
  are 5.2569 off-diagonal and 2.5427 differential (5.6319e-3 and 1.8010e-3)
  (same locators).
- **O-005 (CLM-1215) — Clean negative message increment.** Under the
  repaired information contract the message arm shows a negative
  three-seed-median improvement over the matched no-message arm on both
  endpoints: -78.43% off-diagonal and -26.74% disturbance differential;
  versus the scalar arm it is -34.96% off-diagonal and +14.29%
  differential. This is the first clean single-factor runtime-message
  contrast for this bundle (see
  `endpoint_table.json` `/full_method_improvement_vs_comparators`).
- **O-006 (CLM-1215) — Drift anchor DRIFT, attributed.** The pre-registered
  R402 anchors deviate beyond 1e-6 relative: scalar 5.35%/1.76% and message
  3.25%/22.93% (off-diagonal/differential medians). The deterministic
  evaluation reference is bit-identical between R402 and R410 (3.4260e-4 /
  2.2149e-3), pinning the environment and the evaluation path. The R402
  feed records that R402 training rollouts ran under the pre-repair slew
  projector while evaluation used the repaired projector; R410 trains under
  the current repaired projector. The drift is therefore attributed to the
  R402-registered float32 slew repair (R402 feed Limits), not to an
  unidentified environment change (see
  `endpoint_table.json` `/drift_anchor`; R402 feed Limits).
- **O-007 (CLM-1215) — Physical-only gate.** `reward_used_for_gate=false`
  and the classification consumes only physical traces and manifests
  (`formal_analysis.json` `/reward_used_for_gate`).

## Conclusions

- **Finding, trust V.** With the no-message arm's actor-training mask
  enforced in every actor path, the frozen classifier still returns
  CANARY-FAIL: all learning blocks violate the physical guards, and the
  message arm's three-seed-median increment over the repaired matched
  no-message arm is -78.43% off-diagonal and -26.74% disturbance
  differential. The recorded message-minus-no-message difference is now a
  clean single-factor contrast within the tested bundle.
- **Finding, trust V.** The R402 drift anchors are not bit-reproducible
  because R402 trained under the pre-repair slew projector while R410
  trains under the repaired projector that R402 itself used for evaluation;
  the bit-identical deterministic reference bounds the drift to the
  training path. R410 absolute values supersede R402 values for these arms
  as the corrected-environment measurement; the R410-vs-R402 absolute
  comparison is two-factor (mask + projector repair) and is reported as
  such.
- **Decision, trust S.** The message-contrast hole in the manuscript is
  closed by measurement: the descriptive R402 wording is replaced by the
  R410 measured contrast. The canary-level learner-route stop stands; no
  title change and no successful-MARL claim follow.
- **Evidence boundary.** One fixed-connectivity ANDES formulation, one
  eight-profile partition, three seeds, 43200-step budgets, the registered
  two-component objective and guard family only.

## Limits

- Covers the R402 contract partition and estimator set only; no population
  inference from three seeds.
- The R410-internal message contrast is single-factor; the R410-versus-R402
  numerical anchoring is two-factor (mask fix plus the R402-registered
  projector repair) and is not used for any absolute drift claim.
- All arms still fail the physical guards, so the negative message
  increment is measured inside a failing regime, not at a guard-passing
  operating point.
- The stateful slew/state mismatch, the 50-to-60 observation-adapter
  ambiguity, and the CD objective-to-gate gap remain unfixed and disclosed
  exactly as in R402.
- Evaluation shards executed the sealed runner's own `_evaluate_arm_seed`
  function per (arm, seed) under the plan-registered execution amendment;
  records and classification inputs are identical to a serial evaluation.

## Publication gate

- **Evidence audit**: PASS — every observation binds to the hashed
  `formal_analysis.json` or `endpoint_table.json`; bank completeness,
  guard failures, drift deviations, and the deterministic-reference
  bit-identity are all machine-readable; the drift attribution cites the
  R402 feed Limits and the R402-registered slew repair.
- **Domain audit**: PASS — common/differential endpoints stay physical
  60-Hz quantities; the message contrast is reported as a within-bundle
  seed-median difference under guard failure, not as communication value in
  a guard-passing regime; the two-factor R410-vs-R402 anchor qualification
  is stated; no decoupling or coordination success is claimed.
- **External context**: NOT-APPLICABLE — the repair is an internal
  implementation-contrast measurement; no novelty or superiority statement
  depends on it.
- **Claim disposition**: ENTER — register the clean measured message
  contrast, the reproduced CANARY-FAIL, and the attributed drift.
- **Allowed claim**: under the repaired information contract, the tested
  message-enabled CD-MATD3 arm shows a negative three-seed-median
  improvement over the matched no-message arm (-78.43% off-diagonal,
  -26.74% disturbance differential) while all learning blocks still fail
  the physical guards; R410 absolute values supersede R402 for these arms
  and the R410-vs-R402 comparison is two-factor because R402 trained under
  the pre-repair slew projector.
- **Stay-out**: communication is useless or harmful in general; MARL or
  direct-M/D learning impossibility; guard-passing message benefit; any
  pooling of the R410 learning numbers with the energy-port object.

## Manuscript mapping

- Section 4.3: replace the "not a clean information ablation" audit wording
  with the repaired single-factor contract and the measured contrast.
- Section 5.2: replace the three-arm table with R410 values; report the
  clean negative message increment (-78.43% / -26.74%) and the reproduced
  CANARY-FAIL; state the drift-anchor attribution in one sentence.
- Section 7: shrink the message-contrast limitation to the two-factor
  R410-vs-R402 anchor qualification; keep slew-state and
  missing-diagnostics clauses.
- Section 8: the conclusion sentence on the message contrast becomes a
  measured single-factor statement.
- The R402 absolute table values remain historical in the R402 feed only.

## Follow-up pointers

- If a guard-passing message contrast is ever sought, it needs a bundle
  that first repairs the slew-state contract and passes the physical gates;
  not authorized here.
- A Q for the message-contrast interpretation under guard failure is not
  opened: the measured sign is reported with its regime qualifier.
