# R426 feed — B2 five-seed extension on the R410-repaired bundle: the bit-repro gate is BIT-REPRO-CONFIRMED, the frozen three-seed tree reproduces the R410 verdict exactly, and the five-seed median message contrast stays strongly negative (-74.98% / -35.49% vs the 3-seed -78.43% / -26.74%)

## Identity

- Round: `R426`; reserved claim: `CLM-1295` (experiment); date: `2026-08-18`.
- Authority: owner order 2026-08-18 (B2 five-seed statistical-power upgrade on
  the R410-repaired bundle — the bundle behind the paper's headline negative
  message contrast); program
  `paper/yang_md_decoupling_marl/working/soft_spot_experiment_program.md` §B2;
  design doc `tmp/yang_md_decoupling_marl/b2_five_seed_runner_design.md`;
  parent `CLM-1215` (R410).
- Runner: `scripts/run_r426_b2_five_seed.py`; the R410 contract, classifier
  tree, learner, and stored checkpoints are reused read-only.
- Seal `memory/rounds/R426/formal_seal.json` (sha256
  `7cb6c6d67cd0e406af2a2f1ee4dc039bcefb64fbc202c66506b6f956974b13df`).
- Result root: `results/research_loop/r426_b2_five_seed/`;
  `formal_analysis.json` (sha256
  `df9f7afae812fc04a1cb9595f5396e53541945fd4296e647d441bc8bb097a06d`);
  `formal_manifest.json` (sha256
  `6925e996ce29c94f75da2289030e29305db629be55683dba3524f67f51870101`).
- Stored R410 checkpoints reused (read-only):
  `results/research_loop/r410_message_repair/train/*/seed{401,402,403}/final.pt`
  (sidecars verified at prepare).
- Concurrency: launched concurrently with R425 (declared reserved share =
  R425's 17 processes; concurrent-load ladder, 5% marginal chain waived).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- Base bundle verbatim R410 (7-slot observations, mask semantics, seeds
  401/402/403, 43,200 steps per run, hyperparameters/rewards/estimators/
  guards/checkpoints per the R410 seal contract).
- Bit-repro gate: one live gate run (cd_matd3_message seed 401); its fresh
  final checkpoint sha256 must equal the stored R410 checkpoint (R421 B3
  proved the R410 learner path bit-reproducible under the current learner
  bytes bd924733fe4d71ac). Mismatch = pre-registered DRIFT branch: stop,
  report, owner decides.
- Reuse: seeds 401/402/403 evaluate from the stored R410 checkpoints
  (read-only, sha recorded); seeds 404/405 train fresh (6 runs).
- Five-seed table: per arm per endpoint median/min/max (same per-seed
  aggregation as the R410 3-seed medians), registered as a reporting layer
  (`formal_analysis.json#/b2_five_seed_table`); the frozen canary tree runs
  untouched on the contract's three seeds.
- Anchors: deterministic-reference evaluation verbatim; stored checkpoint
  sidecars verified.
- Budget: concurrent ladder (under R425's live load) sealed 8 workers + 1
  driver; other_reserved_processes 17; seven shards in one wave; serial
  evaluate + classify.

## Observations

- **O-001 (CLM-1295) — Bit-repro gate BIT-REPRO-CONFIRMED.** The fresh
  message|401 gate run's final checkpoint sha256 equals the stored R410
  checkpoint sha256
  (`497ff3c0c50fea267f21214d75910d649663210c517cd5041d6ddc5bc5278080`), so the
  reused 401/402/403 checkpoints are admissible samples and the DRIFT
  branch is not entered (`formal_analysis.json#/b2_gate`).
- **O-002 (CLM-1295) — Complete valid bank.** Seven manifests record 43,200
  steps with valid convergence diagnostics (0 TDS failures); 64 evaluation
  record files (60 learning + 4 deterministic, all sidecar-verified); the
  frozen three-seed tree classifies CANARY-FAIL with all integrity checks
  true (`formal_analysis.json#/classification`, `#/checks`,
  `formal_manifest.json`).
- **O-003 (CLM-1295) — The frozen tree reproduces the R410 verdict
  exactly.** The three-seed classification over the reused stored R410
  checkpoints reproduces the stored R410 guard profile to the block:
  action-RMS 34/36, action-variation 36/36, common-frequency 24/36, RoCoF
  28/36, worst-peak 35/36 (identical to
  `r410_message_repair/formal_analysis.json#/guard_failures`) — the
  stored-checkpoint reuse path is bit-consistent at the decision level
  (`formal_analysis.json#/classification`, `#/guard_failures`).
- **O-004 (CLM-1295) — Five-seed medians, off-diagonal / disturbance
  differential.** cd_matd3_message 0.00176624 / 0.00602085;
  cd_matd3_no_message 0.00100940 / 0.00444383; yang_scalar_td3 0.00133449 /
  0.00657113 — the no-message and scalar five-seed medians coincide with
  the R410 three-seed medians to eight digits, while the message arm's
  median moves from 0.00180105 / 0.00563191 (`formal_analysis.json#/
  b2_five_seed_table`, `r410_message_repair/endpoint_table.json`).
- **O-005 (CLM-1295) — The headline negative message contrast survives the
  seed-count extension.** Five-seed-median improvement of the message arm
  over the matched no-message arm: -74.98% off-diagonal and -35.49%
  disturbance differential (R410 three-seed: -78.43% / -26.74%); versus the
  scalar arm -32.35% / +8.37% (R410: -34.96% / +14.29%) — the negative
  off-diagonal contrast is robust and the differential contrast is more
  negative on five seeds (`formal_analysis.json#/b2_five_seed_table`,
  same (no-message minus message)/no-message formula as R410's
  `endpoint_table.json#/full_method_improvement_vs_comparators`).
- **O-006 (CLM-1295) — Per-seed dispersion.** The no-message arm carries
  the widest spread (off-diagonal min 0.0009637 / max 0.0036688, a 3.81x
  ratio; differential 0.0037150 / 0.0090590, 2.44x), driven by one high
  seed, while the message arm is tighter (1.84x / 1.30x) and the scalar arm
  sits at 1.77x / 1.51x — the three-seed medians hide a fat upper tail on
  the no-message arm (`formal_analysis.json#/b2_five_seed_table`).
- **O-007 (CLM-1295) — Guard verdict unchanged at the reporting layer.** No
  arm-seed block passes all guards in either the frozen three-seed tree or
  the extended seeds: the five-seed table is descriptive reporting and does
  not change the canary verdict (`formal_analysis.json#/classification`,
  `#/b2_five_seed_table`).

## Conclusions

- **Finding, trust V.** The R410 learner path is bit-reproducible today:
  the fresh gate run reproduces the stored checkpoint byte-for-byte, and
  the frozen three-seed tree over the reused checkpoints reproduces the
  sealed R410 verdict and guard profile exactly — the stored-checkpoint
  reuse for the B2 extension is admissible under the pre-registered gate.
- **Finding, trust V.** The paper's headline negative message contrast is
  robust to the seed-count extension: the five-seed-median improvement is
  -74.98% off-diagonal / -35.49% differential versus the three-seed
  -78.43% / -26.74% — still strongly negative on both endpoints, with the
  differential contrast more negative under five seeds.
- **Finding, trust V.** Five seeds expose dispersion the three-seed median
  hid: the matched no-message arm's off-diagonal spread is 3.81x (one seed
  at 0.0036688 versus the median 0.0010094), so the negative message
  contrast is a median-level statement and per-seed orderings are not
  uniform.
- **Evidence boundary.** One fixed-connectivity ANDES formulation, five
  seeds per arm on one bundle, one gate run; descriptive medians/spreads,
  no population inference; the classification verdict stays the frozen
  three-seed tree's.

## Limits

- The five-seed table is a reporting layer; the canary verdict remains the
  frozen three-seed tree's (CANARY-FAIL, guard profile unchanged).
- Descriptive statistics only: five seeds do not support population
  inference; per-seed orderings vary (the no-message arm's upper tail).
- The bit-repro gate covers one arm-seed (message|401) as pre-registered;
  the other eight stored checkpoints are admitted via their verified
  sidecars, not by re-running.

## Publication gate

- **Evidence audit**: PASS — every observation binds to the hashed
  `formal_analysis.json` / `formal_manifest.json`, the hashed per-run
  manifests, the sidecar-verified stored R410 checkpoints, or the stored
  R410 `formal_analysis.json` / `endpoint_table.json`; the five-seed
  contrast uses the same formula as R410's registered
  `full_method_improvement_vs_comparators`; all sidecars verify; no
  trajectory was re-run for analysis reasons.
- **Domain audit**: PASS — the five-seed contrast is reported against the
  same matched no-message arm with the same aggregation and formula; the
  dispersion finding names the driving seed; no guard-passing,
  decoupling-success, or message-mechanism verdict is drawn; the
  reporting-layer status of the table is stated.
- **External context**: NOT-APPLICABLE — bounded internal replication
  evidence; no novelty claim is made.
- **Claim disposition**: ENTER — register the bit-repro confirmation, the
  exact frozen-tree reproduction, the five-seed medians/spreads, and the
  surviving negative message contrast.
- **Allowed claim**: see the CLM-1295 registration card.
- **Stay-out**: population-level claims from five seeds; any claim that the
  canary verdict changed; message-mechanism verdicts beyond the measured
  negative contrast; generalization beyond the R410-repaired bundle.

## Manuscript mapping

- Section 5.2: extend the R410 message-contrast paragraph with the
  five-seed statement (bound to CLM-1295): the negative contrast survives
  the extension (-74.98% / -35.49% on five seeds versus -78.43% / -26.74%
  on three), the frozen tree reproduces the R410 verdict exactly, and the
  no-message arm's 3.81x off-diagonal spread is disclosed; the "no
  five-seed held-out learning comparison is claimed" sentence is updated
  (B2 is a training-seed extension, not the preregistered held-out
  expansion gate).
- Section 7: update the three-seed limitation with the five-seed
  descriptive extension (median robust, dispersion disclosed; still no
  population inference).

## Follow-up pointers

- The held-out expansion gate (fresh bank) remains unrun and unclaimed; B2
  strengthens the median-level negative contrast only.
- C1-SAC exact reproduction stays queued (owner-gated); next single-factor
  training repair = DR-menu P1 critic target normalization (R425/R426
  outcomes unchanged on this selection).
