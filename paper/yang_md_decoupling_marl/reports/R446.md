# R446 feed — DAE first-order authority of the multiplicative M/D action is ZERO at the synchronous power-balanced equilibrium (Object A): B_{u,r} = f_u − f_y g_y⁻¹ g_u = 0 measured for all 8 M/D columns

## Identity

- Round/date/claim: `R446`, 2026-08-20, `CLM-1390`.
- Authority: owner order 2026-08-21 (resolve the advisory's unresolved items,
  journal-extension measurement program); parent `NOTE-0031`, the advisory
  `paper/yang_md_decoupling_marl/working/vsg_failure_math_advisory_20260820`
  (P3), `paper/yang_md_decoupling_marl/working/theory_audit_bundle/IMPORT_NOTE.md`
  (records B_{u,r} as previously unidentified), the offline code analysis
  `tmp/yang_md_decoupling_marl/p3_dae_authority_code_analysis.md`.
- Result root: `results/research_loop/r446_md_authority_fd/formal_analysis.json`
  (sha256 `742a0816bbc0792222793e25aa56f921408772e6063d9f09ecd1eb3263a217f2`),
  sidecar present.
- Inputs: frozen env `andes_vsg_env_v4` + `base_env` (Object A, four GENCLS
  VSGs), `kundur_full.xlsx`; read-only, no training, no bank access.

## Frozen setup

- Equilibrium: `AndesMultiVSGEnvV4` reset to the undisturbed synchronous
  power balance (PFlow + 0.5 s TDS), then `TDS.fg_update` + `j_update` to
  snapshot `f_y`, `g_y` (dense), residuals `f`, `g`.
- Input columns: 8 = {ΔM_i, ΔD_i, i=1..4}; central finite difference over
  h ∈ {1e-2, 1e-3, 1e-4} via `finite_difference_input_jacobians`
  (`model_first_contract.py`), Schur fold via `fold_input_columns`
  (`r405_linearization.py:39`) at frozen x*, y*.
- `set("M", ...)` also mutates `dae.Tf` (M is the omega t_const); f and g do
  not depend on Tf, and each callback rewrites all 8 values, so no residual
  drift is introduced.
- Pre-registered decision tree (R446 plan): ZERO-FIRST-ORDER-AUTHORITY if all
  8 columns max|B_{u,r}| ≤ 1e-6 and h-convergent, g_y cond < 1e10, |ω−1| ≤ 1e-6,
  max|f_ω| ≤ 1e-6; NONZERO if any column > 1e-6 and h-stable; CANARY-INVALID
  otherwise.

## Observations

- [CLM-1390] Equilibrium gate passed: all four VSG `omega = 1.0` exactly,
  max|f_ω| = 1.54e-10, max|f| = 7.23e-9, max|g| = 2.91e-9, g_y cond = 1.14e6,
  state_dim 102, algebraic_dim 284 (`#/snapshot`, `#/equilibrium_ok`).
- [CLM-1390] Every one of the 8 folded columns is exactly zero at every h:
  max|B_{u,r}[:, j]| = 0.0 and ‖·‖₂ = 0.0 for all {ΔM_i, ΔD_i} across the
  three h values, with midpoint_ratios 0.0 (`#/folds/*/columns`).
- [CLM-1390] Verdict `ZERO-FIRST-ORDER-AUTHORITY` (`#/verdict`): the
  multiplicative M/D action has no additive first-order reduced-state channel
  at the synchronous power-balanced equilibrium.

## Conclusions

- **Finding, trust V — SUPPORTED.** For Object A, the index-1 DAE reduced
  input channel B_{u,r} = f_u − f_y g_y⁻¹ g_u is measured zero at the
  synchronous power-balanced equilibrium for all 8 M/D command columns. This
  numerically seals advisory proposition P3.2 and the offline code-structure
  prediction: g_u = 0 (no M/D in any algebraic residual) and f_u = 0 at balance
  (∂f_ω/∂M = −f_ω/M = 0, ∂f_ω/∂D = −(ω−1)/M = 0). The action's leading effect
  at the equilibrium is therefore second-order (state-dependent/bilinear
  modulation of the linearized A(M,D)), not force injection.
- **Scope note.** The measurement is at the env default equilibrium (M=400,
  D=100). The structural zero is M/D-value-independent per P3.2 (only ω=1 and
  tm=te are required), so the value does not affect the verdict. The zero is
  an equilibrium statement: away from balance (during transients) the
  multiplicative channel is nonzero, exactly as P3.2's route 1 states.
- **Manuscript consequence.** This converts the theory-audit "B_{u,r} not
  identified" limitation into a measured, conditional Lemma-1 completion: the
  ODE multiplicative-authority lemma transfers to the index-1 DAE with a
  first-order channel that is zero at balance (paper-ready wording in the
  advisory `report/paper_ready_paragraphs_P1_P3.md` P3).

## Limits

- Single operating point (nominal Kundur equilibrium, no topology variants);
  the measured zero is an equilibrium statement only — away from balance
  (during transients) the multiplicative channel is nonzero (P3.2 route 1).
- Measured at the env default equilibrium (M=400, D=100); the structural zero
  is M/D-value-independent per P3.2, so the value does not affect the verdict,
  but it is not a sweep over operating points.
- Central finite difference over h ∈ {1e-2, 1e-3, 1e-4} with no off-equilibrium
  or nonsmooth active-mode sweep; no complex-response or loop export here.

## Publication gate

- **Evidence audit**: PASS — every inline number re-derived from the hashed
  `formal_analysis.json` (742a0816…); sidecar matches; all 8 columns exactly
  zero at 3 h values; equilibrium gates pass.
- **Domain audit**: PASS — the Schur fold matches the advisory's index-1 DAE
  convention (B_{u,r} = f_u − f_y g_y⁻¹ g_u); the swing-equation structure
  (M as t_const, D in the damping term) is the Object A convention; no guard
  or endpoint semantics touched.
- **External context**: CURRENT — this is the repo-side verification of advisory
  proposition P3.2 (external-theory-intake contract, already ingested); no new
  deep research required.
- **Claim disposition**: ENTER — register the verified zero-authority finding
  (CLM-1390, trust V).
- **Allowed claim**: For Object A at the synchronous power-balanced equilibrium,
  B_{u,r} = 0 for all 8 M/D command columns (max|B_{u,r}| = 0.0, g_y cond
  1.14e6, ω = 1.0, f_ω = 1.5e-10).
- **Stay-out**: any off-equilibrium or margin claim; any causal failure
  mechanism; any claim that M/D has no effect at all (it has a second-order
  bilinear effect).

## Manuscript mapping

- P3 (Lemma-1 completion): the ODE multiplicative-authority lemma transfers to
  the index-1 DAE with a first-order channel zero at balance — converts the
  theory-audit "B_{u,r} not identified" limitation into a measured conditional
  statement (bound to CLM-1390); paper-ready wording in the advisory
  `report/paper_ready_paragraphs_P1_P3.md` (P3 section).
- No margin (gain/phase/delay) or causal-failure claim follows from this
  measurement alone.

## Follow-up pointers

- The advisory's P3 finite-difference recipe is now executed once; the
  remaining P1 (complex response) and P2 (delay sweep) measurements are
  separate rounds.
- C1's certificate instantiation reuses this linearization/Jacobian export
  (`c1_youlas_sls_certificate.md` §7).
