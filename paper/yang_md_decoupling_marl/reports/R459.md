# R459 feed — hash-verified shared Object A/Object B model and protocol export

## Identity

- Round/date/claim: `R459`, 2026-08-21, `CLM-1435`.
- Authority: owner-authorized U1--U8 shared-data prerequisite; no training, controller search, trajectory evaluation, or downstream mathematical classification.
- Decision artifact: `results/research_loop/r459_u1_u8_shared_export/checks/verification_report.json` (SHA-256 `f27fac1b114a6495e49f92e9d8f1e832ec559b2b3610a690eef4e99ad571b5c7`), with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/entry: `memory/rounds/R459/plan.md`, `memory/rounds/R459/formal_seal.json` (SHA-256 `feb50656098ae9543cd08d1f0f5067e5a3607e4dd29fb478728c941a11c626b3`), and `scripts/run_r459_u1_u8_shared_export.py`.
- Source commit: `9b64e6e4a4f6eb5367a69a037b84bf0c2a08bee5`; the dirty worktree is bound by `results/research_loop/r459_u1_u8_shared_export/provenance/git_diff.patch`, `results/research_loop/r459_u1_u8_shared_export/provenance/git_status_porcelain.txt`, and sealed content hashes.

## Frozen setup

- Object A reconstructs the R446 four-GENCLS direct-M/D fixed-equilibrium residual object. It exports the grouped eight-column physical Jacobians, both one-sided normalized mappings at zero, and the exact raw-target/projected-action/environment execution contract.
- Object B reconstructs the R447 nominal four-port energy-port object with four system-pu power-command inputs, three system-pu PQ disturbance inputs, four physical 60-Hz frequency outputs, and a 0.2-s post-step observation convention.
- Object B includes the initialized DAE, selected central-difference bridges, zero-time-constant fold, continuous Schur reduction, pre-ZOH and post-step realizations, R272 headroom, ring band-pass K=3.5, and the frozen local PI composition.
- One WSL Python process used the prospectively selected four-thread native-linear-algebra rung. The formal result root was create-only.

## Observations

- [CLM-1435] The independent checker passes all array-finiteness, dimension, equilibrium, object-separation, Schur-reduction, one-sided action-mapping, and pre/post-ZOH identity checks. Every reported reconstruction error is at or below the frozen `1e-10` tolerance (`verification_report.json#/model_verification`).
- [CLM-1435] Object A exposes three physical finite-difference levels over 102 differential-state rows and eight grouped direct-M/D input columns. The execution metadata records the non-differentiable normalized mapping at zero: positive normalized increments use a physical scale of 600, negative increments use 200, and the per-VSG projector's previous executed action is a separate state (`model_exports/object_a/input_output_maps.npz`; `metadata.json#/mapping` and `/execution`).
- [CLM-1435] Object B exposes a 102-state continuous and sampled realization with seven input columns and four frequency-output rows. The independent checker exactly reproduces the stored continuous reduction and the identities `C_post=C_pre A_d` and `D_post=C_pre B_d+D_pre` (`model_exports/object_b/continuous_reduced_model.npz`; `sampled_model.npz`; `verification_report.json#/model_verification/checks`).
- [CLM-1435] The export keeps the two objects non-poolable. Object A is M/D parameter modulation with a piecewise normalized-to-physical map; Object B is bounded active-power injection with system-pu control/disturbance coordinates and Hz outputs (`contracts/object_registry.json#/objects`; `contracts/units_and_coordinates.json`).
- [CLM-1435] The root `SHA256SUMS` verifies 24/24 declared content files with zero failures. The final checker report and its sidecar are additionally hashed outside that self-referential list (`verification_report.json#/hash_verification`).

## Conclusions

- **Finding, trust V — SHARED-MODEL-EXPORT-VALID.** The declared R446 Object A and R447 Object B source contracts now have a create-only, independently recomputable shared export suitable as an input to separately preregistered U1 and U3--U8 successors.
- **Reproducibility disposition.** The export binds source/runtime/case identity, the dirty-worktree patch and status, exact source and input hashes, package inventory, commands, model arrays, units, guards, profile protocol, and field-level claim pointers.
- **Object disposition.** Object A and Object B remain distinct evidence objects. No ratio, certificate, or performance result may combine them unless a later round explicitly defines and validates a common estimand.
- **Mathematical disposition.** This round supplies data but does not establish Q/Y10 feasibility, infeasibility, total-sensitivity derivatives, fractional-delay margins, mixed second-order tensors, separation bounds, message causality, or learned-policy value.

## Limits

- The matrices are local linearizations at one initialized nominal Kundur operating point. They do not establish nonlinear transient accuracy, a region of attraction, robustness, or global stability.
- Object A's normalized action map has distinct one-sided derivatives at zero; a successor must not replace it with one symmetric derivative. The exported direct-M/D Jacobians represent parameter modulation, not physical active-power injection.
- Object B's post-step sampled convention, positive equilibrium headroom mode, controller definitions, and fixed 60-Hz output map are part of the object. Any changed sample convention, headroom mode, controller, operating point, or input bank requires a successor export.
- No random profile population, held-out topology, hardware, EMT model, training seed, checkpoint, replay buffer, or trajectory endpoint is present.

## Publication gate

- **Evidence audit**: PASS — the claim is limited to the emitted shared export; all decision-bearing checks point to the current formal verifier, the formal root is registered `LOCAL-ONLY`, all declared hashes pass, and no parent performance number is reinterpreted.
- **Domain audit**: QUALIFIED — units, nominal frequency, sample convention, equilibrium, physical actuator distinction, headroom, M/D slew state, and local-linearization boundary are explicit. The export is admissible as reproducibility/method input only, not transient-performance or stability evidence.
- **External context**: NOT-APPLICABLE — this is a project-side reproducibility and model-contract fact; no novelty, literature, or external empirical claim is made.
- **Claim disposition**: ENTER — register the shared export and its non-pooling boundary as a prerequisite evidence object; keep downstream mathematics and performance out.
- **Allowed claim**: the declared direct-M/D and sampled energy-port source contracts have a hash-verified shared export whose stored Schur and post-step sampling identities are independently reproducible under the sealed runtime.
- **Stay-out**: full nonlinear plant equivalence; universal controller-class feasibility or impossibility; transient, stability, safety, robustness, topology-generalization, learned-policy, message-value, HIL, EMT, field, or deployment claims.

## Manuscript mapping

- Methods/Reproducibility supplement: cite the object registry, units, action mapping, complete DAE/reduced/sampled arrays, controller/headroom contract, runtime manifest, and hashes.
- Results: do not use this round as a performance result. Later U1/U5--U8 results may cite this export only if their own prospective checks pass.
- Limitations: retain the local-linearization, one-operating-point, distinct-object, one-sided Object A mapping, and post-step Object B sampling boundaries.
- Keep separate from R458's fixed-profile finite-bank witness and from any U2 learned message/no-message comparison.

## Follow-up pointers

- The authorized successor order is controlled by `paper/yang_md_decoupling_marl/working/route_owner_decision_advisory_unresolved_2026-08-21.md` and the imported additional-data request. Each mathematical or training item requires its own prospective round.
