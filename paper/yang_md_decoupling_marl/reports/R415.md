# R415 feed — soft-spot A4: extra unseen banks (2/3 pass; relaxed-inertia block fails r_d)

## Identity

- Round: `R415`; reserved claim: `CLM-1230`; date: `2026-08-17`.
- Authority: soft-spot program A4
  (`paper/yang_md_decoupling_marl/working/soft_spot_experiment_program.md`),
  owner decision
  (`paper/yang_md_decoupling_marl/working/route_owner_decision_soft_spot_program_2026-08-16.md`),
  creative mode. Successor of the aborted R414 run (R414 abort: the
  seal-vs-runtime block comparison drifted on the tuple/list JSON
  round-trip before the formal attempt; this round re-runs the identical
  frozen protocol with the canonical-form comparison and its regression
  test; see R414 plan abort_reason and the gate calibration log).
- Runner: `scripts/run_r415_energy_port_extra_banks.py`; frozen banks
  `src/andes_rl_kundur/evaluation/soft_spot_energy_port_banks.py`; harness
  imports from the frozen R408/R372 runners and `gate_b3_deterministic.py`
  (all read-only).
- Seal `memory/rounds/R415/formal_seal.json` (sha256
  `a081d421e6ffc87177dc4ced3e8483250b72679a26f7a8de9d9622ef399dcab4`).
- Result root: `results/research_loop/r415_energy_port_extra_banks/`;
  `formal_analysis.json` (sha256
  `58081cd4a990189b140ab4b114aa333fd18f7b8b7236d1dd2b969ef116e8502f`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- Three frozen unseen blocks: a4_conditions_b (new probe/disturbance set,
  nominal plant), a4_md_relaxed (inertia x0.85, damping x1.15),
  a4_md_stiff (inertia x1.15, damping x0.85). Each block runs
  zero/local/bandpass_k3p5 under identical conditions (8 paired probes +
  2 disturbances per arm = 30 records per block, 90 total).
- Endpoints: frozen R409 thresholds r_d <= 0.95, r_cross <= 1.10 (strict
  0.95 recorded), all R379 guards, candidate-versus-local on the same
  block. No retry, no tuning.
- Serial single-process seam (the item's serial estimate is <= 20 min);
  sealed budget 1 process
  (`memory/rounds/R415/capacity_evidence.json`, 32-task serial anchor).

## Observations

- **O-001 (CLM-1230) — Complete bank.** All 90 records completed with
  identity checks passing; every block summarized with the frozen
  estimators (see `formal_execution.json` and per-block `records.json`).
- **O-002 (CLM-1230) — a4_conditions_b PASS.** r_d = 0.9323, r_cross =
  0.9108 (strict pass), all guards pass (see
  `formal_analysis.json#/blocks/a4_conditions_b`).
- **O-003 (CLM-1230) — a4_md_stiff PASS.** r_d = 0.9080, r_cross = 0.7300
  (strict pass), all guards pass (see
  `formal_analysis.json#/blocks/a4_md_stiff`).
- **O-004 (CLM-1230) — a4_md_relaxed FAIL.** r_d = 0.9712 exceeds the
  0.95 threshold (+2.2%); r_cross = 0.9833 passes the primary ceiling but
  not the strict gate; all guards pass, so the failure is an endpoint
  failure, not an execution or safety failure (see
  `formal_analysis.json#/blocks/a4_md_relaxed`).
- **O-005 (CLM-1230) — Verdict BLOCKS-FAIL (2/3).** The pre-registered
  ALL-BLOCKS-PASS/BLOCKS-FAIL verdict reports the failing block with its
  ratios (see `formal_analysis.json#/verdict`, `#/passing_blocks`).

## Conclusions

- **Finding, trust V.** The frozen K=3.5 bandpass transfers to the new
  unseen condition block and to the stiff-plant perturbation (inertia
  x1.15, damping x0.85) with both endpoints inside the frozen thresholds
  and every guard passing.
- **Finding, trust V.** The bandpass does not transfer at its frozen gain
  to the relaxed-inertia/heavy-damping perturbation (inertia x0.85,
  damping x1.15): r_d = 0.9712 fails the 0.95 ceiling while all guards
  pass. The constructive result's scope therefore excludes that plant
  perturbation; no gain retuning was attempted and none is implied.
- **Evidence boundary.** Three frozen unseen blocks, one simulator, one
  candidate gain, the frozen threshold and guard family only; no claim
  about other plant perturbations, no claim that a different gain would
  pass the failing block.

## Limits

- One frozen K=3.5 gain: the failing block is reported as a boundary of
  the frozen controller, not as an impossibility.
- The three blocks are single-use; they add condition diversity, not
  population inference.
- No EMT/HIL/hardware claim.

## Publication gate

- **Evidence audit**: PASS - every observation binds to the hashed
  `formal_analysis.json` and per-block hashed records; ratios, guard
  flags, and the verdict are machine-readable; the R414 abort and the
  canonical-form fix are documented in the round ledger and the
  calibration log.
- **Domain audit**: PASS - the fail is an endpoint-threshold failure with
  all guards passing, reported as a scope boundary; no safety or
  superiority language is used; the constructive claim stays within the
  passing blocks.
- **External context**: NOT-APPLICABLE - bounded internal robustness
  evidence; no novelty claim, so no literature re-check is triggered.
- **Claim disposition**: ENTER - register the block table.
- **Allowed claim**: on the frozen three-block unseen bank, the K=3.5
  bandpass passes the frozen thresholds and all guards on the new
  conditions block (r_d 0.9323, r_cross 0.9108) and the stiff-plant block
  (r_d 0.9080, r_cross 0.7300), and fails the differential ceiling on the
  relaxed-inertia/heavy-damping block (r_d 0.9712 > 0.95, all guards
  passing); the constructive result is therefore scoped to exclude that
  plant perturbation.
- **Stay-out**: claims beyond the frozen blocks, retuning implications,
  population inference, and any use of the failing block as a controller
  defect rather than a scope boundary.

## Manuscript mapping

- Section 5.3 (constructive energy-port result): add the three-block
  unseen-bank sentence: two blocks pass inside the thresholds and every
  guard, one relaxed-inertia/heavy-damping block fails r_d by 2.2% with
  all guards passing, scoping the claim.
- Section 7 (limitations): add the plant-perturbation scope boundary.

## Follow-up pointers

- Program item A3 (law-family expansion) is the next round. No new
  question is opened: the failing block is reported as a measured scope
  boundary.
