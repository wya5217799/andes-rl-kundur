# R425 feed — the sign-corrected guard-aligned constraints reach the guard layer for action stress (36→12 blocks, the family's first movement) but the multiplier ceiling caps the correction short of closure; the bang-bang collapse resolves, the message contrast unmasks to ≈0, and the critic divergence persists

## Identity

- Round: `R425`; reserved claim: `CLM-1290` (experiment); date: `2026-08-18`.
- Authority: feedback loop (creative mode) + owner order 2026-08-18 (fix the
  R424 reward-sign defect found in CLM-1285 and retest the guard-aligned
  constraint as designed); parents `CLM-1285` (R424), `CLM-1270` (R423),
  `CLM-1275` (modal theory layer).
- Runner: `scripts/run_r425_guard_constraints_signfix.py`; repair learner
  `src/andes_rl_kundur/agents/cd_matd3_guard_constraints_vfix.py`
  (`GuardConstrainedSlewAwareCDMATD3Signfix`); the R424-sealed learner file
  `cd_matd3_guard_constraints.py` is byte-untouched.
- Seal `memory/rounds/R425/formal_seal.json` (sha256
  `90767321061a0fd5b3c919d25464d04551f7b3ae35fac461b17ee4d6328bd1a7`);
  rehearsal `memory/rounds/R425/rehearsal.json` (sha256
  `2db9a312f9fe1203268f1d62dfbcabc48853ac3383de23af6a72e078a7ab2a30`) carries
  the objective-semantics probe record (`objective_semantics_probe`;
  rms_penalty_dot +0.573, tv_penalty_dot +14.8, defect_form_dot −3.05).
- Result root: `results/research_loop/r425_guard_constraints_signfix/`;
  `formal_analysis.json` (sha256
  `080b6e2f6c7ffafe9f1bb95634773834458cadb9bcc1d53d50559e2b47e5831e`);
  `formal_manifest.json` (sha256
  `5d03950edd51c369b1a2a4d1fa10aec7cb64813563bd5358707c32df1006da7c`);
  `reference_action_stats.json` (sha256
  `a48ec36faf161dc9a5aa28bb95bfd6017c2e71a0026061c592ef503318a6dd1c`, frozen
  at prepare).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- Single registered factor versus R424: the constraint terms move OUTSIDE
  the negated mean — exact loss
  `loss = -mean(Q_d + lambda*Q_c) + mu_RMS*mean(row**2) +
  mu_TV*mean(|row - prev|)` on the executed (post-slew) action row
  (penalty semantics; the R424 form placed both terms inside the negation,
  i.e. a reward sign — CLM-1285). Everything else is verbatim R424/R419.
- Residual aggregation (declared): the RMS residual normalizes the
  time-MEAN executed-action energy against (1.10*RMS_ref)^2 with RMS_ref
  the deterministic-reference root mean square of the same mean energy; the
  TV residual normalizes the per-episode SUM of per-step absolute changes
  against 1.10*TV_ref_scenario_mean (reference per-scenario TV mean) — the
  two aggregations differ intentionally, mirroring the guard definitions,
  and each reference is measured with the same aggregation as its residual.
- Critic-divergence baseline (declared): the R419-base unclipped Q4/Q1 is
  R424's 5.98-9.61 (same-bundle baseline); R423's "24.4-126.4x unclipped"
  is the pre-repair R410-family readout (cross-bundle, indicative) and is
  not used for the same-bundle comparison.
- The dual mechanism, the reward seam (R419 verbatim, no effort term), the
  lambda mechanism, the scalar arm (byte anchor vs R419), the R419 bundle,
  the frozen thresholds (`reference_action_stats.json`), and the readouts
  (critic-loss quartiles, mu/residual traces, slew diagnostics) are
  verbatim R424.
- Semantics gate (SKILL.md section 2): rehearsal ran the
  `penalty_direction_probe` on the real learner and recorded it; the
  directed tests pin the penalty direction;
  `objective_semantics_lint.py R425` passes.
- Budget: solo ladder (rungs 1/2/4/8/12/16) sealed 16 workers + 1 driver;
  nine shards in one wave; serial evaluate + classify. R426 (B2) joins
  concurrently and declares this round's 17 processes.

## Observations

- **O-001 (CLM-1290) — Complete valid bank, CANARY-FAIL.** Nine manifests
  record 43,200 steps with valid convergence diagnostics (0 TDS failures);
  240 evaluation records plus the deterministic reference; classification
  `CANARY-FAIL` with all integrity checks true (`complete_bank`,
  `all_rows_valid`, `all_manifests_valid`, `budget_frozen`, `reward_unused`
  — `formal_analysis.json#/classification`, `formal_manifest.json`).
- **O-002 (CLM-1290) — Scalar anchor bit-identical.** All three scalar
  checkpoints are byte-identical to R419 (`scalar_anchor_vs_r419` all true)
  and the scalar medians match R419 to the last digit (0.00153891 /
  0.00663988) — the repair is pinned to the CD actor objective
  (`formal_analysis.json#/scalar_anchor_vs_r419`, `/b1_table/medians`).
- **O-003 (CLM-1290) — Pre-registered key contrast met: the action-stress
  guards move for the first time in the family.** `action_rms_no_harm` and
  `action_variation_no_harm` both drop 36 → 12 failed blocks (24/36 blocks
  now pass each guard); all five family rounds R419/R420/R422/R423/R424
  stayed at 36/36. Per the frozen rule, the sign correction produces
  guard-level impact on action stress — no arm passes ALL guards, so the
  canary stays CANARY-FAIL and the report branch applies
  (`formal_analysis.json#/classification/guard_failures`).
- **O-004 (CLM-1290) — The dual binds but is capped at the ceiling.** All
  240 retained multipliers still sit at the 10.0 ceiling in 6/6 runs, but
  the RMS residuals collapse from R424's 38.3-90.8x to 1.1-10.1x (median
  2.9-4.2; retained last-20 traces) and the TV residuals sit at 3.9-6.7x
  (median 4.2-4.4) — the penalty semantics pull the action energy ~10x
  closer to the reference-relative threshold, and the 10.0 multiplier
  ceiling caps the correction before full closure
  (`formal_analysis.json#/guard_multiplier_readout`).
- **O-005 (CLM-1290) — The bang-bang collapse resolves.** CD saturation
  fraction drops to 0.125-0.132 across the six runs (R424 median 0.90,
  23/36 blocks ≥0.90) and the CD execution-mismatch mean drops to
  0.0056-0.0064 (R424: 0.366; R419 bundle: 0.046-0.156) — the policies no
  longer pin at the actuator bounds (manifests `/slew_diagnostics`,
  `formal_analysis.json#/b1_table/slew_diagnostics`).
- **O-006 (CLM-1290) — Endpoints stay far from the unpenalized base.** The
  message arm reaches 2.7477 / 2.1127 median endpoint ratios versus the
  deterministic reference and the no-message arm 2.6953 / 2.0639 (R424:
  2.3419 / 1.7751 with bit-identical arms; R419 base: 0.6954 / 0.7104) —
  the sign-corrected constraint tames the actions without restoring the
  R419 endpoint profile (`formal_analysis.json#/b1_table/medians`,
  `/median_endpoint_ratio_vs_deterministic`).
- **O-007 (CLM-1290) — The message contrast unmasks to ≈0.** The two CD
  arms are no longer bit-identical; the message increment over the matched
  no-message arm is −1.94% / −2.36% (R424: 0.0000 masked by slew clipping;
  R419: +43.45% / +25.59%) — with the clip no longer flattening distinct
  policies into one executed sequence, the measured contrast is measurable
  again and shows no positive coordination increment this round
  (`formal_analysis.json#/b1_table/message_improvement_vs_comparators`).
- **O-008 (CLM-1290) — The no-harm profile reverts as actuation retreats.**
  Common-frequency failures rise to 36/36 and worst-peak to 36/36 (RoCoF
  14/36), versus R424's family-best 12/14/24 and the R419 base 19/21/28 —
  the R424 no-harm gains were bought by maximal actuation and do not
  survive the sign correction (`formal_analysis.json#/classification/
  guard_failures`).
- **O-009 (CLM-1290) — The critic divergence persists, slightly damped.**
  CD-arm Q4/Q1 ratios are 4.65-6.29 (threshold <3 not met) versus R424's
  same-bundle 5.98-9.61 — the value-scale loop still grows, and the
  divergence remains the proximate driver
  (`formal_analysis.json#/critic_loss_readout`).

## Conclusions

- **Finding, trust V.** The sign-corrected guard-aligned constraints DO
  reach the guard layer for action stress: both action-stress guards move
  36 → 12 failed blocks — the pre-registered criterion (36 → <36) is met,
  the first guard-level movement in the five-round family — and the R424
  sign defect (CLM-1285) is thereby confirmed as the explanation of the
  previous null. The mechanism as designed is now actually tested and
  produces a real, bounded effect (sealed-bank evidence; frozen classifier).
- **Finding, trust V.** The constraint binds but is capped: every retained
  multiplier sits at the 10.0 ceiling while the RMS residual stays ~3-4x
  (median) above the reference-relative threshold — the 10x residual
  collapse versus R424 shows the penalty direction works, and the ceiling
  (or the fixed 0.05/step schedule) caps the corrective force before the
  guard threshold is met, so the action-stress gap is narrowed rather than
  closed (`formal_analysis.json#/guard_multiplier_readout`).
- **Finding, trust V.** The tamed policies resolve the bang-bang collapse
  (saturation fraction 0.90 → 0.13, mismatch 0.366 → 0.006) and unmask the
  message contrast, which measures ≈0 (−1.94% / −2.36%): no positive
  coordination increment appears this round either, and the endpoint
  profile (2.75 / 2.11 versus 0.70 / 0.71 for the unpenalized base) shows
  the action-energy penalty trades away the R419 endpoint gains.
- **Finding, trust V.** The critic divergence persists (Q4/Q1 4.65-6.29 ≥
  3), so the value-estimation layer remains the proximate driver; the
  guard-aligned constraint alone cannot steer a well-conditioned policy
  while the value scale grows.
- **Evidence boundary.** One fixed-connectivity ANDES formulation, three
  seeds, one frozen multiplier schedule (step 0.05, ceiling 10.0); bounded
  to the tested bundle.

## Limits

- No arm passes all guards: action stress improves 36 → 12 but stays
  failing in 12/36 blocks per guard; no guard-passing claim is made.
- One multiplier schedule tested; no ceiling sweep, no per-profile
  multipliers, no schedule decay — the ceiling-capped residual admits
  schedule/ceiling alternatives that are untested.
- The no-harm regression (36/36 common-frequency, 36/36 worst-peak) is
  measured on the same three seeds; the message increment ≈0 is a
  descriptive median, not population inference.
- Three seeds per arm: descriptive medians only.

## Publication gate

- **Evidence audit**: PASS — every observation binds to the hashed
  `formal_analysis.json` / `formal_manifest.json`, the hashed per-run
  manifests and critic-loss traces, the hashed sealed evaluation records,
  or the frozen `reference_action_stats.json`; guard counts and residual
  ranges are read from the frozen classifier output and re-derived in the
  loop audit; all sidecars verify; no trajectory was re-run for analysis
  reasons.
- **Domain audit**: PASS — the action-stress gain is reported together
  with the ceiling-capped residuals, the persistent critic divergence, and
  the no-harm regression against both R424 and the R419 base; the ≈0
  message increment is reported as an unmasked measured contrast, not as a
  message-mechanism verdict; no decoupling-success or guard-passing claim
  is made.
- **External context**: NOT-APPLICABLE — bounded internal mechanism
  evidence; no novelty claim is made.
- **Claim disposition**: ENTER — register the sign-corrected action-stress
  movement (36 → 12), the ceiling-capped dual, the resolved bang-bang
  collapse, the unmasked ≈0 message contrast, the reverted no-harm
  profile, and the persistent critic divergence.
- **Allowed claim**: see the CLM-1290 registration card.
- **Stay-out**: guard-passing claims; decoupling-success claims;
  message-mechanism verdicts drawn from the ≈0 increment; any claim that
  the constraint closed the action-stress gap or that the critic
  divergence stopped; generalization beyond the tested bundle.

## Manuscript mapping

- Section 5.2: add the R425 paragraph after the R424 paragraph (bound to
  CLM-1290): the sign-corrected retest moves both action-stress guards
  36 → 12, caps the dual at the ceiling with a ~10x residual collapse,
  resolves the bang-bang collapse, unmasks the message contrast to ≈0,
  reverts the no-harm profile, and keeps the critic diverging.
- Section 6.1: update the two-layer residual statement — the
  constraint-hierarchy layer is now measured as designed (a real, bounded
  action-stress effect capped by the multiplier ceiling) and the
  value-estimation layer still diverges.
- Section 6.4: the guard-aligned-constraint direction gains its
  as-designed outcome (effective but ceiling-capped; multiplier schedule
  or ceiling sweep is an open option; value-estimate stability remains
  the enabling repair).
- Section 7: update the sign-defect limitation — the corrected form is
  retested and the mechanism verdict now stands on the sign-corrected
  bank.

## Follow-up pointers

- Next single factor: DR-menu P1 critic target normalization (registered
  fallback; the critic divergence Q4/Q1 ≥ 3 persists).
- Open option: multiplier ceiling / schedule sweep for the guard-aligned
  constraint (untested; residual stays ~3-4x at the 10.0 ceiling).
- B2 five-seed extension lands in R426 (in flight); C1-SAC stays queued
  (owner-gated).
