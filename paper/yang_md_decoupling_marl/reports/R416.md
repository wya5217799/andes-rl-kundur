# R416 feed — soft-spot A3: law-family expansion + oracle (21 laws, zero headroom)

## Identity

- Round: `R416`; reserved claim: `CLM-1235`; date: `2026-08-17`.
- Authority: soft-spot program A3
  (`paper/yang_md_decoupling_marl/working/soft_spot_experiment_program.md`),
  owner decision
  (`paper/yang_md_decoupling_marl/working/route_owner_decision_soft_spot_program_2026-08-16.md`),
  creative mode.
- Runner: `scripts/run_r416_headroom_expansion.py`; frozen expansion
  contract `src/andes_rl_kundur/evaluation/soft_spot_headroom_expansion.py`;
  shared shard driver `scripts/soft_spot_shard_driver.py`; the frozen
  `md_decoupling_headroom.classify_bank` consumed verbatim.
  Plan-registered analysis-step amendment `probes/r416_headroom_classify.py`
  (the sealed runner's classify mis-read the R399 formal_analysis.json
  shape; the probe applies the same frozen classifier and the registered
  anchor on the sealed records without re-running any trajectory).
- Seal `memory/rounds/R416/formal_seal.json` (sha256
  `7bc632a8477fda08bc28651f765eb146275a5260988fdde044ea3fa88bb126c5`).
- Result root: `results/research_loop/r416_headroom_expansion/`;
  `formal_analysis.json` (sha256
  `f6e4740cc7a1d25c7cd043879ccdce5f89704f8f37bda946dabcf40c897aa21e`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- Expanded frozen family: 20 local-neighbour grid laws (inertia gains
  {0.5, 1.0, 1.5, 2.0, 3.0} x damping gains {0.5, 1.0, 1.5, 2.0},
  containing the original nine) plus one PI-type law
  (per-VSG u = tanh(-kp df - ki I), kp_m = kp_d = 1.0, ki_m = ki_d = 0.4,
  integral clip +/-2, slew 0.25) = 21 candidates.
- R399 assets verbatim: six profiles (2 development + 4 evaluation),
  signed paired scenarios, 30 steps x 0.2 s, seed 399, the frozen
  estimators, thresholds (5% joint improvement, 3% common no-harm, 10%
  action-stress no-harm, saturation and action guards), and the
  outcome-seeing per-profile oracle semantics via `classify_bank`.
- Budget: pre-seal capacity ladder selected rung 8; sealed budget 9
  processes (8 workers + driver launcher), native threads 1, other
  reserved 0 (`memory/rounds/R416/capacity_evidence.json`).
- Anchors: the nine original laws' re-evaluated summaries must reproduce
  R399 bit-identically (row-level), and the nine-law subset classification
  must reproduce the R399 development selection and oracle improvements
  within 1e-6 relative.

## Observations

- **O-001 (CLM-1235) — Nine-law anchor reproduced.** The nine-law subset
  classification returns STOP-NO-JOINT-HEADROOM with development-selected
  `local_neighbour_md_km2_kd2` and oracle improvements 0.0/0.0, exactly
  matching the R399 records (all anchor deviations 0.0) (see
  `formal_analysis.json#/nine_law_anchor`).
- **O-002 (CLM-1235) — Expanded development selection moves to km3_kd2.**
  In the 21-law family the development gate selects
  `local_neighbour_md_km3_kd2` (off-diagonal ratio to zero 0.36128, i.e.
  63.87% reduction; differential ratio 0.35052, i.e. 64.95% reduction)
  versus the nine-law winner km2_kd2 (0.39207 / 0.35873, reproducing the
  R399 60.79% / 64.13% figures) (see
  `formal_analysis.json#/classification` `/development_gate/candidates`).
- **O-003 (CLM-1235) — PI law is development-ineligible.** The frozen
  PI-type law fails the common-mode no-harm guard on development
  (off-diagonal ratio 0.92617, differential 0.89991, common_no_harm
  false) and is correctly excluded from selection and the oracle (see
  `formal_analysis.json#/classification` `/development_gate/candidates/pi_frequency_md`).
- **O-004 (CLM-1235) — Oracle headroom delta is zero.** The outcome-seeing
  oracle selects km3_kd2 on all four evaluation profiles, so the
  improvement over the expanded development-selected law is 0.0% on both
  endpoints and the classification is STOP-NO-JOINT-HEADROOM (see
  `formal_analysis.json#/classification` `/oracle_gate`).
- **O-005 (CLM-1235) — Complete valid bank.** All 22 arms x 6 profiles
  summaries are present and valid; every guard check passes on the
  selected arms (see `formal_analysis.json#/classification` `/checks`).

## Conclusions

- **Finding, trust V.** The R399 no-headroom result generalizes from the
  nine-law family to the frozen 21-law expanded family: the densified
  grid plus a structurally different PI-type law yields a stronger
  development comparator (km3_kd2, 63.87% / 64.95% reductions versus zero)
  but still zero outcome-seeing oracle headroom beyond it on the four
  evaluation profiles.
- **Finding, trust V.** The PI-type law is development-ineligible because
  it harms the common mode, so the headroom statement covers the frozen
  family including that structure, not an unconstrained class.
- **Finding, trust V.** The nine-law re-evaluation reproduces R399
  bit-consistently, certifying the harness for the whole expansion.
- **Evidence boundary.** One simulator, the frozen six profiles, the
  frozen 21-law family and guard family only; no claim beyond this
  finite family and bank.

## Limits

- The family is finite (21 laws): STOP-NO-JOINT-HEADROOM remains a
  finite-family statement, not an impossibility theorem.
- The oracle is outcome-seeing, per-profile, nondeployable; its zero
  delta is measured against the expanded family's own development-selected
  law.
- The PI law is one frozen parameterization; no PI gain search occurred.

## Publication gate

- **Evidence audit**: PASS - every observation binds to the hashed
  `formal_analysis.json` and the hashed per-arm record files; the anchor
  deviations are machine-readable; the analysis-step amendment is
  plan-registered and changes no trajectory or classifier.
- **Domain audit**: PASS - the expanded-selection result is reported as a
  family-extension of the registered baseline (the canary's frozen
  deterministic reference km2_kd2 is unchanged); the PI-law exclusion is a
  guard failure, not a structure dismissal; no universality language is
  used.
- **External context**: NOT-APPLICABLE - bounded internal evidence; no
  novelty claim, so no literature re-check is triggered.
- **Claim disposition**: ENTER - register the expanded-family headroom
  delta and the anchor.
- **Allowed claim**: over the frozen 21-law expanded family on the six
  R399 profiles, the development gate selects km3_kd2 (63.87% off-diagonal
  and 64.95% differential reduction versus zero action), the frozen PI
  law is development-ineligible on the common-mode guard, and the
  outcome-seeing oracle selects the same km3_kd2 on all four evaluation
  profiles for zero additional improvement on both endpoints; the nine-law
  re-evaluation reproduces R399 bit-consistently.
- **Stay-out**: headroom absence beyond the frozen family, PI-law class
  dismissal, and any use of km3_kd2 to replace the canary's frozen
  deterministic reference.

## Manuscript mapping

- Section 5.1: add one sentence that the same oracle over the expanded
  21-law family re-selects km3_kd2 on development and still finds zero
  headroom on the evaluation profiles, with the PI law excluded on the
  common-mode guard.
- Section 4.2: mention the expanded family as the registered follow-up
  of the nine-law bank.
- Section 7: update the "ranges over nine laws only" limitation to name
  the 21-law extension.

## Follow-up pointers

- Default deck complete (A1/A2/A4/A3). No new question is opened; the
  training deck (B1/B2/B3, C1-SAC) remains the extension's registered
  next steps.
