# R449 feed — the state-matrix channel of P1.1 is a mixed candidate/reference effect for both inertia and damping

## Identity

- Round/date/claim: `R449`, 2026-08-20, `CLM-1400`.
- Authority: owner decision to resolve the VSG failure-math advisory; R448 is
  retained `CANARY-INVALID` and is not used as positive evidence.
- Result: `results/research_loop/r449_p1_sensitivity/formal_analysis.json`
  (SHA-256 `621cae2c4cf527fea8fc848f23995ef0a8c4b616be56dbc98ebdd4522ed2fb7d`),
  with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/runner: `memory/rounds/R449/plan.md`,
  `memory/rounds/R449/formal_seal.json` (SHA-256
  `6b407efb78ad5f7f8259509e29ac67e803bdcbe6f69270dca4e6b2e1f84b7dd7`),
  `scripts/run_r449_p1_sensitivity.py`.

## Frozen setup

- Object B reduced sampled model, 0.2 s sampling, four energy-port inputs,
  ring bandpass candidate and local-PI reference, each with 0.072 pu headroom.
- Central log-parameter perturbations use `M=200(1±0.01)` and
  `d0_per_agent=(100(1±0.01), ...)`; every point is independently reset and
  re-linearized.
- The P1.1 terms are aggregated over 0.3–0.5 Hz differential-frequency energy;
  absolute-term ratio above 3 is dominance, otherwise `MIXED`.

## Observations

- [CLM-1400] Both perturbations are effective: in the registered normalized
  coordinates, the maximum absolute sampled state-matrix derivatives are
  3.2579 for log M and 1.7918 for log D
  (`/results/logM/d_state_matrix_max_abs`,
  `/results/logD/d_state_matrix_max_abs`).
- [CLM-1400] For log M, the candidate term is -2.6765 and the reference term is
  +3.0147; their absolute dominance ratio is 1.1264, giving `MIXED`
  (`/results/logM`, `/verdicts/logM`).
- [CLM-1400] For log D, the candidate term is -0.1883 and the reference term is
  +0.2293; their absolute dominance ratio is 1.2180, giving `MIXED`
  (`/results/logD`, `/verdicts/logD`).

## Conclusions

- **Finding, trust V — SUPPORTED.** At the registered nominal Object B
  equilibrium and 0.3–0.5 Hz band, neither the candidate nor local-reference
  contribution induced through the sampled state matrix dominates by the
  pre-registered factor-of-three rule; the A-channel terms for both parameters
  classify as `MIXED`.
- **Mechanism boundary.** The two contributions have opposite signs and similar
  magnitudes. This supports a cancellation interpretation of the local ratio
  derivative only; it does not identify gain margin, phase margin, nonlinear
  endpoint causation, or a universal failure mechanism.

## Limits

- One nominal equilibrium, one reduced small-signal Object B model, one
  0.3–0.5 Hz flat-band weighting, and ±1% centered perturbations only.
- The derivative folds `dA_d/dρ`; sensitivities of B and C are omitted under
  the registered approximation. No nonlinear trajectory, delay, robustness,
  training, message, topology, safety, or deployment conclusion is tested.
- R448's ineffective damping perturbation remains an invalid retained artifact;
  only R449 supports the damping statement.

## Publication gate

- **Evidence audit**: PASS — the result hash and sidecar match; all four terms,
  both derivative checks, both ratios, and both verdicts were independently
  recomputed from the canonical JSON. The narrower A-channel wording matches
  the executed approximation.
- **Domain audit**: PASS — model, 0.2 s timing, differential-frequency output,
  headroom, effective M/D perturbations, and small-signal scope match the sealed
  runner. The audit qualified the original full-P1.1 wording because dB/dρ and
  dC/dρ were not propagated; the feed now claims the A-channel contribution only.
- **External context**: CURRENT — this is the registered repo-side measurement
  for advisory P1.1; no new literature claim is made.
- **Claim disposition**: ENTER — register the bounded A-channel mixed finding.
- **Allowed claim**: in the registered nominal Object B small-signal model and
  0.3–0.5 Hz band, the state-matrix-induced candidate and reference terms are
  both `MIXED` for log M and log D under the factor-of-three rule.
- **Stay-out**: gain/phase-margin attribution; nonlinear endpoint causation;
  general stability, robustness, learning, topology, safety, or deployment.

## Manuscript mapping

- Discussion/theory limitation: the state-matrix channel of P1.1 can be
  instantiated as a bounded small-signal diagnostic, but the registered result
  is mixed rather than a single-source causal attribution; the full derivative
  remains unmeasured because B/C sensitivities are outside this round.
- Stay out of the title and headline performance claims; it does not test MARL,
  coordination, nonlinear endpoint superiority, or generalization.

## Follow-up pointers

- P2 delay sweep and loop-break definition remain under the owner-authorized
  advisory resolution program.
