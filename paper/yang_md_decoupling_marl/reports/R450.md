# R450 feed — P2 supports a bounded pure-delay phase account on the registered bank

## Identity

- Round/date/claim: `R450`, 2026-08-20, `CLM-1405`.
- Authority: owner decision to resolve the VSG failure-math advisory after the
  P1 measurements.
- Result: `results/research_loop/r450_p2_delay_loop/formal_analysis.json`
  (SHA-256 `39339ce2965767337e6a21ee013552339af396422cab729f485655776e4c10e8`),
  with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/runner: `memory/rounds/R450/plan.md`,
  `memory/rounds/R450/formal_seal.json` (SHA-256
  `389191ae82ddb093538e156c400f5fe82d967b1b3451f4c0cc15583a37f7ce89`),
  and `scripts/run_r450_p2_delay_loop.py`.

## Frozen setup

- The nonlinear curve uses the R440 nominal-topology bank, seed 42, 0.2 s
  sampling, 50 steps, three arms, eight paired probes, and two disturbances.
  R450 newly executes only delay 0; hashed R440 delay-1 and delay-2 summaries
  are reused without re-execution.
- The loop is broken at the normalized bandpass output before the 0.072 pu
  power map. The four-dimensional injected command is `u`, the measured output
  is the four-device frequency-deviation vector `y`, and feedback is
  `u=-K(z)y`.
- The registered exact MIMO response is
  `G_n=[I+z^(-n)P_cK]^(-1)P_d`. Comparison uses the 0.3--0.5 Hz
  differential-frequency energy shape normalized by the zero-delay point.

## Observations

- [CLM-1405] All 30 new zero-delay records completed with zero invalid records.
  The nonlinear differential-energy ratios are 0.9389468, 0.9502788, and
  0.9893271 at delays 0, 1, and 2 (`/nonlinear`).
- [CLM-1405] The nominal loop predicts corresponding ratios 0.8884297,
  0.9402695, and 1.0086472. The zero-delay linear/nonlinear seam error is
  5.3802%, within the registered 10% gate (`/linear_loop/predicted_r_d`,
  `/classification/linear_nonlinear_zero_delay_relative_error`).
- [CLM-1405] The normalized delay-curve errors are 4.5729% at one step and
  7.7500% at two steps; both predicted and measured curves worsen from delay
  zero. The minimum return-difference singular values remain 0.9705, 0.9455,
  and 0.9197 (`/classification/comparisons`,
  `/linear_loop/min_return_difference_sigma`).
- [CLM-1405] Delay zero passes the 0.95 differential-ratio threshold, whereas
  delay one narrowly fails it. The registered boundary is therefore
  `BETWEEN-0-AND-1-SAMPLE-ENDPOINT-BOUNDARY` (`/classification`).

## Conclusions

- **Finding, trust V — SUPPORTED.** On this one registered nominal bank, the
  exact small-signal MIMO pure-delay response predicts the direction and
  normalized magnitude of the measured one- and two-sample degradation within
  the pre-registered 10% tolerance. P2 classifies as
  `PHASE-DELAY-CONSISTENT`.
- **Mechanism prediction disposition — SUPPORTED, bounded.** The data support
  pure delay as a quantitatively consistent account of this local delay curve.
  They do not identify a gain margin, phase margin, pole-crossing stability
  boundary, or delay as the unique nonlinear cause.
- **Endpoint statement.** The registered pass/fail threshold changes between
  zero and one sample. This is an empirical endpoint boundary, not a formal
  stability margin.

## Limits

- One nominal Object B equilibrium, one 0.3--0.5 Hz weighting, integer delays
  0--2, and one frozen nonlinear bank only.
- The loop model is a reduced local linearization; the zero-delay absolute
  seam differs by 5.38%, so only the normalized curve is used for the mechanism
  gate.
- No learner, message channel, topology, broader robustness, safety, or
  deployment conclusion is tested. No fractional-delay boundary is localized.

## Publication gate

- **Evidence audit**: PASS — Result and seal sidecars match. An independent
  recomputation recovered the seam error, both normalized curves, both curve
  errors, the boundary, and the registered verdict; all seal source hashes
  match current bytes.
- **Domain audit**: PASS — Sampling, sign convention, command-break location,
  four-input/four-output dimensions, 0.072 pu map boundary, frequency band,
  parent-bank identity, and nonlinear estimator match the frozen contract.
  Wording is restricted to local curve consistency and an endpoint threshold.
- **External context**: CURRENT — This is the registered repo-side P2
  instantiation; no external number or literature claim is imported.
- **Claim disposition**: ENTER — Register the bounded phase-delay consistency
  and the zero-to-one-sample endpoint boundary.
- **Allowed claim**: on the registered bank, the exact local pure-delay model
  is consistent with the normalized one/two-sample degradation curve, and the
  empirical 0.95 endpoint boundary lies between zero and one sample.
- **Stay-out**: gain or phase margin; global or nonlinear stability proof;
  unique causation; learner, topology, safety, or deployment claims.

## Manuscript mapping

- Discussion/theory limitation: a frozen delay perturbation can explain the
  local degradation shape, while the threshold crossing remains specific to
  one bank and one sampling interval.
- Keep out of title and headline MARL claims; no training or coordination
  comparison was performed.

## Follow-up pointers

- The owner-authorized advisory order next enters M3, then M5, M4, M1, and M2.
