# R453 feed — the registered finite generator has guard-clean joint headroom in three of four profiles

## Identity

- Round/date/claim: `R453`, 2026-08-20, `CLM-1410`.
- Authority: owner-authorized VSG failure-math advisory M5, as the aggregation-
  only successor to aborted R452.
- Result: `results/research_loop/r453_m5_aggregate_repair/formal_analysis.json`
  (SHA-256 `fa16eda8f71621a2bc37868c80b7349f30d107d8c627fe3020ea35178e41223d`),
  with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/runner: `memory/rounds/R453/plan.md`,
  `memory/rounds/R453/formal_seal.json` (SHA-256
  `e7053110c9fb8643a40f57393d795e2044c4b3a4b6468d767f067599a31d434e`),
  and `scripts/run_r453_m5_aggregate_repair.py`.

## Frozen setup

- R453 performs no physical rerun and no training. It verifies and reuses the
  immutable R452 execution bank: 68 hashed shards, 1,400 generated candidate
  rows, 8,400 candidate trajectories, and 24 fresh-static trajectories across
  `eval_a..eval_d`, with zero execution errors.
- The candidate generator, direct-M/D object, four profiles, six signed
  scenarios/profile, seed, schedules, summaries, static references, and all
  thresholds remain those frozen by R452 from R439/R441.
- The repaired aggregation separates two populations. Endpoint-only eligibility
  means both endpoint improvements are at least 5%, regardless of `valid`.
  Valid-endpoint eligibility intersects that population with the full validity
  flag. Conditional action minima are reported for both populations.
- Guard-clean joint feasibility still requires endpoint eligibility, full
  validity, all three common-mode ratios at most 1.03, both action-stress ratios
  at most 1.10, and saturation fraction at most 0.05. The Pareto population is
  unchanged: valid, common-clean, saturation-clean rows over the four minimized
  endpoint/action ratios.

## Observations

- [CLM-1410] The corrected endpoint-only counts are 45, 47, 29, and 16 for
  `eval_a..eval_d`; the corresponding valid-endpoint counts are 45, 25, 29,
  and 13 (`/profiles`). This repairs R452's ambiguous stored counts of 45, 25,
  29, and 13 without changing any underlying row.
- [CLM-1410] Guard-clean joint-feasible ID counts are 0, 2, 6, and 1. The IDs
  are none for `eval_a`; `k3_112,k3_116` for `eval_b`;
  `k2_021,k3_112,k3_113,k3_116,k3_117,k5_110` for `eval_c`; and `k3_116`
  for `eval_d`. Every listed feasible ID lies on its profile's registered
  four-objective Pareto front (`/profiles`).
- [CLM-1410] `k3_116`, schedule `[(3,3),(2,2),(1,1)]`, is the common feasible
  row for `eval_b/c/d`. Its disturbance/off-diagonal improvements are
  8.535%/8.109%, 11.775%/5.336%, and 5.360%/10.600%; its action-RMS/action-TV
  ratios are 1.0810/1.0976, 1.0432/1.0342, and 1.0840/1.0864. Saturation is
  zero and all common guards pass in those profiles.
- [CLM-1410] The same `k3_116` row is endpoint-eligible and common-clean in
  `eval_a` (11.846%/7.526% endpoint improvements), but its action-TV ratio is
  1.1465, beyond the registered 1.10 action budget. Although `eval_a` has
  separate endpoint-eligible minima at action-RMS ratio 0.8804 (`k3_105`) and
  action-TV ratio 1.0827 (`k3_106`), no single generated row clears the full
  joint guard.
- [CLM-1410] Pareto-front sizes are 18, 32, 25, and 25. R453 reproduces every
  R452 candidate row, guard-feasible ID, and Pareto ID exactly; only the
  population labels/counts and dual conditional-minimum reporting are repaired
  (`/parent_comparison`).

## Conclusions

- **Finding, trust V — SUPPORTED.** M5 is
  `PARTIAL-GUARD-CLEAN-JOINT-HEADROOM-IN-GRID`: the registered finite R439
  generator contains guard-clean joint headroom in `eval_b/c/d`, but not in
  `eval_a`.
- **Bounded mechanism disposition.** R441's endpoint winners were not the only
  available trade-off: lower-stress nondominated rows recover the registered
  joint target in three profiles. The remaining `eval_a` action-TV failure
  shows that this is not an all-profile resolution.
- **Aggregation correction.** R452's physical rows remain usable, but its
  formal analysis is non-authoritative because the field named
  `joint_endpoint_eligible_count` silently added a validity filter. R453 is the
  first authoritative M5 decision artifact.

## Limits

- Exhaustive only over the registered 350-row generator/profile and its fixed
  four-profile, six-scenario bank. It is not a lower bound over arbitrary
  schedules or controllers and does not prove structural infeasibility in
  `eval_a`.
- R439 segment resets mean repeated schedules are syntactically constant but
  are not asserted dynamically equivalent to one uninterrupted static segment.
- No learner, message channel, optimization convergence, topology extension,
  broad robustness, stability proof, safety, or deployment conclusion is
  tested. This is an oracle-grid evaluation, not a successful-MARL result.

## Publication gate

- **Evidence audit**: PASS — R453 seal/result/profile sidecars match; all 68
  parent shard sidecars and identities were reverified; candidate sequence,
  row count, and trajectory count are exact. Independent recomputation matched
  every stored guard-feasible ID, conditional-minimum ID, and Pareto front.
- **Domain audit**: PASS — The direct-M/D actuator object, static reference,
  profile/scenario bank, summary fields, endpoint definitions, common/action/
  saturation guards, and four-objective dominance rule match the frozen
  R439/R441/R452 object. Wording remains finite-grid and profile-specific.
- **External context**: CURRENT — This is the registered repo-side M5
  instantiation. No external empirical number or literature claim is imported.
- **Claim disposition**: ENTER — Register the partial finite-grid headroom and
  the surviving `eval_a` action-TV limitation.
- **Allowed claim**: within the registered finite generator, guard-clean joint
  headroom exists in `eval_b/c/d` but not `eval_a`; `k3_116` is common to the
  three passing profiles.
- **Stay-out**: all-profile or arbitrary-controller feasibility; structural
  impossibility in `eval_a`; learned-policy or MARL success; topology, safety,
  stability, or deployment claims.

## Manuscript mapping

- Discussion/limitations: use as a bounded oracle-grid trade-off result showing
  that endpoint-only winner selection hid lower-stress alternatives, while one
  registered profile remains unresolved.
- Keep out of title and headline learned-coordination claims. No training or
  message comparison occurred.

## Follow-up pointers

- Continue the owner-authorized advisory order with M4 measurement sufficiency,
  then M1 and M2. R451/M3 remains invalid and is not pooled with this result.
