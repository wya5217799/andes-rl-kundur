# R465 feed — complete Object-B total log-M/log-D sensitivity

## Identity

- Round/date/claim: `R465`, 2026-08-21, `CLM-1455`.
- Authority: owner-authorized GPT Pro U5 data request. This is a local Object B model/sensitivity round; it does not train a policy or produce nonlinear performance evidence.
- Decision artifact: `results/research_loop/r465_u5_total_sensitivity/checks/verification_report.json` (SHA-256 `7b99bb7ebc344c762bfea68c5659eafd2e01f4d5e4904026ddfa149e9a4daa82`), with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/entry: `memory/rounds/R465/plan.md`, `memory/rounds/R465/formal_seal.json` (SHA-256 `7243787c78a27fbfb05080244b7ab9f869532c39977199c286c3950b4a318f69`), and `scripts/run_r465_u5_total_sensitivity.py`.
- Formal root: `results/research_loop/r465_u5_total_sensitivity/`; 71 files, 15,490,057 bytes, with every `SHA256SUMS` entry independently reverified.

## Frozen setup

- Object: R459 Object B, four 60-Hz VSG frequency outputs, four normalized active-power commands mapped by the frozen 0.072 system-pu upper headroom, and three PQ-load disturbance ports.
- Counterfactuals: common `M_i=200 exp(rho)` or `D_i=100 exp(rho)` over all four VSGs, one family at a time, at `rho=0, ±0.04, ±0.02, ±0.01`; 13 unique ANDES initializations in total.
- Every point exports equilibrium and discrete-mode data, DAE Jacobians and input bridges, descriptor reduction, complete continuous and post-step sampled `A/B/C/D`, state/channel names, and headroom. The same nominal physical gauge complement is used at every point.
- Closed loops: exact full-feedthrough K=3.5 differential ring bandpass candidate and local-PI reference under `u=-K y`. The full frequency grid covers a `1e-8 Hz` DC-limit proxy through the 2.5-Hz Nyquist point; the PI transfer itself is singular at exact DC.
- Derivatives use three centered steps and two Richardson extrapolations. Total transfer derivatives include every `A/B/C/D`, headroom/controller, return-difference, and candidate/reference denominator term. Finite-band output energy covers 0.3--0.5 Hz; finite-window energy covers a 30-step differential-output impulse bank.
- Capacity: 13 unique point workers plus one orchestrator, one native thread per process, followed by 13 shared-memory frequency-response tasks. Formal wall time was 59.75 s. GPU was not selected because the work is CPU/DAE-bound and no measured GPU path exists.

## Observations

- [CLM-1455] All 13 points retained the same active-mode and coordinate-name hashes. Maximum equilibrium residuals remain below `7.24e-9`; common-gauge transfer reduction errors are at most `1.43e-13`, and all reduced spectral radii remain below one (`contracts/parameter_points.json`; `checks/gauge_checks.json`).
- [CLM-1455] For both log-M and log-D, all complete-array Richardson checks pass. The ZOH Fréchet/direct derivative relative discrepancies are at most `2.34e-10`; full closed-loop formula/direct transfer discrepancies are at most `3.13e-5`, and band-energy formula/direct discrepancies are at most `2.16e-6` (`checks/richardson_table.json`; `checks/total_sensitivity_checks.json`).
- [CLM-1455] At the nominal point, the bandpass-to-local-PI differential band-energy ratio is `0.8740574911`. Its complete logarithmic derivative is `0.4464230764` for log-M and `0.05222446250` for log-D (`checks/total_sensitivity_checks.json`).
- [CLM-1455] R449's A-only two-term sums are `0.3382153459` and `0.04105111193`. Relative to the complete derivatives, the omitted-channel residuals are `0.1082077305` for log-M and `0.01117335057` for log-D, respectively 24.24% and 21.39% of the complete values. Thus the preregistered prediction that A-only attribution is insufficient is supported for both parameters (`checks/total_sensitivity_checks.json`).
- [CLM-1455] The 30-step bandpass-to-local-PI energy ratio is `0.9957864563`; its complete direct logarithmic derivatives are `0.01624364446` for log-M and `0.003366236754` for log-D. These are finite-window quantities and are not pooled with the frequency-band derivatives (`checks/total_sensitivity_checks.json`).
- [CLM-1455] Maximum nominal plant resolvent condition is `2.02e6`; maximum return-difference conditions are `1.37` for the bandpass loop and `1.24e5` for local PI. All remain finite and below the registered `1e12` gate (`checks/total_sensitivity_checks.json`).

## Conclusions

- **Finding, trust V — TOTAL-SENSITIVITY-VALID.** On the fixed-mode Object B equilibrium neighborhood, complete log-M and log-D derivatives are numerically identifiable and agree with direct rebuilt counterfactuals within the registered tolerances.
- **Mechanism prediction P1 — SUPPORTED.** R449's A-only split omits material contributions for both parameters and cannot be treated as the complete local causal attribution. The detailed JSON field named `P1_A_only_sufficiency` records the preregistered test result; `SUPPORTED` means the plan's A-only-*insufficiency* prediction is supported.
- **Mechanism prediction P2 — SUPPORTED.** Mode, equilibrium, gauge, convergence, conditioning, ZOH, transfer, energy, and denominator checks all pass, so the smooth local total-derivative branch is applicable.
- The complete derivatives remain coordinate-dependent local sensitivities of a frozen physical counterfactual. They do not identify any individual matrix channel as an invariant or unique failure cause.

## Limits

- This covers common multiplicative M or D perturbations near one Object B equilibrium, one fixed active mode, two frozen controllers, one frequency band, and one 30-step bank. Heterogeneous device perturbations, other equilibria/topologies, large disturbances, mode switches, nonlinear saturation, and learned policies remain untested.
- The frequency-band and finite-window ratios use different operators and cannot be interchanged. Neither is a gain, phase, delay, robust-stability, or nonlinear performance margin.
- The exact-DC PI controller transfer is singular; `1e-8 Hz` is an explicit DC-limit proxy. The reported finite conditioning applies to that frozen grid.
- A/B/C/D decomposition terms change under rho-dependent state coordinates. Only the complete physical-port counterfactual is interpreted.
- No probability, unseen-topology, EMT, HIL, field, safety, or deployment claim follows.

## Publication gate

- **Evidence audit**: PASS — every headline value traces to the sealed point registry or total-sensitivity check; point hashes, full matrices, direct counterfactuals, Richardson tables, frequency arrays, and whole-root hashes are retained.
- **Domain audit**: QUALIFIED — physical parameter mapping, 60-Hz output, headroom, feedback sign, gauge, mode, sample convention, energy operators, and conditioning are explicit. The result is local linear model evidence, not nonlinear stability or controller-success evidence.
- **External context**: NOT-APPLICABLE — the external response supplied algebra and acceptance requirements, not numerical or novelty authority.
- **Claim disposition**: ENTER — register the two complete local derivatives and the material A-only omission, with the finite-band/finite-window separation retained.
- **Allowed claim**: at the sealed fixed-mode Object B equilibrium, the complete band-energy log-ratio derivatives are 0.446423 for common log-M and 0.0522245 for common log-D; direct rebuilds validate them, and A-only attribution omits 24.24% and 21.39% of the respective complete values.
- **Stay-out**: A as the unique cause; any matrix component as a coordinate-invariant cause; nonlinear/global sensitivity; gain/phase/delay/robust margin; learned-policy performance; probability/generalization; safety or deployment.

## Manuscript mapping

- Methods/supplement: parameter grid, full equilibrium-to-DAE-to-ZOH-to-loop derivative chain, fixed gauge complement, full frequency arrays, Richardson/direct checks, and conditioning.
- Results/limitations: replace any R449 A-only causal language with the complete local derivatives and explicit 24.24%/21.39% omitted shares; keep band and 30-step values separate.
- Do not use this local sensitivity as title-level MARL success or universal failure evidence.

## Follow-up pointers

- U6 may reuse the exported continuous/sampled points only as model inputs; its delay realization, pole tracking, and nonlinear branch require a separate seal.
- U7/U8 remain separate local-order and separation-bound rounds. U2 remains the final training factorial.
