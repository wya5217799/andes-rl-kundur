# R403 feed — repaired CD-MATD3 development gate (SCRATCH-INVALID)

## Identity

- Round: R403. Claim identity: CLM-1170. Date: 2026-08-15.
- Runner: `scripts/run_r403_cd_matd3_successor.py`.
- Seal: `memory/rounds/R403/development_seal.json` (sha256
  `d0dd5933c348eddbf3303ec85a0adc32d188f34a394ee7c028c5047de83d8a88`).
- Failure classifier: `memory/rounds/R403/failure_analysis.json` (sha256
  `892ead29337f0449cac40f596c2bab2bcb5b8acd91578a94a93b564867f27637`).

## Frozen setup

- Same-family repaired CD-MATD3: immutable common weight 1.0, normalized
  action-effort weight 1.0, unchanged four-actor message/no-message object.
- One seed and 1200 planned interaction steps per repaired arm, using only the
  four disclosed R402 development profiles; all evaluation profiles excluded.
- One create-only attempt; no retry, tuning, or threshold repair in R403.

## Observations

- O1 (CLM-1170): both arms stopped at 256 interaction steps with no TDS-failed
  episode and no final checkpoint. Their first critic loss was finite, while
  `actor_loss_mean=NaN` was the registered critic-only-update sentinel.
- O2 (CLM-1170): the runner treated every nonfinite diagnostic field as learner
  divergence, so it misclassified the sentinel before the first policy update;
  the deterministic failure classifier returns
  `diagnostic-sentinel-misclassified` and `SCRATCH-INVALID`.

## Conclusions

- C1 (analytic lifecycle classification): R403 tests no algorithm efficacy and
  supplies no directional control result.
- C2 (engineering boundary): R403 cannot be retried. A separately reserved,
  resealed correction successor may replace the ambiguous sentinel with an
  explicit policy-update flag and extend rehearsal through diagnostic
  serialization while leaving the scientific development contract unchanged.

## Limits

- No completed training, policy evaluation, common/differential comparison,
  message attribution, unseen bank, topology-generalization, safety,
  cross-simulator, HIL, or deployment evidence.
- The normal short physical prefixes are retained only to diagnose execution;
  they carry no scientific or manuscript-result weight.

## Publication gate

- **Evidence audit**: PASS — the seal fixes the intended contract; the hashed
  failure classifier binds both retained arm manifests to the sentinel-only
  invalidity, and no directional estimate is consumed.
- **Domain audit**: PASS — physical prefixes have no TDS failure but remain
  invalid execution diagnostics; no frequency, actuation, decoupling,
  coordination, stability, or safety statement is admitted.
- **External context**: NOT-APPLICABLE — instrumentation-only invalidity.
- **Claim disposition**: ENTER — CLM-1170 registers only the invalid attempt
  and the bounded correction seam.
- **Allowed claim**: the finalized CLM-1170 statement.
- **Stay-out**: every CD-MATD3 efficacy, decoupling, coordination, common-mode,
  action-stress, robustness, generalization, safety, or deployment conclusion.

## Manuscript mapping

- O1 and O2 -> route-disposition appendix only; stay out of title-supporting
  Results, figures, abstract, contributions, and conclusion.

## Follow-up pointers

- A correction successor is unauthorized until R403 is closed and a new round
  prospectively freezes the unchanged development contract and deeper rehearsal.
