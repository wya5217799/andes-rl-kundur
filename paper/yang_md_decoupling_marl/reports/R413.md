# R413 feed — soft-spot A2: topology-variant robustness (10/10 sound variants pass)

## Identity

- Round: `R413`; reserved claim: `CLM-1225`; date: `2026-08-17`.
- Authority: soft-spot program A2
  (`paper/yang_md_decoupling_marl/working/soft_spot_experiment_program.md`),
  owner decision
  (`paper/yang_md_decoupling_marl/working/route_owner_decision_soft_spot_program_2026-08-16.md`),
  creative mode. Successor of the aborted R412 run (R412 abort: sealed
  runner crashed on init-divergent variants; this round re-runs the
  identical frozen protocol with the graceful-failure fix and a
  failure-path rehearsal; see R412 plan abort_reason and the gate
  calibration log).
- Runner: `scripts/run_r413_topology_robustness.py`; shared shard driver
  `scripts/soft_spot_shard_driver.py`; harness imports from the frozen
  R408/R372 runners and `gate_b3_deterministic.py` (all read-only).
- Seal `memory/rounds/R413/formal_seal.json` (sha256
  `ee4af6819bcea6b4ba3970e826dd204cea6ebb91bb2268aa650079172159ed2b`).
- Result root: `results/research_loop/r413_topology_robustness/`;
  `formal_analysis.json` (sha256
  `bc94e1fe35ed77e4a45aa189f3158a3c0c3178ebdad763cf8c340f963ca7fb9e`).
- Archive status: `LOCAL-ONLY`; no private second-copy locator registered.

## Frozen setup

- Frozen variant bank N=12: nominal + six line outages (inter-area
  corridors 7-8 and 8-9, plus the two VSG tie lines 7-12 and 9-15) + five
  tie-reactance scalings (x0.5/x1.5 on Line_4 and Line_7, x1.5 on
  Line_7_12); one topology factor per variant; Line_2 excluded (R305
  positive-mode precedent). Outages only through `apply_line_outage()`;
  impedance only through ANDES `Line.set`.
- Per variant: the CLM-0665 EIG hard gate first (TDS.test_ok, exit_code=0,
  init residuals, finite spectrum, positive-real guard, every value
  recorded; init failures are recorded as gate failures, never crashes),
  then the frozen R408 disclosed development bank (8 paired probes + 2
  disturbances per arm) for zero/local/bandpass_k3p5.
- Endpoints: frozen R409 thresholds r_d <= 0.95, r_cross <= 1.10 (strict
  0.95 recorded), all R379 guards, candidate-versus-local on the same
  variant.
- Nominal variant = base-case anchor: r_d/r_cross must reproduce R408
  (0.938947 / 0.539791) within 1e-6 relative.
- Budget: pre-seal capacity ladder (rungs 1/2/4/8/12/16, 32 representative
  tasks per rung) selected rung 8; sealed budget 9 processes (8 workers +
  driver launcher), native threads 1, other reserved 0
  (`memory/rounds/R413/capacity_evidence.json`).

## Observations

- **O-001 (CLM-1225) — Base-case anchor reproduced.** The nominal variant
  returns r_d = 0.9389 and r_cross = 0.5398 with relative deviations
  2.2e-7 and 6.4e-7 from the R408 values, inside the 1e-6 tolerance
  (`formal_analysis.json#/base_anchor_deviations`,
  `#/base_anchor_verdict`).
- **O-002 (CLM-1225) — EIG gate: 10 of 12 variants sound.** All five
  inter-area outages, all five impedance scalings, and the nominal variant
  pass the full CLM-0665 gate (PFlow converged, TDS.test_ok, exit 0,
  residuals, finite spectrum, zero positive-real modes). The two VSG-tie
  outages (Line_7_12, Line_9_15) diverge at initialization (NaN spectrum,
  recorded LinAlgError) and are recorded as gate failures
  (`formal_analysis.json#/variants` `/eig_gate`,
  `#/eig_passing_variants`).
- **O-003 (CLM-1225) — 10/10 sound variants pass the endpoints.** Every
  EIG-passing variant satisfies r_d <= 0.95 (range 0.9296-0.9398) and
  r_cross <= 1.10 (range 0.4530-0.6889, strict <= 0.95 also passing on
  all), with candidate and reference guards passing everywhere
  (`formal_analysis.json#/variants` `/summary`,
  `#/endpoint_passing_variants`).
- **O-004 (CLM-1225) — The two unsound variants stay out of the pass
  set.** Their endpoint rows are reported as fails with their EIG
  failures, per the pre-registered rule that only EIG-passing variants
  count toward the robustness set
  (`formal_analysis.json#/variants/out_Line_7_12`,
  `#/variants/out_Line_9_15`).

## Conclusions

- **Finding, trust V.** The frozen K=3.5 bandpass controller passes the
  frozen thresholds and every guard on all 10 EIG-sound topology variants
  of the frozen bank (5 single-line inter-area outages + 5 tie-reactance
  scalings + nominal), with r_d in [0.9296, 0.9398] and r_cross in
  [0.4530, 0.6889]; the disclosed-bank result is therefore not an
  artifact of the nominal topology.
- **Finding, trust V.** Opening either VSG tie line makes the modified
  Kundur equilibrium unsound (divergent initialization), which is
  recorded as an EIG hard-gate failure rather than a controller failure;
  these variants are excluded from the robustness set.
- **Finding, trust V.** The nominal base case reproduces the R408
  endpoints within 7e-7 relative, certifying harness and environment
  identity for the whole variant table.
- **Evidence boundary.** One modified-Kundur topology family (12 frozen
  variants), the frozen disclosed development bank, the frozen thresholds
  and guard family, and the frozen K=3.5 controller only; no unseen-bank
  claim, no larger-grid claim, no stability-certificate claim.

## Limits

- The robustness set covers single-line outages and single-line reactance
  scalings of the frozen corridors only; combined (N-2) or other-line
  variants are untested.
- The two VSG-tie outage variants are excluded as unsound equilibria, not
  counted as controller failures.
- The bank is the disclosed development bank; R409 already consumed the
  registered one-use held-out bank, so this round adds topology diversity,
  not new held-out conditions.
- One simulator, one controller structure, one candidate gain: no tuning
  occurred and none is implied by the table.

## Publication gate

- **Evidence audit**: PASS - every observation binds to the hashed
  `formal_analysis.json` and the per-variant hashed gate/record files;
  the anchor deviations, gate values, endpoint ratios, and pass/fail flags
  are machine-readable; the R412 abort and the fix are documented in the
  round ledger and the calibration log.
- **Domain audit**: PASS - the EIG hard gate (CLM-0665) precedes every
  endpoint claim; unsound equilibria are separated from controller
  outcomes; endpoints stay physical 60-Hz quantities with the frozen
  thresholds; no stability certificate or topology-generalization claim is
  made.
- **External context**: NOT-APPLICABLE - bounded internal robustness
  evidence; no novelty claim, so no literature re-check is triggered.
- **Claim disposition**: ENTER - register the variant table and the
  anchor.
- **Allowed claim**: on the frozen 12-variant topology bank of the
  modified Kundur, the K=3.5 bandpass passes the frozen thresholds and all
  guards on all 10 EIG-sound variants (r_d in [0.9296, 0.9398], r_cross in
  [0.4530, 0.6889]); the two VSG-tie outage variants fail the EIG hard
  gate (divergent initialization) and are excluded; the nominal variant
  reproduces the R408 endpoints within 7e-7 relative.
- **Stay-out**: robustness beyond the frozen variant set, N-2 or combined
  variants, larger grids, unseen banks, stability certificates, and any
  claim that the excluded variants reflect on the controller.

## Manuscript mapping

- Section 5.3 (constructive energy-port result): add one sentence + the
  variant table row count that the candidate passes all sound topology
  variants of the frozen bank (10/10) and that the two unsound variants
  are excluded by the EIG hard gate.
- Methods / evidence validity: the base-case anchor certifies harness
  identity between R408 and R413 executions.

## Follow-up pointers

- Program item A4 (extra unseen banks) is the next round; A3 (law-family
  expansion) follows if time remains. No new question is opened.
