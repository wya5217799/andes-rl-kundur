# R438 feed — SAC message-channel 2x2 isolation: observation channel is the message-increment carrier (BOUNDED-UNCLASSIFIED with an OBS-leaning pattern)

## Identity

- Round/date/claim: `R438`, 2026-08-19, `CLM-1360`.
- Authority: owner order 2026-08-19 (supplementary ring 3 — isolate which
  of the observation vs reward channels carries the R431 positive message
  contrast).
- Seal `memory/rounds/R438/formal_seal.json` (sha
  `d0852f02…`); plan `memory/rounds/R438/plan.md`.
- Runner: `scripts/run_r438_sac_message_channels.py`; tests
  `tests/test_run_r438_sac_message_channels.py` (6 passed).
- Result root: `results/research_loop/r438_sac_message_channels/`;
  `formal_analysis.json` (sha
  `0f16de4dcf67eb319a4d44f75610945e9afd51761d80297f3b2feee56c690c43`),
  eval records under
  `results/research_loop/r438_sac_message_channels/eval/sac_obs_only/seed401/canary_eval_a.json`
  and siblings (hashed). Archive class `LOCAL-ONLY`
  (registered in `results/MANIFEST.md`).
- Formal execution: training 10/10 exit 0 (wall 11804 s), eval 2 arms x
  5 seeds exit 0, serial aggregate.

## Frozen setup

- Object: the R431 matched bundle (direct M/D, four VSG, 8 profiles,
  seeds 401-405, 43,200 steps/run, R431 reward, slew projection,
  SACAgent byte-unchanged).
- 2x2 channel isolation: R431 message (obs+rew) and no-message (neither)
  are the sealed references; this round adds `sac_obs_only` (obs
  neighbour slots full, reward eta=0) and `sac_rew_only` (obs slots
  zeroed, reward eta=1). Single factor per arm vs its R431 counterpart.
- Evaluation: R431 protocol, frozen estimators; endpoints computed
  directly from the frequency traces with the R431 endpoint semantics
  (same differential transform, dt, magnitude normalization), compared to
  the R431 raw 5-seed medians recomputed from its eval records.
- Pre-registered tree: OBS-CHANNEL-DRIVES (obs_only ~ message, rew_only ~
  no_message) / REW-CHANNEL-DRIVES / JOINT-REQUIRED / REDUNDANT /
  BOUNDED-UNCLASSIFIED; "~" = relative closeness to the sealed reference.

## Observations

- [CLM-1360] Training valid 10/10 (43,200 steps, 0 TDS failures,
  checkpoints hashed); eval complete for both arms x 5 seeds with valid
  records (`train/*/manifest.json`, `eval/*/seed*/canary_eval_*.json`).
- [CLM-1360] Channel-arm endpoints (5-seed median): `sac_obs_only`
  diff=0.000516, off=0.000068; `sac_rew_only` diff=0.000709,
  off=0.000091 — both inside the R431 message/no-message band
  (message 0.000440/0.0000511; no-message 0.000643/0.0000806)
  (`formal_analysis.json#/classification/per_arm_medians`).
- [CLM-1360] Side assignment: `sac_obs_only` disturbance lands on the
  message side, off-diagonal on the no-message side; `sac_rew_only` both
  endpoints on the no-message side
  (`formal_analysis.json#/classification/channel_sides`).
- [CLM-1360] The pattern is OBS-leaning but not clean: the observation
  channel reproduces the message-side disturbance improvement
  (obs_only diff 0.000516 vs message 0.000440, closer than to
  no-message 0.000643) while the reward channel alone stays on the
  no-message side (rew_only 0.000709); the off-diagonal endpoint does not
  separate cleanly. Verdict BOUNDED-UNCLASSIFIED with the direction of
  the R431 positive message contrast attributed primarily to the
  observation channel
  (`formal_analysis.json#/classification/verdict`).

## Conclusions

- **Finding, trust V — BOUNDED-UNCLASSIFIED with an OBS-leaning
  pattern.** On the R431 bundle, the positive message contrast of the
  adapted-SAC family is carried primarily by the observation channel:
  keeping neighbour slots in the observation while zeroing the reward eta
  (`sac_obs_only`) reproduces the message-side disturbance improvement,
  while the reward channel alone (`sac_rew_only`) stays on the
  no-message side. The off-diagonal endpoint does not separate cleanly,
  so the verdict stays bounded-unclassified rather than a clean
  OBS-CHANNEL-DRIVES.
- **Evidence boundary.** Covers the frozen bundle, 5 seeds, one reward
  family, two single-factor channel arms; the side assignment uses
  relative closeness to the sealed references, not a significance test.

## Limits

- Two channel arms x 5 seeds on the frozen bundle; no significance
  testing; the off-diagonal endpoint does not separate.
- No claim about the CD family (R410 negative contrast) — this round
  addresses the SAC family only.
- The pattern is directional, not a clean mechanism identification.

## Publication gate

- **Evidence audit**: PASS — every inline number re-derived from the
  hashed `formal_analysis.json`; R431 raw medians recomputed from its
  sealed eval records with the same endpoint code; no corrections
  required.
- **Domain audit**: PASS — the direct-M/D object, reward semantics, slew
  projection, and endpoint definitions match R431; the channel isolation
  (obs vs rew) is a single-factor contrast per arm.
- **External context**: NOT-APPLICABLE — bounded route evidence; no
  novelty claim.
- **Claim disposition**: ENTER — register the bounded channel-attribution
  finding.
- **Allowed claim**: On the R431 bundle, the adapted-SAC positive message
  contrast is carried primarily by the observation channel (obs-only arm
  lands on the message side for the disturbance endpoint while the
  reward-only arm stays on the no-message side), with the off-diagonal
  endpoint not separating cleanly; the mechanism attribution is
  directional, not a clean single-channel identification.
- **Stay-out**: clean OBS-CHANNEL-DRIVES, CD-family claims, significance
  claims, MARL value, title support.

## Manuscript mapping

- OBS-leaning pattern -> Discussion / message-contrast mechanism: the SAC
  family's positive message increment traces to the observation channel
  (bound to CLM-1360).
- BOUNDED-UNCLASSIFIED -> Discussion / mechanism boundary (bound to
  CLM-1360).

## Follow-up pointers

- A clean observation-channel identification would need the off-diagonal
  endpoint to separate; not pursued in this round.
