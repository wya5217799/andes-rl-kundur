# R460 feed — executed-action Bellman semantics and complete trace bank

## Identity

- Round/date/claim: `R460`, 2026-08-21, `CLM-1440`.
- Authority: owner-authorized GPT Pro U3 prerequisite. This round repairs and verifies execution/replay/target semantics before any U2 training; it does not train or rank a policy.
- Decision artifact: `results/research_loop/r460_u3_execution_semantics/checks/verification_report.json` (SHA-256 `2a51f1377b75848f7fef9971c46f0d3bc2a42f10c4fa93bc73643c483a330d52`), with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/entry: `memory/rounds/R460/plan.md`, `memory/rounds/R460/formal_seal.json` (SHA-256 `3236cd831d62a120f61c07735b02b2f9676973e7e0457e2c42b818851dc2d868`), and `scripts/run_r460_u3_execution_semantics.py`.
- Formal bank: 24 unique R431 evaluation trajectories, 30 steps each, 720 retained joint transitions, plus JSONL and compressed numeric mirrors.

## Frozen setup

- Each per-VSG Markov state is `z_t=[obs_t, previous_executed_action_t]`. The actor emits a raw tanh action; amplitude and stateful slew projection produce the executed normalized action; Object A then maps that action to physical M/D.
- Replay preserves observation, previous executed action, raw action, executed action, reward, next observation, and terminal flag. The replay critic consumes executed action. The actor-loss critic consumes the differentiably projected current action. The target projector is centered on the current executed action, and the target critic consumes that projected target.
- Entropy semantics are frozen as `raw_policy_entropy_regularizer`. No executed-action density or physical-exploration entropy is claimed.
- The complete physical bank uses one fixed, untrained, seed-`460031` successor-policy initialization solely to exercise the execution semantics. It is not a policy-performance experiment.
- Capacity was measured prospectively at 1, 8, and 15 workers. Formal allocation was 15 trajectory workers plus one orchestrator, one native thread per worker, in waves of 15 and 9; the GPU was not selected because the job is CPU/DAE-bound.

## Observations

- [CLM-1440] The independent checker classifies `EXECUTION-SEMANTICS-VALID`: all 24 trajectories completed, all 720 transitions were retained, and no TDS failure occurred (`verification_report.json#/verification`).
- [CLM-1440] NumPy versus differentiable Torch projector error is `5.960464477539063e-08 <= 1e-7`; runtime-versus-functional multi-step reconstruction, replay continuity, current/actor/target critic action identities, and physical M/D mapping each have maximum absolute error `0` (`semantic_tests.json`; `verification_report.json#/verification`).
- [CLM-1440] The deterministic four-step toy bank has hand-return versus recursive TD-target maximum error `0 <= 1e-6`. Repeating the same complete `(obs, previous executed action, raw action, exogenous seed)` transition is exact, while deleting previous executed action produces a positive two-valued next-state gap, demonstrating the required actuator-state aliasing counterexample (`semantic_tests.json`).
- [CLM-1440] The R431 result root contains 454 files, but exhaustive filename/type inventory and checkpoint-key inspection found no replay/buffer artifact. The only admissible historical status is therefore `historical_bias_not_reconstructible`, reason `original replay transitions unavailable`; no exact historical training-bias number is reported (`r431_replay_inventory.json`).
- [CLM-1440] A clearly labelled retrospective one-step diagnostic applies the frozen R431 seed-401 message checkpoint to 128 transitions from the new state bank. Historical raw-versus-projected current-action L2 gap has mean `1.088688`, median `1.069181`, and maximum `1.548619`; 512 per-agent target-action gaps have mean `0.266881` and maximum `0.730757`. Projected-minus-raw target-Q difference has mean `-0.012942`, standard deviation `0.052704`, minimum `-0.230419`, and maximum `0.352872`. These are new-bank diagnostics, not historical replay-bias estimates (`r431_retrospective_diagnostic.json`).
- [CLM-1440] The formal 24-job run used 15 workers and finished in 23.296 seconds with zero worker failures. The formal root contains 125 files and 26,724,019 bytes; 122 declared content hashes verify with zero failures.

## Conclusions

- **Finding, trust V — EXECUTION-SEMANTICS-VALID.** The successor SAC seam makes previous executed action explicit in state/replay and routes actual projected actions through replay-current, actor-loss, and target critics. This semantic gate is admissible for later separately preregistered U2 training.
- **Historical disposition.** R431's exact historical Bellman bias cannot be reconstructed because original replay transitions are absent. The retrospective distribution is informative only for the named checkpoint on the new frozen state bank.
- **Training disposition.** No training occurred. This round opens only the semantic prerequisite; it does not establish convergence, return improvement, message value, constraint satisfaction, or physical superiority.

## Limits

- The fixed successor-policy initialization is untrained. Completion and zero TDS failure show that the trace path executed, not that the policy is useful.
- Projector parity, Markov-state tests, and toy Bellman identities establish code-bound consistency under the declared actuator. They do not prove general SAC convergence or stability of the nonlinear power system.
- The full bank is the finite R431 evaluation bank at the frozen four profiles and 24 signed/localized scenarios. No population, probability, unseen topology, EMT, HIL, or field generalization follows.
- The raw-policy entropy regularizer is not an executed-action density. Any future claim about entropy after clipping/slew requires a separate feasible-interval parameterization and density derivation.
- R431 historical replay is unavailable. No later document may relabel the retrospective checkpoint diagnostic as an exact historical target or bias reconstruction.

## Publication gate

- **Evidence audit**: PASS — every claim maps to the formal verifier, semantic test bundle, raw transition/target traces, or the separately labelled retrospective diagnostic; all hashes pass and missing replay remains explicit.
- **Domain audit**: QUALIFIED — the action map, physical M/D commands, 60-Hz frequency outputs, 0.2-s steps, TDS validity, actuator state, and finite-bank boundary are explicit. The result is execution-semantics evidence, not controller-performance or stability evidence.
- **External context**: NOT-APPLICABLE — no novelty, literature, or external empirical claim is made.
- **Claim disposition**: ENTER — register the corrected execution/replay/target semantics as a prerequisite for later training and register historical replay non-reconstructibility as a hard limit.
- **Allowed claim**: under the sealed Object A/R431 finite-bank runtime, the successor path explicitly includes previous executed action and independently reproduces all executed-action replay and critic identities within the preregistered tolerances.
- **Stay-out**: exact historical R431 Bellman bias; executed-action entropy; SAC convergence; learned message value; guard satisfaction; transient improvement; stability, safety, robustness, topology-generalization, HIL, EMT, field, or deployment claims.

## Manuscript mapping

- Methods/Reproducibility supplement: cite augmented state, projector definitions, replay schema, three critic-action paths, entropy label, hardware allocation, raw transition/target schemas, and hashes.
- Limitations: state that R431 replay was not archived and exact historical training bias is not reconstructible.
- Results: do not present the untrained diagnostic bank as controller performance. Later U2 cells may cite R460 only for their semantic implementation prerequisite.

## Follow-up pointers

- U4 must independently recompute physical guards from the R460 raw trajectory schema in its own round.
- U2 remains blocked until U4 also passes; its 18 cells must instantiate this successor executed-action seam without changing the frozen semantics.

