# R440 feed — energy-port robustness expansion (N-2 outages + controller delay): BOUNDED-FAILURE

## Identity

- Round/date/claim: `R440`, 2026-08-20, `CLM-1350`.
- Authority: owner order 2026-08-19 (supplementary ring 6 — robustness
  expansion of the positive bandpass K=3.5 result; manuscript_evidence_map
  next paths #2/#3).
- Seal `memory/rounds/R440/formal_seal.json` (sha
  `2a7fa1562de19c73885ead253484ae33d4b481d7e5e26b71ef357fe471691694`);
  plan `memory/rounds/R440/plan.md` (sha recorded in the contract).
- Execution amendment `memory/rounds/R440/execution_amendment_20260820.md`
  (three-layer runner fix in the summarize call path; first shard wave
  9/10 crashed, the `n2_out_Line_7_Line_8` output from the first wave is
  retained and valid; all other shards re-ran under the amended runner —
  rerun driver sha `8b0015a1…ea857d`).
- Runner: `scripts/run_r440_robustness_expansion.py` (imports the R413
  chain); result root `results/research_loop/r440_robustness_expansion/`,
  `formal_analysis.json` (sha
  `7cd23688fe9cb7f16c721d69728f92c7ba7456e41edbc9d2a19f6d9cfc226758`),
  per-shard summaries under `n2/*.json` and `delay/*.json` (hashed
  sidecars). Archive class `LOCAL-ONLY` (registered in
  `results/MANIFEST.md`).
- Formal execution: 10/10 shard outputs exit 0 (8 N-2 + 2 delay), serial
  aggregate.

## Frozen setup

- Object: the R408/R409 feasibility-native energy-port object (four VSGs,
  buses 12/16/14/15), bandpass K=3.5 frozen structure, mapped through the
  feasibility-native map.
- N-2 axis: 8 frozen two-line outage variants applied through
  `apply_line_outage()`; each variant passes the CLM-0665 EIG hard gate
  (PFlow converged -> TDS init -> EIG run -> finite-spectrum /
  positive-real guard) before its block is judged.
- Delay axis: frozen 1-step (0.2 s) and 2-step (0.4 s) controller-output
  delay on the nominal topology (warm-up steps command zero).
- Every block: `zero_feedback` / `local_feasibility_native` /
  `bandpass_k3p5` arms under identical conditions (8 paired probes + 2
  disturbances), candidate-versus-local ratios, frozen R409 thresholds
  (r_d <= 0.95, r_cross <= 1.10) and all R379 guards.
- Anchor: line-level nominal anchor reproduction is sealed in R413
  (`BASE-ANCHOR-REPRODUCED`, deviations 6.4e-7 / 2.2e-7) and re-verified
  today in R436 (2.2e-7, CLM-1345); this round's blocks cover the two new
  axes (no zero-delay nominal block in the frozen design).

## Observations

- [CLM-1350] 7/8 N-2 variants are EIG-sound and pass: r_d
  0.934614–0.937562 (all <= 0.95) and r_cross 0.562723–0.798922 (all
  <= 1.10) with every guard passing (`formal_analysis.json#/classification/per_n2`).
- [CLM-1350] `n2_out_Line_7_Line_8` (8–9 corridor both circuits out)
  fails to converge power flow after 27 iterations — recorded as a real
  scientific boundary, not judged (`…/per_n2/n2_out_Line_7_Line_8`).
- [CLM-1350] Delay axis fails at both tiers: 1-step delay r_d =
  0.950279 (> 0.95; +0.03% over the threshold) with r_cross = 0.605533
  and guards passing; 2-step delay r_d = 0.989327 with r_cross =
  0.640519 and guards passing (`…/classification/per_delay`).
- [CLM-1350] Verdict BOUNDED-FAILURE: the positive result's robustness
  envelope extends to the frozen N-2 outage list (7 sound variants all
  pass) but does not survive controller output delay >= 1 step
  (`…/classification/verdict`).

## Conclusions

- **Finding, trust V — BOUNDED-FAILURE.** On the frozen energy-port
  object, the bandpass K=3.5 controller passes every EIG-sound N-2
  outage variant (r_d and r_cross inside the R409 thresholds with all
  guards), but a single 0.2 s controller-output delay already pushes
  r_d to 0.9503 — the delay axis is the recorded boundary of the
  positive result. No retune was attempted (pre-registered).
- **Evidence boundary.** Covers the frozen 8-variant N-2 list and 2
  delay tiers on this object; 7 sound N-2 variants judged; the
  non-convergent corridor variant recorded; delay failures are
  boundaries, not impossibility claims.

## Limits

- Eight frozen N-2 variants of one topology family; two delay tiers;
  one deterministic controller (bandpass K=3.5); one object.
- No claim about other corridors, other delay magnitudes, or
  delay-compensation designs.
- The anchor reproduction is cited from the sealed R413/R436 evidence
  (same object, same chain); this round did not re-run a zero-delay
  nominal block.

## Publication gate

- **Evidence audit**: PASS — every inline number re-derived from the
  hashed `formal_analysis.json` and per-shard hashed summaries; sidecars
  verified; no corrections required.
- **Domain audit**: PASS — N-2 outages applied through
  `apply_line_outage()` with the CLM-0665 EIG hard gate per variant;
  delay semantics (warm-up zero command, FIFO) match the frozen
  contract; thresholds and guards are the R409/R379 sets.
- **External context**: NOT-APPLICABLE — bounded route evidence; no
  novelty claim.
- **Claim disposition**: ENTER — register the bounded failure.
- **Allowed claim**: On the frozen energy-port object, the bandpass
  K=3.5 controller passes all seven EIG-sound N-2 outage variants
  (r_d <= 0.95, r_cross <= 1.10, all guards) but fails the R409 r_d
  threshold under both frozen controller-delay tiers (0.9503 at 0.2 s,
  0.9893 at 0.4 s), so the robustness envelope is N-2-outage-sound and
  delay-bounded; one N-2 variant is recorded as power-flow
  non-convergent.
- **Stay-out**: delay compensation claims, other corridors, retuned
  controllers, universal robustness.

## Manuscript mapping

- N-2 pass list -> Results / robustness envelope of the positive result
  (bound to CLM-1350).
- Delay-axis boundary -> Results / recorded failure boundary of the
  positive result (bound to CLM-1350).
- Non-convergent corridor variant -> Methods or Results / recorded,
  unjudged EIG boundary (bound to CLM-1350).

## Follow-up pointers

- Delay sensitivity of the energy-port bandpass controller is now a
  measured boundary; any delay-compensation design requires a new
  owner-authorized round with a new contract.
