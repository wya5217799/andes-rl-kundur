# R439 feed — time-varying action oracle on the R416 evaluation profiles: TIMEVARYING-HEADROOM

## Identity

- Round/date/claim: `R439`, 2026-08-19, `CLM-1355`.
- Authority: owner order 2026-08-19 (supplementary ring 5 — extend RQ2's
  no-headroom result from the static law family to a bounded time-varying
  family).
- Seal `memory/rounds/R439/formal_seal.json` (contract sha
  `2e8f9374…`); plan `memory/rounds/R439/plan.md`.
- Runner: `scripts/run_r439_timevarying_oracle.py`; tests none (probe
  style, WSL rehearsal passed).
- Result root: `results/research_loop/r439_timevarying_oracle/`;
  `formal_analysis.json` (sha `db7cd42422c5dbd30ef15d703eb4c31ad9f74a290f9ca7c42fbb4a1494cddfe5`),
  per-profile summaries under
  `results/research_loop/r439_timevarying_oracle/profiles/eval_a.json`
  and siblings (each hashed with .sha256 sidecars). Archive class
  `LOCAL-ONLY` (registered in `results/MANIFEST.md`).
- Formal execution: 4 profile shards exit 0 (wall 8146 s, 4 workers),
  serial aggregate.

## Frozen setup

- Object: the R416 direct-M/D object (four VSG, evaluation profiles
  eval_a..d, seed 399, 0.2 s x 30 steps, frozen estimators).
- Static reference: the R416 development-selected law
  `local_neighbour_md_km3_kd2` (sealed values cited, never re-run).
- Time-varying oracle: the 30-step window split into K segments
  (K in {2, 3, 5}), each segment holding a constant gain pair from the
  R416 grid (inertia {0.5, 1.0, 1.5, 2.0, 3.0} x damping
  {0.5, 1.0, 1.5, 2.0}); K=2 and K=3 exhaustive (25 and 125 plans),
  K=5 random 200 samples (pre-registered cap); outcome-seeing per-profile
  selection; 350 candidates tested per profile.
- Decision (pre-registered): TIMEVARYING-HEADROOM if the oracle improves
  either endpoint >5% over the static selected law with the other
  endpoint not degraded (r_cross <= 1.10x); else NO-TIMEVARYING-HEADROOM.

## Observations

- [CLM-1355] All 4 profiles complete with 350 candidates each (1400
  trajectories total), all records valid with no TDS failures
  (`formal_analysis.json#/classification/per_profile/*/candidates_tested`).
- [CLM-1355] TIMEVARYING-HEADROOM on all 4 profiles: the best time-varying
  plan improves r_d over the static selected law by 9.3-14.4% (eval_a
  0.000279 -> 0.000241, eval_b 0.000903 -> 0.000819, eval_c 0.000289 ->
  0.000248, eval_d 0.001318 -> 0.001239) with r_cross improving or
  staying inside the ceiling (all best plans keep r_cross <= 1.10x static)
  (`formal_analysis.json#/classification/per_profile/*/best_tv_r_d`).
- [CLM-1355] Best segment counts are K=2 (eval_b) and K=3 (eval_a/c/d);
  no K=5 plan wins — the gain lies in the coarse 2-3 segment schedules
  (`formal_analysis.json#/classification/per_profile/*/best_k`).

## Conclusions

- **Finding, trust V — TIMEVARYING-HEADROOM.** On the frozen R416
  evaluation profiles, a bounded outcome-seeing time-varying gain oracle
  (K in {2,3,5}, R416 grid) improves the disturbance differential energy
  by 9-14% over the static development-selected law on all 4 profiles,
  with cross-response not degraded. RQ2's no-headroom conclusion holds
  for the static family only; a bounded time-varying family shows
  measured headroom.
- **Evidence boundary.** Outcome-seeing oracle (not a causal real-time
  controller); 350 candidates per profile over one grid; the improvement
  is a bounded existence statement on the frozen bank, not a deployable
  control result.

## Limits

- Covers the 4 evaluation profiles, one gain grid, K in {2,3,5}, one
  static reference law; K=5 sampled (200) not exhaustive.
- No claim about real-time schedulability, stability certification, or
  generalization beyond the frozen bank.
- The oracle selects per profile independently (no single universal
  schedule claimed).

## Publication gate

- **Evidence audit**: PASS — every inline number re-derived from the
  hashed `formal_analysis.json`; per-profile candidate counts and ratios
  verified; no corrections required.
- **Domain audit**: PASS — the direct-M/D object, gain grid, profile
  bank, and endpoint definitions match R416; the oracle semantics
  (outcome-seeing, per-profile) are declared and bounded.
- **External context**: NOT-APPLICABLE — bounded route evidence; no
  novelty claim.
- **Claim disposition**: ENTER — register the bounded time-varying
  headroom finding.
- **Allowed claim**: On the frozen R416 evaluation profiles, a bounded
  outcome-seeing time-varying gain oracle (2-3 segments) improves the
  disturbance differential endpoint by 9-14% over the static
  development-selected law on all 4 profiles with cross-response not
  degraded, so the no-headroom conclusion extends only to the static
  family.
- **Stay-out**: deployable control, stability certification, universal
  schedule, MARL value, title support.

## Manuscript mapping

- TIMEVARYING-HEADROOM -> Results / RQ2 boundary refinement: the
  static-family no-headroom claim gains a measured time-varying
  counterpart (bound to CLM-1355).
- K=2/3 wins -> Discussion / coarse schedules capture the headroom
  (bound to CLM-1355).

## Follow-up pointers

- Whether a causal real-time controller can harvest this headroom is
  untested and out of scope; the bounded existence statement stands on
  the frozen bank.
