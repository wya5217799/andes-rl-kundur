# R409 feed — held-out gate for the R408 Q-ENTRY candidate (bandpass K=3.5)

## Identity

- Round/date/claim: `R409`, 2026-08-15, `CLM-1210`.
- Authority: `memory/rounds/R408/plan.md#判定树` (pre-registered next gate for
  the found arm) + owner mission direction 2026-08-15.
- Decision artifacts:
  `results/research_loop/r409_heldout_gate/formal_analysis.json`
  (sha256 `700c77970fdc314414f3d720bc0b20a273d96080937fc0144eb3fcbdcb31e546`),
  `formal_execution.json`
  (sha256 `39a94502a6ebadc007dc2dba443200c7f209818a3ddfae6cf5cb48f8bcdf564e`),
  `formal_attempt.json`
  (sha256 `634926fd8153e5a05e98d57b2393fc2b74434ab327dc104713e7fd1f97c2a4a5`),
  `formal_manifest.json`
  (sha256 `1e82648c961e98bcb639bacae067ed43e01cfe9cd71d5c7d3efee8a4dc3b6312`),
  each with a `.sha256` sidecar; archive class `LOCAL-ONLY`.

## Frozen setup

- The R379 evaluation (held-out) bank, never executed before this round:
  probe condition eval3_probe_pq0_minus_0p40, disturbance conditions
  eval3_disturbance_pq0_plus_0p60 and eval3_disturbance_bus15_plus_0p55,
  seed 42, 0.2 s x 50 steps, 8 paired probes + 2 disturbances per arm,
  three arms (zero_feedback / local_feasibility_native / bandpass_k3p5),
  30 records total.
- Candidate: the frozen 0.4 Hz ring-edge bandpass at K=3.5 (zeta 0.35,
  bilinear with 0.4 Hz gain correction), clip +/-0.70 through the
  feasibility-native map - byte-identical structure to the R408 Q-ENTRY arm.
- Thresholds: differential ratio <= 0.95, probe-cross ratio <= 1.10, all
  R379 guards; strict cross gate <= 0.95 recorded additionally.  No
  training; no retry; no tuning; no algorithm change.

## Observations

- [CLM-1210] All 30 held-out records complete 50 steps with zero TDS
  failures and zero guard errors; the candidate arm passes every guard.
  Source: `formal_execution.json#/arms`.
- [CLM-1210] HELDOUT-PASS: on the unseen evaluation bank, bandpass_k3p5
  reaches differential ratio `0.9382180713649944` (<= 0.95) and probe-cross
  ratio `0.7937304481638234` (<= 1.10, strict <= 0.95 also passing).  The
  differential endpoint transfers almost exactly from the disclosed bank
  (R408 dev: 0.938947); the cross endpoint stays comfortably inside the
  ceiling (R408 dev: 0.539791).
  Source: `formal_analysis.json#/classification` and `#/differential_ratio`
  and `#/probe_cross_ratio`.

## Conclusions

- **Primary judgment — HELDOUT-PASS**: the R408 constructive candidate
  (frozen single-family bandpass, K=3.5) generalizes to the unseen
  evaluation bank with both frozen endpoints and every guard passing.  The
  candidate is now eligible for title-supporting use subject to the line's
  title policy; the V2 problem set P0' receives a fully verified
  constructive answer (dev + held-out).
- **Authority boundary**: no training, no retry, no algorithm replacement;
  the held-out bank is used once and is now consumed; R404 values remain
  outside title-supporting results.

## Limits

- One topology, one held-out bank (3 conditions x 3 arms), one candidate
  arm; the result does not generalize beyond this object and bank by
  itself, and no new controller family is claimed.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| HELDOUT-PASS on the unseen bank | Results / held-out verification of the decoupling candidate | title-eligible subject to line policy |
| Differential transfer dev -> held-out | Methods / protocol consistency | bounded |

## Publication gate

- **Evidence audit**: PASS - every observation resolves to the sealed
  artifacts; sidecars match (hashes recomputed); record completeness and
  identity checks are in the execution payload.
- **Domain audit**: PASS - the object/estimator/threshold identity, the
  zero-sum bandpass channel, the frozen structure identity with R408, and
  the guard-first decision order are consistent across plan, execution,
  and analysis.
- **External context**: NOT-APPLICABLE - bounded route-gate evidence.
- **Claim disposition**: ENTER - CLM-1210 registers the HELDOUT-PASS.
- **Allowed claim**: On the R379 held-out evaluation bank of the four-VSG
  feasibility-native energy-port object (frozen thresholds, all guards),
  the 0.4 Hz ring-edge bandpass at K=3.5 passes both endpoints (r_d =
  0.938218, r_cross = 0.793730) with every guard, so the R408 constructive
  candidate is verified on unseen data.
- **Stay-out**: generalization beyond this object/bank, MARL value, R404
  values, title support beyond the line's title policy.

## Follow-up pointers

- The non-learning joint-headroom gate (V2 problem set) is now closed:
  Q is reachable (dev + held-out verified candidate); P6 is resolved; P5
  falsified; blends bounded negatives.  Next eligible activity is the owner
  manuscript-route decision on title/venue use of the verified candidate.
