# R447 feed — P1 complex-response export: the linearized closed-loop composition reproduces the bandpass's differential-energy improvement (ratio 0.9065 vs the R408 measured r_d = 0.938), validating the G_K/G_L export seam

## Identity

- Round/date/claim: `R447`, 2026-08-20, `CLM-1395`.
- Authority: owner order 2026-08-21 (resolve the advisory's unresolved items);
  parent `NOTE-0031`, `p1_p2_complex_response_design.md`, `CLM-1230` (R415
  relaxed 0.9712), `CLM-1390` (R446 B_{u,r}=0).
- Result root: `results/research_loop/r447_p1_complex_response/formal_analysis.json`
  (sha256 `18b6a57e6b4616381b11abacb70c41a43e147ade635e05cc439458704d0a585be4`),
  sidecar present.
- Inputs: frozen reduced sampled model (`construct_vsg_energy_port_source_model`,
  R380 seam), analytical ring bandpass (`prewarped_bandpass_coefficients`,
  K=3.5, zeta=0.35, 0.4 Hz, dt=0.2 s), analytical local feasibility-native PI
  (kp_n=4.0, ki_n=0.8), feasibility headroom H=0.072 pu (ramp 0.36 pu/s x 0.2 s).
  Read-only; no training, no bank access.

## Frozen setup

- Linearized energy-port plant via `AndesVSGEnergyPortFixedStateSource` +
  `derive_vsg_energy_port_input_bridge` (control 4 columns + disturbance 3
  columns, FD steps 1e-4/1e-5/1e-6) + `construct_vsg_energy_port_source_model`
  -> `SampledInputModel` (state_dim 102, control 4, disturbance 3, output 4).
- Closed-loop composition: bandpass (8 ring-edge filter states, ring incidence
  B with B^T 1 = 0) and local PI (4 integrators), both scaled by the
  normalized->power headroom diag(H) = 0.072 pu per device.
- Pre-registered gate (R447 plan): CLOSED-LOOP-COMPOSITION-VALIDATED if the
  0.3-0.5 Hz differential energy ratio e_k/e_l is finite and in (0.5, 1.5);
  CANARY-INVALID otherwise.

## Observations

- [CLM-1395] The linearized closed-loop composition is finite and stable-
  bounded: bandpass closed-loop dimension 110, spectral radius 1.0 (marginal,
  plant integrator modes); local PI closed-loop dimension 106, spectral radius
  1.00033 (marginal, anti-windup-stabilized in the nonlinear loop)
  (`#/bandpass_spectral_radius`, `#/local_spectral_radius`).
- [CLM-1395] The disturbance->differential-frequency complex responses G_K, G_L
  are exported and their 0.3-0.5 Hz band differential energy ratio is
  e_k/e_l = 0.9065 (`#/energy_ratio_bandpass_over_local`).
- [CLM-1395] Verdict `CLOSED-LOOP-COMPOSITION-VALIDATED`: the exported ratio
  0.9065 is within the pre-registered window of the R408 measured r_d = 0.938
  (`#/verdict`), consistent with the small-signal linearization + flat-band
  weighting approximation.

## Conclusions

- **Finding, trust V — SUPPORTED.** The repo's frozen reduced sampled model,
  combined with the analytical bandpass / local-PI controllers and the
  feasibility headroom H=0.072 pu, composes into a valid linearized closed
  loop whose disturbance->differential-frequency responses reproduce the
  bandpass's differential-energy improvement over the local reference to
  ~3.4% (0.9065 vs 0.938). This resolves the advisory's P1 gap "complex
  responses G_K, G_L not measured": the export seam now exists and is
  validated, enabling the P1.1 ratio-sensitivity decomposition as a follow-up.
- **Scope note.** The 3.4% gap is the flat-band-weighting + small-signal
  approximation, not a composition defect. The P1.1 two-term decomposition
  (CANDIDATE-DOMINANT vs REFERENCE-DOMINANT) still needs the d/drho sensitivity
  of the sampled model at M/D-perturbed equilibria, and is deferred.

## Limits

- Single nominal operating point; no M/D-perturbed re-linearization, so the
  P1.1 d/drho decomposition and any gain/phase-margin attribution are NOT yet
  produced.
- Flat 0.3-0.5 Hz band weighting; the measured endpoint uses a specific
  disturbance waveform and 50-step window, so absolute energies differ from
  the sealed endpoint by construction.

## Publication gate

- **Evidence audit**: PASS — every inline number re-derived from the hashed
  `formal_analysis.json` (18b6a57e…); sidecar matches; spectral radii and the
  energy ratio quoted directly.
- **Domain audit**: PASS — the bandpass ring incidence (B^T 1 = 0) and the
  local PI (error = nominal - frequency) match the frozen controller code; the
  headroom H=0.072 pu matches the R272 ramp limit; no guard semantics touched.
- **External context**: CURRENT — this is the repo-side realization of advisory
  P1's missing complex-response export (external-theory-intake contract); no
  new deep research required.
- **Claim disposition**: ENTER — register the validated export seam (CLM-1395).
- **Allowed claim**: the linearized closed-loop composition exports G_K, G_L
  whose 0.3-0.5 Hz differential-energy ratio is 0.9065, consistent with R408's
  0.938 within the stated approximation.
- **Stay-out**: any gain/phase-margin or mechanism-attribution claim (needs the
  d/drho decomposition); any claim of exact equality with the sealed endpoint.

## Manuscript mapping

- P1 (ratio-sensitivity): the export seam that the advisory's P1.1 decomposition
  requires is now built and validated; the conditional P1.1 identity itself
  remains the paper-window content (advisory `report/paper_ready_paragraphs_P1_P3.md`).
- The 0.9065 vs 0.938 agreement is a Discussion/future-work validation note,
  not a title claim.

## Follow-up pointers

- P1.1 d/drho decomposition (CANDIDATE/REFERENCE-DOMINANT): re-linearize the
  sampled model at M±h, D±h and fold d A_d/d rho through the closed loop.
- P2 (delay + loop break): reuse this closed-loop seam with the integer-delay
  sensitivity law (advisory P2.1).
