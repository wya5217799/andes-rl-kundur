# R468 feed — complete physical M/D tensors and qualified local-order result

## Identity

- Round/date/claim: `R468`, 2026-08-21, `CLM-1465`; parent claims `memory/claims/CLM-1380.md`, `memory/claims/CLM-1390.md`, and `memory/claims/CLM-1435.md`.
- Decision artifact: `results/research_loop/r468_u7_local_taylor/checks/verification_report.json` (SHA-256 `564a2fa7c66c6f3a8b20fe01fe2f78854715582c11406c4591004679569c222b`), with matching sidecar; archive status `LOCAL-ONLY`.
- Frozen plan/seal/entry: `memory/rounds/R468/plan.md`, `memory/rounds/R468/formal_seal.json` (SHA-256 `0aca3d2b2d650aaae8099df74ef56e2db333dbe1d0ccd5743b8e9dbb405d8dee`), and `scripts/run_r468_u7_local_taylor.py`.
- Formal root: `results/research_loop/r468_u7_local_taylor/`; 127 files, 24,830,910 bytes. All 126 `SHA256SUMS` entries independently reverified.

## Frozen setup

- Object A/R446 equilibrium: four GENCLS VSGs at `M=400`, `D=100`; one physical angle gauge removed; 101 quotient states, four frequency outputs, and seven joint additive inputs (four energy-port commands plus three load disturbances); sample period 0.2 s.
- Parameter bank: nominal plus eight per-device physical parameters at three simultaneous central-difference levels and two signs, giving 49 unique ANDES linearizations. Base steps are 4 M units and 1 D unit, halved twice; the finest `h/4` tensors are selected.
- Hardware: 15 workers plus one orchestrator, one native thread each; 49 unique jobs completed in 19.26 s. The GPU was excluded because ANDES has no CUDA DAE path. R444's 288 trajectories were reused by hash, not rerun.
- Output convention: `N=dA/dq`, `E=dB/dq`, `R=dC/dq`, and `S=dD/dq` for physical M/D parameters and the sampled end-of-held-input observation model. Complete tensors, all FD levels, every point matrix, and 30-step lifts are retained.

## Observations

- [CLM-1465] `mixed_tensors.npz` contains the complete requested shapes: `N (8,101,101)`, `E (8,101,7)`, `R (8,4,101)`, and `S (8,4,7)` (SHA-256 `90ed1d1a25f332a05d9bda67c84c00600b36df076ac238564d4b0442c219220b`). Every name and active-mode hash is identical across all 49 points.
- [CLM-1465] The maximum h/2-versus-h/4 relative tensor difference is `1.88e-5`, below the registered 1% gate. Exact-ZOH Frechet versus direct sampled derivatives agree to maximum relative errors `2.85e-7` for A and `3.33e-7` for B. Independent swing-row formulas agree to `6.25e-6` relative or better (`checks/richardson.json`, `checks/zoh_frechet.json`, `checks/swing_formula.json`).
- [CLM-1465] Maximum equilibrium residual is `7.23e-9`, passing the project-registered `1e-8` gate but not the external request's suggested `1e-9` algebraic-residual acceptance. This is recorded as a qualification rather than silently rounded down.
- [CLM-1465] The differential additive-command lift is `90 x 90`, full numerical rank, with singular values from `0.0117654` to `0.2340804`; the principal-direction response divided by amplitude is exactly stable over the six-level ladder (`lifts/additive_lift.npz`, SHA-256 `ff75f1691b7d1c4b972e8913d1594724b3dc0f225ac9e154a362bbadb90ea564`). This is a sampled linear-model result, not a nonlinear trajectory validation.
- [CLM-1465] The complete bilinear pseudo-input lift is `120 x 25920` and maps all 30-step products `(q_j x, q_j r)` to four frequency outputs (`lifts/bilinear_lift.npz`, SHA-256 `65a99c1177fe43c84650a337e195f5adf53654ea4d5d9f052f337776e44a2550`). It is an open pseudo-input operator; a policy-specific Volterra kernel additionally requires a declared feedback derivative.
- [CLM-1465] Reanalysis of the sealed R444 bank shows 12/12 blocks quadratic-leading; in all 12, `||Delta y_MD||/epsilon` decreases at the smallest step and the last two `||Delta y_MD||/epsilon^2` values differ by 2.59%--8.40%, inside the registered 20% stability criterion (`scaling/amplitude_scaling.json`).
- [CLM-1465] The implemented controller has exactly zero equilibrium bias, but its left/right observation derivatives differ by up to 8 and the environment's normalized action decoder has slopes 200 and 600 on the two sides of zero (`checks/policy_and_decoder.json`). Therefore a single smooth normalized-policy Taylor expansion is not applicable.

## Conclusions

- **Physical-parameter tensor outcome — VALID.** On the registered fixed-mode quotient, all complete physical-unit M/D mixed tensors converge and pass independent structural and exact-ZOH checks.
- **Finite-ladder order outcome — SUPPORTED.** R444's sealed direct-M/D law-versus-zero odd response satisfies the requested last-two-level quadratic compensation and decreasing linear compensation tests on all 12 measured blocks. The additive port has a nonzero first-order lifted response.
- **Normalized-policy Taylor outcome — NOT APPLICABLE.** Zero bias is satisfied, but the implemented controller/decoder is piecewise and not differentiable at the equilibrium. The data support a piecewise empirical quadratic-leading statement, not the external solution's smooth normalized-policy theorem.

## Limits

- One fixed topology, equilibrium, mode hash, physical-parameter neighborhood, sample time, and 30-step window only. No global remainder radius, robust uncertainty set, topology generalization, EMT, HIL, safety, or deployment result follows.
- The `7.23e-9` residual misses the external packet's suggested `1e-9` threshold. It remains below the environment/project `1e-8` gate and is invariant across the parameter bank; this round does not claim the stricter acceptance passed.
- The additive lift is a local sampled linearization. The R444 direct-M/D ladder is nonlinear ANDES evidence, but its mode signature records the governed limiter/saturation state rather than proving a full symbolic active-set atlas.
- Physical M/D tensors must not be multiplied by one global normalized-action slope: the two one-sided decoder slopes differ. The bilinear pseudo-input lift is not a closed-loop policy kernel by itself.

## Publication gate

- **Evidence audit**: PASS — 49 complete point exports, all three FD levels, full tensors/lifts, R444 normalized tables, provenance, sidecars, and independently verified whole-root hashes are retained.
- **Domain audit**: QUALIFIED — units, gauge, joint-input order, observation timing, fixed mode, zero bias, nonsmooth interface, and additive-versus-direct evidence separation are explicit; the external `1e-9` residual suggestion is not met.
- **External context**: NOT-APPLICABLE — GPT Pro supplied algebra and an acceptance schema, not numerical evidence, citations, or novelty authority.
- **Claim disposition**: ENTER WITH QUALIFICATION.
- **Allowed claim**: at the registered equilibrium, complete physical M/D parameter tensors converge and the sealed nonlinear finite ladder is quadratic-leading while the additive port is first-order; the implemented normalized controller is piecewise, so no single smooth normalized-policy Taylor theorem is claimed.
- **Stay-out**: unqualified C2/C3 normalized-policy proposition; global asymptotic order; robust or topology-general local authority; nonlinear additive-port validation; closed-loop Volterra kernel without a feedback derivative; compliance with the external `1e-9` residual suggestion.

## Manuscript mapping

- Methods/supplement: tensor schema, physical parameterization, fixed quotient, three-level central differences, exact-ZOH cross-check, and complete 30-step operators.
- Results/limitations: use the finite-ladder direct-M/D versus local additive-order contrast only with the piecewise-policy and residual qualifications. Do not replace the manuscript's existing bounded R444 wording with the blocked smooth theorem.

## Follow-up pointers

- U8 may consume the separately exported shared model and projectors; it must not treat this bilinear pseudo-input map as its separation certificate.
- U2 remains the final high-cost training factorial after U8 is closed.
