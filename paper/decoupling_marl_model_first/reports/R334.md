# R334 corrected physical PQ disturbance identification feed

## Identity

- Round/date: `R334`, 2026-08-04; correction of the R333 failed publication
  evidence gate recorded by `CLM-0875`. The publication-valid R334 finding is
  registered by `CLM-0880`.
- Formal seal: `memory/rounds/R334/pq_disturbance_identification_seal.json`,
  SHA-256 `dbbbf046c3d97702dfe69d47955eec099ea35deb478e61f0289b71300f65c827`.
- Formal-attempt marker:
  `results/r334_pq_disturbance_identification/formal_attempt.json`, SHA-256
  `565fb11b3f5c37c1207f1f2306c824b7f37886a51a4883706c59c3426655038f`.
- Formal execution: `results/r334_pq_disturbance_identification/execution.json`,
  SHA-256 `b42dc268a6c59bd5151d3bfc67bdbc96a42fcc510ab771e0d04ff4d26dd9f512`.
- Provenance: `results/r334_pq_disturbance_identification/provenance.json`,
  SHA-256 `6dca62b3215a1f58493b63c46d858cc14550c3971f5155900381e9ac0c2c0c2b`.
- Run manifest: `results/r334_pq_disturbance_identification/run_manifest.json`,
  SHA-256 `2086554a4593864e5f8e416e427412bf181d40833f0991a5de908eae9a048e63`.
- Formal analysis: `results/r334_pq_disturbance_identification/analysis.json`,
  SHA-256 `453ada22907526f571a4099295b9a9e9056b5a81fc286e0af0f0084f7dd4c86f`.

## Frozen setup

- R334 changes no scientific bank, physical helper, model, input map,
  threshold, or decision rule from R333. It corrects only source closure,
  round identity, diagnostic-reward disclosure, and evidence binding.
- Exactly six open-loop records cross HS0/HS1 with zero, positive, and negative
  active-load perturbations on the existing `PQ_Bus14` device. The signed
  amplitude is `+/-0.05` system p.u. on the 100-MVA base; positive denotes
  `+5 MW` consumption and negative denotes `-5 MW` consumption. Reactive power
  is unchanged.
- Four absolute pre-setup `Alter` events assign P/Q at 0.5 s and restore the
  exact pre-event values at 1.5 s. Each record has five 0.2-s disturbed periods,
  twenty 0.2-s recovery periods, and five frozen TDS subdivisions per period.
  The stored solver rows exactly at 0.5 s and 1.5 s are pre-event; the first
  post-critical rows confirm the applied and restored values.
- Model-level constant-power PQ settings are frozen for all PQ devices; only
  `PQ_Bus14.Ppf/Qpf` are changed. The two points freeze per-device M/D, three
  tie-line R/X scales, and initial SOC. Every M/D action and requested,
  projected, internal, and achieved ESD1 power path remains zero; Line_8 and G4
  remain in service.
- The immutable R316 realization is replayed from zero state under
  `d_node = -delta_P_load * [0, 0, 1, 0]`. No refit, scale correction, time
  shift, sign flip, output selection, threshold change, or case selection is
  permitted.
- The normalized L2 signed-pair midpoint residual is
  `||0.5(r_positive+r_negative)||_2 / [0.5(||r_positive||_2+||r_negative||_2)]`.
  Passing supports approximate odd symmetry for the one registered signed pair,
  not local or global linearity. Its limit remains 0.10. Frozen
  reduced-versus-physical limits remain total NRMSE at most 0.15 and maximum
  peak-normalized vector residual at most 0.20 for every signed record.
  Here total NRMSE is the L2 norm of prediction minus matched-baseline-
  subtracted response divided by the response L2 norm across all times and four
  coordinates. Peak residual is the maximum timewise error-vector L2 norm
  divided by the maximum timewise response-vector L2 norm.
- The seal hashes 126 unique repository-local sources, including every Python
  file under `src/andes_rl_kundur`, the inherited and correction adapters,
  probes and helper, the scratch launcher, and artifact writer. It also pins
  ten installed ANDES source files and the executed Kundur case file.
- The inherited environment computes and stores reward diagnostics, but the
  formal contract forbids their use for action, fitting, selection, training,
  classification, or claims. The classifier does not read reward-field
  presence or values, and mutation/deletion tests leave its result unchanged.

## Observations

- [CLM-0880] The corrected evidence chain and deterministic replay pass.
  All fourteen reported validity guards pass: the twelve unchanged R333 guards
  plus original R334 identity and diagnostic-reward boundary. Source:
  `results/r334_pq_disturbance_identification/analysis.json#/evidence_chain_valid`,
  `/deterministic_replay`, and `/validity_guards`.
- [CLM-0880] All four registered identification guards pass. Every signed
  response is observable above its matched zero baseline and has the required
  common-frequency sign. The normalized L2 midpoint residual is
  `0.0025223265774806784` at HS0 and `0.0022322968749667127` at HS1, both below
  `0.10`; this is one-pair approximate odd symmetry only. Source:
  `results/r334_pq_disturbance_identification/analysis.json#/identification_guards`,
  `/point_metrics`, and `/scope`.
- [CLM-0880] The immutable R316 channel replay passes all four signed
  records. Total NRMSE ranges from `0.04868402889504176` to
  `0.05542826242727986` against the frozen `0.15` limit; maximum vector residual
  ranges from `0.05858779198944524` to `0.07445835005862592` against the frozen
  `0.20` limit. Source:
  `results/r334_pq_disturbance_identification/analysis.json#/record_metrics`.
- [CLM-0880] The formal classification is `QUALIFY`. Exactly one physical
  input column is identified: Bus14 active load, one amplitude and waveform,
  two operating points, and phasor-domain electromechanical scope. A separately
  sealed successor disturbance package remains mandatory before any physical
  closed loop. Source:
  `results/r334_pq_disturbance_identification/analysis.json#/classification`
  and `/scope`.
- [CLM-0880] Reward diagnostics are computed and stored but not used in
  the scientific decision. R334 executes no controller, closed loop,
  distributed runtime, training, topology change, EVAL, or title-result test.
  Source: `results/r334_pq_disturbance_identification/analysis.json#/scope` and
  `/reward_used_for_classification`.

## Conclusions

- **Disturbance-channel judgment - QUALIFY**: the separately executed Bus14
  active-load channel is observable, sign-consistent, approximately odd-
  symmetric for the registered signed pair, and within the frozen R316 mismatch
  envelope for all four signed records.
- **Evidence-correction judgment**: the R333 publication failure is repaired
  prospectively. The complete conservative source superset, executed case,
  R334 identities, reward non-use boundary, create-only attempt chain, and
  deterministic two-pass analysis are bound before interpretation.
- **Root-cause boundary**: the result supports R332's diagnosis that the prior
  bridge blocker was the missing separate physical disturbance entrance, not a
  general inability of ANDES to execute this phasor-domain load change. It does
  not attribute controller or algorithm performance to the simulator.
- **Algorithm boundary**: the model-first deterministic line remains eligible,
  but the frozen R329 arbitrary four-node disturbance object is broader than
  the single physical column identified here. R329 cannot be transferred
  unchanged into a physical closed loop.
- **Programme boundary**: Q-0085 may close qualified-positive after the
  publication gate. Only a separately sealed successor physical disturbance
  model/package may follow. Physical control, residual-headroom testing,
  distributed agents, reward design, training, EVAL, and title-result claims
  remain blocked.

## Limits

- Evidence covers one existing active-load device, one signed amplitude and
  waveform, two operating points, one unchanged topology, and a phasor-domain
  electromechanical simulator only.
- One registered signed pair does not establish local or global linearity,
  other load locations, reactive-load disturbances, outages, larger
  amplitudes, other timings, other topologies, or other operating conditions.
- The bank identifies one physical disturbance column, not the arbitrary
  four-node disturbance vector assumed by R329; it validates no successor
  estimator-controller.
- Open-loop prediction agreement is not controller performance, frequency
  restoration, stability, safety, robustness, or causal value from retained
  cross-coupling, decoupling, coordination, distributed execution, or learning.
- Reward diagnostics are retained only as inherited telemetry. Their values
  support no reward-design statement and are excluded from classification and
  every allowed claim.

## Publication gate

- **Evidence audit**: PASS - independent reconciliation verified all six
  artifact hashes and sidecars, the complete 126-source seal, installed ANDES
  sources and executed case, strict six-record inventory, original R334
  identities, reward non-use, replayed inputs/predictions, and every reported
  metric with no load-bearing mismatch.
- **Domain audit**: PASS - independent review verified bases, signs, exact
  event-row semantics, load-to-node mapping, metric definitions, open-loop
  interpretation, one-pair odd-symmetry wording, phasor-domain platform scope,
  and the successor-package ceiling after the feed stated them explicitly.
- **External context**: NOT-APPLICABLE - this is a bounded implementation and
  physical interface-validity result, not a novelty or nearest-work claim.
- **Claim disposition**: QUALIFY - `CLM-0880` is paper-eligible only within the
  one-column, one-signed-pair, two-operating-point, phasor-domain ceiling below.
- **Allowed claim**: under the complete corrected source-bound contract, the
  one Bus14 active-load physical column is observable, sign-consistent,
  approximately odd-symmetric for the registered +/-5 MW pair, and within the
  frozen R316 mismatch envelope on two operating points. A separately sealed
  successor disturbance package remains required before any physical closed
  loop.
- **Stay-out**: controller performance; complete physical disturbance-model or
  closed-loop validity; local/global linearity; stability; safety; topology
  generalization; distributed-agent, coordination, reward, or learning value;
  EMT/hardware/deployment; and title-result support.

## Claim-evidence audit table

| Claim | Type and scope | Canonical source and locator | Transformation | Status | Safe wording |
|---|---|---|---|---|---|
| `E-001`: R334 identity and inventory are valid | validity; one seal, 126 sources, six physical records | `analysis.json#/evidence_chain_valid`; `/validity_guards`; formal artifacts and sidecars | independent SHA-256, exact inventory, identity, replay, reward non-use, and exclusions | VERIFIED | The corrected source-bound R334 chain is complete and replayable. |
| `E-002`: one separate load channel is identified | physical interface; one Bus14 active-load column | `analysis.json#/identification_guards`; `/point_metrics`; `/scope` | baseline subtraction, sign, signal/drift, and normalized L2 midpoint residual | VERIFIED | One Bus14 active-load column is identified for the registered signed pair and two points only. |
| `E-003`: the frozen reduced column matches the physical response | open-loop prediction; four signed records | `analysis.json#/record_metrics` | registered NRMSE and peak-normalized vector residual | VERIFIED | The frozen R316 column is within its registered mismatch envelope for four signed records. |
| `E-004`: only a successor physical package is eligible | programme decision; narrow identified column | `analysis.json#/classification`; `/scope` | compare one identified column with the broader R329 disturbance object | VERIFIED | No closed loop until a separately sealed successor package is validated. |
| `E-005`: the result proves controller, distributed, reward, or learning value | controller and title claims | no such execution; `analysis.json#/scope` | scope-exclusion check | UNSUPPORTED | R334 provides no controller, coordination, distributed-agent, reward-design, or learning result. |

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| `E-001` | Physical-interface and reproducibility subsection | eligible within CLM-0880 |
| `E-002` | Physical-disturbance identification subsection | eligible; one signed pair only |
| `E-003` | Open-loop model-validation subsection | eligible; four signed records only |
| `E-004` | Method progression and limitations | eligible; successor eligibility only |
| `E-005` | Title, abstract, contributions, and conclusion | stay out until later physical-control, distributed-action, and learning gates pass |

## Follow-up pointers

- Q-0085 closes positive within the CLM-0880 ceiling after both publication
  audits and claim registration; Q-0086 is the sole successor question.
- Q-0086 may prospectively build and validate a separately sealed physical
  disturbance package covering the input directions required by the intended
  deterministic bridge.
- Unauthorized until later gates: physical closed loop, residual headroom,
  distributed runtime, reward design, agent implementation, neural training,
  EVAL, topology generalization, stability, safety, and title-result claims.
