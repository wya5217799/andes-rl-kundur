# R351 matched neighbour-edge execution-gate feed

## Identity

- Round/date: `R351`, 2026-08-07; the bounded finding is reserved as
  `CLM-0920`.
- Formal seal: `memory/rounds/R351/formal_seal.json`, SHA-256
  `0517f7c8d26975f6a34558c9e6154db5215fe97078822327b5c3c78434b7fc95`.
- Capacity evidence: `memory/rounds/R351/capacity_measurement.json`, SHA-256
  `3ad30647b3dffb13f71db51464cebb877b7a8988996e832e5d9357cd86c7414b`.
- Capacity-bound same-path rehearsal:
  `memory/rounds/R351/formal_rehearsal_v2.json`, SHA-256
  `6bbde91125eb6c65d55af3363022e819a9bd1fcaf5d983d03cfcd9ec771fddf5`.
- Formal decision: `results/r351_matched_distributed_bridge/analysis.json`,
  SHA-256
  `8dd75b25ad1c28e9df2334df1e3494cdffb883837ba88462d77da66f42be5e65`.
- Formal manifest: `results/r351_matched_distributed_bridge/manifest.json`,
  SHA-256
  `a7a54b5a6b6c32e62aa2867b556735b545040bca37fc12310d4f0426e4bd2f7c`.
- Archive class: `LOCAL-ONLY`; no public push is authorized.

## Frozen setup

- The public controller seam has three oriented tree-edge actions:
  `(0,1)`, `(1,2)`, and `(2,3)`. One deterministic edge actor receives only
  its two endpoint observations and returns one normalized scalar. Joint plant
  state and global statistics are absent from the interface.
- The same action governor is the required seam for any later neighbour
  policy. It applies the frozen edge order, edge magnitude and slew limits,
  source-positive incidence, endpoint-only headroom allocation, and the
  existing power, ramp, voltage/current capability, energy, and state-of-charge
  projection.
- Public-interface tests separately verify endpoint locality, absence of
  nonendpoint influence, independent edge ordering, action-to-node incidence,
  physical projection identity, and command-level zero sum.
- The sealed physical canary contains two equilibrium zero-action records and
  twelve signed-authority records: three edges, both signs, and the two R341
  registered operating points. Signed commands hold `0.05` system pu for five
  `0.2`-second intervals and recover for twenty intervals.
- R351 has no disturbance-performance comparison. It does not execute the
  linear deterministic controller in closed loop, tune its gains, compare it
  with zero control or R344, or estimate residual headroom. The signed canary
  validates the matched action/governor/plant path only.

## Observations

- [CLM-0920] The create-only attempt contains all `14` sealed records: `2`
  zero-action records and `12` signed-authority records spanning all `3` tree
  edges, both signs, and both registered operating points. Every record is
  integrity-valid, matched-governor-valid, and physical-guard-valid. Source:
  `results/r351_matched_distributed_bridge/analysis.json#/record_count`,
  `/all_matched_governor_checks_pass`, `/all_physical_guards_pass`, and
  `results/r351_matched_distributed_bridge/execution.json#/records`.
- [CLM-0920] Both formal stages obeyed the sealed two-process, one-native-thread
  budget. Each stage used two unique Python processes with maximum overlap two
  and passed its process guard. Source:
  `results/r351_matched_distributed_bridge/execution.json#/zero_process` and
  `/signed_process`.
- [CLM-0920] The frozen classifier returned
  `DISTRIBUTED-EDGE-EXECUTION-ELIGIBLE`. It authorizes only a separately
  registered deterministic tuning/comparison question; training remains
  false and no performance comparison executed. Source:
  `results/r351_matched_distributed_bridge/analysis.json#/classification`,
  `/deterministic_tuning_authorized`, `/training_authorized`, and
  `results/r351_matched_distributed_bridge/execution.json#/performance_comparison_executed`.

## Conclusions

- **Execution judgment - DISTRIBUTED-EDGE-EXECUTION-ELIGIBLE**: the endpoint-
  only deterministic edge interface and its matched three-edge physical
  governor passed the registered structural tests, while every sealed zero and
  signed action profile passed the physical execution guards at the two
  registered operating points.
- **Question disposition**: Q-0092 closes positive under CLM-0920 for execution
  eligibility only.
- **Programme boundary**: R351 permits one new deterministic tuning and matched
  comparison question. It does not establish closed-loop control benefit,
  residual headroom, multi-agent value, or neural-training eligibility.

## Limits

- Structural locality and signed physical authority were tested as two bound
  parts of one frozen seam. The deterministic edge policy itself was not run
  under a physical disturbance, tuned, or compared for control performance.
- The physical canary covers one simulator, one fixed topology, two locally
  constructed operating points, equilibrium zero action, six signed edge
  directions, one magnitude, and one short hold/recovery waveform.
- Command-level edge neutrality follows from the incidence and governor.
  Achieved inverter power is checked only against the registered finite canary
  imbalance guard; exact achieved neutrality is not claimed.
- No stability, recursive feasibility, safety certificate, communication
  delay/loss robustness, unseen operating point, topology generalization,
  reward, agent, training seed, checkpoint, or learning comparison exists.
- R344 remains a non-deployable centralized upper reference. Its joint
  information and four-coordinate action make it ineligible as a matched
  R351 performance comparator.

## Publication gate

- **Evidence audit**: QUALIFIED - all `17` manifest entries and the formal seal
  resolve with matching hashes; all `14` registered records, `3` edge
  identities, both signs, and both points reconcile; every integrity,
  matched-governor, physical, and process guard is true; and the `20` relevant
  public-interface/integration tests pass. The structural controller test and
  signed physical action test remain separate evidence components, so they do
  not establish disturbed closed-loop performance.
- **Domain audit**: QUALIFIED - the source-positive edge signs, system-pu power,
  `0.2`-second timing, node incidence, request-command-achieved distinction,
  power/ramp/current/energy/state-of-charge limits, limiter status, and
  endpoint-only information boundary are consistent with the bounded wording.
  The canary is an authority/feasibility test, not a stability, safety,
  robustness, or controller-benefit experiment. Presentation-only manuscript
  checks are not yet applicable.
- **External context**: NOT-APPLICABLE - this is an internal execution
  qualification and makes no novelty, differentiation, or related-work claim.
- **Claim disposition**: QUALIFY - CLM-0920 may enter only as a finite-canary
  matched execution-seam result and as authorization to register a separate
  deterministic tuning/comparison question.
- **Allowed claim**: the endpoint-only three-edge public controller interface
  passed its structural locality/governor tests, and all fourteen sealed zero
  and signed physical action-seam records passed the registered integrity,
  matched-governor, process, and physical guards at two operating points.
- **Stay-out**: end-to-end disturbed closed-loop controller performance;
  improvement over zero or centralized control; residual headroom; neural or
  multi-agent value; stability, recursive feasibility, safety or robustness;
  unseen-operating-point or topology generalization; and training eligibility.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| endpoint-only three-edge public interface | methods implementation contract | qualified |
| complete guarded signed physical seam | methods validation and evidence ledger | qualified |
| deterministic tuning eligibility | next experiment transition | registration only |
| closed-loop benefit, neural value, stability, safety, and topology claims | title, results, and conclusion | stay out; untested |

## Follow-up pointers

- Q-0092 is eligible to close positive under CLM-0920 after publication-gate
  review.
- The only eligible next experiment is a prospectively registered deterministic
  controller-in-loop tuning and matched comparison study with frozen
  development/holdout separation.
- Unauthorized: neural training, reward or architecture selection, residual-
  headroom claims, multi-agent performance, topology generalization, stability,
  safety, or title-result support from this execution gate.
