# R360 flexible non-neural neighbour-residual learnability feed

## Identity

- Round/date: `R360`, 2026-08-07; the bounded finding is reserved as
  `CLM-0950`.
- Formal rehearsal: `memory/rounds/R360/rehearsal.json`, SHA-256
  `ad64582b07cf00d27dd2fa422ebbc81589ebc0557aa2082f065a4cf3979425ff`.
- Formal seal: `memory/rounds/R360/analysis_seal.json`, SHA-256
  `c11e1b8fd6b035f87983f0bac9825498565b49f02b5f16f43cc5f9adacda4d48`.
- Formal attempt: `results/r360_flexible_neighbour_residual/analysis_attempt.json`,
  SHA-256 `70088b9cff97f5605404260c123b4a986e307af8996425fefa66dc1dbc483d70`.
- Formal decision: `results/r360_flexible_neighbour_residual/analysis.json`,
  SHA-256 `2a87258e37a52578d1bb339542054d6055c1e534378078e4b6afe53687e61ffc`.
- Formal manifest: `results/r360_flexible_neighbour_residual/manifest.json`,
  SHA-256 `5add641992965bb58d77c14e6978dd191a335f2f0aa257586302ea001d551f0b`.
- Archive class: `LOCAL-ONLY`; no public push is authorized.

## Frozen setup

- Reuses the R359 development/holdout partition byte-for-byte: sixteen R352
  development pairs (ten R358 accepted physical witnesses as positive targets,
  six inherited relaxed-infeasible as all-zero negative controls), sixteen
  staggered-rise holdout pairs, twenty-five samples, two startup-zero steps,
  and the `0.05` system-pu edge limit with the exact physical projection.
- Every edge actor receives exactly the R359 fifteen-field
  `LocalEdgeObservation` at the causal pre-action instant; the forbidden-field
  list is unchanged.
- The only change is the controller family: three pre-registered tuning-free
  non-neural maps replace the R359 fixed affine map -- RBF kernel ridge with
  frozen median-distance width and `1e-3` regularization; k-NN with `k=5` on
  standardized Euclidean distance; quadratic polynomial basis with
  standardization and no regularization. No hyperparameter, kernel, seed,
  reward, or candidate scan exists; no neural or reinforcement-learning
  algorithm is used.
- The registered endpoint groups (common-coordinate IAE and
  differential-coordinate energy under nominal and mismatch-bounded response
  maps) and all thresholds (2% paired mean, one-sided 95% upper bound,
  subgroup direction, 1.05 worst-case ratio) are unchanged from R359.
- The decision semantics are a pre-registered OR: any passing family yields
  the positive class; a failure of every family fires the preregistered stop
  of the learning route.

## Observations

- [CLM-0950] All `16` leave-one-scenario-out development projections are
  feasible for all three families, with complete-inventory, exact-information,
  startup-mask, and physical-projection integrity checks passing. Source:
  `results/r360_flexible_neighbour_residual/analysis.json#/development/integrity_checks`.
- [CLM-0950] No family clears the nominal endpoint group. Mean
  common-coordinate improvement is `0.92%` (RBF), `0.89%` (k-NN), and `1.11%`
  (quadratic), each below the registered `2%` paired gate, and the
  differential-coordinate mean improvement is `-14.56x`, `-5.80x`, and
  `-36.93x` (worsening). Source:
  `results/r360_flexible_neighbour_residual/analysis.json#/development/family_gates/*/nominal/endpoints`.
- [CLM-0950] No family clears the mismatch-bounded endpoint group: common
  improvement is `-0.50%`, `-0.54%`, and `-0.32%` (worsening), and
  differential mean improvement is `-15.50x`, `-6.40x`, and `-38.49x`.
  Source:
  `results/r360_flexible_neighbour_residual/analysis.json#/development/family_gates/*/mismatch_bounded/endpoints`.
- [CLM-0950] With all integrity checks passing and every registered family
  failing both endpoint groups, the sealed classifier returns
  `NO-NEIGHBOUR-LEARNABLE-STRUCTURE`. It reads `0` holdout cases and `0`
  holdout residual labels, leaves the holdout result null, and keeps
  training, simulation, distributed runtime, and EVAL unauthorized and
  unexecuted. Source:
  `results/r360_flexible_neighbour_residual/analysis.json#/classification`,
  `/holdout_cases_read`, `/holdout`, and the top-level authorization fields.

## Conclusions

- **Primary judgment - NO-NEIGHBOUR-LEARNABLE-STRUCTURE**: on the exposed
  finite development bank, no member of the pre-registered flexible non-neural
  map family recovers the R358 physical headroom from the exact prospective
  information path while meeting both registered endpoint groups. The R359
  negative result is therefore not specific to the affine function class
  within this family scope.
- **Question disposition**: Q-0097 closes negative for the registered family
  and frozen finite development bank. The preregistered stop fires: the
  learning route terminates, and no holdout residual label, counterfactual
  endpoint, holdout gate, physical intervention, simulation, or training is
  authorized.
- **Programme boundary**: the result does not establish that a nonlinear or
  neural residual is impossible in general, that neighbour information is
  globally insufficient, or that multi-agent learning lacks value; it also
  does not exhaust every possible non-neural function class. It is the
  registered stopping evidence for this route under this contract.

## Limits

- The tested objects are three deterministic tuning-free maps per edge,
  fitted and judged on an exposed finite linear-response development bank.
  They are not neural networks, nonlinear plants, or reinforcement-learning
  algorithms.
- The exact information contract and three independent edge actions are
  represented offline; no deployed distributed runtime or nonlinear plant
  trajectory is executed.
- Holdout inventory identity and source closure were checked prospectively,
  but holdout residual labels and counterfactual endpoints remain unexamined
  and no holdout gate is evaluated because the development gate fails.
- The evidence covers one fixed Kundur topology, two exposed operating
  points, selected disturbance channels and signs, and twenty-five response
  samples. Stability, safety, robustness, topology generalization, hardware,
  and deployment remain untested.
- R358 physical witnesses still establish per-case action-space existence in
  ten exposed cases; R359 and R360 test causal recovery by registered
  information-to-action maps, and their negative results do not invalidate
  that earlier physical-feasibility result.

## Publication gate

- **Evidence audit**: PASS - all sealed source bindings, the five formal
  artifact sidecars, both manifest entries, all sixteen projections per
  family, endpoint ratios, guard outcomes, classification, and zero
  holdout-label count reconcile with
  `results/r360_flexible_neighbour_residual/analysis.json` and its prospective
  seal. The absent verdict and line binding are lifecycle-expected before
  close-out, not evidence conflicts.
- **Domain audit**: QUALIFIED - the physical `60` Hz basis, endpoint-neighbour
  information ownership, independent three-edge normalized actions,
  `0.2`-second timing, `0.05` system-pu edge limits, physical projection, and
  inertia-weighted common/differential endpoints are internally consistent;
  the three frozen families have no tuning or leakage path. The result is an
  offline finite-bank linear-response test with no deployed distributed
  runtime or nonlinear actuator/readback trajectory.
- **External context**: NOT-APPLICABLE - this bounded negative gate makes no
  novelty, differentiation, or related-work claim.
- **Claim disposition**: QUALIFY - use only as the registered stopping result
  and limitation of the tested flexible map family on this route.
- **Allowed claim**: on the exposed sixteen-case development bank, none of
  the three pre-registered tuning-free non-neural neighbour-residual maps
  (RBF kernel ridge, k-NN, quadratic polynomial basis) clears both registered
  endpoint groups after the exact physical projection, so the registered stop
  fires and no holdout, simulation, or training is authorized.
- **Stay-out**: absence or unlearnability of any nonlinear or neural residual
  in general; general insufficiency of neighbour information; multi-agent
  value; nonlinear performance; stability, safety, robustness, topology
  generalization, hardware, or deployment.

## Manuscript mapping

| Observation | Candidate manuscript use | Status |
|---|---|---|
| complete exact-information and physical-path integrity | methods and validity boundary | verified implementation fact |
| all three families fail nominal and mismatch endpoint groups | bounded learnability gate result | qualified negative result |
| preregistered stop before holdout, simulation, or training | limitation and route-termination decision | required qualification |
| nonlinear, neural, multi-agent, stability, safety, and topology claims | none | stay out; untested |

## Follow-up pointers

- Close Q-0097 negative only after both semantic audits pass or qualify this
  same feed and the claim card is bound to the allowed wording.
- R360 authorizes no in-place retry, neural training, new simulator
  trajectory, or large-scale experiment; any future direction requires a
  separately registered falsifiable question under a new round.
